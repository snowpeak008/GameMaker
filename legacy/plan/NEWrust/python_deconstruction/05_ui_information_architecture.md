# Python UI 信息架构

状态：草案。

## 顶层

`MainWindow`：

- 顶部导航：设计工作台、开发流水线、补充开发、打包阶段、运行日志、SDK 知识库。
- 底部状态栏：AI 状态、pipeline 进度、系统状态。
- 主区域：懒加载当前任务区 panel。

## 设计工作台

`CommercialDesignApp` 初始结构：

- 顶部：项目名、导出格式、导出、存档管理、模板查看、另存为模板、重置。
- 主体：左中右三栏。
- 左侧：领域总览、项目画像。
- 中间：节点、checklist、L4/L5、玩法系统。
- 右侧：摘要、缺失项、风险、校验。

待补充：控件级结构。

## 开发流水线

`PipelinePanel` 初始结构：

- 左侧：按组显示 Step00-14。
- 右侧顶部：项目配置、AI 配置、导出到流水线、范围运行、跳过人工确认、运行、停止。
- 右侧主体：阶段详情、semantic quality、Step07 风格确认、运行日志。

待补充：控件级结构。

