# Rust 重建暂停交接与后续推进计划

## 当前状态

本文件是 2026-07-05 暂停前的恢复入口。用户已明确要求暂停，当前只允许写入记忆和计划，不继续开发功能。

注意：`plan/` 目录被 `.gitignore` 忽略，本文件是本地交接计划，普通 `git status` 不会显示它。

Rust 重建目标仍未完成，目标保持不变：完整替换 Python 运行项目，以 `E:\workwork\CrewAi\AutoDesignMaker\RUST` 作为 Rust 代码根目录，保留并重建“游戏设计 + 游戏开发流水线 + 游戏打包 + 游戏 SDK”核心功能。

最新已验证的 Rust 工作记忆是：

- `knowledge/ai_memory/session_history/2026-07-05-092.md`
- 内容：OpenAI 兼容 AI provider presets，包含 `openai`、`openrouter`、`deepseek`、`local_openai`
- 桌面发行产物：`RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`
- 发行 hash：`fnv64:c2d49df11f4ff151`
- 最近一次全工作区 Rust 验证：`cargo check --workspace` 与 `cargo test --workspace` 通过，121 个 Rust 单元测试通过
- `release-doctor` 与 `delivery-doctor` 均为 `ready=true`

## 暂停规则

自动 goal 续跑上下文不能视为继续开发授权。只有用户明确说“继续”“继续开发”“恢复”等含义明确的指令后，才继续写代码、运行构建或执行功能验证。

恢复后第一步必须重新读取：

- `AI_README.md`
- `knowledge/ai_memory/INDEX.md`
- `knowledge/ai_memory/session_history/2026-07-05-093.md`
- `knowledge/ai_memory/session_history/2026-07-05-092.md`

然后再用当前工作区状态作为权威依据。

## 工作区风险

当前 `git status --short` 很脏，包含大量 Python、知识库、模板、`newplan/` 删除项和未跟踪项。这些不是本暂停交接任务要处理的内容。

恢复开发时必须遵守：

- 不回滚用户或其他会话已有改动。
- 不整理无关 Python 工作区。
- `RUST/` 仍是未跟踪目录，普通 `git diff` 看不到 Rust 内部变化，需要直接读文件和运行 Rust 测试确认。
- 新 Rust 开发继续放在 `RUST/` 下。

## 当前架构方向

已确定的 Rust 重建方向：

- 彻底更换语言，Python 项目只作为参考。
- 同一个 git 库，Rust 新代码在 `RUST/`。
- 先底层框架，再链条内容。
- 业务存档不是 git 仓库，是软件内项目存档。
- 支持双击打开程序。
- 允许多个程序会话并行存在。
- 必须阻止两个会话同时打开同一个正式项目存档。
- 每个程序会话应有独立临时工作台。
- AI 不是常驻强制接入，而是在评分不足、内容不足、生成后检查等需要时介入。

## 后续推进顺序

### 1. 恢复基线检查

恢复后先做最小验证，不直接大改：

- 读取 Rust workspace 结构。
- 检查 `RUST/Cargo.toml`、`RUST/README.md`、`apps/adm-desktop`、`apps/adm-cli`、`crates/adm-application`、`crates/adm-packaging` 当前状态。
- 运行轻量检查：
  - `cargo fmt --check`
  - `cargo check --workspace`
  - 必要时 `cargo test --workspace`
- 如果当前验证失败，先修复 Rust 范围内问题。

### 2. 生产深度流水线扩展

下一步优先补“生产可用度”，不要继续停留在演示内容。

推荐第一个切片：

- 新增确定性生产就绪报告，例如 `validation/production_readiness.adm`。
- 从现有设计、开发、资产、SDK、打包目标、acceptance matrix 中派生结论。
- 报告应包含：
  - playable scenario 覆盖
  - development task 覆盖
  - asset requirement 覆盖
  - SDK/build/package readiness
  - blocking/warning/ready 状态
  - 可追溯 artifact id
- 将该报告写入核心 archive。
- 将该报告纳入 package support files 或 doctor 检查。
- 桌面 smoke 至少验证报告存在并显示关键状态。
- README 说明报告用途。

### 3. 存档与恢复链条

必须继续补齐：

- 创建项目时持久化原始 configurable brief。
- resume/rerun 不再用 demo default 重构 brief。
- 正式存档打开加锁。
- 同一个正式存档禁止双开。
- 临时工作台隔离。
- 存档导入/导出 CLI 与 GUI 路径。
- 崩溃或强制关闭后的锁恢复策略。

### 4. Slint 桌面 GUI 完成

桌面 GUI 后续至少需要：

- 项目创建/打开/最近项目列表。
- 存档锁状态显示。
- 工作台 artifact 浏览。
- 流水线运行状态。
- AI provider presets、named secrets、diagnostics 的完整交互。
- 打包/SDK/Unity scaffold 操作按钮。
- doctor 结果展示。
- 错误状态可读，不吞掉失败。

### 5. AI 接入闭环

AI 接入保持按需触发：

- 分数不足时补充内容。
- 内容不足时生成候选。
- 生成后做检查。
- provider 可配置、secret 不入日志。
- 缺少 secret 时可诊断但不崩溃。
- 真实 provider 调用必须等用户提供安全 endpoint/model/secret 配置后再做 acceptance。

### 6. 打包、SDK 与 Unity 验证

继续保证：

- game build bundle 可 staging。
- SDK bundle 包含必要运行、历史和契约信息。
- Unity scaffold 包含设计、开发、验证、项目 brief 等生成文件。
- 本机没有 Unity 时只做 dry-run/preflight。
- 有 Unity 的机器上再做 guarded real build。

## 推荐恢复命令

在 `RUST/` 下使用本机 MSVC 环境包装：

```powershell
& cmd.exe /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\VC\Auxiliary\Build\vcvars64.bat" >nul && set PATH=C:\Users\admin\.cargo\bin;%PATH% && cargo fmt --check'
& cmd.exe /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\VC\Auxiliary\Build\vcvars64.bat" >nul && set PATH=C:\Users\admin\.cargo\bin;%PATH% && cargo check --workspace'
& cmd.exe /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\VC\Auxiliary\Build\vcvars64.bat" >nul && set PATH=C:\Users\admin\.cargo\bin;%PATH% && cargo test --workspace'
```

## 不要忘记

本 goal 不应被标记完成。当前只是暂停交接，后续还需要大量 Rust 功能补齐和端到端验证。
