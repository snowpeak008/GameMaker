# V4 开发进度与交接（2026-08-29）

> 写给下一个接续会话的主开发。当前状态：**W2 验收后暂停**（用户指令）。恢复开发从本文第 5 节继续。

## 1. 协作模式（用户已定）

- V4 是完全独立项目，前三版（Python 一代 / RUST 二代 / NEWrust 三代）只是参考，零代码关联。
- 主开发（orchestrator）只负责：计划、任务派发、跨任务集成、验收、门禁。**不写代码。**
- 开发由子 agent 承担，允许并行开发、并行验收。
- 并行冲突控制：同一波次内任务的可写文件集互不相交；共享文件（`adm4-app/src/services.rs`、`universal/core.json`、根 `Cargo.toml`）单独派发或由主开发在波次间集成。

## 2. 波次进度总览

| 波次 | 任务 | 状态 |
|------|------|------|
| — | 计划四件套（`docs/plan/01-04`） | ✅ 定稿 |
| W1 | T1 塔防包 L6 深化 | ✅ 验收通过 |
| W1 | T2 战棋包新建 | ✅ 验收通过 |
| W1 | T3 逆向工具链产线 | ✅ 验收通过 |
| W1 | T4 AI 访谈分层确认 | ✅ 验收通过 |
| W1.5 | universal 跨包 unlock 修复（计划外，主开发裁决后派发） | ✅ 验收通过 |
| W1 | 统一门禁（fmt / clippy 零警告 / 39 测试全绿 / 双包 validate OK） | ✅ 通过 |
| W2 | T5 AppServices 门面扩展 + e2e 场景 | ✅ 验收通过（详见文末） |
| W3 | T6 CLI 子命令补齐、T7 桌面 UI 四面板 | ✅ 验收通过（门禁：48 测试全绿、冒烟脚本主开发复跑通过） |
| W4 | T8a 门禁走查 / T8b 红线审查 / T8c 文档一致性 | ✅ 全部完成（T8a 修 6 组 --help + 3 处错误信息；T8c 重写 README + 4 处文档偏差已标注） |
| W5 | F1 红线修复；F2 校验器立项 + freeze check 补修 | ✅ 全部验收通过（测试 48→61→80；四个检查类命令退出码语义统一；最终六项门禁全绿） |

> **Phase 1 已收官（2026-08-29 20:11），终态见 `2026-08-29_Phase1交付报告.md`。**

## 重大转折：用户验收裁定返工（2026-08-29 20:33）

用户验收 V4 后裁定：**内容实现与 V2 完全不同，未理解 V2 的内容设计逻辑与界面交互逻辑**。主开发复查确认错误属实——把 redesign_v3 决策模型当成了对 V2 内容组织的替代而非补充，丢失了 16 领域巡视、检查单、L4 主选、MDA、26 内置模板、摘要/缺失/风险/校验四视图、项目画像等 V2 能力，且流水线在 C0-C6 后断头。

**用户三原则（W6 返工的最高准绳）**：
1. UI 布局交互用 V2 的（三栏工作台 + 顶部六视图导航）。
2. 语义冲突以新设计为准，但新设计必须覆盖旧设计——V2 每项能力必须有归宿（保留或吸收），不许消失。
3. 设计流程不许清理缩水；开发流水线不许打断开发计划（不许断头）。

**返工依据文档**：`docs/plan/05_V2能力覆盖矩阵与W6返工设计.md`（逐项归宿矩阵 + 冲突裁决 + T9-T13 分解），用户已确认并下令开工。

**W6 进度**：T9 ✅ 验收通过（2026-08-29 21:35，测试 80→101，主开发复跑六项门禁全绿）→ T10 知识库迁移 + T11 UI 返工并行进行中 → T12 流程版图与占位 → T13 矩阵逐行验收。git 已推送的 1.0.0 版本是返工前基线；返工完成后需另立版本节点。

**T9 交付要点**：领域/节点两级组织（DesignOrganization，保留域"未分域"代码内置；DecisionPoint+node_id）；多选+主选（SelectionMode::Multi{allow_primary}，Selection 平铺+additional_options 保持旧档字节兼容，全消费方统一走 selected_options()，冻结落 with_primary_first()，C0 多选产出 决策id#选项id 元素）；design_question 新增、mda_layer 保留既有枚举（V2 五值完全吻合）；节点 design_note/risk_note + N/A 人工豁免（理由码+说明+署名三要素强制，冻结门 1 可见不拦截）；project_profile L0/L1 聚合；workbench_overview 四聚合 + decision_points 全图 DTO。迁移格式说明与 DTO 清单在 T9 报告原文（子 agent 转录 8c1a22f0）。

**T9 偏离裁决**：mda_layer 保留枚举 → 接受；画像展示名用 question → 接受（短标签作为可选清单键归 T10）；NaJustification 与 na_signoffs 双 map → 接受，T11 获可选授权合并；访谈不支持多选（确认后缺主选可见拦截）→ 接受，T11 补"设主选"UI，访谈原生多选登记待办；DecisionPoint.domain 与领域组织概念冗余 → 记设计债不动。

**T10 交付（2026-08-29 22:14，✅ 已验收：夹具修复后 96 测试全绿（--exclude adm4-desktop，T11 在途），space validate 2596/2602 点 OK。e2e 三处夹具跟随迁移更新——前两处仅加 16 入口点豁免（走 authoring_set_not_applicable，署名 e2e），断言零改动；第三测试更名 workbench_aggregates_work_on_migrated_space 并按迁移语义更新期望，未迁移形态覆盖由 organization 单元测试保底）**：16 领域/104 节点/2575 通用决策点（全部 multi+allow_primary）/12902 选项/25 份 Certified 模板（64395 答卷）/皮肤词表 50 词；"未分域"清空（u.* 4 点与两包 40 点全部补 node_id）；space validate 双包 OK（2596/2602 点）、冒烟退出码 0（§5b 新增 16 入口点豁免）、R5 全文扫描零命中。id 规则 v2.<节点>.<项>.<组>；level 只用 L3/L4（L2 会让上千点无条件激活，偏离任务卡有理，已接受）；L4 统一 spec_role=profile（无效果语义不发明，R2）；每领域一条顺序 unlocks 链、入口点 baseline。未迁清单三类留档（画像取值 280、节点文本 2575 条含真实游戏名、软冲突规则 6+81）——明细在 tools/v2_migration/unmigrated_report.json。迁移工具 tools/v2_migration/（幂等 Python 生成器 + 校验脚本）。
**T10 遗留裁决**：① 3 个 e2e 红测试（真实空间当夹具，迁移后必红）→ 已派小修 agent 按 T10 方案修复（16 入口点豁免 + 第三测试断言意图更新）；② PointRequirement::Optional（2575 建议点不该全员强制——V2"建议巡视"语义还原的关键）+ TemplateAnswer 多选 + 通用模板跨包预填 + u.* 画像选项集补齐（D4 允许增选项，可回收 280 条未迁答案）→ 待 T11 落地后开 F3 模型缺口修复；③ CLI 多选子命令 → 并入 T12；④ 节点级并行 unlocks、软冲突/警告级关系、通用层规则容器 → 设计债记录在案。

**T10/T11 任务卡要点**：T10 全量迁移 16 领域（映射裁决：V2 每个 L4 选项组→一个 V4 决策点，id 形如 v2.<节点>.<项>.<组>，level 按 role_class 分配 L2/L3/L4/L5）+ 26 内置模板批量认证迁移 + 皮肤词表 + 跨层规则；可写 knowledge/tools。T11 按 V2 main.slint 重做六视图+三栏工作台（设计工作台完整，其余五视图骨架，运行日志可接数据），可写 apps/adm4-desktop + NaJustification 合并授权。两者范围零交集。

## 3. 本轮架构裁决（对后续开发有约束力）

1. **通用层品类选项不得 unlock 品类包内的点。** 原 `u.genre/lane_defense` 直接 unlock `ld.*` 四点，装配任何第二品类包必然悬空 BLOCKED。裁决：删除跨包 unlocks；包内 L3 点作为根点随包加载自然激活（`applicability.rs`：非 unlock 目标即根点恒激活）；校验器保持严格、不加跨包豁免；loader 不改。已落地并回归通过。
2. **L6 深化链经"进阶选项"进入**（T1）：新 L6 点挂在新增的进阶选项（如 `guard_table_advanced`）的 unlocks 下，而不是挂在既有已选选项下——否则会改变既有 e2e 的完备度分母。扩展 e2e 覆盖新链时需选进阶分支。
3. **关卡表归 L6 单点**（T2 遗留裁决）：Table schema 同时承载结构与行数据，不拆冗余 L5+L6 两点，与设计文档 01 层级定义一致。
4. **既有决策点/选项/阶段 id 只增不改不删**（设计决定 D4，e2e 与存档按 id 锚定）。

## 3.5 W4 验收发现与裁决（2026-08-29 晚）

**T8b 红线审查结论**：R1/R4/R5/R7 默认路径完全生效；无"AI 失败转成功/无证据指标/参考名入产物"级实质违规。需修项（已全部并入 W5-F1，裁决=修复）：
- R6 路径级失效：`c3_content.rs` 缺基数期望键默认 `0..usize::MAX`；grid_strategy 缺 `asset_specs` 键 → 战棋 C3 基数门永不触发。
- R2 三处 AI 解析吞错：`c1_validation.rs` 73/88、`freeze.rs` 407/419——severity 缺失默认 warning（blocker 可静默降级）、findings 缺键视为零发现。
- R3：`verify_review_batch` 零调用（需接线 C1 红队/冻结门4/逆向 S3）；`confirm_human_gate` 不校 actor 非空；S3 无机器证据。
- 工程规范 §2：非测试代码 4 处 expect()（含 adm4-ai/testkit.rs 锁、拓扑排序 child indexed）。
- 三条既有待办全部裁决立项：跨表外键规则（最实质，悬空行引用可进 FrozenDesign）→ F2；选项≥2 机器化 → F2；S3 哈希证据 → 并入 F1。

**T8a 裁决**：`space validate` 含 BLOCKED、`project doctor` 含 PROBLEM 时必须非零退出 → 并入 F1。

**W5 计划**：F1（红线修复，单 agent 串行，Opus 5）→ 门禁 → F2（校验器立项：跨表外键 consistency 规则 + 选项≥2 机器化 + 两包数据补规则声明）→ 门禁 → 交付报告。

**F1 验收结论（2026-08-29 19:45）**：五项全部完成。R6 缺基数键即 blocked + grid_strategy 补 `asset_specs: 8..42`；R2 两处 parse_findings 收紧（findings 缺键/severity 缺失或非法 → Err，显式空数组合法）；R3 三处接线（FrozenDesign 新增 `red_team_proof` 字段供 C1 与冻结门红队合批哈希互异检测、复读即 rubber stamp 判 fail；`confirm_human_gate` actor 非空强制；Template 新增 `mapping_hash`/`crosscheck_proof` 落 S3 双会话哈希证据，均 serde(default) 兼容旧档）；非测试 expect() 清零 4 处（desktop build.rs 的 expect 保留，构建脚本口径）；space validate/project doctor 命中问题即非零退出。测试 48→61（新增 13），无断言改弱，e2e/冒烟脚本无需补键。主开发复跑 fmt/clippy/test/space validate/冒烟全绿后验收。

**F1 遗留裁决**：① 空答卷 S3 快路径不留哈希证据 → 接受（无 AI 会话则无复读可防）；② finding 的 id/target/text 吞错（unwrap_or("")）→ 并入 F2 收紧（blocker 必须有 text，id/target 必填）；③ ai doctor 退出码 → 并入 F2；④ S3 兼容性收紧（旧 Mapped 模板需重跑 S2）→ 接受，仓内无受影响数据。

**F2 任务范围（已派发）**：跨表外键 row_reference 规则（decision 规则体系 + 冻结门 2 接线 + 两包补声明）、选项≥2 机器化（validate_graph + 存量约 5 个单选项点补真实选项）、finding 字段收紧、ai doctor 退出码。

**F2 验收结论（2026-08-29 20:08）**：四项全部完成，测试 61→80（+19），主开发复跑五项门禁全绿。要点：
- `adm4-decision/consistency.rs` 新增 `row_reference` 规则（源表列 ⊆ 目标表键列），接线完整度 + 冻结门 2 + 清单结构校验（列名必须真实存在）；违规信息点名"哪张表第几行哪个值"。两包共补 9 条外键声明（lane_defense 5 / grid_strategy 4）。
- `validate_graph` 新增 `insufficient_options`：非表格点选项必须 ≥2；lane_defense 补齐 5 个存量单选项点（均真实设计分支，无占位敷衍，R5 合规）；`_template/pack.blank.json` 与 README 同步。
- 两处 `parse_findings` 收紧：id/target 必填、blocker 的 text 不得为空；`AuthoringState::Finding` 新增 target 字段（serde default 兼容旧档）。
- `ai doctor` 命中 [BLOCKED] 非零退出；冒烟脚本新增该负例断言。

**F2 遗留裁决**：① `freeze check` 打印 [BLOCK] 仍退出 0 → 统一修（已追加补修任务给 F2 agent）；② 目标侧选 custom 选项判违规 → 接受 fail-closed 口径（R2），不降级为 warning；③ 完成度侧适用性裁剪（Inactive/未确认点不重复报）→ 接受，冻结门 2 全量兜底。

## 4. 已登记待办（裁决状态见 §3.5）

- ~~跨表外键一致性规则~~ → 已裁决立项，归 **F2**。
- ~~选项≥2 机器化~~ → 已裁决立项，归 **F2**。
- ~~S3 两会话哈希证据~~ → 已裁决立项，归 **F1**。
- `InterviewTurn` 枚举未派生 serde；T6/T7 若需序列化访谈回合需包装 DTO（T5 可能已处理，见其报告）。
- Phase 2（P0-P5 EXE 产线）按设计决定 D16 本期不立项。

## 5. 恢复开发的下一步（W5 修复波次）

1. **F1 红线修复**（进行中，Opus 5 单 agent）：R6 缺键即 Err + grid 补 asset_specs、R2 三处缺键即 Err、R3 接线 verify_review_batch + actor 非空 + S3 哈希证据、4 处 expect() 清理、CLI 两处退出码。可写范围含 crates/apps/grid pack/冒烟脚本（输入合同收紧允许更新测试脚本，禁改弱断言）。
2. F1 报告回来后：主开发复核 + 复跑全量门禁与冒烟；通过后派 **F2 校验器立项**（跨表外键 consistency 规则 + 选项≥2 机器化 + 两包数据补规则声明/单选项点补齐）。
3. F2 通过后产出最终交付报告，全部记忆文档收尾更新。
4. 子 agent 任务卡模版：必读 `docs/plan/03_工程规范.md` 全文 + 对应任务卡 + 相关设计文档节；写明可写范围、上游 API 事实、自验收命令、报告格式。子 agent 模型统一 Claude Opus 5 Thinking（用户指令）。

## 6. 门禁基线（暂停时点）

- W1 关闭时：`cargo fmt --check` 无 diff；`cargo clippy --workspace --all-targets` 零警告；`cargo test --workspace` 39 通过 0 失败；`space validate`：grid_strategy 27 点 OK、lane_defense 21 点 OK。
- T5 验收后的最新基线见文末补录。

## T5 验收结果（暂停前最后一个任务）——✅ 验收通过（2026-08-29 18:27）

**主开发门禁**：fmt 无 diff；clippy --workspace --all-targets 零警告；`cargo test --workspace` 全套件 ok 零失败（**43 个测试** = W1 基线 39 + T5 新增 4）；双包 validate OK。RunLog 类别 `"template"`/`"interview"` 与方法落位已代码抽查。

**T5 交付内容**（只改 adm4-app）：
- 逆向产线五步 + 对照：`template_new_draft` / `template_search_corpus`（候选池按 source_url 去重，持久化于 `<design_space_root>/<pack>/references/.candidates/<id>.json`，可多轮）/ `template_map[_with]` / `template_cross_check[_with]` / `template_review` / `template_certify` / `template_compare`（只读）。
- 访谈四命令：`interview_next[_with] -> InterviewTurnDto`（serde tag `"turn"`：structural_point / table_proposal / complete）/ `interview_confirm(archive_id, &InterviewProposal, overrides) -> Vec<String>`（返回待填清单，非空不阻断）/ `interview_reject` / `interview_progress`。
- `*_with` 变体收 `&dyn AiProvider` 供脚本测试；无后缀版内部 `build_provider()`。
- 新 DTO：`InterviewTurnDto` / `TemplateComparison` / `TemplateCompareEntry`；`RunLogEntry` 补导出。
- 新 e2e ×2：全链（逆向认证→词表断言→预填→换皮门拦模板名理由→访谈补齐含整表提案+例外下钻+拒绝重提→冻结五门→C0-C6 含人工门）+ 负例（Draft 模板预填 Blocked、未检索先映射被拒、重复建草稿被拒）。既有 e2e 零改动。
- 指纹策略：新命令全走 `with_project` 事务（commit_draft 自带指纹刷新）或写设计空间目录（不属存档），无需额外 refresh——与既有约定一致。

**T5 给 T6/T7 的注意事项**（派发 W3 时抄进任务卡）：
1. confirm/reject 必须绑定用户手势，收到提案后不得自动调用（D11）。
2. `interview_next` 返回的 proposal 必须原样传回 `interview_confirm`。
3. lane_defense 同层拓扑序中 `ld.counter_matrix` 先于名单表被提案，UI/冒烟脚本可用"拒绝"先填名单（e2e 已示范该模式）。
4. 预填理由固定含模板游戏名，认证后必被换皮门拦截属预期——UI 应引导用户重写理由。
5. `TemplateMode::Compare` 状态位不落盘，对照是无状态查询（`template_compare`）。
