# T-W7-3d AI 访谈三段式升级（概念/组合/机制访谈）——断点申报

任务：三段访谈（概念/组合/机制）+ 大战略嗅探（|H|>4 逐重核）+ 融合型嗅探（X+Y 双核并集）+ services 收口 + CLI + scripted schema 文档。

## 里程碑记录

- [2026-09-05 开工] 基线 `cargo test --workspace` 559 全绿确认。开始速读必读材料（W7 定稿 §9.2b/§4.2c、13_W7执行计划 §4、interview.rs、services.rs interview 段、3a/3b 申报尾部接口契约、system_module.rs PromptLibrary、seed.json、knowledge/systems 四模块、adm4-ai provider 形态）。
- [2026-09-05 调研完毕，设计定案]（接力者以此为准）：
  1. **purpose 键五个**：`interview_concept`（概念提案）/`interview_concept_tier`（逐重核档位理清）/`interview_composition`（违例解释+修复选项）/`interview_mechanism`（实例范围逐点提案，弹药注入）/`interview_mechanism_custom`（custom 草案起草）。schema 文档落 docs/design/08_AI访谈三段式与Scripted脚本schema.md。
  2. **概念访谈落点**：新文件 adm4-authoring/src/concept.rs。ConceptProposal{systems[{instance_id,module_id,suggested_tier,core_link,rationale,noun_bindings,clarified}],library_external[],core_loop[{verb,instance_id}],fusion{cores[],transition},notes,heavy_core_candidates,per_heavy_core_mode,hints}。提案由调用方持有（proposal 原样传回 confirm 的既有纪律），propose/clarify 不写状态。校验：module_id 必须在模块目录（发明即 Err）、tier 必须在该模块阶梯、core_link 枚举、core_loop/fusion 动词绑定必须指向提案或 pack 既有实例。
  3. **noun_bindings 自动推导**（确定性，非 AI）：显式绑定优先（校验目标 = pack 核心名词或 `<提供实例>.<名词>` 且提供方 provides）；未显式时——带点名词 `sys.X.n` → 组合内唯一 sys.X 实例绑 `<inst>.n`，无实例但 n∈core_nouns 绑核心名词，歧义/缺失 Err 点名；裸名词 → 模块自身 provides 则自绑 `<self>.n`，否则 core_nouns，否则 Err。
  4. **大战略嗅探**：重核候选 = 建议档（或理清后档）rating.total()≥9 且 core_link∈{core,strong}；候选数 >4 → per_heavy_core_mode=true + hints 提示（不设阻）；clarify_tier 逐系统落 TierClarification{tier_id,rationale,user_answer}；confirm 前置：heavy 模式下全部候选必须已理清。总体形态确认仍走既有 compose_confirm_form（概念确认不代签）。
  5. **确认落盘**：新持久化文件 content/system_refs.json（Vec<SystemRef> 项目私有实例引用，与 system_modules.json 私有模块同款纪律）；services.project_space_at 合并读取（extra_refs = system_refs.json + system_modules.json 实例）；确认事务内：写 refs → 重装空间 → 逐系统 select+confirm `<inst>.tier`（Provenance::AiInterviewConfirmed，rationale=理清/建议理由）→ state.core_loop 落盘 → transcript 留痕。
  6. **core_loop 回填（3b 遗留①）**：AuthoringState +`core_loop: Vec<CoreLoopVerb{verb,instance_id}>`（serde default+skip 空，I2）；compose.rs 的 assess_composition 增参数 core_loop_verbs 回填 CompositionInput（3b 标注的单点）。
  7. **组合访谈**：新文件 compose_fix.rs。CompositionFixProposal{explanation,options[{option_id,kind,…}]}，kind∈{tier_change,confirm_form,replace_system,add_binding}；apply：tier_change 走既有 select 链路（用户选定=手势）；confirm_form 要求 --signer 转发 compose_confirm_form（AI 不代签）；replace_system/add_binding 校验+呈现但不自动执行（Err 指路既有通道，报告申报遗留）。
  8. **机制访谈**：interview.rs 加 propose_next_scoped（pending 过滤 `<instance>.` 前缀，复用同层选点/解析/transcript）；PromptLibrary 弹药按实例 module_id 过滤注入 user_prompt；ScriptedProvider 加调用记录（calls()）供断言 prompt 含弹药；custom 草案 AI 起草 → 解析为 CustomMechanicDraft → validate_draft 预校验 → 仍走 custom_add 用户确认。
  9. PromptLibrary 路径：`<system_modules_root>/../prompt_library/seed.json`，文件缺失=空库（弹药是增强不是前提），JSON 坏 → Err。
  10. CLI：interview 组加 concept/concept-clarify/concept-confirm/mechanism/draft-custom；compose 组加 fix/fix-apply。
  下一步：state.rs CoreLoopVerb → adm4-ai 调用记录 → compose.rs 回填 → concept.rs。
- [里程碑 1] state.rs（CoreLoopVerb + AuthoringState.core_loop 条件键）、adm4-ai testkit（ScriptedProvider.calls() 调用记录）、compose.rs core_loop_verbs 回填（assess_composition +core_loop 参数，engine.composition_assessment 传 state.core_loop）落盘，`cargo test --workspace` 560 全绿（+1 为 core_loop_verbs_flow_into_composition_input）。**环境坑申报**：PowerShell 管道 Set-Content 会把 UTF-8 文件写成 GBK——compose.rs 一度损坏，删除后用 Write 全量重写恢复；后续一律不用 PowerShell 改源文件。下一步：concept.rs。
- [里程碑 2] authoring 三个访谈模块全落盘全绿（adm4-authoring 44 测试）：concept.rs（propose_concept/clarify_tier/proposal_to_refs + 9 单测：解析绑定推导/发明模块 id 拒/发明档位拒/悬空动词拒/5 重核切模式/4 重核不切/理清落档+守门+发明档拒+空 rationale 拒/融合解析+悬空核拒/绑定三规则/refs 转换）；compose_fix.rs（propose_composition_fix + FixActionKind 闭集 {tier_change,confirm_form,replace_system,add_binding} + 5 单测：V2 修复选项+prompt 断言/发明实例+档位+种类拒/零违例拒空谈/confirm_form 免实例/留白拒）；interview.rs +propose_next_scoped（实例前缀过滤+弹药注入 user_prompt）+draft_custom_mechanic（GWT 三段非空+host 以调用方为准）+2 单测。engine.rs +set_core_loop/append_interview_entry。
- [里程碑 3] services.rs 收口 + CLI 完成：新持久化 content/system_refs.json（read_project_system_refs，project_space_at 与私有模块并读进 extra_refs）；新门面 9 个：interview_concept[_with]/interview_concept_clarify_with/interview_concept_confirm（事务：refs 落盘→重装校验→逐实例 tier select+rationale+confirm（Provenance::AiInterviewConfirmed）→core_loop 落盘→transcript 留痕）/interview_compose_fix[_with]/interview_compose_fix_apply（tier_change 走 select 链路；confirm_form 强制 signer；replace/binding 显式拒并指路）/interview_mechanism_next[_with]（弹药 = seed.json 按实例 module_id 过滤）/interview_mechanism_draft_custom_with。PromptLibrary 路径 = system_modules_root 兄弟目录 prompt_library/seed.json，缺文件=空库。CLI：interview +concept/concept-clarify/concept-confirm/mechanism/draft-custom；compose +fix/fix-apply；INTERVIEW_HELP/COMPOSE_HELP/usage 更新；adm4-cli +serde 依赖（workspace 内非新第三方）。
- [里程碑 4] app 级 e2e 一次全绿（interview_three_stage_e2e.rs 4 测试）：①full_three_stage_interview_chain（概念确认落盘断言 4 实例 tier+rationale+core_loop+transcript→真实 V2 违例→组合访谈解释+prompt 断言→选升档→违例消除→机制访谈范围断言+弹药问句"背包容量给多紧"进 prompt 断言+逐点确认）②heavy_core_mode…（计划 §4 硬断言：5 重核候选→per_heavy_core_mode 切换+提示不设阻→未理清确认拒+点名→逐 5 系统理清（4 重 1 轻）→确认后逐系统档位+rationale 落盘断言+理清降档覆盖建议档+用户原话留痕）③fusion_sniff（X+Y→双核并集分解结构断言+可正常确认落盘+留痕）④ai_overstep（发明模块/档位→Err+零落盘断言；custom 草案 GWT 缺段拒）。夹具坑：通用层必须 L0/L1/L2 齐备（universal.missing_level）。
- [里程碑 5·完工] 全门禁通过：cargo fmt --check 无 diff；clippy --workspace --all-targets 零警告；cargo test --workspace 全绿 **589**（基线 559 → +30 全为新增，零弱化零删除零 ignore）；schema 文档落盘 docs/design/08_AI访谈三段式与Scripted脚本schema.md（五个 purpose 键 + 应答 schema + 绑定推导规则 + 越界防线表）；cli_smoke.ps1 退出码 0（追加 8j 三段访谈段：概念融合嗅探断言→确认落盘→V2 违例→fix/fix-apply 升档→违例消除→机制访谈范围断言→draft-custom；夹具补 knowledge/ 副本与 concept_smoke 包 + config 补 system_modules_root）；desktop 构建成功（零适配——3d 未新增 desktop 消费的枚举臂）；金样 golden_diff.ps1 -SelfTest 三场景过。T-W7-3d 全部完成，待主开发验收。
- 【给后续卡的接口契约】AppServices 新方法：`interview_concept[_with](archive_id, [provider,] pitch) -> ConceptProposal`（只提案）；`interview_concept_clarify_with(archive_id, provider, proposal, instance_id, answer) -> ConceptProposal`（提案原样传回+更新返回）；`interview_concept_confirm(archive_id, &proposal) -> ConceptConfirmReport{instances, tier_selections, core_loop_len}`（用户手势：refs 落 content/system_refs.json + 逐实例 tier 确认 + core_loop 落盘 + 留痕）；`interview_compose_fix[_with](archive_id[, provider]) -> CompositionFixProposal`；`interview_compose_fix_apply(archive_id, &proposal, option_id, signer: Option<&str>) -> String`（tier_change 自动执行 / confirm_form 需 signer / replace_system+add_binding 显式拒并指路）；`interview_mechanism_next[_with](archive_id[, provider], instance_id) -> InterviewTurnDto`（弹药自动注入）；`interview_mechanism_draft_custom_with(archive_id, provider, host_system_id, idea) -> CustomMechanicDraft`（不登记）。CLI：`interview concept|concept-clarify|concept-confirm|mechanism|draft-custom`、`compose fix|fix-apply`。purpose 键：interview_concept / interview_concept_tier / interview_composition / interview_mechanism / interview_mechanism_custom。项目私有实例引用落 `content/system_refs.json`（Vec<SystemRef>，与 system_modules.json 并读进 extra_refs）。
- 【遗留与声明】① replace_system/add_binding 修复选项**不自动执行**（改变装配结构，指路概念访谈重确认 / system_module_add——结构化建议已在提案呈现，符合任务卡"换系统/添加接口边（改绑定）"的提案义务；自动执行归后续波裁决）。② 概念确认落盘的 refs 目前只支持**追加**（实例 id 冲突即拒），撤销/替换实例走手改 system_refs.json + 重开（fail-closed 校验兜底）或后续波加删除入口。③ heavy_core_candidates 按**提案时刻的建议档**冻结——理清降档不缩减候选清单（模式触发是提案时刻事实，理清义务覆盖全部候选；落盘档位以理清为准）。④ desktop 零改动（未触发编译连锁）。⑤ PromptLibrary 路径口径 = system_modules_root 的兄弟目录 prompt_library/seed.json；模块库根被改配置时弹药路径随之移动（同一进程期不变量锚）。
