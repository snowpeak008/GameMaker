# T-W7-3a 断点申报（系统模块加载器与运行时接入）

## 2026-09-03 21:20（里程碑 0：基线确认 + 调研完成）

- 已完成：
  - `cargo test --workspace` 基线全绿，逐套加和 = **513**（12+25+27+1+19+6+6+163+5+8+70+1+47+4+59+1+11+26+12+11 = 513，另有若干 0 用例空套）。
  - 必读清单读完：定稿 §2.5/§5.1/§5.2/§5.4/§5.6 末条；system_module.rs（镜像 ConsistencyRule 与 adm4-space 版逐字节同形，serde 往返可转）；model.rs/loader.rs；freeze.rs 506-578（custom_points 条件键先例）；c0_compile.rs graph 臂（结构化 Err + 锁定测试在 1131 行附近）；services.rs load 段（space_cache 按 pack_id 缓存、frozen_space 用 custom_points.json 增广）；config.rs（AppConfig 无 deny_unknown_fields，default fn 先例 design_space_root）。
- 已落盘文件：仅本申报文件。
- 设计决定（接力者请沿用，除非发现矛盾）：
  1. `SystemRef` 落在 adm4-space model.rs（space 依赖 decision，可用 `adm4_decision::system_module::CoreLink`）。字段：instance_id / module_id / version_req（无第三方依赖，手写 semver 匹配：空=任意，精确=相等，`^x.y.z`=同主版本且 ≥ 基准）/ allowed_tiers（空=全部档位）/ noun_bindings: BTreeMap<名词, 绑定目标> / core_link: CoreLink。GenrePack 加 `system_refs: Vec<SystemRef>` + `core_nouns: Vec<String>`（V6 的「pack 核心名词」需要声明位，均 serde default）。
  2. 加载器：adm4-space 新文件 system_loader.rs；纯函数 instantiate 接「已加载模块表」，fs 解析单独函数；`load_design_space_with_modules(root, pack_id, module_dirs: &[PathBuf])`，旧 `load_design_space` 委托（空 dirs，旧包行为不变）。实例化产物直接并进 pack.decision_points / cardinality / consistency_rules（内存态，不落盘），genre_scope 重写为 Pack(pack_id) 以过既有校验。
  3. 名词绑定 V6：consumes∪modifies 每个名词必须有绑定，绑定目标 = pack 核心名词 或 `<提供方实例>.<名词>`（该实例模块 provides）。悬空 → Err 点名实例/端口/名词。
  4. tier 合成点 `<instance_id>.tier`：L3 单选、requirement=Unlocked（非 unlock 目标 → 恒 Active 进分母）；每允许档一个选项；unlocks = tier.activates（累计口径）∪ 所有 tier_gate.rank ≤ 本档 rank 的点（重写后 id，BTreeSet 排序确定）。
  5. DesignSpace 加运行时字段 `system_instances: Vec<SystemInstanceInfo{instance_id,module_id,semver}>`（DesignSpace 非 serde，测试字面构造需机械补字段——helper 适配允许）。
  6. 冻结哈希：payload 条件键 `module_versions: BTreeMap<module_id, semver>`（pack.system_refs 非空才进）；FrozenDesign 加同名字段 `#[serde(default, skip_serializing_if = "BTreeMap::is_empty")]`（旧档零漂移）；frozen_space 复演时比对当前加载模块 semver 与 frozen.module_versions，不符 → Err（fail-closed）。**不做模块文件快照**：semver 进哈希已锁语义，版本漂移在复演路径显式报错而非静默换语义。
  7. GraphSchema 落 adm4-decision types.rs：node_payload/edge_payload（Vec<ScalarField>）、directed、acyclic、entry（自有枚举 GraphEntryConstraint，decision 不能依赖 spec）、cardinality_key；校验纯函数（端点已声明 + acyclic 拓扑检查，Kahn）。ParameterSchema 加 `Graph(GraphSchema)`（tag=graph）。C0：graph 值沿 Curve 先例走标量 `graph` 键装 GraphSpec JSON（ParameterValues 零改动），C0 以 schema 为真相覆盖 directed/acyclic/entry 并产 GraphSpec 进 GameSpec.graphs。
- 下一步：GraphSchema（adm4-decision）→ C0 graph 臂 → adm4-space SystemRef/加载器 → freeze → app 接线 → 测试与门禁。
- 尚未跑的门禁项：fmt/clippy/golden_diff.ps1/cli_smoke.ps1/space validate（改动完成后统一跑）。
