# T-W7-3a 系统模块加载器与运行时接线 — 断点申报

接力者须知：本文件按时间追加，每行 = 时间戳 + 已完成 + 下一步。以代码落盘为准。

- 2026-09-03 21:03 开工。已创建本文件。下一步：跑基线 cargo test --workspace 确认 513 全绿，同时速读必读清单。
- 2026-09-03 21:15 基线确认：cargo test --workspace 全绿，各套件加和 = 513。必读清单读完。发现同目录有前次接力的 `3a_断点申报.md`（只完成调研、无代码落盘，git 干净），其设计决定基本沿用：SystemRef 落 adm4-space/model.rs；加载器新文件 adm4-space/system_loader.rs；semver 手写匹配（空=任意/精确=相等/^x.y.z=同主版本且≥基准）；冻结哈希条件键；GraphSchema 落 types.rs。
- 2026-09-03 21:15 关键偏差申报：任务④要求给 ParameterSchema/ParameterValues 加 Graph/Curve tag 分支，但 adm4-decision/completeness.rs（不在可写清单）与 apps/adm4-desktop（禁止改）存在对这两个枚举的**穷尽 match**——不加 match 臂无法编译（验收 1/7 又要求全 workspace 编译+desktop build 成功）。裁决：对这些文件做**纯机械新增 match 臂**（零行为改动），在报告里逐处申报。
- 计划顺序：④ types.rs Graph/Curve → 修编译 → C0 真实编译翻转 → ① adm4-space SystemRef/加载器 → ② tier 合成点 → ③ freeze module_versions → ⑤⑥ services.rs → 门禁。
- 下一步：写 types.rs 的 GraphSchema/CurveSchema/GraphEntryConstraint + ParameterValues::Graph/Curve + 校验纯函数 + 测试。
- 2026-09-04 22:55 第三任接力开工。基线 cargo test --workspace 全绿，逐套加和 = 517（>513：并行卡 3c 已落盘 knowledge/systems JSON 与 adm4-decision 侧 4 个测试，符合任务卡备注，基线成立）。验证式速读完成，沿用前任全部设计决定，无矛盾。计划顺序照旧：④ Graph/Curve → ①② SystemRef/加载器/tier 合成点 → ③ freeze → ⑤⑥ services → 门禁。下一步：types.rs 落 GraphSchema/GraphEntryConstraint/Curve 分支+校验纯函数。
- 2026-09-04 23:20 ④ Graph/Curve 完成：types.rs 落 GraphSchema/GraphEntryConstraint/CurveSchema + ParameterSchema::Graph/Curve（tag=graph/curve，旧 tag 零改动）+ validate_graph_value 纯函数（端点/环 Kahn/单入口/必填负载）+ 7 个单测；c0_compile.rs 翻转 graph 臂为 compile_graph（schema 为真相覆盖 directed/acyclic/entry，id 取决策 id，产 GraphSpec 进 GameSpec.graphs，source_map 登记 graphs/ 路径），波 1 锁定测试按豁免翻转为正路径 + 保留悬空端点/环 2 个负测试 + 缺键缺 schema 负测试。机械 match 臂 3 处：completeness.rs（Graph/Curve 空臂零行为）、interview.rs schema_hint+check_parameter_shape（未预见的第 3 处编译连锁，同性质机械臂）、desktop main.rs（JSON 编辑标签）。decision/pipeline/authoring 测试全绿。下一步：①② adm4-space SystemRef+system_loader.rs。
- 2026-09-05 01:15 第四任接力开工。基线：3 个测试文件缺新字段编译失败（前任断点如实），机械补齐（GenrePack +system_refs/core_nouns、DesignSpace +system_instances，共 3 文件 6 处）后 cargo test --workspace 全绿（逐套加和 517）。验证式速读 system_loader.rs/loader.rs 完成，沿用前任 7 条设计决定（3a_断点申报.md 2026-09-03 21:20 节）无矛盾。任务 A 完成。
- 2026-09-05 01:50 ③ freeze module_versions 完成：FrozenDesign +`module_versions: BTreeMap<String,String>`（serde default + skip_serializing_if 空表，旧档零漂移）；execute_freeze 从 engine.space().system_instances 收集 module_id→semver，payload 条件键（custom_points 先例同款，无模块逐字节零漂移）；4 个新测试全绿（负测试逐字节比对扩展前 payload 哈希/serde 略空键+往返/同版本哈希稳定+semver 变哈希变/多实例单条目）。附带：c0_compile.rs 测试夹具 frozen_raw 机械补 module_versions 字段。
- 2026-09-05 01:50 ⑥ services.rs load 段收口完成：AppConfig +`system_modules_root`（default fn "knowledge/systems"，design_space_root 先例同款）；AppServices +system_modules_root 进程期不变量（reload_config 改动即拒）+module_dirs()（目录不存在给空清单，引用方 fail-closed 点名）；load_space_shared 改走 load_design_space_with_modules；loader.rs 加载时序优化：无 system_refs/私有模块时不读模块目录（旧包零接触，库内坏 JSON 不拖垮无关包）。既有方法签名零改动。e2e 的 7 处 AppConfig 字面量补字段。
- 2026-09-05 01:50 ⑤ 系统级 custom module 完成：adm4-space +`load_design_space_customized`（库模块 + 项目私有模块表 + 额外 SystemRef；私有遮蔽库模块即 Err）；services.rs +`system_module_add`（SystemModule::validate → 实例一致性 → 重复登记拦截 → 整装校验走加载器同一套代码 → 引擎复核 → 持久化 content/system_modules.json 事务提交）+`system_module_list`；open_engine/with_project/frozen_space 全部改走 project_space_at（无私有模块 = 缓存句柄零开销）。
- 2026-09-05 01:50 ③ 复演 fail-closed 完成：frozen_space 比对 frozen.module_versions 与当前装配 system_instances，模块缺失/semver 漂移 → blocked 点名模块与两侧版本。
- 2026-09-05 01:50 加载器补强（前任未覆盖）：allowed_tiers 收窄时 tier_gate 高于最高允许档的点剔除（否则成"无人 unlock 的根点"恒 Active 进分母）+ 引用它们的一致性规则与 unlocks 连带剔除；模块决策点 compiler_tags["system"]==module_id 时重写为 `<instance>.tier`（否则 C0 mechanic_dangling_system 拦死全链）。下一步：任务 E app 级 e2e 测试（含 system_refs 的 pack 全链/双 pack 不同档/双实例/失败点名/Graph 全链）。
- 2026-09-05 02:40 任务 E 完成：adm4-authoring/tests/module_versions_freeze_hash.rs（4 测试）+ adm4-space system_loader.rs 单测 mod（8 测试：semver 三形态/命名空间重写+tier 合成点/双实例共存+实例 id 重复拒/allowed_tiers 收窄剔除/版本失配点名/绑定悬空三分支点名/门控矛盾点名/模块缺失点名）+ adm4-app/tests/system_modules_e2e.rs（4 测试：双 pack 不同档激活集与分母（5→8 / 15 逐点断言）/含模块 pack 全链（建项→tier→确认→冻结 module_versions 落盘→C0 模块机制归属 tier 合成系统+GameSpec.graphs 非空 directed/acyclic/single+R4 source_map graphs/ 路径→C1 全绿）+ semver 漂移复演 blocked 点名两侧版本/app 级三失败点名/私有模块登记正反例）。全部绿。
- 2026-09-05 03:10 任务 F 全门禁通过：fmt --check 无 diff；clippy --workspace --all-targets 零警告（顺手清了自己引入的 3 处 doc/clone 警告）；cargo test --workspace 全绿 548（基线 517 → +31 全为新增，零弱化零删除）；space validate 双包 OK（grid_strategy 2610 / lane_defense 2604）；cli_smoke.ps1 退出码 0；desktop build 成功；金样：用当前代码重跑 golden_make 后与既有金样做 golden_diff 结构化比对 = 干净（漂移仅 C0/C1 contract 尺寸差，全部命中既有 3 条豁免，无新增豁免项），SelfTest 三场景过，金样目录已恢复原字节。T-W7-3a 全部完成，待主开发验收。
