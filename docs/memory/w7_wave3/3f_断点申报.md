# T-W7-3f PromptLibrary 种子化 — 断点申报

## 2026-09-03 半程申报

- **进度**：约 50%。发现工区已有一版 seed.json（30 条）与永久校验测试 `tests/prompt_library.rs`（基线全绿，70+1 通过）。已按本卡规格改写 seed.json：
  - id 统一为 `pl.<域>.<序号>` 格式（原为下划线自由命名）；
  - domain 规范化：首批四模块用 `sys.equipment/sys.inventory/sys.loot/sys.economy`，跨系统条目改用领域词 `combat/progression/meta/balance/content`（原为 `gameplay_system_design` 等 v2 层名）；
  - 全部 follow_ups 补齐至 ≥2 条（原 6 条只有 1 条追问）；
  - source_ref 保持 `v2:<决策点id>` 可溯源格式不变。
- **文件清单**：
  - `knowledge\prompt_library\seed.json`（改写，30 条）
- **下一步**：新增 `crates\adm4-decision\tests\prompt_library_seed.rs` 结构自验（条数 20-30、id 唯一、字段非空、follow_ups ≥2、四首批 domain 各 ≥3），跑单测与 fmt --check。

## 2026-09-03 完成申报

- **进度**：100%。全部验收标准通过：
  - `cargo test -p adm4-decision` 全绿（70 单元 + 1 溯源测试 + 3 新增结构自验）；
  - `cargo fmt --check -p adm4-decision` 无 diff；
  - seed.json 确认 UTF-8 无 BOM，30 条，id 全部 `pl.<域>.<序号>` 格式。
- **文件清单**：
  - `knowledge\prompt_library\seed.json`（改写：id 格式化、domain 规范化、follow_ups 补齐至全条 ≥2）
  - `crates\adm4-decision\tests\prompt_library_seed.rs`（新建：3 个测试——条数与 id 唯一性、必填字段与追问下限、首批四模块覆盖 ≥3）
- **domain 分布**：sys.equipment 5 / sys.inventory 4 / sys.loot 5 / sys.economy 5 / combat 3 / progression 2 / balance 3 / content 2 / meta 1，合计 30。
- **来源可溯性**：30 条全部 `v2:<决策点id>` 引用，且被既有溯源测试（prompt_library.rs）校验存在于 universal 层；问句均为基于 v2 决策点素材的取舍化改写（v2 原句多为开放描述题，本库改写为二难/档位选择题并配追问），无定稿锚引用、无编造 id。
- **下一步**：无。卡完成，等待 3d 机制访谈卡消费弹药；波 4 做全量去重聚类时以本种子库 id 格式为准。
