# 会话记忆（2026-08-29）

> 本文件记录本次对话的来龙去脉与用户意图，供后续会话还原上下文。

## 用户指令时间线

1. **项目起源（前序会话）**：用户要求开发第四版——完全独立于前三版（Python 一代、`RUST/` 二代 Slint、`NEWrust/` 三代 Tauri+Web），保留二代的设计结构、以 `plan/redesign_v3/` 未开发设计为最终形态；要求实际读代码而非只看记忆文档。主开发深读三版代码 + plan 全部设计文档后，在 `V4/` 建成全新 `adm4-*` workspace（13 crate），端到端链路（建项→创作→红队→冻结→C0-C6→人工门→导出导入）打通，25 测试全绿。
2. **本次会话指令一（协作模式切换）**：第四版视为完全独立路径，之前项目只是参考。先产出设计计划、开发计划、工程规范、子任务排序；然后主开发只派发任务，子 agent 并行开发、并行验收，**主开发不承担任何开发任务**。
3. **本次会话指令二（暂停令）**：完成当前小任务（T5）后暂停开发，用户要出门；把开发计划、子计划、本次开发内容、开发/对话/内容记忆全部留在 `V4/` 文件夹内。

## 主开发的执行方式（本次会话）

- 产出计划四件套 `docs/plan/01_设计计划 / 02_开发计划 / 03_工程规范 / 04_子任务排序与派发`，16 条设计决定（D1-D16）、W1-W4 波次、T1-T8 任务卡。
- W1 四个子 agent 并行派发（塔防包深化 / 战棋包新建 / 逆向产线 / 访谈分层），每个任务卡包含：必读文档清单、可写文件范围、上游 API 事实、逐条验收标准、自验收命令、报告格式。
- 验收方式：子 agent 自验收 + 主开发抽查关键声明（代码级 grep/read 核对红线相关逻辑）+ 波次统一门禁（fmt/clippy/test/space validate）。
- 过程纠偏一次：T1/T2 同时上报 universal 跨包 unlock 悬空问题，主开发裁决"通用层不 unlock 包内点"，计划外派发修复 agent（只动 `universal/core.json`，与在跑任务写入范围不相交）。

## 关键经验（对后续派发有用）

- 并行任务共用 `target/` 会偶发 LNK1104 链接文件锁，重试即过，非代码问题。
- 数据任务（T1/T2）与 crate 任务（T3/T4）并行安全；涉及共享文件的修复必须确认与在跑任务的可写集不相交后再派。
- 子 agent 报告质量高的共性：任务卡里写死"验收标准逐条自评 + 自验收命令输出摘要 + 遗留与声明"三段式。
- T3/T4 报告中主动给出的门面方法签名建议直接变成了 T5 任务卡的接口契约，这个"下游承接声明"机制值得保留。

## 与 V4 外部的关系

- 工作区级跨会话记忆在 `C:\Users\admin\.claude\projects\E--workwork-CrewAi-AutoDesignMaker\memory\`（索引 MEMORY.md，条目 v4-standalone-rust-rebuild.md）；V4 内本目录是其项目内副本与细化，两处以 V4 内为准（用户要求记忆留在 V4 内）。
- V4 尚未做任何 git 提交（用户未要求）。
