# T13 · V2 能力覆盖矩阵终验报告

> 日期：2026-08-31
> 任务：W6-T13「V2 能力覆盖矩阵终验」（只读走查 + 全量门禁复跑）
> 验收基准：`docs/plan/05_V2能力覆盖矩阵与W6返工设计.md` §2.1 / §2.2 / §2.3 / §4
> 判定口径（§4 原文）：**每一行 V2 能力，在 V4 中要么可直接操作，要么在明确的新功能里可见其归宿；任何一行找不到归宿即验收不通过。**
>
> **总判定：不通过（21 行中 3 行存在无归宿的子能力）。**

---

## 1. 改动清单

本任务为只读验收，**未修改任何代码、数据、测试、设计/计划文档，未执行任何 git 操作**。

| 文件 | 说明 |
|------|------|
| `docs/memory/2026-08-31_T13覆盖矩阵终验报告.md` | 新增（本文件），T13 唯一产物 |

走查中发现的问题一律**只记录不修复**，见 §2.4 缺口清单与 §4 遗留声明。

---

## 2. 设计符合性

### 2.0 关于清单行数的更正

任务卡写「§2.1 表 11 行」，实际计数为 **12 行**（`docs/plan/05` 第 24-37 行，从「16 领域总览卡片 + 进度」到「导出（markdown 等格式）」）。本报告按真实的 **12 + 5 + 4 = 21 行**逐行判定，不做合并省略。

### 2.1 设计工作台视图（12 行）

| # | V2 能力 | 代码级证据 | 可达路径 | 结论 |
|---|---------|-----------|---------|------|
| 1 | 16 领域总览卡片 + 进度 | 数据：`knowledge/design_space/universal/domains.json` 声明 16 个领域（`order` 1-16，从 `product_positioning_design` 到 `launch_readiness_design`）+ 104 个节点。装配：`view.rs::domain_cards`（含 0 点域，保留域「未分域」无点时隐藏）。呈现：`main.slint` 属性 `domain-cards`，左栏 ListView，行内进度条读 `domain.percent`，点击触发回调 `select-domain`。后端聚合：`adm4-decision/src/organization.rs::OrganizationProgress`、`services.rs::organization_progress` / `workbench_overview`。单测：`view.rs::tests::domain_cards_include_empty_domains_and_hide_empty_reserved`、`reserved_domain_is_visible_when_it_has_points` | 设计工作台 → 左栏「领域总览（全部领域，含 0 点域）」 | **通过** |
| 2 | 设计节点列表（roleClass/status/依赖） | 装配：`view.rs::node_cards`（`role_class` → 「角色分类：X」；`counts.confirmed/applicable` + `percent` → 状态文本；`description` → detail）。呈现：`main.slint` 属性 `node-cards` + 回调 `select-node`。数据：`domains.json` 每个 node 带 `role_class`。依赖：`adm4-decision/src/types.rs` 的 `unlocks` + `applicability.rs`，UI 以徽章「未激活」呈现（`view.rs::badge_of`）。V2 对照：`RUST/apps/adm-desktop/ui/main.slint` 的 `DesignNodeRow{id,name,role,status,detail}` 字段一一对得上，V2 同样未在节点行显示依赖 | 设计工作台 → 中栏「节点列表 · 领域「X」」 | **通过** |
| 3 | 三级决策项检查单勾选 | 装配：`view.rs::check_rows` + `badge_of`（已确认/待确认/未选/N/A 豁免/未激活/超深度六态）。呈现：`main.slint` 属性 `check-rows`（标题文案即「决策点检查单（= 二版三级检查单；勾选语义 = 已确认）」）+ 回调 `select-decision`；确认按钮文案「确认此决策点（= 二版检查单勾选）」→ 回调 `confirm-decision` → `main.rs::on_confirm_decision` → `engine.confirm_selection`。单测：`badges_cover_every_applicability_state`、`check_rows_filter_by_node_then_domain` | 设计工作台 → 中栏检查单 → 决策点详情底部「确认此决策点」 | **通过** |
| 4 | L4 选项组（单/多选、必填、设主选、MDA 层、设计提问） | 单/多选：`adm4-decision::SelectionMode` + `view.rs::selection_mode_label`；`main.slint` 属性 `decision-multi` 分叉「选择」/「勾选」按钮。主选：`main.slint` 按钮文案「设主」/「主选」→ 回调 `set-primary-option` → `services.rs::authoring_set_primary_option`；缺主选可见拦截 `view.rs::primary_missing` → 属性 `decision-primary-missing`。必填性：`PointRequirement{Unlocked/Baseline/Optional}` → `view.rs::decision_meta`。MDA：`DecisionPointView.mda_layer` → `decision_meta` 的「MDA X」；数据 `v2_checklist.json` 2575 条**全部**带 `mda_layer`。设计提问：`view.rs::design_question_text` → 属性 `decision-question`；数据 2575 条**全部**带 `design_question`。单测：`primary_missing_only_when_multi_selected_without_primary` | 设计工作台 → 中栏决策点详情 → 选项行 | **通过** |
| 5 | L5 实体 JSON 编辑 | 结构化：`main.slint` 属性 `structured-active` / `table-columns` / `table-rows-model`，回调 `edit-table-cell` / `add-table-row` / `remove-table-row` / `save-table`。高级 JSON：`param-editor := TextEdit` + 回调 `save-params`，双向互切 `set-advanced-mode` → `main.rs::toggle_advanced_mode`（转换失败停留原模式不吞错）。转换层：`apps/adm4-desktop/src/convert.rs`（5 个单测）。矩阵编辑同路径（`ParameterSchema::Matrix`） | 设计工作台 → 中栏决策点详情 → 选项「编辑参数」 | **通过**（V2「清空 L5」按钮无同名按钮，可用高级 JSON 写 `{"values":"none"}` 达成，见 §2.4-L1） |
| 6 | 节点文本（设计说明/风险说明/不适用原因） | 设计/风险说明：`main.slint` 元素 `design-note` / `risk-note` LineEdit + 按钮「保存节点文本」→ 回调 `save-node-notes` → `services.rs::authoring_set_node_design_note` / `authoring_set_node_risk_note`。不适用原因：元素 `na-code` / `na-note` / `na-actor` + 按钮「标记不适用」→ 回调 `set-not-applicable` → `services.rs::authoring_set_not_applicable` → `NaJustification`（理由码+说明+署名三要素强制）；解除走 `clear-not-applicable`。冻结门审计：`freeze.rs` 门 1 产 `not_applicable_exemption` 逐条在案（冒烟输出实测 16 条）。集成测试：`adm4-authoring/tests/exemption_and_node_notes.rs` 5 测 | 设计工作台 → 中栏节点详情（说明）/ 决策点详情底部（N/A 三元组） | **通过**（粒度按矩阵 §2.1 行 6 裁决从节点级下移到决策点级，属既定设计） |
| 7 | 项目画像六字段 | 聚合：`services.rs::project_profile`（L0/L1 已确认决策点，代码零硬编码字段名）。装配：`view.rs::profile_rows`（主选打 ★，hint 带层级/MDA/设计提问）。呈现：`main.slint` 属性 `profile-rows` + `profile-title`。数据：`universal/core.json` 共 12 个 `u.*` 点（10 个 L0：business_model / platform / platform_scope / operation_model / social_model / session_band / target_scale / content_rating / region_scope / dimension；L1：experience；L2：genre）。单测：`profile_rows_mark_primary_and_handle_empty` | 设计工作台 → 左栏「项目画像（L0/L1 已确认 N 项）」 | **部分通过**：卡片机制与聚合口径完备，但六字段中**「美术风格」在 L0/L1 无对应点**（其归宿是 `v2.art_direction_decision.feng_ge_ding_wei.*`，属 L3/L4，按 `project_profile` 的 L0/L1 过滤条件不会上画像卡）；**「目标用户」无单一对应点**（近似散在 `u.content_rating` / `u.session_band` / `u.region_scope`）。见 §2.4-M4 |
| 8 | 26+ 内置游戏模板（载入预填 / 另存） | **载入预填**：`knowledge/design_space/universal/references/*.json` 共 **25 份**，全部 `Certified`，每份 2585 条答卷。取用关卡 `adm4-template/src/library.rs::approved_for_prefill` + `resolve`（跨包落通用层）+ `list_available`；写入 `services.rs::project_prefill_template`；UI 按钮「预填到当前项目」→ 回调 `template-prefill` → `main.rs::template_prefill`。冒烟实测：`template list lane_defense` 返回 26 份（25 通用 + 本轮草稿），`builtin_midcore_arknights` 跨包预填「写入 2585 个决策点（含 7 个附加多选选项），跳过 0 条」。数量对账：V2 `knowledge/design_data/project_templates/` 目录 26 个 json = 1 个 `template_index.json` + **25 份模板**，与 V4 的 25 份**一一对应，无丢失**（矩阵「26+」是约数）。**另存模板**：`services.rs` 无任何「项目 → 模板」导出方法（全量 `pub fn` 清单已核，62 个方法中无此项）；`main.slint` 无对应按钮；CLI `template` 子命令组只有 `list/new-draft/search-corpus/map/cross-check/review/certify/compare`。V2 该能力在 `RUST/apps/adm-desktop/src/main.rs:1449`「已另存为模板」+ 回调 `save-design-template` | 载入预填：设计工作台 → 顶部「模板/逆向维护」覆盖面板 → 左栏选模板 → 「预填到当前项目」。另存：**无路径** | **部分通过**：载入预填完备且数量无损；**「另存模板」零归宿**（无服务/无 CLI/无 UI/无占位说明）。见 §2.4-H1 |
| 9 | 右栏四页签：摘要/缺失项/风险/校验 | `view.rs::summary_rows` / `missing_rows` / `risk_rows` / `validation_rows`；`main.slint` 属性 `right-tab` 与四个按钮「摘要」「缺失项」「风险」「校验」，行模型 `summary-rows` / `missing-rows` / `risk-rows` / `validation-rows`。缺失项带 `target` 可点击跳转中栏（`row.target != "" : TouchArea → select-decision`）。数据源 `services.rs::workbench_overview` 一次取齐四块。单测：`summary_rows_report_totals_and_domains`、`missing_rows_carry_jump_target`、`risk_rows_flag_missing_red_team`、`validation_rows_keep_passed_gate_with_findings_green` | 设计工作台 → 右栏四按钮 | **通过** |
| 10 | AI 访谈（生成问题/记录回答/调用 AI） | UI：`main.slint` 中栏底部访谈区（可折叠 `interview-open`），按钮「下一提案」/「确认提案」/「拒绝」→ 回调 `interview-next` / `interview-confirm` / `interview-reject`；分层进度 `interview-levels`（`LevelRow`）。后端：`services.rs::interview_next[_with]` / `interview_confirm` / `interview_reject` / `interview_progress`；`adm4-authoring/src/interview.rs`（7 单测）。整表提案可编辑参数做例外下钻（`interview-params-editable`）。CLI 同能力：`interview next|confirm|reject|progress`。冒烟实测 10 个回合含拒绝重提与例外下钻 | 设计工作台 → 中栏底部「AI 访谈」区；或 CLI `adm4 interview ...` | **通过** |
| 11 | 存档管理 / 保存 / 载入 / 重置 | 管理面板：`main.slint` 属性 `archive-panel-open`（顶部按钮「存档管理」），项目列表 `projects` + 回调 `refresh-projects` / `create-project` / `open-project`。保存：事务自动保存，`services.rs::with_project` 每次变更原子提交，界面属性 `autosave-text` 明示「事务自动保存：每次变更原子提交（无手动保存按钮）」+ 面板标题同款说明。载入：点击项目行 → `open-project`。**重置**：`services.rs` / `main.slint` / CLI 三处均无任何重置入口，界面亦无「V4 不提供重置」的说明文案，`docs/design/**` 与 `docs/plan/**` 未见裁决记录。V2 该能力在 `RUST/apps/adm-desktop/src/main.rs:1642` 回调 `reset-design-workbench` + `:5221 fn reset_design_workbench` | 存档管理/保存/载入：设计工作台 → 顶部「存档管理」。重置：**无路径** | **部分通过**：**「重置」零归宿且零说明**。见 §2.4-H2 |
| 12 | 导出（markdown 等格式） | 工作台快照：`main.slint` 元素 `export-path` + 按钮「导出快照」→ 回调 `export-workbench` → `main.rs::export_workbench` → `view.rs::workbench_markdown`（档案/画像/领域进度/决策点清单/缺失/风险/校验七段），原子落盘 `atomic_write`。单测 `workbench_markdown_contains_all_sections`。冻结后 C0-C6 产物：`content/pipeline/v{N}/{C0..C6}/document.md`（`runner.rs` 落盘），清点打包 `adm4-app/src/deliverable.rs::DeliverableManifest::build` + `services.rs::deliverable_package`。运行日志导出：`view.rs::log_markdown` + 回调 `export-logs` | 设计工作台 → 顶部「导出快照」；打包阶段 → 「打包文档集」；运行日志 → 「导出」 | **通过** |

### 2.2 其余五个视图（5 行）

| # | V2 视图 | 代码级证据 | 可达路径 | 结论 |
|---|---------|-----------|---------|------|
| 13 | 开发流水线（Step00-14 全列表 / 范围运行·停止·恢复 / Step07 风格人工确认 / 阶段详情·重跑 / 运行日志） | **全列表**：`view.rs::stage_rows` = `design_compile_registry()` 7 段（C0-C6，`segment="C 段"`，`placeholder=false`）+ `phase2_registry()` 6 段（P0-P5，`segment="P 段"`，`placeholder=true`），共 13 行；单测 `stage_rows_cover_c_and_p_segments` 钉 13 行。映射注记 `view.rs::pipeline_note` 逐条给出 Step00-06→冻结门+C0-C4、Step07→C5、Step08-10→C6、Step11-14→P0-P5。**恢复/断点续跑**：`runner.rs::run_range` 对 `state.is_succeeded(&stage.id)` 直接 `continue`；冒烟两轮续跑实测（第一轮停 C5 人工门 → 确认 → 第二轮停 C6 → 签收 → 全绿）。**范围运行**：`runner.rs::run_range(from,to)` + CLI `pipeline run --from --to`；**桌面端只有固定按钮「运行 C0-C6」**（`main.slint` → `pipeline-run()` → `main.rs` 硬编码 `services.pipeline_run(&archive,"C0","C6")`）。**Step07 风格门**：`main.slint` `if stage.waiting : Button{ text:"人工确认" }` → 回调 `pipeline-confirm` → `runner.rs::confirm_human_gate`（署名非空强制，2 单测）。**阶段详情**：右侧列表逐段显示 `segment/id/name/summary/status`；**无产物入口**（`services.rs` 无读取 `document.md`/`contract.json` 的查询方法）。**停止**：无（`pipeline_run` 同步阻塞，无取消通道；V2 `stop-pipeline-run`）。**重跑**：无（`run_range` 对已成功段无条件跳过，无 force 参数；UI/CLI 均无入口；V2 `rerun-stage(stage.id)`）。**运行日志**：`refresh_logs` 在 `pipeline_run` 后自动刷新 | 开发流水线视图（顶部第 2 个导航按钮）；范围运行需走 CLI | **部分通过**：全版图/恢复/风格门/阶段详情/日志有归宿；**「停止运行」与「阶段重跑」零归宿**；「范围运行」桌面端缺入口；「产物入口」缺。见 §2.4-H3 / H4 / M1 / M2 |
| 14 | 补充开发（追加需求分析 / 补充任务列表） | 模型：`adm4-app/src/change.rs`（`ChangeStatus{Drafted→ImpactAnalyzed→Scheduled→Applied}` + 任意非终态可 `Rejected`；`set_impact` 校验受影响段 ⊆ C0..C6；`advance` 跳级 blocked + 署名/结论双必填；7 单测）。服务：`services.rs::change_list/change_add/change_set_impact/change_advance`。UI：`main.slint` `active-view=="patch"` 视图，元素 `chg-title`/`chg-requester`/`chg-desc`/`chg-segments`/`chg-actor`/`chg-note`，按钮「登记变更」「影响分析」「推进→X」「拒绝」→ 回调 `change-add`/`change-impact`/`change-advance`；行模型 `view.rs::change_rows`（按钮可用性由后端状态机推出，单测 `change_rows_gate_buttons_by_status`） | 顶部导航「补充开发」 | **通过**（矩阵要求「至少出数据模型与视图占位」，实际已做成可操作视图） |
| 15 | 打包阶段（暂存发布 / 诊断 / 构建包 / SDK 套件 / Unity 构建 / 运行时验证） | 文档集交付：`adm4-app/src/deliverable.rs::DeliverableManifest::build`（逐段清点 + sha256 + 缺段不静默，4 单测）+ `services.rs::deliverable_package/deliverable_status`；UI 按钮「打包文档集（最新冻结版）」「刷新清点」→ 回调 `deliver-package`/`deliver-refresh`，行模型 `view.rs::deliverable_rows`。整包导出导入：元素 `pkg-export-path`/`pkg-import-path`/`pkg-import-name` → 回调 `project-export`/`project-import` → `adm4-archive/src/package.rs`（`.adm4proj`，1 单测）。Phase 2 占位：`main.slint` 视图底部固定文案逐项点名「游戏构建 / Unity 工程导出 / 运行时验证 / 发布包……二版「暂存发布 · 诊断 · 构建包 · Unity 构建 · 运行时验证」在此有归宿，本期不放不可用按钮」。诊断：`adm4-archive/src/store.rs::doctor` + CLI `project doctor`（命中 `[PROBLEM]` 非零退出），**GUI 无诊断按钮** | 顶部导航「打包阶段」；诊断走 CLI `adm4 project doctor <id>` | **部分通过**：五项 Phase 2 能力有明确可见的占位归宿（符合矩阵「视图占位」要求）；「诊断」归宿在 CLI 而非 GUI。见 §2.4-M3 |
| 16 | 运行日志（过滤 / 清空 / 导出 / AI 诊断） | 过滤：元素 `log-filter` + 回调 `refresh-logs` → `view.rs::log_rows`（类别或消息命中，大小写不敏感，最新在前；单测 `log_rows_filter_case_insensitively_and_newest_first`）。导出：元素 `log-path` + 回调 `export-logs` → `view.rs::log_markdown`。数据源 `services.rs` 的 `log.tail(400)`（`adm4-app/src/runlog.rs`，jsonl 追加）。**清空**：故意不做，`main.slint` 视图内固定文案「四版语义：日志是 jsonl 追加的审计流，不提供「清空」（清空会破坏可追溯性）——二版的清空按钮在此照实省略」。**AI 诊断**：仅 CLI `adm4 ai doctor`（`apps/adm4-cli/src/main.rs` 的 `(Some("ai"),Some("doctor"))` 分支 → `services.rs::build_provider`，`[BLOCKED]` 非零退出，冒烟负例实测）；**桌面端无 AI 诊断按钮，也无任何 AI Provider 配置面板**（V2 有 `refresh-ai-diagnostics` / `apply-ai-provider-preset` / `save-ai-provider` / `save-ai-secret` / `check-ai-provider` / `invoke-ai-provider` / `disable-ai-provider` 共 7 个回调） | 顶部导航「运行日志」；AI 诊断走 CLI | **部分通过**：过滤/导出完备；「清空」有界面明示的裁决（但矩阵写「保留」，裁决未进 design/plan）；「AI 诊断」归宿在 CLI，GUI 缺。见 §2.4-M5 / M6 |
| 17 | SDK 知识库（新增 / 审批队列 / 已索引资源） | 模型：`adm4-app/src/sdk.rs`（`SdkReviewStatus{Pending/Approved/Rejected}` 三态分叉终态；只有 `Pending` 可裁决，重复审批 blocked；署名+意见双必填 R3；6 单测）。落盘 `data_root/config/sdk_knowledge.json`（全局跨项目）。服务：`services.rs::sdk_list/sdk_add/sdk_approve/sdk_reject`。UI：`main.slint` `active-view=="sdk"` 视图，元素 `sdk-name`/`sdk-url`/`sdk-cat`/`sdk-purpose`/`sdk-reviewer`/`sdk-review-note`，按钮「提交待审」「批准」「拒绝」→ 回调 `sdk-add`/`sdk-approve`/`sdk-reject`；计数条 `sdk-counts`；行模型 `view.rs::sdk_rows`（单测 `sdk_rows_mark_pending_and_signed`） | 顶部导航「SDK 知识库」 | **通过**（V2 的 `mark-sdk-resource-pending` 回退态被 T12「终态不可翻案」裁决覆盖，属既定设计） |

### 2.3 四条冲突裁决逐条验证（4 行）

| # | 裁决 | 验证证据 | 结论 |
|---|------|---------|------|
| 18 | V2 勾选（布尔）→ 由「已确认」派生 | `view.rs::badge_of` 的判定顺序：适用性优先（超深度/未激活/N/A），其后 `point.confirmed → 已确认(done)`、有已选未确认 `→ 待确认(warn)`、无已选 `→ 未选(bad)`——**布尔勾选态完全由 `confirmed_by_user` 派生，UI 不存第二份勾选状态**。写入路径唯一：按钮「确认此决策点（= 二版检查单勾选）」→ `engine.confirm_selection`。派生一致性：`services.rs::project_profile` 与 `workbench_overview` 的缺失项判定同样只看 `confirmed_by_user`（`services.rs:546`、`services.rs:628`）。单测 `badges_cover_every_applicability_state` 钉住六态含「待确认」不与「未选」混同 | **成立** |
| 19 | 未认证模板不可预填（走到报错） | 关卡：`adm4-template/src/library.rs::approved_for_prefill` —— `if !template.is_certified() { return Err(Adm4Error::blocked("模板 X 未完成认证（当前状态 {:?}），只有 Certified 模板可用于预填")) }`，且 `template_compare` 走同一关卡。测试：`adm4-app/tests/end_to_end.rs::uncertified_template_prefill_is_rejected`、`adm4-authoring/tests/optional_points_and_universal_prefill.rs::uncertified_universal_template_is_rejected_at_the_engine_too` 与 `non_universal_template_from_other_pack_is_still_rejected`。**冒烟实测**：`cli_smoke.ps1` 的「负例：未认证模板预填必须被拒」段与「F3 负例」段实际输出 `[Blocked] 模板 tpl_dawnline 未完成认证（当前状态 CrossChecked），只有 Certified 模板可用于预填`，退出码 1。批量认证通道：25 份 v2 内置模板全部落 `Certified` 状态入库（`universal/references/`） | **成立** |
| 20 | 冻结门预检结果进右栏「校验」页签 | `main.slint` 的 `if root.right-tab == "validation"` 块内含：按钮「四门预检」→ 回调 `freeze-check` → `services.rs::freeze_check`；按钮「AI 红队」→ `run-red-team`；按钮「执行冻结」`enabled: root.freeze-ready`（`main.rs::apply_overview` 只写 `overview.validation.all_gates_passed`，GUI 不自行判定）；明细列表 `validation-rows` ← `view.rs::validation_rows`（跨表外键违规 + 逐门 pass/block + 逐条 finding）。冻结成功后同页签给出 `freeze-hint` 与「前往开发流水线」按钮 —— 是设计流程的末站而非另一套系统。门数说明：`freeze.rs::evaluate_freeze_gates` 产 4 道**检查**门（gate1_completeness / gate2_consistency / gate3_skin / gate4_red_team），第 5 道「冻结」是执行动作（`docs/design/03` §3 表），故 UI 文案「四门预检」+「执行冻结」与设计一致，非缺门。单测 `validation_rows_keep_passed_gate_with_findings_green` 钉住「门 1 带 N/A 明细但 passed=true 不得渲染成失败」 | **成立** |
| 21 | 流水线呈现 C0-C6 实跑 + P0-P5 占位的完整版图（不断头） | `view.rs::stage_rows` 拼接 `design_compile_registry()`（C0 规格编译 / C1 验证与红队 / C2 玩法文档 / C3 内容与资产 / C4 程序需求 / C5 风格 / C6 计划）与 `phase2_registry()`（P0 引擎工程骨架 / P1 程序任务执行 / P2 资产批量生产 / P3 装配与集成 / P4 验收场景执行 / P5 打包交付），共 13 行；P 段状态固定为「Phase 2 占位（数据模型已立，执行器另行立项）」，`placeholder=true` 用灰底区分但**照常显示**。单测 `stage_rows_cover_c_and_p_segments` 断言 `rows.len()==13` 且 P 段 6 行。左栏说明文案「设计冻结 → C0-C6（文档编译段，已实现）→ P0-P5（开发执行段，Phase 2）」；`view.rs::pipeline_note` 在视图内注明 Step00-14 的完整映射关系。**未断头** | **成立** |

### 2.4 缺口清单（按严重度排序）

**H = 高（矩阵某行的子能力零归宿，直接导致 §4 判定不通过）**

| 编号 | 缺口 | 位置 | 建议归属 |
|------|------|------|---------|
| H1 | **「另存模板」（项目 → 模板导出）完全缺失**。矩阵 §2.1 行 8 明写为「新增能力」，实际无服务方法、无 CLI 子命令、无 UI 按钮、无占位说明 | `crates/adm4-app/src/services.rs`（62 个 `pub fn` 中无此项）；`apps/adm4-desktop/ui/main.slint` 模板面板；`apps/adm4-cli/src/main.rs::template_command`。V2 原貌：`RUST/apps/adm-desktop/src/main.rs:1449` + 回调 `save-design-template` | **修复任务**（新开 T14：项目当前选择 → Template 结构，落 `Draft` 状态；是否直接给 `Certified` 需按 D8/R5 裁决，建议落 Draft 并走 S4/S5） |
| H2 | **「设计工作台重置」完全缺失且零说明**。矩阵 §2.1 行 11 归宿写「保留（已有）」，但 V4 无任何重置入口，界面没有像「清空日志」那样的省略说明，设计/计划文档也无裁决 | `crates/adm4-app/src/services.rs`；`apps/adm4-desktop/ui/main.slint` 存档管理面板。V2 原貌：`RUST/apps/adm-desktop/src/main.rs:1642` 回调 `reset-design-workbench` / `:5221 fn reset_design_workbench` | **需用户裁决**：V4 事务自动保存语义下「重置」指什么（① 清空当前项目全部选择并保留存档 ② 另建空项目 ③ 明确不做并在界面写明原因）。裁决后再定修复任务或设计债 |
| H3 | **流水线「阶段重跑」零归宿**。`run_range` 对已成功阶段无条件 `continue`，无 force 通道；UI/CLI 均无入口。后果：C2 文档不满意时无法只重跑 C2，只能新开冻结版本 | `crates/adm4-pipeline/src/runner.rs::run_range`（`if state.is_succeeded(&stage.id) { continue; }`）。V2 原貌：回调 `rerun-stage(string)` | **修复任务**（建议给 `run_range` 加 `force_stages` 或新增 `rerun_stage`，重跑需重置该段及其下游状态以免产物错版） |
| H4 | **流水线「停止运行」零归宿**。`pipeline_run` 同步阻塞执行，无取消通道；C1-C5 含 AI 调用，长时运行期间桌面端不可中断 | `crates/adm4-app/src/services.rs::pipeline_run_with` → `runner.rs::run_range`（无 cancel token）。V2 原貌：回调 `stop-pipeline-run` | **需主开发裁决**：是否本期补（需引入取消信号 + 后台线程，会动 GUI 线程模型），还是记设计债并在流水线视图写明「本期同步执行、不可中断」 |

**M = 中（能力有归宿但不在矩阵指定的位置，或语义打折）**

| 编号 | 缺口 | 位置 | 建议归属 |
|------|------|------|---------|
| M1 | 「范围运行」桌面端无入口，只有固定按钮「运行 C0-C6」；能力在 CLI（`pipeline run --from --to`） | `apps/adm4-desktop/ui/main.slint`（`Button{ text:"运行 C0-C6" }`）；`apps/adm4-desktop/src/main.rs::on_pipeline_run` 硬编码 `"C0","C6"` | 修复任务（低成本：两个下拉/输入框转发给已有的 `pipeline_run(from,to)`） |
| M2 | 「阶段详情」无产物入口。UI 只显示 `summary/status`，无法查看该段 `document.md` / `contract.json`；`services.rs` 亦无读取产物的查询方法。设计文档 06 §4 写的是「每卡状态/耗时/产物入口」 | `crates/adm4-app/src/services.rs`（无 `pipeline_artifact` 类方法）；`apps/adm4-desktop/ui/main.slint` 流水线右栏 | 修复任务（新增只读产物查询 + 详情面板） |
| M3 | 打包视图无「诊断」按钮。V2 的 `release-doctor`/`delivery-doctor` 在 V4 的归宿是 `adm4 project doctor`（CLI）与 `deliverable_status` 的缺段清点，但 GUI 打包视图没有诊断入口 | `apps/adm4-cli/src/main.rs`（`project doctor`）；`crates/adm4-archive/src/store.rs::doctor`；`apps/adm4-desktop/ui/main.slint` 打包视图 | 设计债（能力存在且可达，仅 GUI 入口缺） |
| M4 | 项目画像六字段中「美术风格」不上画像卡（其决策点 `v2.art_direction_decision.feng_ge_ding_wei.*` 是 L3/L4，被 `project_profile` 的 L0/L1 过滤挡住）；「目标用户」无单一对应决策点 | `crates/adm4-app/src/services.rs::project_profile:540`（`if !matches!(point.level, L0 \| L1) { continue; }`）；`knowledge/design_space/universal/core.json` | **需主开发裁决**：① 给画像卡加一份「白名单决策点」配置（不改层级）② 补 `u.art_style` L0 点（D4 只增不改，但会改完成度分母）③ 接受现状并在矩阵备注 |
| M5 | 「清空运行日志」按裁决省略。界面有明示文案，但矩阵 §2.2 行 4 归宿写「保留」，该裁决只存在于 T11 报告与界面文案，未进 `docs/design/**` 或 `docs/plan/**` | `apps/adm4-desktop/ui/main.slint:1612` | 设计债（补一条裁决记录进 06 号设计文档，由主开发执行——本 agent 无权改 design 文档） |
| M6 | 「AI 诊断」GUI 缺失。归宿在 CLI `adm4 ai doctor`；桌面端不仅日志视图无诊断按钮，整个 AI Provider 配置面板都缺（V2 有 7 个相关回调），意味着桌面用户无法配置 AI，而 C1-C5 与访谈/红队全依赖 AI | `apps/adm4-cli/src/main.rs`（`ai doctor` 分支）；`apps/adm4-desktop/**`（无对应）；设计文档 06 §5 已注「`ai secret-set\|provider-set\|invoke-check` 细分本期未实现」 | **需主开发裁决**（桌面端可用性问题：无 GUI 配 AI 通道时，桌面用户走不通访谈/红队/流水线全链） |

**L = 低**

| 编号 | 缺口 | 位置 | 建议归属 |
|------|------|------|---------|
| L1 | V2 的「清空 L5」按钮无同名入口（可用高级 JSON 写 `{"values":"none"}` 达成） | `apps/adm4-desktop/ui/main.slint` 决策点详情 | 设计债 |
| L2 | CLI 缺 `deliver` / `sdk` / `change` 子命令（T12 已登记遗留），也缺多选/主选子命令（`authoring add-option` / `set-primary`，T10 遗留③ 曾说并入 T12，未落地）。后果：SDK 审批、变更流、文档集打包、多选主选**无法脚本化/纳入冒烟** | `apps/adm4-cli/src/main.rs`（`authoring` 分支只有 `status/select/set-param/set-rationale/confirm/na`）；`scripts/cli_smoke.ps1` | 修复任务（低优先，补齐后冒烟可覆盖 T12 三视图与多选链路） |
| L3 | 任务卡与矩阵行数对不上：`docs/plan/05` §2.1 实为 12 行，任务卡写 11 行 | `docs/plan/05_V2能力覆盖矩阵与W6返工设计.md` §2.1 | 记录（不改文档） |
| L4 | `change_add` 的 `target_frozen_version` 桌面端固定传 0（T12 已登记遗留 c） | `apps/adm4-desktop/src/main.rs::on_change_add` | 设计债（T12 已在案） |

### 2.5 需人工试用才能确认的 UI 项

以下项**无法由自动化验证**（Slint 运行时布局/交互，无 GUI 自动化测试）。装配层单测钉住的是行模型的正确性，不能证明控件在真机上可见、可点、不被裁切。本报告对它们的结论均为**「装配层证据充分，真机行为需人工试用」**，未按通过计。

| 项 | 试用步骤 |
|----|---------|
| U1 左栏 16 领域卡片可滚动可见 | 启动 `AutoDesignMaker4.exe` → 「存档管理」→ 选 `lane_defense` 建项目 → 载入 → 左栏应出现 16 张领域卡（含 0 点域灰字），逐张滚到底部确认第 16 张「上线准备设计」可见且可点 |
| U2 领域→节点→检查单三级联动 | 点左栏「玩法系统设计」→ 中栏标题应变为「节点列表 · 领域「玩法系统设计」」→ 点任一节点卡 → 检查单应只剩该节点的决策点 |
| U3 多选点「勾选/取消勾选/设主」三按钮 | 选一个 `v2.*` 决策点（全部为 multi+allow_primary）→ 勾选两个选项 → 确认 → 顶部状态栏应提示「但该多选点尚未设主选」→ 详情区应出现红字「多选点缺主选」→ 点「设主」→ 红字消失、选项前出现 ★ |
| U4 N/A 三元组必填校验 | 只填「N/A 理由码」不填说明与署名 → 点「标记不适用」→ 状态栏应显示后端拒绝原因（非静默） |
| U5 表格/矩阵编辑器与高级 JSON 互切 | 选一个 L5 表结构点的选项 → 「编辑参数」→ 应出现列头与行编辑框 → 「新增行」→ 填格 → 「高级JSON」应带着未保存内容切换 → 「返回结构化表格」应切回；把 JSON 改成非法再切，应停留在 JSON 模式并报错 |
| U6 右栏「缺失项」点击跳转 | 右栏 →「缺失项」→ 点任一条目 → 中栏应自动切到该点所在领域/节点并选中该决策点 |
| U7 「校验」页签冻结衔接 | 右栏 →「校验」→「四门预检」→ 逐门明细应出现 →「执行冻结」按钮在未全绿时应灰（不可点）→ 全绿后可点 → 冻结后应出现「前往开发流水线」按钮并能跳转 |
| U8 流水线 13 段全版图可见 | 「开发流水线」→ 左栏列表应能滚到 P5，P 段 6 行灰底且状态写「Phase 2 占位」；右栏「人工确认」按钮只在等待人工的段出现 |
| U9 补充开发按钮门控 | 「补充开发」→ 登记一条变更 → 该行应只出现「影响分析」；填 `C2,C3` 点「影响分析」→ 该行应同时出现「影响分析」「推进→已排期」「拒绝」；推进到「已应用」后三按钮全消失 |
| U10 SDK 三态审批 | 「SDK 知识库」→ 提交一条 → 行应显示「待审核」并有「批准/拒绝」→ 留空评审人点「批准」→ 应报错；填全后批准 → 按钮消失、署名可见；再点（已消失）无法重复审批 |
| U11 打包视图逐段清点 | 「打包阶段」→ 未冻结时汇总条应显示「项目尚未冻结……」；跑完 C0-C6 后「刷新清点」→ 7 行 C0-C6 应全「齐备」并带 sha256 前 12 位 |
| U12 运行日志过滤与导出 | 「运行日志」→ 过滤框输入 `sdk` → 只剩 SDK 类日志；填导出路径点「导出」→ 目标 md 文件生成 |
| U13 模板面板 26 份列表与预填 | 设计工作台 →「模板/逆向维护」→ 左栏应列出 25 份「通用层」模板 + 本包模板 → 选一份「已认证」→「预填到当前项目」→ 状态栏应显示写入条数与跳过数 |
| U14 深色主题下文字可见 | 切系统为深色主题后重启 exe，确认 `build.rs` 锁定的 fluent-light 生效、文字未消失（T11 曾修此坑） |

---

## 3. 测试证据

全部命令在 `E:\workwork\CrewAi\AutoDesignMaker\V4` 下由本 agent 实测复跑（2026-08-31），非引用他人结论。

| 门禁 | 命令 | 实测结果 |
|------|------|---------|
| 格式 | `cargo fmt --check` | 退出码 **0**，无 diff |
| Lint | `cargo clippy --workspace --all-targets` | 退出码 **0**，输出仅 `Finished dev profile`，**警告 0 条 / 错误 0 条** |
| 测试 | `cargo test --workspace` | 退出码 **0**，**152 通过 / 0 失败 / 0 ignored**（18 个测试二进制 + 10 组 doc-test 均 0 失败） |
| 数据 | `cargo run -p adm4-cli -- space validate` | 退出码 **0**；`[OK] grid_strategy: 2610 个决策点，参考游戏 4 款`、`[OK] lane_defense: 2604 个决策点，参考游戏 3 款` |
| 冒烟 | `powershell -ExecutionPolicy Bypass -File scripts\cli_smoke.ps1` | 退出码 **0**，末行 `[冒烟通过] 逆向五步 -> 模板预填 -> 访谈补齐 -> 冻结 -> C0-C6 全链 OK` |

**152 个测试的分布（逐二进制核对）**：`adm4-ai` 0、`adm4-app` lib 19、`adm4-app` e2e 9、`adm4-archive` 1、`adm4-authoring` lib 14、`exemption_and_node_notes` 5、`optional_points_and_universal_prefill` 6、`adm4-cli` 0、`adm4-contracts` 6、`adm4-decision` 28、`adm4-desktop` 23、`adm4-foundation` 4、`adm4-pipeline` lib 11、`multi_selection_chain` 1、`adm4-space` 9、`adm4-spec` 4、`adm4-template` lib 3、`reverse_pipeline` 9 = **152**。与主开发基线一致。

**冒烟脚本内验证到的关键事实**（供矩阵取证）：
- 未认证模板预填被拒（`[Blocked] ... 只有 Certified 模板可用于预填`，退出码 1）；
- 冻结门四道逐门 `[PASS]/[BLOCK]` 明细，门 1 含 16 条 `not_applicable_exemption` 与 1 条 `optional_not_answered`（8 个非必做点不进分母）；
- 换皮门 gate3 拦截 8 条 `reference_name_hit`，改写理由后放行；
- C0-C6 两轮断点续跑 + C5 风格人工门 + C6 签收人工门；
- 通用层模板跨包预填 `builtin_midcore_arknights` → 写入 2585 个决策点、跳过 0 条；
- 模板列表 26 份（25 通用层 + 1 本轮草稿），全部 `Certified`。

**数据侧核对（只读统计）**：
- `universal/domains.json`：领域 16 个（order 1-16），节点 104 个，全部带 `role_class`；
- `universal/v2_checklist.json`：`design_question` / `mda_layer` / `node_id` / `domain` / `allow_primary` 各 **2575** 处，即 2575 个决策点**逐点齐全**；
- `universal/core.json`：12 个 `u.*` 决策点；
- `universal/references/`：**25** 份内置模板 json，与 V2 `knowledge/design_data/project_templates/`（26 个文件 = 1 个 index + 25 份模板）**数量一致，无丢失**。

---

## 4. 遗留与声明

1. **总判定：不通过。** 21 行中 18 行通过（含 §2.3 四条裁决全部成立），3 行为部分通过且其子能力零归宿：§2.1 行 8「另存模板」、§2.1 行 11「重置」、§2.2 行 13 的「停止运行」与「阶段重跑」。按 §4「任何一行找不到归宿即验收不通过」，判定不通过。其余部分通过项（画像六字段、打包诊断、AI 诊断、清空日志、桌面范围运行、产物入口）能力均有可指认的归宿，不构成判定不通过的理由，但需主开发裁决位置是否可接受。
2. **未做的事**：未修改任何代码/数据/测试/文档（除新增本报告）；未执行 git 操作；未为使判定通过而放宽口径。发现的问题全部只记录不修复。
3. **对其他 crate 的新 API 需求**（若主开发决定修复）：
   - H1 需 `adm4-app` 新增「项目 → Template」导出方法（依赖 `adm4-template::Template` 构造与 `CertificationStatus::Draft` 起点）；
   - H3 需 `adm4-pipeline::PipelineRunner::run_range` 增加强制重跑参数并处理下游段状态重置；
   - H4 需引入取消信号，会影响 `adm4-desktop` 的 GUI 线程模型（当前为同步阻塞调用）；
   - M2 需 `adm4-app` 新增只读产物查询（读 `content/pipeline/v{N}/{stage}/document.md`）。
4. **发现的既有 bug**：本轮走查未发现功能性 bug（无崩溃路径、无红线违规、无静默吞错）。上述缺口均为**能力缺失**而非实现错误。
5. **文档偏差记录**（无权修改，交主开发）：
   - `docs/plan/05` §2.1 实为 12 行（本报告 §2.0）；
   - `docs/plan/05` §2.1 行 8 写「26+ 内置游戏模板」，V2 与 V4 实际均为 25 份；
   - `docs/design/06` §4 写流水线「每卡状态/耗时/产物入口」，实现无耗时显示与产物入口；
   - 「不提供清空日志」的裁决只在界面文案与 T11 报告里，未进 `docs/design/06`。
6. **人工试用清单**：§2.5 的 U1-U14 共 14 项需真机试用，本报告未把它们计为「已验证通过」。
