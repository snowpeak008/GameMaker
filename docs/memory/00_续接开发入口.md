# V4 续接开发入口（新会话从这里开始）

> 目的：用户回来后开新会话，AI 读完本文件即可无缝接管主开发角色，不依赖任何历史对话。
> 最后更新：2026-08-30 00:05（Phase 1 收官后追加 **W6 V2 对齐返工**：T9 内容模型扩展、T10 知识库全量迁移、T11 UI 返工为 V2 三栏工作台、F3 模型缺口修复均已验收，基线 132 测试全绿；已发布 v1.0.1（552051a）与 v1.0.2（b5f5b70）至 GameMaker，main 已跟进。**T12 流程版图与占位已完成**（补充开发变更流 / SDK 审批流 / 文档集交付三视图从占位做成可操作视图 + import 双真相修复；基线升至 152 测试全绿、clippy 零警告、双包 validate OK、cli_smoke 全链通过、desktop 构建 OK；见 `docs/design/07`）。T13 覆盖矩阵终验待派）。
>
> **注意：本文件第 5-7 节的"恢复开发步骤/W3 任务卡/W4 要点"是 W2 暂停点的历史预案，已全部执行完毕，仅作归档参考。当前状态与后续方向以 `2026-08-29_开发进度与交接.md` 和 `2026-08-29_Phase1交付报告.md` 为准。**

---

## 0. 给新会话的开场提示词（用户可直接复制）

```
你是 AutoDesignMaker V4 的主开发（orchestrator）。先读 V4/docs/memory/00_续接开发入口.md，
按其中"恢复开发步骤"继续：你只负责计划、派发、集成、验收，不写任何代码；
开发由子 agent 并行承担。从暂停点（W3 波次）继续。
```

## 1. 一句话现状

V4 是与前三版零关联的独立 Rust 项目（决策驱动游戏设计工作台：L0-L6 决策创作 → 冻结门五道 → C0-C6 文档编译）。**Phase 1 已全部完成**：W1（内容与核心能力）、W2（服务门面）、W3（CLI + 桌面 UI）、W4（三路验收）、W5（红线修复 F1 + 校验器立项 F2）全部验收通过。四个检查类命令（space validate / project doctor / ai doctor / freeze check）命中问题均非零退出。**Phase 1 之后又完成 W6 V2 对齐返工**（T9 内容模型 / T10 知识库迁移 16 领域 2575 点 26 模板 / T11 三栏工作台 UI / F3 模型缺口修复），当前基线：**152 测试全绿**、clippy 零警告、双包 validate OK（lane_defense 2604 / grid_strategy 2610 点）、cli_smoke 全链通过、desktop 构建 OK；GameMaker 仓库已发 v1.0.2，main 同步（T12 产物尚未提交，待用户指令）。**T12 已完成**：SDK 三态审批流（`sdk.rs`）、补充开发变更流状态机（`change.rs`）、文档集交付清点（`deliverable.rs`）三个 adm4-app 模块 + import_project 双真相修复 + 桌面三视图接线（补充开发/打包阶段/SDK 知识库从占位做成可操作视图），设计文档见 `docs/design/07`。**2026-08-31 更新：Phase 1 开发已全部收官。** T13 终验判不通过（3 行零归宿）→ F4a-F4e 五轮串行修复（流水线强制重跑/协作取消/阶段产物查询、另存模板/工作台重置/画像取点/体检上提/阶段耗时、桌面端全面接线含 AI 配置面板、换皮豁免按项目排除 + 认证证据旁路收口、豁免收窄到 ProjectExport 登记词 + sdk/change/deliver/多选主选 CLI 补齐）→ T13b 复验**代码级通过**。当前基线：**218 测试全绿、0 ignored**、clippy 零警告、双包 2604/2610、冒烟全链（含 SDK 审批/变更流/交付清点/多选主选）退出码 0、desktop 构建 OK。详见 `2026-08-31_Phase1缺口闭合与F4波次.md` 与 T13/T13b 两份报告。**剩余前置：真机走查**（T13b §⑤ 的 12 项最小清单，需用户或主开发真机点一遍）。Phase 2（P0-P5 EXE 产线）立项设计已定稿（`docs/plan/06-10` 五册）。**G1 治理骨架 + 插件框架已完成并发布 v1.0.4**（`7350d5c`，2026-08-31）：新建 `crates/adm4-build`（六治理契约 / 制品注册与拓扑 / Phase2Runner 与 C0-C6 runner 同构 / 确定性对齐层 / 权威顺序校验器 / EngineBackend 接缝，unity 字样零命中），P0-P5 执行器全部诚实空实现（Blocked + 归属波次），`build_*` 门面 + CLI build 组 + 桌面 P 段注册表驱动。基线升至 **288 测试全绿**。G1 报告的 P0-P5 制品一览表是 G2-G5 接活依据（在 G1 子 agent 报告，要点已进本目录记忆）。主开发裁决：P0 两条线契约派生执行器归 **G3 开头**；`StageSpec.kind` 的真实人工门（P2 预算门/P4 verdict 门）待 G3/G5 落地时改。**G2 美术风格锚点门已完成**（2026-09-01 凌晨，发布 v1.0.5）：图像通道落 `adm4-ai`（`ImageProvider` + OpenAI 兼容 images API + `ScriptedImageProvider` 手写 PNG，零新增第三方依赖，配置为 `config/app.json` 的 `image_provider` 段与文本通道分开）；风格门落 `adm4-build/art/style_anchor.rs`（真源=创作态已确认画像点同 profile_points 口径，提示词 AnchoredNarrative+SkinScanner，改词重生成断点续跑不重复花钱，确认 R3 双必填且 auto_accept 类型层面不存在，不可变历史 `content/style/anchors/v{N}` 自增版本，readiness 实读实校）；桌面设计工作台右栏第 5 页签「风格」（单列卡片+全窗大图，右栏 348px 放不下 3 列属呈现适配）；CLI `style generate/regenerate/confirm/status`（`--scripted-image` 冒烟开关）。基线 **328 测试全绿**。主开发已修 plan/08 两处口径：锚点版本自增（非同冻结号）、代表资产锚图留 G3 以新版本追加。`StyleReadiness::require_ready()` 是 P2 executor 的硬门。
**G3 资产生产线已完成**（2026-09-01，发布 v1.0.6；本波起主开发直接开发，不再用子 agent）：
- **P0 两条线派生真跑**：`adm4-build/src/program/derive.rs`（GameSpec+C3/C4 → 程序线/美术线契约+资产表初版+对齐报告，确定性、只映射不发明、缺事实登记 CoverageGap；C3 的 `asset_{entity}` 按 `sprite2d→T_ / model3d→SM_ / ui→UI_` 映射成命名权威合规 id）；P0 契约 `TwoLineContract` 含权威顺序自检与 `engine_seed.status=pending_g4`（引擎种子如实未产，归 G4）。
- **P2 资产批量生产真跑**：`executors.rs::P2Executor`——风格硬门（实读实校）→ 预算人工门（首跑自动申报停下，`build budget-confirm` 署名批准，R3；额度=资产数，超额自动 Exhausted 再批）→ 逐资产生产（`asset_producer.rs`：提示词=契约 prompt_prefix+资产语义+用途约束，先过 SkinScanner 再发调用；`ExternalToolProducer` 诚实占位）→ 内容哈希缓存（`cache.rs`：规格+提示词+尺寸+provider/model 为键，命中零调用零扣费；键去掉 sha256: 前缀因 Windows 文件名禁冒号）→ 台账（godogen 七字段，`genome_backfill.rs`）→ 基因表回填对账（path=运行时加载 path）→ 确定性一致性比对（格式+前缀合规；真视觉比对如实标注机器判不了）→ R6 基数对账 → 干净才放行，Block 漂移进 repair_queue。
- **代表资产锚图**：`StyleGate::append_representative_anchors`（C3 前 4 个资产，新锚点版本追加，旧版逐字节不变；CLI `style append-representatives`）。
- **接线**：`PENDING_STAGES` 缩至 4 段（P1/P3/P4/P5）；`Phase2Runner::new()` 对 P0/P2 装 NotWiredExecutor（诚实报未接线），真实装配走门面 `build_runner`（注入 Phase1 产物仓/风格根/图像通道）；门面新增 `build_budget / build_budget_confirm / build_run_with_images / build_rerun_with_images / style_append_representatives[_with]`；CLI `build budget / budget-confirm`、`build run/rerun --scripted-image`；桌面 `stage_rows_with_build`（P 段有真状态显示真状态）。
- **注意**：`build run P0..P5` 区间会停在 P1（待 G4）；跑 P2 用单段区间 `--from P2 --to P2`（依赖检查只看 P0 已成功）。`ScriptedImageProvider` 改为 Clone（Arc 共享调用账本）。
基线 **350 测试全绿**（+22）。
**Q1/Q2 已由用户拍板（2026-09-02）**：Q1 = Unity **2022.3 LTS**；Q2 = **CoplayDev/unity-mcp**（GitHub 13.6K 星，MIT，Unity 2021.3 LTS-6.x，47 个 MCP 工具入口，stdio 传输，`uvx --from git+https://github.com/CoplayDev/unity-mcp@<tag>#subdirectory=Server mcp-for-unity --transport stdio` 启动，需 Python 3.10+/uv + Unity 侧装 MCP 插件）。**用户要求 MCP 部分隔离、可后置**——G4 因此拆两波：**G4a** 引擎无关（P1 切片/清单/指南/轮次 + 通用 MCP stdio 客户端协议层 + Mock 后端测试），**G4b** 后置（unity_mcp 具体后端真机接线，届时 `engine/unity_mcp/` 目录是接缝纪律允许 unity 字样的唯一例外，验收扫描命令要相应排除该目录）。协作模式恢复：主开发派任务，子 agent 开发自验收，完成一个销毁一个。
**G4a 已完成并主开发验收，已发布 v1.0.7**（`797d104`，2026-09-02，分支 1.0.7 + tag v1.0.7 + main 已同步；任务卡 `docs/plan/11`）。**用户裁决（2026-09-02）：G4b（unity-mcp 真机接线）暂缓，以项目本身推进为优先**：
- T-G4a-1：`program/{slice,manifest,engine_guide,dev_round}.rs`——`extract_playable_slice` 确定性抽取（1 主场景/1 主操作/1 成败反馈，缺事实 Blocked 点名；多候选 Blocked 点名全部候选交设计侧收敛【主开发裁决：多候选属 R2 未收敛，Blocked 而非 validation/Failed】）；`RuntimeManifest`（Markdown 每条事实可溯源到契约字段）；引擎无关 `EngineGuide` + `EngineGuideSource` trait + `NotProvidedGuide`；`DevRoundLog` durable 落盘可停可续（Failed 必有 failures / Succeeded 必有 commands 的 R7 钉子）。
- T-G4a-2：`engine/types.rs`（EngineProjectSeed/SliceTask/DevContext/DevRound/RunResult/ProofBundle）；`EngineBackend` 扩为六方法（id/preflight/open_or_create_project/agent_develop/run_playmode/capture_proof，对象安全）；**通用 MCP stdio 客户端协议层 `engine/mcp/`**（JSON-RPC 2.0 一行一帧、initialize→notifications/initialized→tools/list（跟随 nextCursor）→tools/call，协议版本 `2025-06-18`，服务端 error/isError/未握手全 Err 不吞；`StdioTransport` 可 spawn 可内存注入，`ScriptedTransport` 测试回放）；`MockEngineBackend` 确定性回放 + `calls()` 审计（**不做预检门控，门控是 P1Executor 的责任**）。
- T-G4a-3：P0 `engine_seed` 真产（`status="produced"` + `EngineProjectSeed`，旧档 `pending_g4` 兼容）；`P1Executor`（P0 契约→切片→清单→指南（无来源如实进 reasons）→durable docs→预检门控（不就绪不跑开发）→Mock 一轮开发→四制品落盘，Blocked 产物照落）；`PENDING_STAGES` 缩至 3（P3 归 G4b）；门面 `build_run_with_engine/build_rerun_with_engine/build_p1_summary` + `config.engine_backend`；CLI `build run/rerun --mock-engine`、`build p1-status`；冒烟追加 P1 段（lane_defense 预期 Blocked：**P0 派生 3 个 Command 候选未收敛，属设计侧事实非缺陷**——成功链在 `executors.rs::tests::p1_integration` 用最小夹具验证）。顺带清理 sdk/deliverable/desktop/cli 里 6 处遗留 unity 字样（`target_engines` 默认改「未指定」，第 34 行旧登记作废）。
- 基线：**407 测试全绿**、clippy 零警告、双包 validate OK、`cli_smoke.ps1` 退出码 0、desktop 构建 OK、`rg -i unity` 三目录零命中。主开发已亲自复跑全部门禁。
- **给 G4b 的接缝**：真机接线要提供 ①`engine/unity_mcp/` 后端（实现六方法，经 MCP stdio 驱动）②该引擎的 `EngineGuideSource` 实现（`NotProvidedGuide` 会让 P1 落 Blocked）③验收扫描命令排除 `engine/unity_mcp/`；`StdioTransport::recv` 无超时，真机若需超时在该层加。
- 本轮工程事实：子 agent 多次因平台/网络中断，**派发时必须限定调研时间（≤10 分钟）并要求先跑测试再动手**；子 agent 模型 slug 现为 `claude-fable-5-high`（平台更名，原 slug 已失效）。
**设计方法论独立审计（2026-09-02，用户质疑"设计层是否真能支撑绝大多数游戏"后由两个独立子 agent 执行，报告全文在本目录 `2026-09-02_设计方法论审计A_覆盖度.md` / `_审计B_全链演练.md`）**：
- 审计 A（10 款跨品类游戏压力测试）总分 **3/10**。硬事实：①通用层 2575 点是模板冲压（103 节点 × 25 问、选项去重后仅 130 套、全 multi、零 parameter/Table/Matrix/custom、L4 虚标实为 L1 意向）；②25 份逆向模板证据 100% 自引用、Dofus=Tibia 等多对答卷逐条全同，违反项目自身 R1/宁缺勿造红线，属负资产；③10 款游戏 47 条核心机制：4% 可直接表达、64% 完全无表达位；④结构性天花板：EffectSpec 七封闭枚举装不下位移/规则修改器/图结构/连续曲线/实时时序——不改类型系统只能诚实收窄为"数值表驱动品类的设计工作台"。品类包（17/23 点）质量真实、方向成立，缺量产（可上线需 150-300 点/包）。
- 审计 B（真人视角全链演练：建项→1615 点确认→冻结→C0-C6）总分 **2.5/10**。硬事实：①手动路径约 4900 次 CLI 操作（人力估 40+ 小时），预填路径更贵（2585 confirm + 2574 set-primary + 2585 理由改写）；②原创机制（可编程索敌/波次自适应变异）无结构化入口，寄生在 rationale/裸字段里，**C0 丢弃 rationale → C4/C6 彻底消失**；③C4 仅 3 个能力契约（全部来自品类包 15 点），C6 115 任务中 101 条装配任务是选项标签复读、依赖边仅 3 条；④红队门离线 scripted 通道可自证通过，形同虚设。
- 两份审计一致结论：**品类包→C4 这条窄链是真的，通用层 2575 点+25 模板是空心的，原创机制无入口**。修复方向收敛为三件事：①L4 机制表达能力扩展（分层 EffectSpec + 图类型参数 + custom_mechanic 一等入口 + rationale 进编译链）；②v2 层从"决策点"降级为"访谈提示词库"（不进完成度分母/冻结门），通用决策点按 core.json 12 点标准重写 150-300 个；③25 份模板标记烟测数据禁入预填，跑真实逆向五步重建证据资产。C6 装配任务去重+依赖图、C3 user_prompt 带属性值等速修项见审计 B §⑤。
**W7 多 agent 方法论商讨已完成（2026-09-02 深夜，五份文档在 docs/plan/：12_立项简报 → W7_提案A（设计理论派：39 系统库/五维重度/装备宠物双谱系）∥ W7_提案B（工程建模派：SystemModule/tier_gate 复用 unlocks/EffectSpec 7→16/Graph+Curve/5 波 21-26 人日）→ W7_红队评审（6 款压测/12 裁决/16 指令/37/60 分）→ W7_定稿（16/16 指令执行、5 处综合者注、10 款覆盖 3 完整+6 部分+1 骨架 vs 审计基线 4% 直达））。**
**用户裁决进度（2026-09-02）**：开放问题 2 已裁决 = **设计复杂度**（用户以装备系统 6 子系统影响战斗表现为例，直接对应五维 M/C/κ）；**新增用户指示：AI 访谈升级为主创作路径**（沿 v1 Python 版经验，系统级三段访谈：概念访谈出系统清单+重度档+core_loop → 组合访谈解释违例给修复选项 → 机制访谈逐点提案，PromptLibrary 做追问弹药；custom 草案可 AI 起草仍用户确认；落点定稿 §9.2b，并入波 3 与波 5 验收）。**四个开放问题已全部裁决（2026-09-02 深夜）**：问题 1 = 接受收窄 **但必须保留可拓宽通道**（定稿 §8 已列四条：系统库开放入库 / EffectSpec 受控扩展层可重复加变体 / 参数类型可增 / 射程外参数位）；问题 2 = 设计复杂度；问题 3 = P 维人工评定（用户体验最终裁决）；问题 4 = 默认值**跨多项目试用制**（每个真实项目结束时收集阈值反馈，多项目后定终值，不在波 2 单点锁定）。
**用户提问暴露系统库缺口（已登记定稿 §9.2a）**：39 系统缺 **#40 职业流派**。用户二轮修正（重要）：职业是**从技能构筑涌现的身份概念**（"强壮男性装法师技能就是法师；瘦弱女性装持盾技能就是战士"）——#40 修正为两档形态：轻档涌现式（无职业表，安装约束+派生标签）/ 重档预设式（职业表+门控+转职），机制核心=安装约束（"须持盾才能释放"落在技能对装备槽位状态的 consumes 接口）。
**第二轮排查（2026-09-03 凌晨，两个并行 agent，产物 `W7_附录_系统库缺口扫描.md` 与 `W7_附录_极端组合形态测试.md`）**：
- 缺口扫描（24 品类穷举）：**13 个候选新模块全过边界三测试**——P0×5（建造放置/对局赛制/计分连击/视野迷雾/单位编队指挥）、P1×6、P2×2；最致命三个：建造放置（塔防/生存建造/经营/工厂 4 品类必需）、对局赛制（格斗/派对/体育的局分-淘汰-名次层，#31/#34 之间无人承载）、计分连击（音游 #29 只产判定事件无人消费成分数，音游现库实际不成立）。另 8 条清单修订（含勘误：定稿判例 2 认定"新手引导是系统"却未入库）。实证发现：§6.3 MOBA 分解漏了视野/眼位系统但组合校验器照常通过——**校验器看不见未声明的系统**，人工/AI 访谈的系统清单完备性检查是必要防线。缺口聚集在"空间放置"与"多单位/多局结构"两横轴，属库采样偏差非方法论缺陷。
- 极端形态测试：**EU4 型**（个个系统都重）= 规则原则对、参数缺档——实测 H=8 个重核、连通与强耦合全过、只爆 |H| 上限；方案 = 新增"大战略档 |H|≤10"+ 该档追加双连通（无割点）守卫，已验证能拦"卡牌+农场靠货币桥钉接"的最强反例且不误伤 EU4；B(G)≈84，大战略档预算占位 95-105。**融合型**（合成大西瓜+塔防）= 现行规则**原样装得下零修正**：H={堆叠合成} 单重核通过；R-C1′ 双核强耦合要求意外成为"真融合 vs 浅拼接"的机器判别器；已归纳任意 X+Y 融合的六步通用方法论（系统并集→识别双核→core_loop 嵌套→跨核转换规则表→组合校验→访谈口述路径）。修正案 7 条（2 条待用户裁决：①新增大战略档位 ②系统级 custom schema 提级波 3），组合规则终评"能用，需小修"。
**用户裁决（2026-09-03 凌晨，两条，已写入定稿 §4.2(c) 与 §9.2b）**：
1. **|H| 上限改制——"提示义务，非设计义务"**："大战略档 |H|≤10"方案作废（档位枚举会无穷倒退：大战略+MMO 混合比 EU4 轻比 MMO 重，无档可归）。新制度：|H| 档位表降为参考线，超线 → 提示 + 用户一次性署名形态确认（R3 留痕）→ AI 设计义务转向**逐重核系统的轻重需求访谈**（"外交要 EU4 级还是文明级"逐个理清档位与理由），总体形态用户自行设计；R-C2 预算在形态确认后同样降为提示；(a) 连通 (b) 强耦合仍硬 block 不可豁免（防钉接，与玩法大小无关）；双连通守卫降提示级。
2. **缺口无痛接入立为验收纪律**：新系统模块 = 仅新增 module.json 文件，零类型/校验器/编译器改动；每模块入库验收单含"仅新增文件"一项，违反即结构性缺口须上报用户。13 个候选缺口模块（P0：建造放置/对局赛制/计分连击/视野迷雾/单位编队）用户已同意进量产清单。
**W7 执行计划已完成两轮子 agent 评审（2026-09-03 凌晨，`docs/plan/13_W7执行计划.md` v0.3 可开工版）**：
- 第一轮（工程可行性）抓到：0a/0c 依赖冲突须串行、人日系统性乐观 1/3、桌面端整块漏排（新增 3e 卡）、C4 拆臂禁 todo!() 改结构化 Err 臂、PromptLibrary 波 3/波 4 倒挂（类型前移）、金样与迁移两 diff 工具缺失、"407 零修改"口径矛盾（澄清为 helper 适配允许/断言零弱化）。
- 第二轮（设计保真与交付价值）抓到：**P0 模块清单两版都抄错编号**（正确五件 = G1 建造放置/G2 对局赛制/G5 战术棋盘/G8 编队指挥/G11 计分连击；战术棋盘曾丢失、视野 G4 误入）、|H|>4 访谈模式切换无验收断言（已补 scripted 场景）、真 AI 访谈零验证属"scripted 自证"同型风险（波 5 尖塔强制真 AI）、C6 装配去重无落点（1b 实做+双断言）、样板矩阵扩为四样板（尖塔全量/lane_defense 重表达/自走棋薄样板压 G2+G5+G8/音游微样板压 G11）、覆盖声明须标注实证等级、逆向模板重建显式入"不做清单"（有理由推迟至量产波）。
- v0.3 关键结构：波 0 类型底座（0a→0c 串行）→ 波 1 编译链（1c 四臂降机动卡）→ 波 2 custom → 波 3 系统模块+访谈+桌面（3a→3b→3d 串行，3c/3e/3f 并行）→ 波 4 迁移+7 模块入库 → 波 5 标定+四样板 → 波 6 文档定型与上呈（注 ③ 补裁/阈值反馈/v1.1.0 询问）。人日 50-62（初稿 25-30 经两轮修正）。
**用户已批准（2026-09-03）：`W7_设计方法论重构_定稿.md` 转正为设计真源（取代/修订 docs/design/01 的工作排在波 6）；执行计划 v0.3 生效。协作模式：主开发派卡/集成/验收，子 agent（claude-fable-5-high）开发自验收。**
**W7 波 0 已完成并发布 v1.0.8**（`b8cdf7b`，2026-09-03 凌晨，分支 1.0.8 + tag + main 同步）：
- 0a 系统模块类型（system_module.rs：SystemModule/NounDecl 五分/三端口/HeavinessLadder 五维/Induction 名词析取/CoreLink/tier_gate/PromptLibrary 类型；装备 E0-E6 谱系夹具锁档带）；
- 0b 效果与图（effects.rs/graph.rs：EffectSpec 7→16 单层 tag 旧档逐字节稳定（原始 JSON 回归测试锁定）/GraphSpec+CurveSpec/GameSpec.graphs/design_notes 五挂点；ParameterSchema 的 Graph/Curve tag 查证归属 adm4-decision 留波 1 协调）；
- 0c 组合校验器（composition.rs 纯函数：R-C1′ (a)(b) 硬 block、(c)|H| 参考线 advice+form_confirmation_required、双连通割点 advice、R-C2 预算 advice 降序表、R-C3 传导 worklist 不动点+NounProvided 析取、V4/V6；四撞墙案例测试全过：尖塔/MOBA/EU4 提示不 block/钉接反例 block）；
- 0d 金样工具（golden_make.ps1 -SourceRoot 基于 **v1.0.7 worktree** 生成（当时工作区半成品编译不过）、golden_diff.ps1 结构化比对+豁免清单+SelfTest 三场景；金样 14 文件三次重跑哈希一致；**PowerShell 5.1 脚本源文件需 UTF-8 带 BOM** 坑已记）；
- 0e 集成收口（pipeline/build 字面量最小适配 + C4 project_scenario 签名改 Adm4Result + 9 新变体诚实 Err 臂 + collect_data_structures 穷尽化空集注释 + 锁定测试防糊假渲染）。
- 主开发亲自复跑门禁：**476 测试全绿**（基线 407→476）、clippy 零警告、双包 validate OK、cli_smoke 退出码 0、desktop 构建 OK、golden SelfTest 三场景过、rg unity 零命中。
- 波 0 工程事实：0a 的 ConsistencyRule 因 adm4-space 反向依赖成环改为**同形镜像**（JSON 互通，波 3a 加载器须知）；E:\workwork\CrewAi\adm4_golden_base（v1.0.7 worktree）**保留**给波 1 对照用，波 1 验收后清理。
**W7 波 1 已完成并发布 v1.0.9**（`23d7ba9`，2026-09-03 上午，8 文件 +2374/-176）：
- 1a：C0 把 rationale/statement 编译进五类 spec 元素的 design_notes；Custom 缺 GWT 段 R2 阻塞（点名机制与缺段、递归覆盖嵌套）；Curve→两列 Table+插值注记；**Graph 编译入口如实降级为结构化 Err**（schema 侧 ParameterSchema 无 Graph 变体——归 0a 范围未交付，波 3a 补，豁免与申报已记）；C2 source_text 追加设计注记块+图结构摘要（system_prompt 未动）。
- 1b：C4 递归化（深度上限 8）+ Displace/Schedule/ModifyRule（含叠加序 GWT 文案）/DrawFromPool 四臂真渲染 + Custom 转录投影；AreaApply/Attach/Detach/RollCheck 四臂保持诚实 Err+锁定测试（1c 机动卡待样板需要）；collect_data_structures 覆盖全部 16 变体（审计"数据结构为空"根治）；C1 追加 graphs 闭合/rulemod 目标复检/Custom 非空复检/custom 红队必审清单；C6 装配任务按 spec_role/domain 聚合去重 + ModifyRule 跨机制依赖边 + 金样常驻断言（零同名重复、边数>任务数 10%）。
- 两卡断线接力实录：初派双卡 4:35 同时断线；resume 通道被平台拒（Sand traffic 错误）；重派收尾者接力完成——1b 收尾者遇"前任写入延迟到达"造成 build_tasks 重复定义，正确处置为保留前任更完备版本。
- 主开发复跑门禁：**506 测试全绿**（476→506）、clippy 零警告、双包 validate、smoke 退出码 0、desktop 构建 OK、金样豁免清单 3 条（graphs 空数组/design_notes/custom_review_targets）既有键值零漂移。
**W7 波 2 已完成并发布 v1.0.10**（`dad8707`，2026-09-03 晚，12 文件 +1644/-31；三任接力：初派 13:44 断线（authoring 全套+R5+gate4 已落盘）→ 一次收尾 15:59 断线（services 骨架）→ 二次收尾完成）：
- custom 机制一等入口：`add_custom_mechanic`（草案校验：归属系统/rule_text/effects 全集反序列化/Custom GWT 三段/rationale 必填）→ `is_custom:true` 项目私有 L4 单选点走全部既有链路；`SpecEffectTemplateValidator` 在门面层实现（EffectSpec 反序列化+GWT 递归+ModifyRule 悬空三通道）；
- 冻结落盘 `frozen/v{N}/custom_points.json` + 流水线跑增广空间（`frozen_space`）——不接这两处 custom 点会在 C0 被"清单中不存在"拦死；
- R5 覆盖 custom（不豁免）；gate4 每个 custom 必须显式处置（accept/revise+署名，CLI `freeze dispose`）；40% advisory；
- CLI `custom add --draft <json>/list/remove`；冒烟 §6b custom 段（负例拒收→add→confirm→gate4 拦→dispose→冻结→C4 断言）；
- **审计回归钉子 `audit_regression_custom_mechanics_reach_c4_and_c6` 通过**："可编程索敌"与"波次自适应变异"（审计 B 中彻底消失的两个原创机制）现在全链走到 C4 能力契约（GWT 非空）+ C6 程序任务 + C2 rationale 注记——W7 的核心承诺兑现。
- 主开发验收：**513 测试全绿**（506→513）、冒烟退出码 0（含 custom 段）、金样零漂移。
**W7 波 3 已完成并发布 v1.0.11**（`f7e01c4`，2026-09-05 上午，55 文件 +12648/-97；分支 1.0.11 + tag v1.0.11 + main 已同步；六卡全验收，多任接力实录见 `docs/memory/w7_wave3/` 各断点申报）：
- **3a 加载器**（四任接力：两任断线于调研、第三任完成 Graph/Curve+SystemRef+system_loader 后断线、第四任收尾）：`adm4-space/system_loader.rs`（SystemRef 引用+绑定/命名空间重写/tier 合成点 `<instance_id>.tier` 复用 unlocks/V6 绑定悬空 fail-closed/activates-tier_gate 矛盾拦截/allowed_tiers 收窄剔除不可达点/模块点 compiler_tags["system"] 重写为合成点 id）；`FrozenDesign.module_versions` **条件键**（无模块项目哈希逐字节零漂移，有模块进哈希+frozen_space 复演 semver 比对 fail-closed）；ParameterSchema Graph/Curve 补齐（0a 欠账），C0 Graph 臂翻转真编译（GameSpec.graphs+R4 闭合），波 1 降级 Err 锁定测试按豁免翻转；`system_module_add/list`（项目私有模块，content/system_modules.json）；AppConfig.system_modules_root（default `knowledge/systems`）。
- **3b 组合校验接线**：`adm4-authoring/compose.rs`（assess_composition 纯函数）；gate2 合流（blocks 拦冻结/advices 提示/tier 未选=block 不默认档兜底/composition_unavailable fail-closed）；|H| 署名形态确认流（`compose_confirm_form` R3 留痕含 h_set 快照，h_set 变化确认失效重签，未被要求拒签，AI 永不代签，硬 block 确认后仍拦）；`composition_report` authoring 即时反馈与 gate2 逐字节一致；CLI `compose report/confirm-form`。
- **3c 首批 4 模块**（两任接力）：sys.equipment（E0-E6 七档 13 点，传导按定稿 §4.4 三条）/sys.inventory（basic_capacity/classify/batch_ops）/sys.loot（quality_affix_weights 档，provides drop_table/gem_entity/material_entity/skill_effect_def）/sys.economy（recycle_loop 档）——传导 min_tier 逐字节咬合；**永久无痛接入门禁** `adm4-decision/tests/knowledge_modules.rs`（遍历全库：validate+档带对表+传导咬合+L4 effects_template 非空，第 5 个模块零代码自动纳入）；无痛接入验收单 `w7_wave3/3c_无痛接入验收单.md`。
- **3d 访谈三段式**：`concept.rs`（口述→系统清单+建议档+core_loop 提案；**大战略嗅探**：重核候选>4 切逐重核轻重理清模式、只提示不设阻、未理清确认拒；**融合嗅探**：X+Y→双核并集分解）/`compose_fix.rs`（违例人话解释+修复选项闭集，升降档走既有 select，|H| 引导署名 AI 不代签）/机制访谈（propose_next_scoped 实例命名空间+PromptLibrary 弹药注入 prompt 可断言+custom 草案 AI 起草仍走 custom_add 确认）；core_loop 落 AuthoringState 回填 CompositionInput（3b 遗留①闭合）；purpose 五键+scripted schema 文档 `docs/design/08_AI访谈三段式与Scripted脚本schema.md`；计划 §4 硬断言测试过（5 重核→模式切换→逐系统档位+rationale 落盘）；AI 越界（发明模块/档位 id）Err+零落盘。CLI `interview concept/concept-clarify/concept-confirm/mechanism/draft-custom`、`compose fix/fix-apply`；冒烟 8j 段。
- **3e 桌面**：「系统组合」面板（实例清单档位 W 分 κ/缺档待办点击跳 tier 点走既有 select/私有模块只读/组合报告红 block 黄 advice/|H| 确认卡署名→留痕→stale 重签）；GUI 零业务规则；**真机走查清单 7 项在 3e 报告，待用户/主开发点验**。
- **3f PromptLibrary 种子**：30 条真取舍问句（8 域含四模块），source_ref 全部 v2 可溯源（机器断言）；`knowledge/prompt_library/seed.json` + 永久校验测试。
- 主开发亲自复跑门禁：**589 测试全绿 0 ignored**（513→548（3a）→559（3b）→589（3d/3e））、fmt 无 diff、clippy 零警告、双包 validate OK、cli_smoke 退出码 0（含 compose 段+8j 访谈段）、desktop 构建 OK、golden SelfTest 三场景过、rg unity 三目录零命中。
- **波 3 工程事实**：①3d 发现 PowerShell 管道 Set-Content 会把 UTF-8 源文件写成 GBK（compose.rs 一度损坏，Write 全量重写恢复）——**子 agent 不得用 PowerShell 管道改源文件**；②robocopy /MIR 会删掉 worktree 的 .git 指针文件，发版时须重建（`gitdir: <主仓>.git/worktrees/<name>` 一行）；③3b 遗留：CompositionBudget 恒空表（波 5 标定回填）、u.target_scale 未选择时产品档按超休闲最严参考线（提示噪声可接受，若嫌吵再裁）；④3c 遗留：skill_effect_def 现由 sys.loot provides（PoE 口径），正式技能系统入库后成为析取第二源。
**W7 波 4 已完成（2026-09-05，五卡全验收，待发版 v1.0.12）**：
- **4-0 迁移 diff 工具**：独立 crate `tools/spec_diff`（`cargo run -p adm4-spec-diff -- --old --new [--map]`；映射表 exact/prefix/ignore_paths 三段 deny_unknown_fields、最长前缀优先、碰撞即 Err；换算覆盖元素 id/system_id/effects 公式内引用/design_notes source/source_map SpecRef；退出码 0 零 diff/1 有差异/2 输入错；两侧先 parse 成 GameSpec 再比对，serde default 抹平缺键假差异）。金样 C0 自比对零 diff 实证。11 测试。
- **4a1+4a2 七模块入库**（每模块仅新增 module.json 零代码，knowledge_modules.rs 门禁自动纳入，库内共 **11 模块**）：sys.build_placement（G1 四档 BP0-BP3，11 点；BP2 档写明"物理坍塌出射程、支撑规则表在射程内"边界）/sys.match_format（G2 四档，8 点；胜负事件以 sys.objective.* 命名空间声明，提供方待 #31 入库）/sys.tactical_board（G5 四档，10+4 点；TB3 位移连锁=Displace 主场；**供给方无传导属正确不虚构**）/sys.squad_command（G8 四档至 Gambit 策略，9 点）/sys.scoring_combo（G11 四档至对抗性计分，7 点；衰减曲线用 Curve、K3 裸名词 provides 供赛制）/sys.class_archetype（#40 定稿 §9.2a 两档形态+A2 裁量档，10 点；轻档涌现式安装约束+派生标签、"须持盾"=add_precondition 谓词落 sys.equipment 真实名词；A 主项表达居首）/sys.onboarding（G6 四档至自适应引导，6 点）。传导一律 NounProvided 析取或库内真实档 id，无跨批 Module 点名。
- **4b 两包拆解（三任接力：初派断线于方案、收尾者断线于报告但产物全落盘，主开发代验收）**：三个结构性发现是关键工程事实——**发现 A**：重写=纯前缀替换+实例 id 包内唯一 → 每包最多一个模块实例可保 id 迁移；**发现 B**：**lane_defense 本卡零迁移**（system_refs→module_versions 进冻结哈希→金样 identity.frozen_hash 必漂移且豁免不许新增；ld.tier 恒 Active 破 cli_smoke 既有断言）——ld 拆解归量产波金样重固化窗口；**发现 C**：grid_strategy 可迁（无金样）。执行：grid 4 点（battlefield_system/move_rule/terrain_effect_rule/terrain_table）迁入 sys.tactical_board（semver 1.1.0，新增 MG0/MG1 迁移承接档——tier 合成点须 ≥2 选项是结构原因；迁移点 compiler_tags.system 保持字面 grid.battlefield_system 才能保 C0 归属）；pack 加 system_refs（instance_id=grid 保 id）+migration_map.json（注释进 .notes.md 侧车，映射表本体 deny_unknown_fields）；`grid_migration_equivalence.rs` 断言 C0 语义等价（spec_diff 零 diff，豁免仅 frozen_hash/project_id 同源+tier 新增 added）+C6 零同名重复；分母对账 2610→2611（+1=grid.tier）；**1 个 ignored 测试属一次性迁移前基准捕获工具（夹具固化禁重生成，主开发裁决接受）**。25 模板标 smoke_test（Template 加 serde default 字段不破坏 answers_digest 指纹；project_new 默认拒/project_new_allowing_smoke+CLI --allow-smoke 放行；三条负测试）。**量产波拆解预案 10 条在 4b_迁移方案.md §四**（含 ld deploy/economy 二选一保 id、grid 战斗六点须新前缀改名迁移、MG 档归并正式 TB 谱系等）。
- **4c PromptLibrary 全量**：**211 条**（seed 30 + 新 181，distance 300 上限余 89——素材真金见底宁缺勿滥）；10 文件分域；11 模块域各 ≥5；打捞率 8.2%（211/2575），104 节点逐行对账单（`4c_PromptLibrary对账单.md`）；测试升级为全库遍历（总量 ≤300/id 唯一/溯源/前 12 字符去重）。
- 主开发验收基线：**606 测试全绿**（589→600（4-0）→603（4c）→606（4b））、fmt 无 diff、clippy 零警告、validate 双包 OK（grid 2611/ld 2604）、cli_smoke 退出码 0、golden SelfTest 过、desktop 构建 OK。
**v1.0.12 已发布**（`3090faa`，2026-09-05，分支/tag/main 三处同步）。
**W7 波 5 已完成（2026-09-06，五卡全验收，待发版 v1.0.13）**：
- **5-0 标定回归**：11 款样本（10 审计款+EU4 锚）逐款系统分解/五维评分/κ 判定/B(G)，散点无排序倒挂（机器断言锁定）；五档预算建议值 **hyper_casual 12/casual 28/mid_core 42/hard_core 55/mmo 68**（占位试用态入库 `knowledge/calibration/budget.json`，删除即回滚静默态；**报告 `w7_wave5/5-0_标定报告.md` 含签字栏待用户签字**——签字后方法论才定稿）；等权检验不改公式、档带 4/8/12 合理不改；R-C1′ 11 款零硬违例误伤；上呈 4 条（大战略档 95-105/O 维语义分横/割点提示措辞/超休闲与 MMO 补样）。接线：assess_composition 增 budget 穿参、services.load_composition_budget。基线 606→621。
- **5a 杀戮尖塔全量样板**（两任接力）：**三模块入库**（sys.turn_combat #2/sys.run_deckbuild #13/sys.rule_modifier_collect #14，主开发裁决入库而非项目私有——库内自此 14 模块）；spire_like pack（三实例 H={构筑,战斗,遗物} 连通、|H|=3 超中核参考线→**署名确认流第一个真实品类实证**；B(G)=31.5≤42 预算联动实证）；全链 e2e 四伤疤全过：DrawFromPool 契约、**两遗物不同 priority 的叠加序 GWT 文字（定稿指令 7 正式验收）**、Graph acyclic 地图、C6 ModifyRule 跨机制边；冒烟 8k 段（PowerShell 5.1 传 JSON 吞引号坑→ConvertTo-NativeJsonArg 转义函数）。**真 AI 概念访谈未验证=已知缺口**（数据根无 AI 配置，验收单已明示补跑路径）。数据修复：三模块+pack 的 effects_template 裸语义名 entity 改 {param:xxx_table_id} 占位+必填参数（**C1 effect_dangling_entity 拦裸名——后续所有模块/样板的硬纪律**）。基线 621→623。
- **5b 自走棋薄样板**（重派一次）：autochess_thin pack 五实例（赛制 mf2 重档 core/棋盘 tb1/编队 s1/构筑 db0/经济 T0）；**H={format_main} |H|=1 零提示路径实证**（与尖塔超线确认流形成对照）；三模块 C4 证据（G2 名次判定+随机对阵 DrawFromPool/G5 网格初始化/G8 人口预算编成）；羁绊=ModifyRule 叠加序、利息=pack 薄点；**1c 上报两条**：G8 阵型协同（synergy_bonus 全 Attach）与 G2 循环积分（RollCheck）被未交付臂挡住正名口径，1c 交付后本样板即现成验收场景。实战教训：**pack 选项 summary 里的参考游戏名撞 R5 换皮词表**（reference_games 自身是词表源）——商业游戏名只进 reference_games。基线 623→627（含 5c 并入）。
- **5c 音游微样板**：sys.beatmap_timeline（#29）入库（15 模块）；rhythm_micro pack 两实例；**「判定事件→分数」供需链三层证据**（命名咬合/装配级+V6 反例/产物级 GWT）；**Curve→两列 Table+插值注记通路首个真实模块实证**；SV 变速段退化四列 Table 如实申报（§5.4 已知缺陷）；combo_window 绕行 RollCheck 臂如实申报。
- **5d 塔防 G1 轻档 + C3 复核**（范围调整：ld 重表达对照因 4b 发现 B 推迟至量产波金样重固化窗口，验收单引用说明）：towerdef_thin pack 三实例（BP0 预设槽位 core/经济 T0/计分 K0），**|H|=0 空集第三条判定路径**（B(G)=9.75）；BP0 占用约束 claim_exclusive C4 字面断言；波次=pack 薄点（#7 未入库）。**C3 user_prompt 属性值欠账已修**（表行实体渲染 key=value、取不到退键名不发明数据；金样 C3 产物不含 user_prompt 字段，修复=纯提示词增强）——**主开发亲自复核金样重固化比对：C0/C1 全部差异命中既有 3 条豁免（graphs/design_notes/custom_review_targets），零未豁免漂移**。上报：slot_legality 撞 RollCheck（1c 第三场景）、Rows 参数不走占位符替换（基建缺口，宜与 1c 同窗）。基线 627→630。
- 主开发验收基线：**630 测试全绿（1 ignored=4b 一次性夹具捕获工具，已裁决接受）**、fmt/clippy 干净、validate 六包 OK（autochess 2642/grid 2611/ld 2604/rhythm 2605/spire 2610/towerdef 2617）、cli_smoke 退出码 0（含 8k/8l/8m 三新段）、desktop 构建 OK、金样实比对干净。
- **主开发操作事故与恢复（记档）**：金样验证时误用不存在的脚本参数（golden_make 无 -DataRoot 参数，直接覆盖了基准）→ 从 v1.0.12 tag worktree 恢复 → 正确流程=备份 tests\golden → 重固化 → JSON 级豁免比对 → 恢复。**golden_make.ps1 无参数、恒写 tests\golden——跑它之前必须先备份**。
**v1.0.13 已发布**（`044120f`，2026-09-06）。
**用户三项裁决全部完成（2026-09-06）**：
1. **标定报告已签字定稿**（对话式 Q1-Q6 确认，主开发代录进 `w7_wave5/5-0_标定报告.md` §9）：11 款排序无异议；五档预算值 12/28/42/55/68 接受；预算刻度=设计复杂度维持；五维等权+档带 4/8/12 维持（问题转"后台标记记录制"，攒实例再议不因单例改公式）；**超预算永久制度化**：不开任何新档（大战略档 95-105 方案正式作废——用户原话"很可能出现更大更复杂的，超限制的都不做限制"），超线一律提示+一次性署名形态确认；mmo/hyper_casual 外推值接受先用。已回填定稿 §4.3。
2. **1c 批准开工并已完成**（见下）。
3. **DeepSeek 密钥已配置**（named:deepseek_key + .adm4_data/config/app.json，provider=deepseek/model=deepseek-chat；invoke-check 411ms 实证连通；密钥值不进日志/存档/git）。
**T-W7-1c 四臂真渲染已完成**（2026-09-06）：AreaApply（空间查询+内层递归）/Attach（挂载+生效期+priority 叠加序文字与 ModifyRule 同款）/Detach（卸载）/RollCheck（判定+成功/失败两分支递归）四臂真渲染；新增 check_effect_references 悬空复检（Attach/Detach target 判据同 effect_dangling_entity、Detach 的 modifier 须被某 Attach 声明过）；锁定测试翻转+9 个负测试；**四场景全部翻正**（自走棋阵型协同=叠加序 GWT、循环积分=晋级/淘汰两分支、塔防槽位判定=判定+拒绝分支、音游全连 combo=断连归零判定——5c 裁量翻正也做了）；三处模块数据占位符化申报（含 slot_legality 撞"Rows 不走占位符"基建缺口后改 scalar 三表引用的数据路线，5d 遗留 3 命中）；金样比对干净（差异只命中既有 3 条豁免）。**16 个效果变体自此全部真渲染，C4 无未交付臂。**
**T-W7-6pre 真 AI 访谈缺口修复已完成**（2026-09-06）：缺口 A=concept.rs 注入已占用实例 id 清单+system prompt 硬约束（校验防线不拆只增文案）；缺口 B=lane_defense 补 core_nouns（combat_attribute 未加——系统间名词该走 provides；frozen_hash 实证不变 sha256:64bda8cc…）；真 AI 复验 6 次（撞名从必然降为特定行为、全部拒收点名修复方向零静默）；**新遗留登记波 6**：①spire 侧"重列既有系统"约束待复验（费用上限用尽）②derive_binding 核心名词兜底用裸名 command_intent 对不上六包统一命名 player_command_intent（推导逻辑改动待授权）③两个复验临时项目留数据根备查。
- 集成基线（主开发复跑）：**643 测试全绿（1 ignored 既裁）**、fmt/clippy 干净、六包 validate OK、cli_smoke 退出码 0、desktop 构建 OK、golden SelfTest 过。
**v1.0.14 已发布**（`7a02e26`，2026-09-06，推送前密钥扫描零命中）。
**W7 波 6 已完成（2026-09-06，两卡各两任接力，W7 全部七波收官）**：
- **6a 文档定型**：①`docs/design/01_决策模型与设计空间.md` 按 W7 定稿重写转正（文首实况锚：15 模块/六包/16 变体全渲染/643 测试）；②`docs/design/09_覆盖性声明.md` 新增（三级实证：样板实证 4 款各附 pack+验收单引用/纸面复核 2 款/推演 7 款；Custom 兜底列；射程外四类+四条拓宽通道；未实证面如实声明）；③`docs/plan/03_工程规范.md` 追加 §8 七条 W7 纪律（断线接力/C4 Err 臂/修正案 7 双环写法/golden_make 跑前必备份/PowerShell GBK 坑/禁商业游戏名/entity 占位符）；④`docs/memory/w7_wave6/6_上呈清单.md`（响应式回合两方案草案待用户裁向/阈值反馈汇总/量产波待办/**v1.1.0 建议升**）。纸面复核结论：星露谷 9 系统表达位全齐判定维持（8 模块待入库）、幸存者 5 系统同（3 模块待入库），去重 10 模块入库清单进上呈待办。
- **6b 遗留收口**：①derive_binding 命名口径兜底修复（尾段后缀唯一命中兜底、歧义宁缺；正反测试）；②**真 AI 复验 4 次（累计 10 次）两场景各 1 次完整通过校验**——spire 零撞名零重列（6pre 约束实证生效）、lane_defense 的 command_intent 自动绑定成功（兜底修复实证有效）；另两次拒收均为口述超 pack 表达域的语料问题，fail-closed 点名正确非缺口；③autochess pack「未交付臂」历史注记清零；④复验临时项目清理。
- 主开发复跑门禁：**646 测试全绿（1 ignored 既裁）**、fmt/clippy 干净、六包 validate OK、cli_smoke 退出码 0、desktop 构建 OK、golden SelfTest 过。
**W7 全线收官。剩余用户裁决/动作**：①**是否升 v1.1.0**（上呈清单建议升，依据齐备）；②响应式回合两方案草案选向（上呈清单 §响应式回合节）；③真机走查（3e 七项+T13b 12 项）。**量产波（W7 后下一主线）待办已汇总在 6_上呈清单.md**：ld/grid 剩余拆解 10 条预案（金样重固化窗口）/13 缺口模块 P1P2/星露谷幸存者 10 模块/逆向模板重建/Rows-Matrix 占位符基建/MG 档归并。Phase 2 后半（G4b/G5）仍按用户裁决暂缓。
**工程注意事项（本轮实证）**：①子 agent 断线频繁且 **resume 通道不可用**（Sand traffic 平台错误）、断线后最终报告经常收不到——主开发必须靠**文件系统证据**（transcript 时间戳、代码落盘时间、cargo check、rg 关键符号）判断真实进度，断线即起全新收尾者并在卡文写明"前任已完成清单"；②并行卡的类型新增会打破下游编译，属预期（穷尽匹配纪律生效），收口卡处理；③发版走 worktree+robocopy（V4 被根 .gitignore 忽略），worktree 删除偶发 Permission denied，Start-Sleep 2 后 Remove-Item 重试即可。
**G4a 已完成（2026-09-02，任务卡 `docs/plan/11_G4a任务卡.md`，未发版）**：三卡两波——T-G4a-1 `program/{slice,manifest,engine_guide,dev_round}.rs`（可玩切片确定性抽取（1 主操作/1 主场景/1 成败反馈，多候选按 R2 落 Blocked 点名候选——主开发裁决见任务卡）+ 薄运行时清单 + 引擎无关指南骨架（`EngineGuideSource`/`NotProvidedGuide`）+ 轮次记录与四份 PLAYABLE_*.md durable docs）；T-G4a-2 `engine/{types,mock,mcp/*}.rs`（`EngineBackend` 扩至六方法、`EngineProjectSeed` 等六契约、通用 MCP stdio 客户端（JSON-RPC 2.0 / initialize / tools.list / tools.call，协议版 2025-06-18，零引擎语义）、`MockEngineBackend` 确定性回放）；T-G4a-3 P0 `engine_seed` 真产（`status="produced"`+`seed`，旧档 `pending_g4` 兼容）+ `P1Executor`（P0 契约→切片→清单→指南→durable docs→预检门控→Mock 一轮开发；Blocked 产物照落）+ `PENDING_STAGES` 剩 3（P3 归 G4b）+ 门面 `build_run_with_engine`/`build_p1_summary` + CLI `--mock-engine`/`build p1-status` + 冒烟 P1 段。**基线 407 测试全绿**、clippy 零警告、双包 validate OK、冒烟退出码 0、desktop 构建 OK、`rg -i unity crates/adm4-build/src crates/adm4-app/src apps` 零命中（顺带清理了 sdk/deliverable/desktop/cli 里 6 处历史 unity 字样，`target_engines` 默认值改「未指定」）。要点：lane_defense 夹具经 P0 派生 3 个 Command 能力，切片抽取对它如实 Blocked（设计侧未收敛，R2）——P1 成功链在 `executors.rs::tests::p1_integration` 用最小夹具验证；`P1Executor` 拿 Succeeded 需指南来源返回 Ok（`NotProvidedGuide` 进 reasons），G4b 接线须同时提供引擎指南来源。**波次纪律教训**：子 agent 常在 30-40 分钟断线，任务卡必须写明「调研 ≤10 分钟、先跑测试后动手」，断线后重派要附「现状+根因+逐文件改动清单」。下一波 G4b（unity_mcp 真机接线，`engine/unity_mcp/` 是 unity 字样唯一例外）或 G5（proof 验收）待用户拍板。
**G4a 已完成并验收（2026-09-02，主开发 Cursor 会话；子 agent 模型 Claude Fable 5.1 high；任务卡 `docs/plan/11_G4a任务卡.md`；未发版，待用户指令 v1.0.7）**：
- T-G4a-1 `program/{slice,manifest,engine_guide,dev_round}.rs`：确定性切片抽取（core_loop←intent.experience_promise / primary_input←唯一 Command 能力 / scene←唯一 content / success_or_fail←唯一 acceptance.then；缺→Blocked 点名，多候选→**Blocked 点名候选（主开发裁决：设计未收敛属 R2，非程序错误）**）、`RuntimeManifest` 一页 Markdown 只含契约事实、引擎无关 `EngineGuide`/`EngineGuideSource`/`NotProvidedGuide`、`DevRoundLog` 可停可续 + 四份 `PLAYABLE_*.md`。
- T-G4a-2 `engine/{types,mock}.rs` + `engine/mcp/{jsonrpc,transport,client}.rs`：`EngineBackend` 扩为六方法（id/preflight/open_or_create_project/agent_develop/run_playmode/capture_proof，对象安全）；契约 `EngineProjectSeed/SliceTask/DevContext/DevRound/RunResult/ProofBundle`；**通用 MCP stdio 客户端**（JSON-RPC 2.0、一行一帧、initialize→notifications/initialized→tools/list→tools/call，server error/isError/未握手均 Err 不吞，协议版本 `2025-06-18`，`StdioTransport::spawn` 显式拉起、`ScriptedTransport` 测试）；`MockEngineBackend::new(id, MockEngineScript{preflight_ready,rounds,run,proof})` 确定性回放 + `calls()`，**不做预检门控**（门控在 P1Executor）。零第三方依赖，`unity` 字样零命中。
- T-G4a-3 `executors.rs`：P0 `engine_seed` 真产（`status="produced"` + `seed: Option<EngineProjectSeed>`，engine_id 来自 `config.engine_backend` 默认 `none`；旧档 `pending_g4` 可读）；`P1Executor::new(backend, guide_source, workspace_root)` 全链：读 P0→抽切片→渲清单→guide()（Err 记 reasons 继续）→durable docs + DevRoundLog 落 `<store>/P1/workspace/`→preflight 不就绪则**不调 open/agent_develop**→一轮开发→`P1Contract` 落盘（Blocked 也落，产物即证据）。`PENDING_STAGES` 缩至 3（P3 归 G4b、P4/P5 归 G5）。门面 `build_run_with_engine/build_rerun_with_engine`（既有 `*_with_images` 委托）、`build_p1_summary`（`P1Summary.contract_present=false` 时只给阻塞摘要）；CLI `build run/rerun --mock-engine`、`build p1-status`；冒烟追加 P0→P1 段。
- **已登记设计侧问题**：lane_defense 包经 P0 派生 3 个 Command 能力（`cap_ld.counter_damage/deploy_cost/income_rule`），切片"1 主操作"无法收敛 → **P1 对该包永久 Blocked**（冒烟以此作 R2 证据）。需在 pack 侧标出主操作或给切片规则加收敛依据，G4b 前裁决。
- 其他登记：`crates/adm4-app/src/{sdk,deliverable}.rs`、`apps/**` 有 6 处既有 `unity` 字样（SDK 默认 `target_engines: "Unity"`、Phase 2 占位文案），非 adm4-build，未清理待裁决；`P1Executor` 成功链要求 `EngineGuideSource::guide()` 返回 Ok，G4b 真机接线须同时提供该引擎指南来源；`executors.rs::tests::p1_integration::Harness` 可作 G4b 集成测试模板。
- 基线：**407 测试全绿**、clippy 零警告、fmt 无 diff、双包 2604/2610、desktop 构建 OK、cli_smoke 退出码 0、`rg -i unity crates/adm4-build/src` 零命中。
- 过程教训：Fable 子 agent 有 3 次在长调研（>30 分钟不写代码）阶段被平台中断（`Sand traffic is not supported`）；后续任务卡一律写明"调研 ≤10 分钟，开工即改，用测试失败驱动"。
- **下一波 G4b**（后置，需本机 Unity 2022.3 LTS + Python 3.10+/uv + CoplayDev/unity-mcp）：`engine/unity_mcp/` 具体后端（是接缝纪律允许 `unity` 字样的唯一例外，扫描命令排除该目录）+ Unity 版 `EngineGuideSource` + P3 现场装配。开工前先裁决 lane_defense 主操作收敛问题。

## 2. 阅读顺序（新会话必读，按序）

1. 本文件（入口与恢复步骤）
2. `docs/memory/2026-08-29_开发进度与交接.md`（进度、架构裁决、待办、T5 验收结果）
3. `docs/plan/04_子任务排序与派发.md`（任务表与验收标准）
4. `docs/plan/03_工程规范.md`（派发任务卡时必须附带的规范）
5. 需要时再查：`docs/plan/01/02`（设计计划/开发计划）、`docs/design/00-06`（设计真相源）、`docs/memory/2026-08-29_内容记忆.md`（已产出内容明细）、`docs/memory/2026-08-29_会话记忆.md`（用户意图原文脉络）

## 3. 环境与门禁命令

- 环境：Windows + PowerShell；Rust 1.96.0（`rust-toolchain.toml` 锁定）；一切操作在 `V4/` 内。
- 波次统一门禁（主开发在每波结束后亲自跑）：

```powershell
cd V4
cargo fmt --check                              # 无 diff
cargo clippy --workspace --all-targets         # 零警告
cargo test --workspace                         # 全绿
cargo run -p adm4-cli -- space validate        # 双品类包 OK
```

- 已知的坑：并行子 agent 共用 `target/` 偶发 LNK1104 链接锁，重试即过；Slint 生成码与 workspace `unsafe_code=forbid` 冲突，`adm4-desktop` 已单独降为 `deny`，勿改回。

## 4. 协作模式规则（用户已定，不可变）

-1. **git 版本规则（2026-08-29 用户指定）**：每次完成更新后提交 git 推送到 https://github.com/snowpeak008/GameMaker （V4 内容放分支根目录，main 跟进），版本号补丁位自动 +1（当前 v1.0.0 → 下次 v1.0.1 → v1.0.2 ……），每个版本打对应 tag 与同名分支；大版本号（次版本/主版本）只在用户明确通知时变更。Python v1 归 https://github.com/snowpeak008/AutoDisignMaker （分支 1.2）。注意：本工作区根 .gitignore 忽略了 V4/，向 GameMaker 提交走独立 worktree 方式（参考 W5 后的推送操作：git worktree + robocopy 剔除 target/.adm4_data）。

0. **子 agent 模型策略**：子 agent 只能用 Claude 旗下模型。**用户首选 Claude Opus 5 high（2026-09-02 晚重申）**——但当前 Cursor 环境 Task 工具仅开放 `claude-fable-5-high` 一个 slug（Opus 5 slug 会被拒绝），故在平台开放前暂用 Fable 5 high 代替；**每个新会话应先探测 Opus 5 是否已可用，可用即切换**。
1. 主开发不写代码，只派发/集成/验收；子 agent 并行开发。
2. 同一波次内任务的可写文件集必须互不相交；共享文件（`adm4-app/src/services.rs`、`universal/core.json`、根 `Cargo.toml`）单独派发或波次间由主开发集成。
3. 任务卡三要素照抄既有格式：必读文档清单 + 可写范围/禁止事项 + 逐条验收标准与自验收命令与报告格式（参考 `04_子任务排序与派发.md` 与本文件第 6 节的预写任务卡）。
4. 验收 = 子 agent 自验收 + 主开发抽查关键声明（代码级核对红线逻辑）+ 波次统一门禁。
5. 既有决策点/选项/阶段/存档 id 只增不改不删（D4）。

## 5. 恢复开发步骤

1. 确认基线：跑一遍第 3 节门禁命令，应全绿（若不绿，先查 `git status` 是否有会话外改动）。
2. 读 `2026-08-29_开发进度与交接.md` 文末「T5 验收结果」，拿到 T5 暴露的门面方法清单（T6/T7 的接口契约）。
3. 并行派发 W3 两张任务卡（第 6 节预写好，把其中【接口契约】占位段替换为 T5 报告的方法清单）。
4. W3 齐后主开发跑统一门禁 → 并行派发 W4 三路验收（T8a 全量门禁+CLI 冒烟走查、T8b 红线 R1-R7 合规审查、T8c 文档一致性与 README 更新）。
5. W4 通过 → 产出交付报告 → 询问用户是否 git 提交（用户未要求前不提交）。
6. 更新本目录记忆文件。

## 6. W3 预写任务卡（派发时直接用）

### T6 CLI 子命令补齐

```
你是 AutoDesignMaker 第四版（V4）的开发子 agent，领取任务 T6「CLI 子命令补齐」。
工作区根 E:\workwork\CrewAi\AutoDesignMaker，项目在 V4\，Windows + PowerShell。

【必读】V4\docs\plan\03_工程规范.md 全文；04_子任务排序与派发.md 的 T6 任务卡；
V4\crates\adm4-app\src\services.rs（门面 API 事实）；V4\apps\adm4-cli\src\main.rs（现有命令风格）。
【接口契约】adm4_app::AppServices 新方法（*_with 变体收 &dyn AiProvider 供脚本，无后缀版内部 build_provider()）：
- template_new_draft(pack_id, template_id, game_name, aliases: &[String], depth: DesignLevel) -> Template
- template_search_corpus(pack_id, template_id, corpus_root: &Path, decision_question: &str, keywords: &[String])
  -> Vec<EvidenceCandidate>（候选池按 source_url 去重累积，持久化于 <design_space_root>/<pack>/references/.candidates/<id>.json）
- template_map[_with] -> usize；template_cross_check[_with] -> CrossCheckReport
- template_review(pack_id, template_id, reviewer, note) -> Template；template_certify(pack_id, template_id) -> Template
- template_compare(archive_id, template_id) -> TemplateComparison（只读对照）
- interview_next[_with](archive_id) -> InterviewTurnDto（serde tag "turn"：structural_point/table_proposal/complete；
  其 proposal 必须原样传回 confirm）
- interview_confirm(archive_id, &InterviewProposal, overrides: Option<ParameterValues>) -> Vec<String>（待填清单，非空不阻断）
- interview_reject(archive_id, decision_id, note)；interview_progress(archive_id) -> InterviewProgress
预填走 project_new(..., Some(template_id))，未认证模板 Blocked。
注意：confirm/reject 必须绑定用户手势（D11）；lane_defense 同层拓扑序中 ld.counter_matrix 先于名单表被提案，
冒烟脚本可用"拒绝"先填名单（adm4-app e2e 已示范）；预填理由含模板游戏名会被换皮门拦截属预期，需引导重写。
【任务】只允许修改 V4\apps\adm4-cli\**。新增子命令：
- template：new-draft / import-corpus（或 search-corpus）/ map / cross-check / review / certify
- interview：next / confirm / reject / progress
- prefill：用 Certified 模板预填项目（未认证被拒的错误信息要可读）
全部命令有中文 --help；输出结构化、可脚本断言；AI 相关命令支持通过配置走 ScriptedProvider 以便冒烟。
写一个冒烟脚本 V4\scripts\cli_smoke.ps1：无人值守走完 逆向五步→预填→访谈补齐→冻结→C0-C6。
【自验收】cd V4; cargo fmt --check; cargo clippy -p adm4-cli --all-targets（零警告）;
cargo test --workspace（全绿）; powershell -File scripts\cli_smoke.ps1（退出码 0）
【禁止】改其他 crate 逻辑/docs/plan/docs/design；git 操作；新增第三方依赖。
【报告】改动清单；任务卡逐条自评；自验收输出摘要；遗留与声明。
```

### T7 桌面 UI 四面板

```
你是 AutoDesignMaker 第四版（V4）的开发子 agent，领取任务 T7「桌面 UI 四面板」。
工作区根 E:\workwork\CrewAi\AutoDesignMaker，项目在 V4\，Windows + PowerShell。

【必读】V4\docs\plan\03_工程规范.md 全文；04_子任务排序与派发.md 的 T7 任务卡；
docs\plan\01_设计计划.md 第 3.4 节（D13-D15）；docs\design\06_存档AI与UI.md；
V4\apps\adm4-desktop\（ui\main.slint 与 src\main.rs 现状）；
V4\crates\adm4-app\src\services.rs（门面 API 事实）。
【接口契约】与 T6 任务卡相同（见上方 T6 的【接口契约】段，两卡共用同一份 AppServices 方法清单与注意事项）。
【任务】只允许修改 V4\apps\adm4-desktop\**。四块：
1. L5/L6 结构化表格编辑器：Table 行增删/格编辑、Matrix 格编辑，替代现有 JSON 文本框
   （JSON 文本框可保留为高级模式）。
2. 访谈面板：显示提案（单点/整表）、确认/拒绝按钮、整表例外下钻编辑、分层进度（InterviewProgress）。
3. 逆向维护面板：五步产线状态（Draft→Mapped→CrossChecked→HumanReviewed→Certified）、
   逐步触发按钮、交叉核验冲突清单、人工审核（署名+意见必填）。
4. 冻结门明细面板：五道门逐条 finding 列表与全绿才可冻的提示。
GUI 无业务规则（D14）：一切校验走 AppServices，UI 只渲染+转发。
注意：adm4-desktop 的 [lints.rust] unsafe_code="deny" 是刻意的（Slint 生成码），勿改回 forbid。
【自验收】cd V4; cargo fmt --check; cargo clippy -p adm4-desktop --all-targets（零警告）;
cargo build -p adm4-desktop（链接成功）; cargo test --workspace（全绿）
【禁止】改其他 crate 逻辑/docs；git 操作；新增 slint 之外的第三方依赖。
【报告】改动清单；任务卡逐条自评；自验收输出摘要；遗留与声明（含未能自动验证的 UI 交互清单，供人工试用）。
```

## 7. W4 派发要点（W3 齐后）

- T8a（修复权限）：全量门禁 + 跑 `scripts\cli_smoke.ps1` + 就地修小问题。
- T8b（只读+报告）：按工程规范 §5 逐条审查 R1-R7 在默认路径强制生效；同时裁决三条已登记待办（跨表外键规则、选项≥2 机器化、S3 双会话哈希证据）是否立项。
- T8c（可写 README）：README 与实际能力对齐，补 CLI/GUI 使用说明。
- 三者并行，互不相交（T8a 修复范围若与 T8c 冲突则 T8a 优先）。

## 8. 更远的路线（用户未拍板，勿自行启动）

- Phase 2（P0-P5 EXE 产线）：设计决定 D16 本期不立项，待 Phase 1 产品验收后由用户决定。
- 逆向联网检索通道（D5 本期只做本地语料，trait 已可插拔）。
- 品类包继续扩深度/扩品类（E4：内容由用户供给，工程只保证 schema 与校验）。
