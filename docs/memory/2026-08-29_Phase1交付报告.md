# V4 Phase 1 交付报告

> 日期：2026-08-29
> 范围：Phase 1 —— 决策驱动设计工作台 + 冻结五门 + C0-C6 文档编译产线（Phase 2 的 P0-P5 EXE 产线按设计决定 D16 本期不立项）。
> 结论：**Phase 1 全部完成并验收通过。**

---

## 1. 最终质量基线（主开发亲自复跑，2026-08-29 20:11）

| 门禁 | 结果 |
|------|------|
| `cargo fmt --check` | 无 diff |
| `cargo clippy --workspace --all-targets` | 零警告零错误（全程未新增 `#[allow]`） |
| `cargo test --workspace` | **80 通过 / 0 失败 / 0 忽略** |
| `cargo run -p adm4-cli -- space validate` | grid_strategy 27 点 OK / lane_defense 21 点 OK |
| `cargo build --workspace --release` | 通过，产出 `adm4-cli.exe` / `adm4-desktop.exe` |
| `scripts\cli_smoke.ps1` | 退出码 0（逆向五步→模板预填→访谈补齐→冻结→C0-C6 全链 + 多条负例断言） |

测试数演进：39（W1）→ 43（W2）→ 48（W3/W4）→ 61（F1）→ 80（F2）。

## 2. 交付物清单

- **13 个 crate + 2 个应用**：foundation / contracts（红线类型）/ decision（L0-L6 决策模型 + row_reference 外键规则）/ template（逆向五步工具链）/ spec（GameSpec 4.0）/ ai（fail-closed Provider + ScriptedProvider）/ authoring（创作引擎 + 冻结五门）/ pipeline（C0-C6 + 人工门）/ space / archive（存档 + `.adm4proj` 导入导出）/ app（AppServices 门面）；adm4-cli（全子命令中文帮助）、adm4-desktop（Slint 四面板）。
- **知识库**：universal 通用层 + lane_defense（21 点，L6 数值级，5 条外键声明）+ grid_strategy（27 点，4 条外键声明）+ 空白模板与填写规范。
- **文档**：设计文档 7 份（`docs/design/00-06`，含实现对齐说明）、计划四件套（`docs/plan/01-04`）、记忆五件套（`docs/memory/`）、README。

## 3. 红线 R1-R7 终态（T8b 审查 + F1/F2 修复后）

| 红线 | 状态 |
|------|------|
| R1 指标必须有证据 | 生效（MeasuredMetric 类型级强制） |
| R2 未知即停、禁止静默兜底 | 生效（F1/F2 已修全部审查点名的吞错：findings/severity/id/target/blocker-text 缺失一律 Err；C3 基数缺键即 blocked） |
| R3 评审必须署名且防橡皮图章 | 生效（verify_review_batch 接线 C1×冻结门合批 + 逆向 S3 双会话哈希；人工门 actor 强制署名） |
| R4 AI 失败不得转成功 | 生效 |
| R5 皮肤红线（参考名不入产物） | 生效（SkinScanner + 冒烟负例） |
| R6 基数声明必须履约 | 生效（缺期望键即 blocked；grid_strategy 补 asset_specs） |
| R7 测试禁真网 AI | 生效（ScriptedProvider） |

## 4. 主开发裁决记录（W4-W5 期间）

1. T8a 两处退出码问题 → 修（F1）；后又统一 ai doctor（F2）与 freeze check（F2 补修）。四个检查类命令退出码语义现完全一致：命中问题即非零退出。
2. T8b 三条待办全部立项：跨表外键规则、选项≥2 机器化 → F2；S3 哈希证据 → F1。
3. 空答卷 S3 快路径不留哈希证据 → 接受（无 AI 会话则无复读可防）。
4. S3 输入合同收紧（旧 Mapped 模板需重跑 S2）→ 接受，仓内无受影响数据。
5. 外键规则目标侧选 custom 选项判违规 → 接受 fail-closed（R2），不降级 warning。
6. 完成度侧适用性裁剪 → 接受（冻结门 2 全量兜底，悬空引用穿不过冻结）。
7. desktop `build.rs` 的 expect 保留 → 构建脚本口径，构建期失败本就该中止。

## 5. 已知边界与后续方向（非缺陷）

- **Phase 2（P0-P5 EXE 产线）**：设计已在 `docs/design`，按 D16 本期未立项，是后续开发第一候选。
- **桌面 UI 未含**打包面板与 SDK 知识库面板（已在 design/06 实现对齐说明中标注）。
- `InterviewTurn` 枚举未派生 serde（下游用 `InterviewTurnDto` 包装，已够用）。
- 红队 finding 的 warning 级 text 允许为空（blocker 已强制非空）——现口径已够守住硬门。

## 6. 开发方式备注

全程由主开发（orchestrator）派发子 agent 完成：W1 四并行 + W2 单任务 + W3 两并行 + W4 三路并行验收 + W5 两串行修复。代码与前三版（Python v1、`RUST/` v2、`NEWrust/` v3）零复用、零依赖，设计继承 `RUST/` 工作台结构与 `plan/redesign_v3` 全量规范。用户要求 git 提交前须经确认，本轮未做任何 git 操作。
