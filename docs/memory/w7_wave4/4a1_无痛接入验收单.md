# 4a1 无痛接入验收单 · P0 第一批（G1 建造放置 / G2 对局赛制 / G5 战术棋盘）

> 无痛接入纪律（定稿 §9.2b 第 3 条，用户裁决）：每个新系统 = 仅新增一个 module.json，零类型改动、零校验器改动、零编译器改动。本单逐模块勾验。

## 逐模块勾验

### 1. sys.build_placement（建造放置，附录 G1）
- [x] 仅新增 `V4\knowledge\systems\sys.build_placement\module.json` 一个文件
- [x] 零 `src\**` 改动、零 `tests\**` 改动、零编译器/校验器改动
- [x] 现有类型底座完全装下：世界格归属表=Property(of=world_map)、放置指令=Signal、放置态实体=EntityClass、邻接修饰器语义=RuleSlot（adjacency_rule_slot 挂点）+Property（structure_function_value 作用目标）——按语义拆两名词，未硬塞
- [x] 谱系照附录 G1 落：BP0 预设槽位（塔防塔位，仅合法性检查）→ BP1 网格自由放置+邻接加成 → BP2 多层+支撑规则表+蓝图复制（档 summary 写明射程边界：物理坍塌出射程、支撑规则表在射程内）→ BP3 可破坏地形+建造经济闭环
- [x] BP3 传导 `Module(sys.economy) min_tier=recycle_loop`（拆除返还/维护费的账本承接），BP2 传导 `NounProvided(material_entity)`——两者均过门禁咬合断言
- [x] 结构自查过 validate：档带 4/8/12/15，p_floor 兑现，interface_floor 不超 C 档上界（C1→3 以内、C2→5≤6、C3 无上界），activates 递增包含，11 点全带 `sys.build_placement.` 前缀

### 2. sys.match_format（对局赛制，附录 G2）
- [x] 仅新增 `V4\knowledge\systems\sys.match_format\module.json` 一个文件
- [x] 零代码改动
- [x] 现有类型完全装下：名次实体=EntityClass、晋级状态/局分/赛程表=Property、奖励事件=Signal、种子规则挂点=RuleSlot
- [x] 谱系照附录 G2 落：MF0 单局胜负+名次结算 → MF1 局分制 BO-N+回合换边 → MF2 多人淘汰晋级树 → MF3 联赛赛季+晋降级+赛程日历
- [x] consumes 胜负/淘汰事件用命名空间形式 `sys.objective.win_signal` / `sys.objective.elimination_signal`（#31 未入库，dotted 门禁只查库内提供方，不拦；遗留申报见下）
- [x] MF3 传导用 `NounProvided(currency_main)` 析取形态（sys.economy 已 provides，门禁过），未点名任何并行卡 4a2 模块
- [x] 结构自查过 validate：档带 4/8/11/15，8 点全带前缀

### 3. sys.tactical_board（战术棋盘，附录 G5）
- [x] 仅新增 `V4\knowledge\systems\sys.tactical_board\module.json` 一个文件
- [x] 零代码改动
- [x] 现有类型完全装下：棋盘格占用表/单位位置状态=Property、地形格=EntityClass、地形修饰器=RuleSlot（terrain_modifier_slot，修饰器经挂点输出）、移动结算=Signal
- [x] 谱系照附录 G5 落：TB0 静态站位（JRPG 前后排）→ TB1 网格移动+射程/移动力 → TB2 地形效果+高低差+朝向背刺 → TB3 可破坏可改造棋盘+推拉位移连锁
- [x] TB3 位移连锁两选项均以 `displace` 变体为主效果（碰撞即停 vs 动量传递连锁），Displace 主场兑现
- [x] consumes 用命名空间形式 `sys.turn_combat.unit_entity` / `sys.turn_combat.move_command`（#2 未入库，遗留申报见下）；0 条传导——本模块在依赖方向上是供给方（provides 位置状态供 #2 伤害公式消费），对下游无最低档要求，不虚构传导
- [x] 结构自查过 validate：档带 4/8/12/15，10 点全带前缀

## 结构性缺口申报

无。三模块全部被现有 NounKind 五分类、HeavinessTier、Induction、EffectSpec 16 变体装下，未遇到需要停下上报的结构性缺口。

## 验证记录

- `cargo test -p adm4-decision`：86 项全绿（77 单元 + 5 knowledge_modules 门禁 + 1 prompt_library + 3 seed），三模块被门禁自动纳入（load_all_modules 现遍历 7 个模块）。
- `cargo fmt --check`：无 diff（exit 0）。
- 传导咬合：`inductions_interlock_across_module_library` 通过——BP3→sys.economy@recycle_loop（真实档 id）、BP2→material_entity（sys.loot provides）、MF3→currency_main（sys.economy provides）。
- dotted 名词闭合：`dotted_interface_nouns_resolve_to_real_providers` 通过——sys.loot.material_entity / sys.economy.currency_main 均真实存在于提供方 provides。

## 遗留与声明

1. `sys.match_format` consumes `sys.objective.win_signal` / `sys.objective.elimination_signal`：提供方 #31 目标占夺尚未入库，按任务卡指示以命名空间形式声明。**#31 入库时需核对其 provides 含 `win_signal` 与 `elimination_signal` 两个裸名词**（否则 dotted 门禁将在其入库当刻拦截，属预期行为）。
2. `sys.tactical_board` consumes `sys.turn_combat.unit_entity` / `sys.turn_combat.move_command`：提供方 #2 回合战斗尚未入库，同上口径。**#2 入库时需核对其 provides 含 `unit_entity` 与 `move_command`**。
3. 附录 G2 提到极重档可向 G11 计分供需（体育得分即胜负输入）：G11 由并行卡 4a2 落盘、时序不可控，按任务卡指示未做任何 Module 点名；MF3 的经济承接改走 `NounProvided(currency_main)` 析取形态，语义等价且门禁安全。
4. 与并行卡 4a2 的四个模块（sys.squad_command/sys.scoring_combo/sys.class_archetype/sys.onboarding）零互相引用。
