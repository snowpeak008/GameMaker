# T-W7-4a2 断点申报 · P0 模块入库第二批（G8 / G11 / #40 / G6）

> 执行子 agent：4a2。日期 2026-09-05。工作区 `V4\`，Rust 1.96，Windows PowerShell。
> 纪律确认：每模块仅新增 `module.json`，零代码改动；每模块开工前在此申报一次。

## 0. 开工基线（调研 ≤10 分钟内完成）

- `cargo test -p adm4-decision` 基线**全绿**：lib 77 通过、knowledge_modules 5 通过、prompt_library 1 通过、prompt_library_seed 3 通过。
- 必读已读：附录 §② G8（86-91 行）/ G11（93-99）/ G6（101-107）；定稿 §9.2a 全节（#40 两档形态用户二轮修正）；`sys.equipment/module.json` 格式基准；`system_module.rs` validate 规则；`tests/knowledge_modules.rs` 门禁四测试。
- 门禁关键约束核对（写模块前锁定）：
  1. validate：决策点 id 带 `<module_id>.` 前缀；接口裸名词必须本地声明（带点号=外部命名空间）；档位 activates 递增包含且指向真实点；tier_gate 指向真实档；五维 0-3；`rating.p ≥ p_floor`；`interface_floor ≤ C 档边数上界`（C1→3 / C2→6 / C3→无界）。
  2. 传导咬合：`Module` 目标必须在库内且 min_tier 真实——**因此本批一律不用 Module 点名（并行卡 4a1 的三模块落盘时序不可控，库内既有四模块无语义必需）**；`NounProvided` 名词必须被库内某模块 provides——只用 `skill_effect_def`（sys.loot）与 `equipment_entity`（sys.equipment）两个真实名词。
  3. 带点号名词：提供方在库内则裸名词必须真实（本批用到 `sys.loot.skill_effect_def`、`sys.equipment.equipment_entity`、`sys.equipment.equip_signal`、`sys.loot.drop_signal`、`sys.economy.income_signal`、`sys.inventory.inventory_full_signal`，全部逐一对过提供方 provides 表）；提供方不在库内（`sys.combat.*` / `sys.rhythm_judgement.*` / `sys.match3_board.*` / `sys.player_input.*` / `sys.progression.*`）按加载器 V6 职责放行，入遗留申报。
  4. 内容红线：L4 点选项 ≥2、summary 非空、effects_template 非空（16 变体 snake_case + `{param:KEY}` 占位）。

## 1. 模块申报 · sys.squad_command 编队指挥（G8）

- **状态**：已完成（validate + 门禁四测试通过）。
- **谱系四档**（附录 G8 原文照录映射）：S0 出战编成（Light，W=4）→ S1 +阵型站位/协同加成（Medium，W=8）→ S2 +编组/命令队列（Heavy，W=10）→ S3 +行为策略配置 Gambit（UltraHeavy，W=13）。
- **独占状态**：编队表（`squad_assignment`）+ 命令队列（`queued_command`）。consumes 单位实体 + 玩家指令事件（均为库外带点号名词，遗留申报）；provides 上场集合状态（`active_roster`）+ 命令事件（`command_signal`）+ 编成变更事件（`roster_signal`）。
- **决策点**：9 个（编成容量/换人时机/阵型结构/协同结算/编组绑定/队列语义/常驻命令/策略规则行/策略仲裁），全 L4。
- **边界申报**：站位仅编成期布置——局内网格移动力/地形归战术棋盘系统（4a1 并行卡，互不引用）；真人组队关系归 #33；伴随实体自身培养归 #11。
- **传导**：无 Module/NounProvided 传导——单位实体来源（#1/#2 战斗）未模块化，无法安全声明 NounProvided，入遗留。

## 2. 模块申报 · sys.scoring_combo 计分连击（G11）

- **状态**：已完成（validate + 门禁四测试通过）。
- **谱系四档**：K0 行为计分表（Light，W=4）→ K1 +连击计数/倍率/衰减窗口（Medium，W=7）→ K2 +多维评级结算 DMC S-D（Heavy，W=10）→ K3 +对抗性计分（UltraHeavy，W=13——得分策略成为构筑级承诺，P3）。
- **独占状态**：分数累计（`score_pool`）/连击计数（`combo_state`）/评级表（`rating_grade`）。consumes 判定/击中/消除事件——三个带点号外部名词，提供方 #29/#38/#1 未模块化，遗留申报。K3 按任务卡纪律用**裸名词 provides**（`score_pool`/`rating_grade`/`score_signal`）供赛制消费，不点名 4a1 的 sys.match_format。
- **决策点**：7 个（计分规则/连击窗口/倍率成长/衰减规则【Curve 参数落此】/评级维度/评级门槛表/胜负绑定），全 L4。
- **边界申报**：#29 只管判定窗产出判定事件、#38 只管交换/下落/消除——本系统消费其事件产出分数，是"横切束"型系统（附录特别检查裁定照录）。

## 3. 模块申报 · sys.class_archetype 职业流派（#40，定稿 §9.2a 为设计真源）

- **状态**：已完成（validate + 门禁四测试通过）。
- **两档形态 + 裁量极重档**：A0 涌现式（Light，W=4——无职业表：安装约束规则 + 派生标签规则，标签仅 UI/叙事）→ A1 预设式（Heavy，W=10——职业定义表 + 技能子集门控 + 成长偏向 + 转职规则）→ A2 多职业并行（UltraHeavy，W=13——主副职/职业池切换 + 跨职业继承；裁量依据：FF14/FF5 型多职业是预设式之上的真实重度增量；**转生/重置不入此档**——附录 D15 已判归 #35 prestige，避免重复建模）。
- **机制核心**：安装约束落在 `install_gate_rule_slot`（规则挂点）——"须持盾才能释放"= AddPrecondition 读 `sys.equipment.equipment_entity` 槽位状态的谓词；A 主项 = **表达**（首位）/幻想，按用户二轮修正。
- **传导**（本批唯一带 Induction 的模块，两条均为库内真实名词的 NounProvided 析取）：A0 起 ① `skill_effect_def`（安装约束的作用对象须有源——sys.loot 已 provides）② `equipment_entity`（装备前置要能读槽位状态——sys.equipment 已 provides）。等级前置（#9）无法传导，遗留申报。
- **决策点**：10 个（约束形态/互斥组/标签派生 ‖ 职业表结构/技能门控/成长偏向/转职规则/相克矩阵【Matrix 参数】‖ 多职业结构/跨职业继承），全 L4。
- **扩义**：种族/血统按修订 R7 涵盖（创建期一次性安装约束+初始修饰器与涌现式同构），summary 声明。

## 4. 模块申报 · sys.onboarding 新手引导（G6，判例 2 勘误 R1 入库）

- **状态**：已完成（validate + 门禁四测试通过）。
- **谱系四档**：N0 首次触发提示（Light，W=4）→ N1 +分步强制流程/进度门控（Medium，W=7）→ N2 +情境化触发（Heavy，W=9）→ N3 +自适应调节（W=11，**全局档带仍为"重"**——G6 是横切元系统，五维如实标定封顶在 Heavy，谱系语义上的"极重档"与全局档带不划等号，如实申报不虚标）。
- **独占状态**：引导进度表（`onboarding_progress`）。consumes 首次行为事件——**四个库内真实信号**（穿戴/掉落/入账/背包满）+ 一个库外表现事件（遗留）；provides UI 修饰器（`ui_guidance_state`）+ 功能门控（`feature_gate`）+ 引导事件（`guidance_signal`）。
- **决策点**：6 个（触发口径/跳过策略/流程结构/门控口径/缺失检测/密度调节），全 L4。
- **A 主项**：消遣（单主项，判例 2 与附录 G6 一致口径）。

## 5. 断点与风险

- 五维标定的 interface_floor 逐档与 C 档上界核对过（无 C1 配 >3 下限类自矛盾）。
- 若 validate/门禁出现无法以 module.json 内容修复的结构性问题 → 按无痛接入纪律**停下上报**，不改任何 src/tests。
