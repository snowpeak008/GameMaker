# T13b · Phase 1 缺口闭合复验与重新判定

> 日期：2026-08-31
> 任务：W6-T13b「Phase 1 缺口闭合复验与重新判定」（只读复验 + 全量门禁自跑）
> 核对基准：`docs/memory/2026-08-31_T13覆盖矩阵终验报告.md` §2.4 缺口清单 + `docs/plan/05` §2 矩阵 / §4 判定口径
> 被复验对象：`docs/memory/2026-08-31_Phase1缺口闭合与F4波次.md`（F4a-F4d 的声称）
>
> **代码级总判定：通过**（21 行矩阵逐行有归宿，且 T13 的 4 个高缺口均有代码 + 测试 + 冒烟三重证据）。
> **真机走查判定：未做**（Slint 运行时交互无 GUI 自动化，见 §4.5 前置清单，不自评为通过）。

---

## 1. 改动清单

本任务为只读复验，**未修改任何代码、数据、测试、设计/计划文档，未执行任何 git 操作，未修复任何发现的问题**。

| 文件 | 说明 |
|------|------|
| `docs/memory/2026-08-31_T13b缺口闭合复验报告.md` | 新增（本文件），T13b 唯一产物 |

复验方法：所有结论均来自本 agent 亲自读取的源文件与亲自复跑的门禁命令；F4 波次交付说明只作为**待验命题清单**使用，其结论一律不予引用。

---

## 2. 设计符合性

### 2.1 T13 缺口清单逐条核对

判定口径：**已闭合** = 有可指认的代码符号/元素 id/子命令 + 有钉住行为的测试；**部分闭合** = 主路径在但有明确缺项；**未闭合** = 无实现；**仍属待办** = T13 已判为设计债或需裁决，本轮未动。

#### 高缺口（4 条，直接决定 §4 判定）

| 编号 | 缺口 | 代码级证据 | 结论 |
|------|------|-----------|------|
| **H1** | 另存模板（项目 → 模板） | 服务：`crates/adm4-app/src/services.rs:1264 template_export_from_project`（只导出 `confirmed_by_user` 的点，未确认计入 `skipped_unconfirmed`；选项已不在装配空间内的计入 `skipped_unknown` 不静默；空答卷 `Blocked`；同名模板 `Conflict` 不覆盖；落盘前整份序列化过 `SkinScanner` 命中即 `RedLine`）。模型：`crates/adm4-template/src/model.rs:224 TemplateOrigin::ProjectExport{source_archive_id, source_project_name, exported_at}` + `:382 record_project_export_review`（只对 ProjectExport 开放、只能从 `Draft` 落、reviewer/note 双必填）。CLI：`apps/adm4-cli/src/main.rs:509 template save-as <archive> <tpl> --reviewer --note [--game --alias]`。GUI：`ui/main.slint:243 callback template-export(...)` + 元素 `tpl-export-id/name/aliases/reviewer/note`。测试：`end_to_end.rs:2285 template_save_as_exports_only_confirmed_selections_then_certifies_and_prefills`（19 组断言，含「未确认不导出」「不塞假 evidence」「同名不覆盖」「认证前不可预填」）+ `model.rs` 2 单测。冒烟：脚本第 520/522/528/537/542/548/551 行 | **已闭合** |
| **H2** | 设计工作台重置 | 服务：`services.rs:432 project_reset_workbench`（走既有 `with_project` 事务，actor/note 双必填，清空计数 + 署名 + 理由三条进 runlog）。引擎：`crates/adm4-authoring/src/engine.rs:737 reset_workbench`（返回 `WorkbenchResetReport{cleared_selections, cleared_exemptions, …}`）。CLI：`main.rs:144 project reset <archive> --actor --note`。GUI：`main.slint:244/245 workbench-reset-arm() / workbench-reset(actor, note)`（二次确认）+ 元素 `reset-actor` / `reset-note`。测试：`end_to_end.rs:2591 workbench_reset_clears_authoring_state_but_keeps_frozen_history`（含「缺署名/缺理由被拒且一条都没清掉」的负例钉子）。冒烟：第 580/581/583/587/590/596 行，含「重置后已冻结版本的流水线产物仍齐备 + 状态仍全绿」 | **已闭合** |
| **H3** | 流水线阶段重跑 | 运行器：`crates/adm4-pipeline/src/runner.rs:252 rerun_from` / `:278 reset_from` / `:318 downstream_stages`（registry 顺序 ∪ `depends_on` 传递闭包取并集；先落状态后删产物；参数校验在动手之前）。服务：`services.rs:1094 pipeline_rerun_with_cancel`（作废署名逐条落日志）。CLI：`main.rs:330 pipeline rerun`。GUI：`main.slint:226/227 pipeline-rerun-arm(stage) / pipeline-rerun-confirm(stage, to)`，警示条由后端 downstream 清单生成（`main.slint:1530`）。测试：runner 4 单测 + `end_to_end.rs:2020 pipeline_rerun_invalidates_downstream_stages_artifacts_and_human_gates`。冒烟：第 445/453/463 行。**`run_range` 原型行为一字未动**（`:106` 走 `CancelSignal::never()`），既有调用方零影响 | **已闭合** |
| **H4** | 流水线停止运行 | 原语：`crates/adm4-pipeline/src/cancel.rs CancelSignal`（`Arc<AtomicBool>`，4 单测含跨线程与 `reset()` 复用）。运行器：`runner.rs:121 run_range_with_cancel`，段边界检查，取消时写 `StageStatus::Pending`（**非 Failed**）且 `cancelled_at` 上报。服务：`services.rs:1038 pipeline_run_with_cancel` + `:1136 log_cancellation`。GUI：`main.slint:225 pipeline-stop()` + `apps/adm4-desktop/src/main.rs:1478 on_pipeline_stop`，工作线程 `std::thread::spawn` + `upgrade_in_event_loop`（`main.rs:1710/1713`），`slint::Timer`（`main.rs:121`）轮询进度。测试：`end_to_end.rs:1923 pipeline_cancellation_stops_at_stage_boundary_without_marking_failure`（三段场景 + 日志留痕断言）。CLI 无 stop（同步单次运行，无可停之处），属合理 | **已闭合** |

#### 中缺口（6 条）

| 编号 | 缺口 | 代码级证据 | 结论 |
|------|------|-----------|------|
| M1 | 桌面端范围运行 | `main.slint:1371` 段清单来自 `root.stage-ids`（Rust 侧取 `design_compile_registry()`，UI 不硬编码）+ `:1394` 按钮「运行选定范围」→ `callback pipeline-run-range(from, to)` → `desktop/src/main.rs:1452 on_pipeline_run_range` | **已闭合** |
| M2 | 阶段详情产物入口 | 服务：`services.rs:1159 pipeline_artifact` → `crates/adm4-app/src/pipeline_artifact.rs::StageArtifactView::build`（缺文件 `document_text=None` 且 `missing` 列名；预览 256 KiB 截断置 `document_truncated`，`bytes`/`sha256` 恒为整份真值）。CLI：`main.rs:349 pipeline artifacts [--stage --show-document]`。GUI：`main.slint:228 pipeline-artifact(stage)` + `:1516` 每行「产物」按钮 + `:1649 artifact-view` 预览框。测试：`end_to_end.rs:2177 stage_artifact_query_exposes_documents_and_reports_gaps`。冒烟第 431/439/443 行（含未知阶段 id 必须非零退出的负例） | **已闭合** |
| M3 | 打包视图诊断按钮 | `main.slint:1788` 打包视图按钮「存档体检」→ `callback project-doctor()`，结果落 `doctor-rows`（`:1799/:1810`）；存档管理面板 `:2063` 同一入口。服务侧判定已上提：`services.rs:307 project_doctor -> ProjectDoctorReport`（结构化，CLI 只做呈现） | **已闭合** |
| M4 | 画像卡缺美术风格 / 目标用户 | `knowledge/design_space/lane_defense/pack.json:6 profile_points`（14 个 id，含 `v2.art_direction_decision.feng_ge_ding_wei.presentation_feeling_target` 与 `v2.target_player_decision.wan_jia_hua_xiang.age_band`），`grid_strategy/pack.json:6` 同结构；`services.rs:693 project_profile` 按清单取点，清单为空回退旧 L0/L1 过滤。测试：`end_to_end.rs:2725 profile_points_drive_profile_card_without_touching_completeness_denominator`（明确钉住完备度分母不变） | **已闭合** |
| M5 | 「不提供清空日志」的裁决未入档 | 裁决文案仍只在 `apps/adm4-desktop/ui/main.slint:1913`；`docs/design/**` 与 `docs/plan/**` 全文检索「清空」只命中 `docs/plan/05` 第 35 行（重置行）与第 45 行（矩阵原文仍写「过滤/清空/导出/AI 诊断……**保留**」）。**没有任何一处记录「清空按钮照实省略」的裁决** | **未闭合**（文档债，能力侧无影响） |
| M6 | AI 诊断与 Provider 配置的 GUI | `main.slint:234-240` 七个回调 `ai-refresh / ai-apply-preset / ai-save / ai-doctor / ai-invoke-check / ai-save-secret / ai-disable`，元素 `ai-preset-box / ai-id / ai-model-edit / ai-timeout-edit / ai-url / ai-key / ai-secret-name-edit / ai-secret-value-edit`，诊断明细 `ai-doctor-rows`。服务：`services.rs:1733 ai_doctor`（返回 `AiDoctorReport`，无 `Result`——「AI 不可用」是结论不是失败）、`:1769 ai_save_secret`（回执只回名字 + 字符数）、`:1798 ai_invoke_check`。CLI 同步补齐 `ai invoke-check / secret-set / secret-list`。测试：`end_to_end.rs:3234 config_hot_reload_secret_write_and_invoke_check` + `view.rs::provider_form_round_trips_config_and_flags_missing_fields`。冒烟第 613/619/624/633/649/653 行 | **已闭合** |

#### 低缺口（4 条）

| 编号 | 缺口 | 复验结果 | 结论 |
|------|------|---------|------|
| L1 | V2「清空 L5」无同名按钮 | `main.slint` 参数编辑区仍只有「编辑参数」`:885` / 「高级JSON」`:918` / 「返回结构化表格」`:966`，无清空按钮 | **仍属待办**（T13 已判设计债） |
| L2 | CLI 缺 `deliver` / `sdk` / `change` 与 `authoring add-option` / `set-primary` | 全文检索 `apps/adm4-cli/src/main.rs`：`sdk` / `change` / `deliver` **零命中**；`authoring` 分支仍只有 `status/select/set-param/set-rationale/confirm/na`。顶层新增的是 `project reset|doctor`、`pipeline rerun|artifacts`、`template save-as`、`ai invoke-check|secret-set|secret-list` | **未闭合**（F4 波次未认领此项；后果同 T13：SDK 审批 / 变更流 / 文档集打包 / 多选主选无法进冒烟） |
| L3 | 任务卡与矩阵行数 11 vs 12 | `docs/plan/05` §2.1 复点仍为 **12 行**（第 24-37 行）。主开发已改的是行 8「26+ → 实为 25 份」与行 11「保留（已有）→ 存档已有，重置为 W6 后补齐」，两处均已落盘且与实现一致 | **部分闭合**（措辞已修正，行数记录项无需改） |
| L4 | 桌面端 `change_add` 固定传 `target_frozen_version=0` | `apps/adm4-desktop/src/main.rs:2591` 仍硬传 `0` | **仍属待办**（T12 已在案的设计债） |

### 2.2 F4 波次红线声称的独立验证（四处重点）

#### ① 换皮豁免作用域 —— 声称属实

`crates/adm4-contracts/src/red_lines.rs:206 SkinScanner::with_exemptions`：豁免词在**构造时**从生效词表剔除（`scan` 无第二套判定，`wordlist()` 返回的即真正会命中的词），归一化口径与词表共用 `normalize_words`（trim + lowercase），过滤条件是 `!exempted.contains(word)` —— **整词相等，无前缀/子串/别名放行**。调用侧 `services.rs:215 skin_scanner_for_project` 只传 `vec![project_name]` 一个词；产物落盘钩子 `services.rs:999` 用 `frozen.project_name`（不是创作态当前名），创作态侧用 `state.project_name`。

三条方向的断言各自到位，且是三处独立现场：

| 方向 | 单元层 | 服务层 e2e | 冒烟 |
|------|-------|-----------|------|
| ① A 自己的产物含 A 不被拦 | `red_lines.rs:363` | `end_to_end.rs:3070` 走**流水线产物落盘钩子**（`pipeline_rerun_with` C0 必须 succeeded，并回读 C0 文档确认标题真的带项目名——否则用例什么也证明不了）；`:2462` 走**另存钩子** | 第 548 行 |
| ② B 的产物带 A 名被拦 | `red_lines.rs:365-368` | `end_to_end.rs:3095` 与 `:2510`，均断言 `finding.code == "reference_name_hit"` 且 message 含 A 名 | 第 555 行（`freeze check` 期望非零退出） |
| ③ 逆向外部游戏名照旧拦 | `red_lines.rs:369-374`（两种视角各扫一次） | `end_to_end.rs:3110` 对 A、B 两个存档各断言一次 `kingdom rush` 被拦 | 第 261/263 行 `换皮词 2 个` |

另有 `red_lines.rs:378-387` 专钉「豁免只按整词相等」：词表同时有 `霜落峡谷` 与 `霜落`、豁免 `霜落峡谷` 时，`霜落` 照旧命中（且注释如实写明这是刻意不放宽的代价）。**结论：不是文字游戏，行为被真钉住。**

**但本 agent 发现一处 F4d 未申报的残留收窄不足**（见 §4.2 遗留 R1）：豁免按**词面**剔除，不区分该词是谁登记的。若某项目名归一化后恰好等于词表里某个**逆向外部游戏名**（如项目取名「Kingdom Rush」），该外部游戏名对这个项目就整体失效。这是相对 T13 基线**新引入**的一处窄缝（T13 时点无豁免机制）。修法方向明确（豁免应限定为「由本存档 `ProjectExport` 模板登记的词」，`TemplateOrigin::ProjectExport.source_archive_id` 已带够信息），但**本 agent 只记录不修**。

#### ② 认证证据关卡 —— 声称属实

取用侧关卡：`crates/adm4-template/src/library.rs:133 approved_for_prefill` 在状态位判定之后**再调一次** `template.require_certification_evidence()`，与 S5 `certify`（`:108`）共用同一份判定。`model.rs:428 require_certification_evidence` 逐来源分流：BulkMigration 查三项登记非空 + `answers_digest` **现算比对**（`:449 let actual = self.answers_digest();`，比的是重算值不是登记值自证）；ProjectExport 查人工审核署名与结论；其余（含**缺 `origin` 键的旧档/伪造档**，`TemplateOrigin` 的 `#[serde(default)]` 落 `Reverse`）走最严的逆向分支，要求 `mapping_hash` 非空 + `crosscheck_proof` 存在 + `proof.mapping_hash == self.mapping_hash`。fail-closed 方向正确。

手工伪造被真拒：`end_to_end.rs:3172-3207` 往 `references/` 写一份 `status=certified`、无任何证据的 JSON，先断言 `is_certified()` **确实为 true**（证明旁路形态成立），再断言预填 `RedLine` 且 message 含「机器证据」，并**同时堵住只读侧门** `template_compare`。冒烟第 573/575/576 行同款三连。

25 份内置模板：`knowledge/design_space/universal/references/` 实测 **25 个 json**，`end_to_end.rs:3145-3170` 逐份断言 `is_bulk_migration()` 且 `require_certification_evidence()` 通过（即 25 份的 `answers_digest` 全部现算比对成功），随后 `builtin_midcore_arknights` 预填成功。篡改检测：`:3210` 截断答卷到 3 条而不改登记 → 取用被拒且 message 含「指纹」。

已申报局限属实：`answers_digest` 只覆盖 `decision_id/option_id/additional_options/primary_option` 四元结构，不覆盖 `parameters`/`evidence`（`model.rs:346` doc 注释如实写明范围，字段名叫 digest 而非 content_hash）。**这是如实的不多许诺，不是遮掩。**

#### ③ 阶段重跑连带作废下游产物与人工门署名 —— 声称属实

`runner.rs:278 reset_from` 对 `downstream_stages` 内每段 `state.stages.remove(target)`，`human_confirmation` 存在则进 `revoked_confirmations`；随后 `save_run_state` 再 `clear_stage`（**先落状态后删产物**，失败时留在「状态已作废 + 残留旧产物」这一安全侧）。旧署名能否背书新产物：`end_to_end.rs:2100` 断言重跑后 `pipeline_confirm(C6, 初审评审员)` 返回 `NotFound`（记录已被移除，无从沿用），C5 必须重新署名，末尾 `:2117` 断言 C5/C6 的 `human_confirmation.actor` 全为「复审评审员」。**旧署名确实无法为新产物背书。** 负例 `:2153/:2159`：区间非法与未知段被拒后，七段产物 `complete` 全为真（失败请求不误伤）。

#### ④ 取消写 Pending 而非 Failed，且不留 Running 幽灵段 —— 声称属实

`runner.rs:139-155`：取消分支写 `StageStatus::Pending`、`started_at` 留空（耗时如实为未知）、`finished_at` 落时刻，随即 `save_run_state` + `break`。**取消判定排在「落 Running」之前**（`:181` 才写 Running），因此不会出现「先写 Running 再改回 Pending、进程死在两次写盘之间」的幽灵段。`end_to_end.rs:1936/1960` 用 `assert_stage_statuses` 逐段比对字符串状态，`"pending"` 与 `"failed(...)"` / `"running"` 在同一断言里不可能混同；`:1971-1982` 断言已完成段产物保留、被取消段如实报缺；`:1985` 复位信号后断点续跑一路到 C5 人工门；`:2009` 断言取消进 runlog。另有 `end_to_end.rs:2920 pipeline_records_running_state_and_stage_durations` 单独钉 `Running` 落盘与耗时。

### 2.3 三处反转断言的正当性 —— 独立判断

三处反转指向的是**同一个行为改变**：`TemplateOrigin::ProjectExport` 从「不贡献换皮词条」改为「照常登记 + 扫描侧按当前项目名豁免」。

| # | 反转的断言 | 原断言主张 | 现断言 | 正当性判断 |
|---|-----------|-----------|--------|-----------|
| 1 | `project_export_contributes_no_skin_words` → `model.rs:608 project_export_contributes_skin_words_like_any_other_origin` | 「本项目导出的模板不进换皮词表」 | 断言 `skin_words()` 返回 `[game_name, alias]`，注释写明「源项目名必须进词表，否则别的项目抄它没人拦」 | **正当。** 原断言把一条跨项目漏洞钉成了规格：A 的名字不入表 ⇒ B 预填 A 的模板后，B 的产物带着 A 的名字可以通过冻结门，R5 对「抄另一个项目」完全失效。修漏洞必然要求这条断言反向。 |
| 2 | e2e「认证后词表不变」 → `end_to_end.rs:2438-2457` | 「ProjectExport 模板认证后全局词表长度/内容不变」 | 先取 `wordlist_before`、认证、再取 `wordlist_after`，断言两个词**认证前不在、认证后在** | **正当。** 与 #1 同一行为的服务层投影。且现断言比原断言**更强**：它逐词点名而非只比长度。 |
| 3 | 冒烟「换皮词 0 个」 → `cli_smoke.ps1:544 Assert-Contains $out '换皮词 1 个'` | ProjectExport 模板 `certify` 时登记 0 个词 | 断言登记 1 个词，说明文字写「源项目名进词表（否则别的项目抄它没人拦）」 | **正当。** 同一行为的 CLI 层投影。 |

**明确表态：三处反转全部是修漏洞的必然结果，不是为让新实现通过而放宽要求。** 三条独立理由：

1. **方向是收紧不是放松。** 反转后，「B 的产物带 A 的名字」从**放行**变为**拦截**（`end_to_end.rs:3095`、`:2510`、冒烟第 555 行三处钉住）。放宽要求的反转会让某类输入从被拒变为被放行；这里恰好相反。
2. **放行面被单独立了三条钉子，且刻意收窄。** 新引入的唯一放行面是「当前项目名整词相等」，`red_lines.rs:356 r5_exemption_covers_own_project_name_only` 在同一个测试里同时钉住①放行、②拦截、③外部名不变，并额外钉住「子串不豁免」。这是**把放行面写成规格**，不是把放行面藏进实现。
3. **一份都没删、一份都没 ignore。** 本 agent 逐测试二进制核对（§3 分布表）：与 T13 基线 152 的分布对比，**16 个二进制无一减少**，T13 报告里点名引用的 16 个测试名（`badges_cover_every_applicability_state` / `stage_rows_cover_c_and_p_segments` / `uncertified_template_prefill_is_rejected` 等）在当前树中**全部仍在**；全量 29 个 test result 行的 ignored 计数**全为 0**。

唯一的保留意见不在「反转是否正当」，而在「豁免作用域是否已经收到最窄」——见 §2.2① 末段与 §4.2 遗留 R1。该问题**不改变**上述三处反转的正当性结论（那三处反转即便在收得更窄的实现下也同样必须发生）。

### 2.4 21 行矩阵重新判定

判定口径（`docs/plan/05` §4 原文）：**每一行 V2 能力，在 V4 中要么可直接操作，要么在明确的新功能里可见其归宿；任何一行找不到归宿即验收不通过。**
状态列区分两类：**代码级已闭合** = 有代码符号 + 测试证据；**待真机走查** = 该行的 Slint 运行时交互仍需人工点一遍（不自评通过，也不据此判不通过——§4 判的是「有无归宿」，归宿在代码层已可指认）。

#### §2.1 设计工作台（12 行）

| # | V2 能力 | T13 结论 | 本轮变化 | 代码级判定 |
|---|---------|---------|---------|-----------|
| 1 | 16 领域总览卡片 + 进度 | 通过 | 无变化，`view.rs::domain_cards` 及 2 单测仍在 | **通过** |
| 2 | 设计节点列表 | 通过 | 无变化 | **通过** |
| 3 | 三级决策项检查单勾选 | 通过 | 无变化，`badges_cover_every_applicability_state` 仍在 | **通过** |
| 4 | L4 选项组（单/多选、必填、主选、MDA、设计提问） | 通过 | 无变化 | **通过** |
| 5 | L5 实体 JSON 编辑 | 通过 | 无变化（L1「清空 L5」仍无同名按钮，高级 JSON 可达） | **通过** |
| 6 | 节点文本（设计说明/风险/不适用原因） | 通过 | 无变化 | **通过** |
| 7 | 项目画像六字段 | 部分通过（M4） | `pack.json::profile_points` 14 点覆盖六字段含美术风格与目标用户；`project_profile` 按清单取点；e2e 钉住分母不变 | **通过** |
| 8 | 内置游戏模板（载入预填 / 另存） | **部分通过（H1 零归宿）** | 另存全链闭合（服务 + CLI `template save-as` + GUI + 20 项 e2e + 冒烟）；载入预填侧新增证据关卡后 25 份内置模板仍全部可用 | **通过** |
| 9 | 右栏四页签 | 通过 | 无变化 | **通过** |
| 10 | AI 访谈 | 通过 | 无变化 | **通过** |
| 11 | 存档管理 / 保存 / 载入 / 重置 | **部分通过（H2 零归宿）** | 重置全链闭合（`project_reset_workbench` + CLI `project reset` + GUI 二次确认 + e2e + 冒烟）；`docs/plan/05` 行 11 措辞已如实改为「存档已有 + 重置为 W6 后补齐」 | **通过** |
| 12 | 导出（markdown 等） | 通过 | 无变化 | **通过** |

#### §2.2 其余五个视图（5 行）

| # | V2 视图 | T13 结论 | 本轮变化 | 代码级判定 |
|---|---------|---------|---------|-----------|
| 13 | 开发流水线 | **部分通过（H3/H4 零归宿 + M1/M2）** | 停止（`CancelSignal` + GUI 工作线程）、重跑（`rerun_from` + 两步确认）、范围运行（段清单来自 registry）、阶段详情产物（`pipeline_artifact` + 预览/路径/sha256/截断提示）四项全闭合；13 段全版图与断点续跑未被破坏（`stage_rows_cover_c_and_p_segments` 仍在） | **通过** |
| 14 | 补充开发 | 通过 | 无变化（L4 桌面端 `target_frozen_version=0` 仍在） | **通过** |
| 15 | 打包阶段 | 部分通过（M3） | 打包视图新增「存档体检」按钮 + `doctor-rows`；`project_doctor` 判定上提到服务层 | **通过** |
| 16 | 运行日志 | 部分通过（M5/M6） | AI 诊断与 Provider 配置面板齐（7 回调 + 密钥写入 + 实调用检查）；**「清空」的裁决仍未进 design/plan**（M5 未闭合，属文档债：能力侧界面有明示归宿，§4 判定不受影响） | **通过**（附 M5 文档债） |
| 17 | SDK 知识库 | 通过 | 无变化（CLI 侧仍无 `sdk` 子命令，见 L2） | **通过** |

#### §2.3 四条冲突裁决（4 行）

| # | 裁决 | 复验结果 | 判定 |
|---|------|---------|------|
| 18 | 勾选（布尔）由「已确认」派生 | `view.rs::badge_of` 判定顺序未动；`end_to_end.rs:2486` 新增反向钉子——预填条目 `confirmed=false` 且不上画像卡，逐条确认后才计入 | **成立**（较 T13 更强） |
| 19 | 未认证模板不可预填 | 关卡未松反紧：状态位之外新增 `require_certification_evidence`，且 `template_compare` 同关卡；`uncertified_template_prefill_is_rejected` 原测仍在 | **成立**（较 T13 更强） |
| 20 | 冻结门预检进右栏「校验」 | 无变化 | **成立** |
| 21 | C0-C6 实跑 + P0-P5 占位不断头 | `stage_rows` 仍 13 行，P 段 6 行占位；`main.slint:1866` 打包视图 Phase 2 占位文案仍在 | **成立** |

**21 行代码级判定：21 行全部有归宿 → 按 §4 口径通过。**

---

## 3. 测试证据

全部命令由本 agent 于 `E:\workwork\CrewAi\AutoDesignMaker\V4` 下亲自复跑（2026-08-31），**未引用任何他人结论**。

| 门禁 | 命令 | 实测结果 |
|------|------|---------|
| 格式 | `cargo fmt --check` | 退出码 **0**，无 diff |
| Lint | `cargo clippy --workspace --all-targets` | 退出码 **0**，输出仅 `Finished dev profile`，**零警告零错误** |
| 测试 | `cargo test --workspace` | 退出码 **0**，**210 通过 / 0 失败 / 0 ignored**（29 个 test result 行，逐行核对无非零 failed、无非零 ignored） |
| 数据 | `cargo run -p adm4-cli -- space validate` | 退出码 **0**；`[OK] grid_strategy: 2610 个决策点，参考游戏 4 款`、`[OK] lane_defense: 2604 个决策点，参考游戏 3 款`（与 T13 基线一致，`profile_points` 未动分母） |
| 桌面 | `cargo build -p adm4-desktop` | 退出码 **0**，链接成功 |
| 冒烟 | `powershell -ExecutionPolicy Bypass -File scripts\cli_smoke.ps1` | 退出码 **0**，末行 `[冒烟通过] 逆向五步 -> 模板预填 -> 访谈补齐 -> 冻结 -> C0-C6 全链 -> 另存模板/重置/体检 -> 换皮豁免/认证证据/AI 配置 OK` |

**210 个测试的逐二进制分布（与 T13 的 152 基线对比，用于验证「无测试被删」）**：

| 测试二进制 | T13 | T13b | 增减 |
|-----------|-----|------|------|
| `adm4-ai` lib | 0 | 0 | — |
| `adm4-app` lib | 19 | 25 | +6 |
| `adm4-app` `end_to_end` | 9 | 20 | +11 |
| `adm4-archive` lib | 1 | 1 | — |
| `adm4-authoring` lib | 14 | 14 | — |
| `exemption_and_node_notes` | 5 | 6 | +1 |
| `optional_points_and_universal_prefill` | 6 | 6 | — |
| `adm4-cli` | 0 | 0 | — |
| `adm4-contracts` lib | 6 | 7 | +1 |
| `adm4-decision` lib | 28 | 28 | — |
| `adm4-desktop` | 23 | 38 | +15 |
| `adm4-foundation` lib | 4 | 4 | — |
| `adm4-pipeline` lib | 11 | 24 | +13 |
| `multi_selection_chain` | 1 | 1 | — |
| `adm4-space` lib | 9 | 11 | +2 |
| `adm4-spec` lib | 4 | 4 | — |
| `adm4-template` lib | 3 | 10 | +7 |
| `reverse_pipeline` | 9 | 11 | +2 |
| **合计** | **152** | **210** | **+58** |

**无任何二进制的测试数下降**；T13 报告正文点名引用的 16 个测试名在当前树中逐一命中（15 个在 `apps/adm4-desktop/src/view.rs`，1 个在 `crates/adm4-app/tests/end_to_end.rs`）。结合 ignored 全为 0，可判定「前三轮新增测试无一被删除或 ignore」的声称属实。

**数据侧只读核对**：`universal/references/` 实测 **25** 份 json，全部带 `origin: bulk_migration` 登记；双包决策点数 2604 / 2610 与 T13 基线逐字相同。

---

## 4. 遗留与声明

### 4.1 判定

1. **代码级总判定：通过。** 21 行矩阵逐行有归宿；T13 认定为「无归宿」的 4 个高缺口（另存模板 / 工作台重置 / 阶段重跑 / 停止运行）全部闭合，每条均有服务层实现 + CLI 或 GUI 入口 + 钉住行为的测试 + 冒烟覆盖三重证据。
2. **真机走查判定：未做，不自评。** §4.5 清单未走前，Phase 1 不应宣布收官。
3. **本 agent 未做的事**：未修改任何代码/数据/测试/设计/计划文档（除新增本文件）；未执行任何 git 操作；未修复本轮发现的任何问题；未为使判定通过而放宽口径，也未把已闭合的项目说成未闭合。

### 4.2 本轮新发现（只记录，交主开发裁决）

- **R1（中，新引入）· 换皮豁免按词面剔除，不区分词条来源。** `SkinScanner::with_exemptions` 的过滤条件是「归一化后与豁免词整词相等」，而豁免词只有当前项目名。若项目名恰好等于词表里某个**逆向来源的外部游戏名**（如项目取名「Kingdom Rush」，或某外部游戏别名与项目名撞车），该外部名对这个项目整体失效，R5 出现一条窄缝。这是相对 T13 基线**新引入**的放宽（T13 时点根本没有豁免机制）。可行修法：豁免集合改为「由当前存档的 `ProjectExport` 模板登记的词」——`TemplateOrigin::ProjectExport{source_archive_id}` 已带够信息；或在构造豁免时先检查该词是否同时由非 ProjectExport 来源登记，命中则不豁免。**建议本期修**（改动面小、只动 `services.rs::skin_scanner_for_project` 与词表来源查询）。
- **R2（低）· M5 文档债仍在。** 「不提供清空日志」的裁决只活在 `main.slint:1913` 的界面文案里，`docs/design/**` 与 `docs/plan/**` 无记录，而 `docs/plan/05` §2.2 第 4 行仍写着归宿为「保留」。矩阵原文与实现存在字面冲突，下一次矩阵验收会再次被绊。
- **R3（低）· L2 CLI 缺口 F4 波次未认领。** `deliver` / `sdk` / `change` 三个子命令组与 `authoring add-option` / `set-primary` 仍不存在。后果与 T13 时点相同：矩阵第 14/15/17 行的能力只在 GUI 可达，无法进冒烟脚本，回归风险只能靠单测覆盖。
- **R4（低）· 本轮未发现功能性 bug。** 无崩溃路径、无静默吞错、无 `unwrap_or` 兜底新增；四处红线声称与代码事实一致。

### 4.3 与 T13 遗留清单的对账

T13 §4 列的 4 项「若主开发决定修复」的新 API 需求，本轮核对全部落地且形态与建议一致：`template_export_from_project`（H1）、`rerun_from` + 下游状态重置（H3）、`CancelSignal` 取消信号（H4）、`pipeline_artifact` 只读产物查询（M2）。T13 §5 的文档偏差中，「26+ → 25 份」与「重置行归宿」两项已修正；「流水线每卡耗时」已由 `StageRecord::duration_seconds()` 补上；「清空日志裁决未入档」未修。

### 4.4 不构成不通过的已知取舍（保留在案，供主开发确认口径）

- `answers_digest` 只覆盖答卷结构，不覆盖 `parameters` / `evidence`（`model.rs:346` 已如实写明范围）。篡改参数值而不动选项结构可以躲过指纹比对；因该字段名与文档均未越界许诺，本 agent 不判其为缺陷，但它是「批量迁移模板可信度」的实际上限。
- 换皮词表仍是全局单表、词条不记来源，无法表达分组可见性（F4d 已登记）。R1 与此同源。
- `CrossCheckService::cross_check` 对空答卷仍直接推进 `CrossChecked` 且不写 proof（F4d 已登记）。取用侧证据关卡拦得住，上游口径可疑。

### 4.5 真机走查前置清单（合并去重后的最小必走项）

来源：T13 §2.5 的 U1-U14 + 本轮从代码识别出的 F4c/F4d 新增 GUI 面（F4c/F4d 的逐项清单未落盘到 `docs/memory/`，故按代码事实重建）。按「不走完就不能宣布 Phase 1 收官」的重要度排序，取前 12 项。

| # | 操作 | 预期 |
|---|------|------|
| 1 | 「运行日志」→ AI 面板套用 preset → 填 base-url/model/密钥引用 → 「保存并诊断」 | 诊断行如实显示可用/不可用；不可用时面板、状态栏、底栏三处红字一致（R7），保存后无需重启即生效 |
| 2 | 「开发流水线」→ 选「从 C0 到 C6」→ 「运行选定范围」→ 运行中点「停止运行」 | 按钮文案写「将在当前阶段结束后停止」；停下后该段显示为未运行（非失败），已完成段产物仍在，再点运行能从断点续上 |
| 3 | 流水线右栏任一已跑段点「重跑」 | 出现二次确认条，列出后端算出的下游段清单与将作废的署名；点确认后回执给出重置段数与作废条数 |
| 4 | 重跑后回到 C5/C6 人工门 | 旧署名消失，必须重新署名才能推进；C6 在未运行时点确认应报错而非静默 |
| 5 | 流水线任一已跑段点「产物」 | 右下出现 document.md 预览 + 双格式路径 + sha256 前 12 位；超大文档显示截断提示；未跑过的段两个按钮都不出现 |
| 6 | 设计工作台 → 模板面板 → 填模板 id/审核人/审核结论 → 「另存模板」 | 回执给出导出点数、跳过的未确认点数；不填审核人或结论应被后端拒绝并在状态栏显示原因 |
| 7 | 存档管理 → 填操作人与理由 → 「工作台重置」 | 二次确认后给出清空计数；重置后创作态回到未作答，而已冻结版本与流水线产物仍在（切到打包视图「刷新清点」应仍齐备） |
| 8 | 打包阶段 → 「存档体检」 | 出现结构化体检行；有问题时逐条列出而非只给一句「有问题」 |
| 9 | 左栏 16 领域卡滚到底 → 点「玩法系统设计」→ 点任一节点 → 检查单联动 | 第 16 张「上线准备设计」可见可点；中栏标题与检查单逐级收窄（U1+U2 合并） |
| 10 | 多选点勾两个选项 → 确认 → 「设主」；再走 N/A 三元组只填理由码 | 缺主选时红字提示，设主后出现 ★；N/A 缺说明/署名时状态栏显示后端拒绝原因（U3+U4 合并） |
| 11 | 右栏「校验」→「四门预检」→「执行冻结」→ 冻结后点「前往开发流水线」；右栏「缺失项」点条目跳转 | 未全绿时冻结按钮灰；全绿后可点；缺失项能跳到对应领域/节点并选中（U6+U7 合并） |
| 12 | 切系统为深色主题后重启 exe，走一遍 1-3 项 | `build.rs` 锁定的 fluent-light 生效，文字不消失（T11 曾修此坑） |

未进前 12 的 T13 原项（U5 表格/JSON 互切、U9 补充开发按钮门控、U10 SDK 三态审批、U11 打包逐段清点、U12 日志过滤导出、U13 模板列表 26 份、U8 流水线 13 段可见）本轮未新增改动面，建议作为第二批走查，不阻塞 Phase 1 收官判定。
