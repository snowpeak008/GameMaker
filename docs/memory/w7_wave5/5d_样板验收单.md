# T-W7-5d 塔防薄样板 验收单（G1 建造放置轻档实战 + C3 复核）

任务：执行计划 §6 样板矩阵最后一件（波 5），范围经主开发调整为两件独立事：
① 独立新 pack（`knowledge/design_space/towerdef_thin`）实证 **G1 sys.build_placement
轻档 BP0 预设槽位放置**（塔防语义，不碰 ld）；② C3 user_prompt 属性值欠账复核。
开发子 agent，2026-09-06。

## 0. ld 对照为什么推迟（引用说明）

原计划「lane_defense 重表达 = 迁移等价对照 + G1 轻档实战」。波 4b **发现 B**
（`docs/memory/w7_wave4/4b_迁移方案.md` §一）已论证 lane_defense 本卡零迁移：
任何 system_refs 都会让 `execute_freeze` 把非空 `module_versions` 写进冻结哈希
载荷 → 金样 `identity.frozen_hash` 必漂移（豁免清单固定不许新增）；且 tier 合成点
`ld.tier` 恒 Active 进分母，破 cli_smoke 既有断言（访谈回合 5）与冻结完备门。
ld 拆解归模块量产波金样重固化窗口（4b §四预案表第 1/2/4 行覆盖 deploy/economy/
wave 三域），主开发已接受。本卡 G1 实战改走独立新 pack。

## 1. pack 组合概览

| 实例 | 模块 | 声明档 | W / 档带 | κ | 绑定要点 |
|---|---|---|---|---|---|
| placement_main | sys.build_placement | **bp0_preset_slots（G1 轻档主角）** | 4 轻 | core | 放置指令/格归属/塔功能值 = pack 核心名词；建造扣费 → economy_main.currency_main（供需边） |
| economy_main | sys.economy | basic_income T0 | 5 中 | strong | material_entity → td_battlefield_salvage（战场残料，核心名词）；money_supply = 核心名词 |
| score_main | sys.scoring_combo | k0_score_table K0 | 4 轻 | weak | 三个事件源 consumes 全部绑 td_kill_signal（击破事件——塔防语义下战斗命中/判定/消除统一折算为击破） |

- reference_games：Kingdom Rush / Plants vs. Zombies / Bloons TD 6（≥3 ✓，
  游戏名只进 reference_games，pack 文案零商业游戏名——5b/5c R5 双实证教训）。
- **allowed_tiers 收窄实证**：placement_main 声明 `[bp0_preset_slots,
  bp1_grid_adjacency]` → BP2/BP3 谱系五点（support_rule/blueprint_copy/
  demolish_refund/upkeep_model/terrain_destruct）按不可达剔除、模块一致性规则
  `refund_and_upkeep_answered_together` 连带剔除（e2e 逐点断言在案）。
  towerdef_thin 装配后 2617 点。
- pack 本体薄点 ≤6 ✓（7 决策点 = 3 机制/结构薄点 + 4 实体/数据表）：
  wave_system（L3 结构，#7 未入库的薄点替位——如实标注）、wave_spawn_rule
  （按表投放/压力成长，Schedule 表达）、kill_bounty_rule（击破赏金——击破事件
  同时是计分事件源）、slot_roster / tower_roster / enemy_roster / wave_table。
- 波次表 enemy_id 外键由 pack 级 `row_reference` 一致性规则锁定（wave_table.enemy_id
  → enemy_roster.enemy_id）。
- 绑定按 provides 事实逐字节咬合：V6 反例（击破事件绑定改指 td_ghost_signal）
  装配失败点名实例/名词/幽灵目标（e2e 负测试）。

## 2. R-C1′ 组合判定实录（|H|=0 路径——第三条判定路径实证）

- **H = ∅**：BP0 W4 轻（core）/ T0 W5 中（strong）/ K0 W4 轻（weak），
  无人 W≥9——G1 轻档实战的组合形态如实声明。空集平凡连通。
- **零 block 零 advice 零署名确认**。与尖塔（|H|=3 超线确认流）、自走棋
  （|H|=1 压线免确认）合成三条判定路径的对照系：**超线 / 压线 / 空集**。
- **B(G) = 4×1.0 + 5×0.75 + 4×0.5 = 9.75 ≤ mid_core 42**（真预算表副本消费）。
- 判定实录出处：`crates/adm4-app/tests/towerdef_sample_e2e.rs::
  towerdef_thin_pack_assembles_and_bp0_light_composition_is_clean`；CLI 面
  `scripts/cli_smoke.ps1` 8m 段（`重核集合 H：（空）（|H|=0，连通）` /
  `重度预算 B(G)：9.75` / 零 [ADVICE] 零 [CONFIRM-REQUIRED] 字面断言）。

## 3. G1 轻档 C4 证据（全链：冻结五门绿 + 三模块版本锁 → C0-C6 全绿）

全链出处：`towerdef_full_chain_reaches_phase1_with_bp0_placement_at_c4`。
冻结锁 build_placement 1.0.0 / economy 1.0.0 / scoring_combo 1.0.0。

| 语义 | C4 能力契约 | GWT Then 断言（字面） |
|---|---|---|
| BP0 格占用约束（放置合法性） | cap_placement_main.occupancy_rule | `实体 towerdef.slot_roster 的 world_grid_occupancy 按公式 claim_exclusive(cell, structure_id) fail_if_occupied 变化`——一格一物、重叠即拒，落在槽位表实体上（**含槽位约束语义** ✓）|
| BP0 即时结算落成 | cap_placement_main.build_cost_timing | `资源 sys.economy.currency_main 按 build_cost(structure_type) 消耗` + `生成实体 towerdef.tower_roster` + `发出信号 placement_done_signal`（塔防塔位口径三拍）|
| 波次投放（pack 薄点） | cap_towerdef.wave_spawn_rule | `每 next_spawn_interval(towerdef.wave_table) 秒 触发一次：生成实体 towerdef.enemy_roster；发出信号 td_wave_signal` |
| 击破赏金（pack 薄点） | cap_towerdef.kill_bounty_rule | `资源 economy_main.currency_main 按 bounty_of(enemy_id) 增加` + `发出信号 td_kill_signal`（经济与计分的共同事件源）|

- **塔实体表进 GameSpec** ✓：C0 tables 含 towerdef.tower_roster，三行塔实体
  （arrow_spire/frost_pylon/ember_mortar）逐一在场。
- **C6 含放置任务** ✓：task_cap_placement_main.occupancy_rule /
  task_cap_placement_main.build_cost_timing / task_cap_towerdef.wave_spawn_rule
  在场，全部任务 id 零同名重复（BTreeSet 计数断言）。

### 撞臂豁免申报（1c 纪律：不改 c4_capabilities，署名 N/A 逐条上报）

| 决策点 | 撞臂 | 豁免理由 |
|---|---|---|
| placement_main.slot_legality | 两选项（预设槽位白名单/谓词现场裁定）effects **均为 RollCheck** | **G1 BP0 的正名点被未交付臂挡住**（与 5b squad_main.synergy_bonus 同款「正名口径撞臂」实证，两例合并成 1c 机动卡的最强验收场景）。放置合法性语义由 occupancy_rule 的 claim_exclusive…fail_if_occupied 占用约束 + 槽位表 enabled_from_wave 解锁列承载到 C4——上报 1c，交付 RollCheck 真渲染后解除豁免改选 preset_slot_whitelist 重跑全链 |

全链只用旧 7 变体 + Schedule，C0-C6 全绿即证明未触四臂（1c 纪律核对 ✓）。

## 4. C3 复核结论摘要（全文见 `5d_C3复核结论.md`）

**欠账属实（user_prompt 只有属性键名不带值）→ 判定安全 → 已修（非挂账）。**

- 安全依据：金样 C3 产物**不含 user_prompt 字段**（contract 只有 assets 四段），
  固化/比对链走 ScriptedProvider 按 purpose 出应答不消费 prompt，frozen_hash
  载荷不含 C3 请求面——修复 = 纯提示词增强，产物字节不动。
- 修复：`c3_content.rs::entity_property_text`——表行实体按 C0 id 约定
  （`<表id>.<行键值>`）回查行数据渲染 `key=value`；取不到值退回键名（不发明数据）。
- **金样实比对零漂移实证**：修复后代码上照 golden_make §1-§6 从零重建 lane_defense
  项目跑 C0-C6（脚本 AI、临时数据根、不碰金样目录），`golden_diff.ps1 -DataRoot`
  输出「[金样比对] 干净」退出码 0。
- 调用现场证据：塔防 e2e 断言 6 个 sprite2d 实体的画面描述请求带
  `cost=120`/`attack=18`/`hp=600`/`bounty=60` 逐值（ScriptedProvider.calls() 探针）。

## 5. 增点归档申报（模块 JSON 修改，5a/5b 接力同款占位符纪律）

C1 effect_dangling_entity 纪律：entity 引用一律 `{param:xxx_table_id}` 占位 +
必填 scalar text 参数。只修本卡全链选用的 BP0 干净选项（新增必填参数，未动
选项语义/选项集/档位结构；两点无任何既有测试作答，零弱化）：

- sys.build_placement：occupancy_rule/exclusive_cell（schema none → scalar
  +occupancy_table_id，modify_property entity `world_map` 裸名 → 占位符）；
  build_cost_timing/instant_build（schema none → scalar +tower_table_id，
  spawn_entity entity `placed_structure` 裸名 → 占位符）。
- 未修：slot_legality 两选项（RollCheck 撞臂本卡豁免未选用，且 preset 选项是
  Table schema——Rows 参数不走占位符替换，5b §6 遗留 3 的同款基建缺口）、
  BP1-BP3 各点（本卡未选用，MG 教训：不动没作答的档位面）。

模块门禁（knowledge_modules 13 断言）修后全绿；calibration_regression 5 全绿
（档位 rating 零改动）；prompt_library 门禁 4 全绿。

## 6. 阈值体感反馈（标定试用态数据点）

- **|H|=0 的「无判定压力」区间很宽**：三实例 B(G)=9.75 只吃中核预算 23%，
  离参考线与预算线都很远——轻档组合在当前判定体系下几乎「自由通行」。体感
  合理（轻组合本就不该有结构风险），但也意味着**判定体系对轻组合的唯一实质
  约束是 V6 供需闭合**（本卡的反例证明它在工作）。
- **BP0→BP1 的档间跳变温和**（W4→W7 仍不入重核）：塔防 pack 若未来升 BP1
  开邻接加成，组合判定面零变化——G1 谱系的判定敏感段在 BP2（W12 入重核），
  与 5b 观察到的「档位量表粒度决定参考线敏感度」互为印证。

## 7. 全门禁输出

| 门禁 | 结果 |
|---|---|
| cargo test --workspace | **630 全绿**（基线 627 + towerdef_sample_e2e 2 条 + c3 属性值单测 1 条；只增无减） |
| cargo fmt --check | 0 diff |
| cargo clippy --workspace --all-targets | 零警告 |
| space validate | **六包全 [OK]**（autochess 2642 / grid 2611 / ld 2604 / rhythm 2605 / spire 2610 / **towerdef_thin 2617**），退出码 0 |
| cli_smoke.ps1 | 退出码 0（含新 8m 塔防段：装配 + |H|=0 薄判定；既有段零改动） |
| cargo build -p adm4-desktop | 0 |
| golden_diff -SelfTest | 三场景全部通过 |
| **golden_diff 金样实比对**（动了 C3 的硬义务） | 「[金样比对] 干净」，退出码 0（修复后代码从零复演 lane_defense 全链比对） |
| knowledge_modules / calibration_regression / prompt_library | 13 / 5 / 4 全绿 |

## 8. 遗留与声明

1. **（上报 1c）slot_legality 正名点撞 RollCheck**（§3 豁免表）——与 5b 的
   synergy_bonus/Attach、bracket_shape/RollCheck 并列，1c 交付真渲染后本样板是
   现成验收场景（解除豁免 → 改选 preset_slot_whitelist → 重跑全链，塔防塔位
   白名单语义即得完整 C4 渲染）。
2. **（上报量产波）ld 对照欠账**：本卡只交付 G1 轻档实战一翼；「lane_defense
   重表达 = 迁移等价对照」按发现 B 预案（4b §四第 1/2/4 行）归金样重固化窗口。
   届时 sys.build_placement 承接 ld.deploy_system/deploy_cost（实例 id=ld 保 id
   与 sys.economy 二选一，另一方走映射表改名）。
3. **（上报）Rows/Matrix 参数不走 substitute_placeholders**（5b §6 遗留 3 复现）：
   slot_legality/preset_slot_whitelist 是 Table schema 选项，其 effects 里的
   `placed_structure`/`world_map` 裸名无法用占位符路线修复——1c 交付 RollCheck
   渲染时会同时撞上这个基建缺口，两项宜同窗处理。
4. **（申报）u.genre 无塔防选项**：通用层不在可写范围，冒烟 8m 段走装配+判定
   不触 L2；e2e 用测试内嵌通用层（含 tower_defense 选项）。与 5a/5b 同款缺口。
5. **真 AI 访谈未验证**：数据根无 ai_provider 配置，全链走 ScriptedProvider
   （与 5a/5b/5c 同状态）。
6. **并行卡边界**：未改 ld/grid/spire/autochess/rhythm pack；模块修改仅
   sys.build_placement 两处增参（§5 申报）；未改 tests/golden/**、docs/plan；
   零 git 操作、零新增依赖。C3 修复在授权范围内（「仅当判定安全时」条款，
   金样零漂移已实证）。
