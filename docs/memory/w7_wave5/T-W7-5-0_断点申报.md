# T-W7-5-0 断点状态申报 · 标定回归

> 子 agent 开发。每里程碑追加，接力者先读本文件再跑测试。

## M0 开工（2026-09-05）

- 基线确认：`cargo test -p adm4-decision` 全绿（V4\ 内执行，退出码 0）。
- 必读完成：定稿 §3/§4.1-4.3/§6.2-6.3/§8 问题 2、附录《极端组合形态测试》（EU4 分解 B(G)≈84、|H|=8、修正案 3 预算占位 95-105）、执行计划 §6（标定集补 EU4 → 11 样本）、composition.rs（CompositionBudget 键=ProductGrade::key、B(G)=Σ W×weight(κ)、0c 语义=空表静默跳过）、审计 A 的 10 款清单。
- 三锚点直接采用：尖塔（§6.2，B=37.75）、MOBA（§6.3，B=50.25）、EU4（附录 §1，B=84.0）。

### 接线路径侦察结论（申报一处必要越界）

预算数据流：`services.rs open_engine → engine.composition_assessment → compose.rs assess_composition → CompositionInput.budget`。
`assess_composition`（adm4-authoring/src/compose.rs L180）目前硬编码 `CompositionBudget::default()`，且该函数是 gate2 与 authoring 即时反馈的唯一合流点（两处逐字节一致纪律）——预算值**无法只经 services.rs/config.rs 到达校验器**。

**申报**：除任务卡可写范围外，需最小改动两个文件（均为穿参，不改任何判定逻辑）：
1. `adm4-authoring/src/compose.rs`：`assess_composition` 增加 `budget: &CompositionBudget` 参数，替换 default 硬编码（含该文件测试调用点同步）；
2. `adm4-authoring/src/engine.rs`：引擎增 `composition_budget` 字段 + `set_composition_budget` setter（默认空表 = 现状语义），`composition_assessment` 穿参。

不动 composition.rs / system_module.rs / 模块 JSON / docs\plan。空表语义保留：未配置 = 静默跳过（提示制不阻塞）。

## 计划

1. budget.json（knowledge/calibration，形态 = CompositionBudget serde）+ config.rs `calibration_root` + services.rs 加载注入；
2. adm4-decision/tests 新标定回归测试（11 款分解编码为 CompositionInput，锁 B(G)/R-C1′/预算提示，读真 budget.json）；
3. adm4-app/tests 集成测试：MOBA 级组合 mid_core 触发 V5、mmo 不触发；
4. 5-0_标定报告.md（含签字栏）；
5. 全门禁。

## M1 标定 + 入库 + 测试全绿（2026-09-05）

- **11 款标定完成**：五档建议值 hyper_casual 12 / casual 28 / mid_core 42 / hard_core 55 / mmo 68，落 `knowledge/calibration/budget.json`（占位试用态）。config.rs 未动——预算路径按 prompt_library 同一先例从 `system_modules_root` 的兄弟目录派生（`knowledge/calibration/budget.json`），零新配置键。
- **接线**（申报过的两处越界已做，均为穿参零判定改动）：
  - `adm4-authoring/src/compose.rs`：`assess_composition` 增 `budget: &CompositionBudget` 参数（原硬编码 default），文件内 10 处测试调用点同步；
  - `adm4-authoring/src/engine.rs`：`composition_budget` 字段 + `set_composition_budget`（默认空表 = 现状）+ `composition_assessment` 穿参；
  - `adm4-app/src/services.rs`：`inject_system_modules` 后注入 `load_composition_budget()`（文件缺失 → 空表静默跳过；解析失败 → Err）。
- **新测试**：
  - `adm4-decision/tests/calibration_regression.rs`（13 测试）：11 款分解编码为 CompositionInput、B(G) 逐款锁定、R-C1′ 逐款自检、预算文件五键严格递增、散点排序无倒挂断言；
  - `adm4-app/tests/budget_advice_e2e.rs`（2 测试）：真四模块重档组合 B=43.5，mid_core 触发 V5（含贡献降序表、不进 gate2 block）、mmo 不触发；budget.json 缺失 → 占位期静默（回滚护栏）。
- **规则回归发现（第二产出）**：旷野之息"单主核轮毂"拓扑触发双连通割点提示（提示级不拦，语义可辩护但对 ARPG 常见形态偏敏感）——上报不改；星露谷/极乐迪斯科在直觉档下的提示为语义正确（设计复杂度 > 市场直觉档），非误伤。零硬违例误伤。
- 待办：标定报告落盘 → 全门禁。

## M2 收口（2026-09-05）——任务完成

- 标定报告落盘：`docs/memory/w7_wave5/5-0_标定报告.md`（11 款分解全表 + 散点表 + 五档推导 + 等权/档带检验 + R-C1′ 自检表 + 上呈项 4 条 + **待用户签字栏**）。
- 全门禁：
  - `cargo test --workspace` 全绿 **621 通过**（基线 606 + 15 新增：calibration_regression 13 + budget_advice_e2e 2；1 ignored 为既有）；
  - `cargo fmt --all -- --check` 干净；
  - `cargo clippy --workspace --all-targets` 零警告。
- 追加修复：`compose_fix.rs` 两处测试调用点随 assess_composition 签名同步（纯测试适配，断言零弱化）。
- cli_smoke.ps1 未追加：预算提示走 compose report 既有通道，app 级 e2e 已覆盖任务卡验收场景，无新 CLI 面。
- **禁改项守约**：composition.rs / system_module.rs / 既有模块 JSON / docs\plan 零改动；无 git 操作；无新增依赖。
