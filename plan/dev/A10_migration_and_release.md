# A10 迁移与发布

状态：待执行
依赖：A09

## 目标

安全切换默认写路径，完成旧项目迁移、回滚和独立发布证明。

## 变更

- 添加版本迁移器、迁移预览、备份、回滚和幂等测试。
- 经过稳定期后，新项目默认启用 V2，旧项目按项目选择迁移。
- 删除不再使用的角色化 AI 路径和类型硬编码，但保留兼容读取窗口。
- 执行完整 Web、Rust、UI、跨结构、便携包、搬迁和 EXE smoke 门禁。

## 验收

迁移失败不改变原保存；迁移后可重新打开并继续流水线；复制到另一台满足运行前提的电脑后不访问旧项目内容。
