# A08 流水线 07–14

状态：待执行
依赖：A07

## 目标

把视觉、架构、任务、开发、资产、验证和发布全部绑定到冻结规格与验收场景。

## 变更

- Step07 从表现规格编译结构化图像任务，增加分辨率、构图、用途、风格和图像质量门禁。
- 08–10 编译运行时架构、资产计划和有所有权的任务 DAG。
- 11–12 只允许执行声明写入范围并保留工具证据。
- 13 运行场景、性能、可访问性和回归验证。
- 14 复用独立性、完整性、EXE smoke 和发布证据。

## 验收

每个产物都可反查规格和场景；UI 只显示当前步骤内容；失败不伪装成功。
