# A07 流水线 00–06

状态：待执行
依赖：A06

## 目标

把设计阶段改为冻结规格驱动的确定性编译链。

## 变更

- 为 00–06 定义版本化输入/输出契约和纯编译器。
- 建立意图、能力、动作、世界、场景和冻结交接门禁。
- 每个产物写入源规格哈希、编译器版本和追踪引用。
- 禁止步骤从旧目录猜测输入或用空模板静默成功。

## 验收

从相同冻结规格重复执行得到相同语义哈希；任一上游错误在最早步骤阻塞。
