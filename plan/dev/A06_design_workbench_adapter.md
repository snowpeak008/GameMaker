# A06 设计工作台适配

状态：待执行
依赖：A03、A04、A05

## 目标

保持现有 D1–D4 操作入口，同时让 V2 规格成为可验证的并行产物。

## 变更

- 添加 `game_spec_v2` 开关和只读旧状态转换器。
- 将工作台命令转换为 `SpecPatch`，现有 UI 视图改为规格投影。
- D1–D4 生成意图、能力、玩法契约和冻结规格。
- 旧交接与 V2 交接双写并生成语义差异报告。

## 验收

开关关闭时行为完全兼容；开启时旧保存不被改写，四个样例可完成 D1–D4。
