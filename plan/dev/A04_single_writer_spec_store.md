# A04 单写者规格仓库

状态：待执行
依赖：A02

## 目标

建立 `SpecPatch`、乐观并发、原子提交和不可变审计记录。

## 变更

- 定义带 `base_revision` / `base_hash` 的补丁操作。
- 实现内存应用、影响分析、完整验证、临时文件写入和原子替换。
- 拒绝陈旧修订、旧值不匹配、保护字段删除和越界扩展写入。
- 增加崩溃恢复、并发写入和审计重放测试。

## 验收

任意提交可从初始快照和日志重放得到同一哈希；失败提交不留下部分状态。
