# A05 有界 AI 补全

状态：待执行
依赖：A03、A04

## 目标

让 AI 补全成为真实、可观察、不能越权的数据流。

## 变更

- 按未决问题打包最小相关子图和允许写路径。
- 模型只返回 `CandidateSpecPatch`、依据、置信度和未决项。
- 复用现有配置解析和 adapter，不创建角色代理。
- 增加越界、提示注入、无效 JSON、陈旧补丁、超时、重试和关闭 AI 测试。
- 记录模型配置 ID、输入/输出摘要哈希、验证结果和用户确认。

## 验收

重复运行 20 次不出现越权提交；未调用、失败、拒绝、确认和提交状态在 UI 与产物中可区分。
