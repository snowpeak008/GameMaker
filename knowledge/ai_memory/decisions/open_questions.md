# 待解决问题

> 尚未解答的问题，下次会话优先处理

---

## 流水线 UI 相关

- [ ] AI 对话面板在"流水线模式"下的系统 prompt 具体内容
  - 需要包含：当前步骤说明、制品文件摘要、常见问题、故障排查建议
  - 格式：Markdown？纯文本？
  - 上下文长度限制？

- [ ] 底部面板高度初始值和最小值
  - 初始高度：180px 还是 200px？
  - 最小高度：100px？
  - 是否需要"收起"按钮？

- [ ] 步骤树的折叠/展开状态是否持久化
  - 用户折叠某个分组后，下次打开是否记住？
  - 存储在哪里？project_settings.json？内存？

---

## 记忆系统相关

- [ ] 会话结束时机如何触发
  - 选项1：用户主动说"完成"/"结束"
  - 选项2：每次对话结束时自动检测修改
  - 选项3：手动命令（如 `/save-session`）
  - 倾向：选项1（用户主动）

- [ ] freshness.json 的更新频率
  - 每次会话结束时全量更新？
  - 增量更新（只更新修改的文件）？
  - 工具脚本 `update_freshness.py` 何时调用？

- [ ] 缓存失效后的处理策略
  - 自动重读失效文件并更新缓存？
  - 提示用户"缓存已过期，是否刷新"？
  - 静默使用旧缓存，标记为 stale？

---

## GUI 性能优化相关

- [ ] CommercialDesignApp 的局部更新方案
  - 是否需要给每个 widget 分配 ID（类似 React 的 key）？
  - 状态变更时如何计算影响范围？
  - 是否需要引入虚拟 DOM 或 diff 算法？

- [ ] cross_layer_rules.lint() 的优化方案
  - 缓存规则匹配结果？
  - 只在相关选项变更时重新 lint？
  - 异步执行，防抖触发？

---

## 技术债务相关

- [ ] generation.py 是否需要拆分
  - 3795行确实过大，但16个函数互不调用
  - 拆分收益：可维护性提升
  - 拆分成本：import 变复杂，单元测试需调整
  - 当前优先级：低（不影响功能）

- [ ] ucos/ 目录被删除的文件是否需要恢复
  - 哪些功能依赖这些文件？
  - 是否有替代方案？
  - 当前优先级：待调查

---

## 配置与部署相关

- [ ] api_config.toml 的默认 base_url 是否需要修改
  - 当前：`https://vip.auto-code.net/v1`（代理）
  - 是否应该默认指向官方 OpenAI API？
  - 还是让用户首次启动时选择？

- [ ] PyInstaller 打包是否包含 Unity 预检逻辑
  - 打包后的 .exe 能否正确检测 Unity 路径？
  - project_settings.json 在打包后如何处理？
