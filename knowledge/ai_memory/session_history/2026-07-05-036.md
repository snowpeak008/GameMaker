# 会话记录：2026-07-05-036

## 摘要

继续 goal 模式推进 Rust 全项目重建，补强 Slint desktop 的正式存档并行打开语义：项目列表读取真实 `.archive_lock`，选择存档会持有桌面层选中锁，第二个窗口/会话再选择同一正式存档会得到明确锁冲突，而不是静默读取或表现为“不知道为什么打不开”。

## 完成内容

- `adm-desktop`
  - 增加 `adm-archive` 依赖，用于复用 `ArchiveLock`。
  - 新增 `SelectedArchiveLockState` 和 `SelectedArchiveLock`。
  - `Select` 成功后持有正式存档 `.archive_lock`。
  - 同一窗口持有锁时允许读取详情、阶段详情和导出。
  - 其他窗口/会话持有锁时拒绝读取详情，返回 `AlreadyLocked`。
  - `Create + Run`、`Resume`、`Resume Failed`、`Rerun` 执行前释放桌面选中锁，业务写入路径继续使用归档层锁；执行成功后重新持有当前 archive。
  - `Import` 成功后释放旧锁、选中并锁住导入 archive。
  - `ProjectRow.locked` 不再固定为 false，而是读取 archive 根目录 `.archive_lock`。
  - `refresh_projects(...)` 能识别本窗口 owner session，自己持锁时可渲染详情，外部锁时渲染不可用状态。
  - `Export`、`Inspect Stage` 支持本窗口锁 owner session。
  - desktop smoke 覆盖：
    - 本窗口选中锁可读详情。
    - 竞争选中锁被拒绝。
    - 外部 `.archive_lock` 显示 `locked=true` 和不可用详情。
- 发布产物
  - 重新构建 release。
  - 重新 staging 到 `RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`。

## 验证

- `cargo run -p adm-desktop -- --smoke`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 75 个 Rust 单元测试通过。
- `cargo build -p adm-desktop --release`：通过。
- `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`：通过。
- `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`：通过。
- `cargo fmt --check`：通过。

## 后续事项

- 继续补齐桌面 GUI 的真实运行、校验、打包闭环。
- 增加可视窗口手工验收：两个 Rust 窗口选择同一 archive 时，第二个窗口必须显示锁冲突。
- 后续可考虑 stale lock 检测/清理策略，但清理必须是显式用户动作，不能自动删除未知锁。
