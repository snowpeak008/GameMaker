# Session 2026-06-28-001

## Summary

Executed `plan/pending_tasks_20260628.md`: committed the Codex CLI subprocess/image fixes, added orphan draft and snapshot pruning, performed one-time draft cleanup, and completed real Codex CLI image generation verification through both the image tool and Step07.

## Completed

- Fixed Codex subprocess handling by removing the obsolete stdin `-` argument, passing `stdin=None` so `input=` can pipe prompts, and decoding output with UTF-8 plus replacement fallback on Windows.
- Updated `CodexCLIImageGenerator` to recursively scan `CODEX_HOME/generated_images/**.png`, matching the current Codex CLI image_gen output layout.
- Added regression coverage for recursive Codex image copying, UTF-8 subprocess decoding, and stdin handling.
- Changed draft pruning so drafts linked to missing save directories are treated as pruneable orphan drafts.
- Added `prune_draft_snapshots(project_root, keep_per_draft=0)` and GUI startup snapshot pruning for pruneable drafts only.
- Added regression tests proving snapshot pruning skips the current draft and drafts protected by existing saves.
- Ran the one-time cleanup: pruned snapshots from 22 drafts and deleted 34 pruneable drafts; `drafts/` dropped from roughly 49 dirs / 1.69 GB to 16 dirs / 551.51 MB.
- Updated local Codex CLI to `codex-cli 0.142.3`; `codex.cmd` is the usable command on PowerShell because `codex.ps1` is blocked by execution policy.
- Verified real Codex CLI image generation with a rigorous Lumen Archive prompt; output copied to `sandbox/codex_cli_image_e2e_20260628/ig_069bb28281db1b16016a3ffec35894819180bf64e1aa4edb70.png`.
- Verified Step07 end to end against `drafts/codex_template_2d_redesign_fixes_validation`: 5 generated style images were produced with manifest status `success`.

## Verification

- `python -B -m pytest core\tests\unit\test_codex_image_tool.py core\tests\unit\test_model_adapters.py core\tests\unit\test_draft_archive_paths.py -q`: 18 passed.
- `python -B -m pytest -q`: 154 passed.
- `PYTHONPYCACHEPREFIX=.cache\pycache python -B -m compileall core pipeline tools\validators\pipeline_quality.py tools\asset_production tools\config`: passed.
- `git diff --check`: passed with only CRLF working-copy warnings.
- `codex.cmd --version`: `codex-cli 0.142.3`.
- `codex.cmd exec` using the sandbox `CODEX_HOME` failed because that home had no credentials; real image verification used `CODEX_HOME=C:\Users\admin\.codex` with approval outside the filesystem sandbox.

## Git

- `f1eb62f fix: resolve codex exec image subprocess handling`
- `7090277 fix: prune orphan drafts and draft snapshots`

## Follow-up

- User should visually review the Step07 images under `drafts/codex_template_2d_redesign_fixes_validation/outputs/artifacts/stage_07/generated_images/`.
- Future automated Codex CLI image runs from the Codex-managed sandbox need an authenticated `CODEX_HOME` or an outside-sandbox run; normal GUI launches should use the user's own Codex home when `CODEX_HOME` is unset.
- Continue excluding `plan/`, runtime `drafts/`, `sandbox/`, ignored local AI config files, and `sunny_girl_image2.png` from commits unless explicitly requested.
- CC-Panes shared memory was skipped because required environment variables were absent.
