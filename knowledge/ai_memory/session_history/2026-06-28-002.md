# Session 2026-06-28-002

## Summary

Continued the interrupted Step07/Codex image-generation follow-up after the previous Codex CLI process-cleanup attempt terminated the active session. Kept the code change minimal: Step07 image generation is now forced to run serially, and the project memory records a hard ban on broad Codex/sandbox process termination.

## Completed

- Removed the Step07 image-generation concurrency path so style option image generation calls the configured generator sequentially.
- Removed the unused per-worker Codex home helper that was only needed by the abandoned parallel approach.
- Added a small regression test proving Step07 image-generation workers stay at `1` even if config asks for more.
- Added a project anti-pattern rule: never terminate `codex`, `node`, or sandbox-related processes by broad process-name/PID matching; only do read-only diagnostics unless targets are proven outside the active session process tree and the user confirms.

## Verification

- `python -B -m pytest core\tests\unit\test_manual_style_confirmation.py core\tests\unit\test_codex_image_tool.py -q`: 12 passed.
- `python -B -m pytest -q`: 158 passed.
- `python -B -m compileall core pipeline tools\validators\pipeline_quality.py tools\asset_production tools\config`: passed.
- `git diff --check`: passed with only CRLF working-copy warnings.

## Follow-up

- Do not run broad process cleanup for Codex/sandbox helper issues. If a popup appears again, inspect logs and command lines only; ask the user before terminating any external process.
- `sunny_girl_image2.png` remains untracked local output and should not be committed unless explicitly requested.
- CC-Panes shared memory was skipped because required environment variables were absent.
