# 2026-07-14-004

## 摘要

将包含旧 Python 代码历史在内的完整 AI 记忆系统复制到 NEWrust，并以 PowerShell 维护工具替代 Python 工具，确保开发记忆可随 Rust 源码迁移但不进入产品运行时。

## 完成内容

- [x] 在根项目完成 `2026-07-14-003`、会话 JSON 和 freshness 同步后，复制完整 `knowledge/ai_memory` 到 `NEWrust/knowledge/ai_memory`。
- [x] 保留旧 Python 关键文件哈希为 `project_understanding/legacy_python_freshness.json`，并为 NEWrust 生成独立的当前 freshness。
- [x] 新增 NEWrust `AI_README.md`，合并 `AGENTS.md` 入口规则，明确旧 Python 历史只作为开发档案。
- [x] 新增 `memory_config.json`、`Update-MemoryFreshness.ps1` 和 `Test-MemoryFreshness.ps1`；NEWrust 记忆维护不依赖 Python。
- [x] 明确 `knowledge/ai_memory` 不属于产品资源组，便携构建和桌面运行时不得读取或打包该目录。

## 验证

- [x] 288 个源历史文件（排除已转换的当前 freshness）逐一 SHA-256 一致，缺失 0、差异 0。
- [x] 旧 Python freshness 副本与根项目源快照 SHA-256 一致。
- [x] NEWrust freshness 跟踪 43 个业务与记忆系统关键文件，stale 0、missing 0。
- [x] 常见私钥、云密钥、GitHub token 和 API key 模式命中 0。
- [x] NEWrust 独立性门禁扫描 224 个文件，父级依赖命中 0；现有和正式便携目录均不含 `knowledge/ai_memory`。

## 当前状态

- [x] NEWrust 源码目录现在可独立携带完整历史记忆和本地维护工具。
- [x] 历史 Python 文件名、路径和架构描述只是只读上下文，不是运行时依赖。
- [ ] 累计 NEWrust 改动提交后仍需从干净树运行正式 `tools/verify-standalone.ps1`。
