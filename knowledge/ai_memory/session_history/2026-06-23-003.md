# 会话记录：2026-06-23-003

## 摘要

按 `plan/pipeline_optimization/` 的流水线优化计划继续 goal 模式开发，补齐真实 D4 -> Step 00-06 跨进程执行缺口、无显式 L5 实体时的本地实体兜底，以及美术资产字段完整性。

## 关键发现

- D4 单独进程会把 source packages 写入自己的 per-session draft；后续新进程运行 Step 00-06 时只看当前 draft，导致 Step 00 报 `Stage 00 has no submitted Concept source package`。
- 导入器和 `core/engines/generation.py` 的源包查找逻辑不一致；前者可扩展，后者硬看当前 `SOURCE_ARTIFACTS_DIR`。
- 当前 D4 导出的设计文档可能没有显式 `L5实体` selections，导致 Step 02 entity_count=0、entity_coverage=0、Step 04 asset_count=3。
- Step 04 selection-derived assets 缺少 `priority`，造成 Step 06 美术评审 warning。

## 完成内容

- `core/source/finder.py`
  - 新增 `source_artifact_roots()`，按当前 draft、最新历史 draft、legacy `sandbox/source_artifacts/` 顺序查找源包。
  - `find_sources()` 改为在第一个有匹配源包的 root 内选择 latest/all，避免跨 root 混选。
- `core/engines/generation.py`
  - `_latest_concept_package()` 和 `_load_package_by_source_id()` 改用同一 source-root precedence。
  - selection-derived art assets 增加 `priority`、`complexity`、`trace_kind`。
- `pipeline/step_02_design_review_freeze/helpers.py`
  - 当没有显式 `L5实体` 时，从 design selections 合成最多 47 个可追踪本地实体。
  - 合成实体包含 kind/schema/source/source_selection_id/node_id/inference 元数据。
- `pipeline/step_04_art_requirements/helpers.py`
  - entity-derived assets 增加 `complexity` 字段。
- `core/tests/unit/test_pipeline_optimization_helpers.py`
  - 新增跨 draft source package fallback 测试。
  - 新增无显式 L5 时实体合成测试。
  - 补充 asset complexity 断言。
- `plan/pipeline_optimization/README.md` 与 `plan/status_snapshot_2026-06-23.md`
  - 记录本次完成状态和最新质量指标。

## 最新质量指标

在重新运行 `python -m core.main --stage D4` 与 `python -m core.main --from-step 0 --stop-step 6 --auto-approve --skip-preflight` 后，`python tools/validators/pipeline_quality.py` 输出：

- 问答覆盖率：0.4
- core_loop 输出率：1.0
- system definition rate：1.0
- entity coverage：1.0
- entity count：47
- requirement binding：1.0
- placeholder rate：0.0
- asset count：50
- Step 05 warning/blocking：0/0
- Step 06 warning/blocking：0/0

## 验证

- `python -m pytest core\tests -q`：26 passed；仅 `.pytest_cache` 写入权限 warning。
- `python -m compileall core pipeline tools\validators\pipeline_quality.py`：通过。
- `python tools\validators\pipeline_quality.py`：指标达标，读取最新 D4 -> 00-06 draft。
- `git diff --check`：通过，仅 Windows line-ending 提示。
- `python -m core.main --stage D4`：success。
- `python -m core.main --from-step 0 --stop-step 6 --auto-approve --skip-preflight`：Step 00-06 全部 success。

## 后续关注

- GUI 中手动验收 D4 handoff 和 Step 00-06 展示路径。
- 验证 Codex/Claude adapter 切换。
- 验证存档创建、重命名、关闭重开、加载和旧存档过滤。
- 在 GUI/本地配置下验证引擎切换 preflight。
