# 会话记录：2026-07-05-006

## 摘要

进入 goal 模式后继续推进 Rust 全项目开发，完成 Slint GUI 从内联 POC 到可维护 UI 文件的升级，并补齐 GUI 导入/导出、项目详情、pipeline 状态和 `adm-ui-model` 绑定使用。

## 完成内容

- 创建 goal：将 Rust 重建推进到全项目可用完成状态。
- `adm-desktop`
  - 新增 `build.rs`，使用 `slint_build::compile("ui/main.slint")`。
  - 新增 `ui/main.slint`，把原内联 Slint UI 拆到独立文件。
  - 增加 `slint-build = "1"` build dependency。
  - GUI 增加：
    - selected archive id 输入
    - export path 输入
    - import path 输入
    - Export 按钮
    - Import 按钮
    - Select 按钮
    - project detail 面板
    - pipeline status 面板
  - `--smoke` 扩展为创建项目、运行核心流水线、导出 `.admproj`、刷新项目列表。
- `adm-desktop` 开始使用 `adm-ui-model`：
  - `ShellState`
  - `ProjectListItem`
  - `PipelineStatusView`
- GUI 回调现在覆盖：
  - 创建并运行项目
  - 刷新项目列表
  - 选择项目并显示详情
  - 导出项目
  - 导入项目

## 验证

- `cargo fmt --check`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 34 个 Rust 单元测试通过。
- `cargo run -p adm-desktop -- --smoke`：通过。
- smoke 输出确认：
  - 创建项目成功
  - pipeline succeeded
  - validation passed
  - artifact count 可见
  - `.admproj` 导出成功
  - project list 使用 `locked=false` 状态渲染

## 后续事项

- 下一步继续 goal：真实 AI Provider 配置、task persistence、retry 和 secret ref。
- GUI 后续仍需要真实文件选择器、可点击列表、进度条/阶段视图和可见窗口人工验证。
- `adm-ui-model` 已开始使用，但还需进一步减少 GUI 层字符串拼接。
