# 2026-06-23-008 — bug收集文档4.md 第四轮修复

## 摘要

修复 `bug收集文档4.md` 中列出的 3 个一致性问题：Step00 新品类 genre inference、Step04 roguelike 品类键名兼容、CQ-013/CQ-014 实际证据入口过窄。

## 完成内容

- BUG-015：Step00 `_genre_key` 补充 `strategy/rpg/moba`，`GENRE_DEFAULT_EVIDENCE` 为三类补齐 CQ-005~CQ-012 默认证据。
- BUG-016：Step04 市场库品类键统一支持 `roguelike_action`，并保留 `roguelike.json` 文件名别名兼容。
- BUG-017：CQ-013/CQ-014 增加 `项目规模/平台范围/运营模式/商业模式/社交模式` 等实际 L4 字段和关键词。
- 补充回归测试覆盖 strategy/rpg/moba genre inference、CQ-013/CQ-014 常见字段命中、`roguelike_action` 市场库读取。

## 验证

- `python -m pytest core\tests -q`：47 passed（仅 `.pytest_cache` 写入权限 warning）。
- 本次改动范围 `black --check` / `flake8` / `mypy --explicit-package-bases`：通过。
- `python -m core.main --from-step 0 --stop-step 6 --auto-approve --skip-preflight`：Step 00-06 全部 success。
- `python tools\validators\pipeline_quality.py --check plan-002`：通过，entity coverage 0.4563 >= 0.38。
- 最新指标：question coverage 0.9333、entity coverage 0.4563、requirement binding 0.9753、placeholder 0.0、asset count 56、Step05 warning/blocking 1/0。

## 后续关注

- 真实 L5 高覆盖目标仍需用户在 DesignEngine 录入 >=40 个 L5 实体后验证。
- 全仓 pre-commit 仍有既有问题，当前只保证本次改动范围门禁通过。
