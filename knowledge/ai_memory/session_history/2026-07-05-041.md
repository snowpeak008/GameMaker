# 会话记录：2026-07-05-041

## 摘要

继续 goal 模式推进 Rust 全项目重建，将设计、开发、资产三个核心 artifact 从“只在 pipeline 成功状态里可见”提升为 Slint desktop 的结构化 Core Artifacts 面板。桌面工作台现在会从正式存档 content 中读取 `design/project.adm`、`development/plan.adm`、`assets/plan.adm`，展示核心循环数、开发任务数、资产任务数和每类 artifact 的摘要。

## 完成内容

- `adm-desktop`
  - Slint 新增 `CoreArtifactRow`。
  - Slint 新增 `core-artifact-text` 和 `core-artifact-items`。
  - 新增 Core Artifacts 摘要面板。
  - `DesktopRunSummary` 和 `ProjectInspection` 新增 core artifact 文本与 rows。
  - 新增 `inspect_core_artifacts(...)`。
  - 新增 `design_artifact_item(...)`，解析 design title/genre/quality/AI intervention/core loop。
  - 新增 `task_artifact_item(...)`，解析 development/assets 任务行。
  - 新增 `find_key_value(...)`。
  - 新建、恢复、重跑、恢复失败、导入/刷新项目都会从正式存档 content 读取 core artifact 状态。
  - desktop smoke 覆盖：
    - `design_core_loop=3`
    - `development_tasks=3`
    - `asset_tasks=2`
    - Core artifact row model 数量为 3
    - 导入项目后 core artifact row model 仍然存在。
- 发布产物
  - 重新构建 release。
  - 重新 staging 到 `RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`。
  - 新 staged hash：`fnv64:943cca65d35977c2`。

## 验证

- `cargo check -p adm-desktop`：通过。
- `cargo fmt --check`：通过。
- `cargo run -p adm-desktop -- --smoke`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 75 个 Rust 单元测试通过。
- `cargo build -p adm-desktop --release`：通过。
- `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`：通过。
- `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`：通过。

## 后续事项

- 继续把 Slint POC 整理成更清晰的工作台布局，避免纵向内容过长。
- 增加可视窗口手工验收：两个 Rust 窗口选择同一 archive 时，第二个窗口必须显示锁冲突和 owner 信息。
- 后续可将 Core Artifacts 面板改为可点击 artifact 详情。
