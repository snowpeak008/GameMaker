# 会话记录：2026-07-05-005

## 摘要

按用户要求优先接入 Slint POC，将 `adm-desktop` 从控制台占位改为真实 Slint 桌面壳，并连接到 `adm-application` 服务层。

## 完成内容

- `adm-desktop` 增加 Slint 依赖，当前解析为 `slint 1.17.0`。
- `adm-desktop` 接入：
  - `adm-application`
  - `adm-ai`
  - `adm-foundation`
- 新增 Slint 内联 UI：
  - 项目名输入
  - 数据目录输入
  - `Create + Run` 按钮
  - `Refresh` 按钮
  - 状态输出区
  - 项目列表区
- GUI 回调已连接 `adm-application`：
  - 创建项目
  - 运行核心流水线
  - 刷新项目列表
- 新增 `adm-desktop --smoke`，用于不打开持久窗口的自动化验证。
- 因 Slint 宏内部 lint 需要，`adm-desktop` 从继承 workspace `forbid(unsafe_code)` 改为本 crate `deny(unsafe_code)`；手写代码仍禁止 unsafe，避免与 Slint 宏生成代码冲突。

## 验证

- `cargo check -p adm-desktop`：通过。
- `cargo fmt --check`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 34 个 Rust 单元测试通过。
- `cargo run -p adm-desktop -- --smoke`：通过。
- smoke 输出确认 Slint 组件能调用应用服务创建项目、运行核心流水线并列出项目。
- 已生成 `RUST/target/debug/adm-desktop.exe`。

## 后续事项

- 下一步可把 Slint UI 从内联宏拆到 `.slint` 文件，便于维护复杂界面。
- POC 还没有文件选择器、导入/导出按钮、真实项目详情页和 pipeline 进度视图。
- 后续需要验证真实可见窗口交互，当前验证通过的是 smoke 路径和编译链路。
- 若继续 GUI 主线，应把 `adm-ui-model` 真正作为 Slint 绑定层，而不是让 UI 直接格式化字符串。
