# Session 2026-07-05-061

## Summary
Added SDK bundle staging across packaging, CLI, and the Slint desktop workbench.

## Completed
- [x] Added `SdkBundle`.
- [x] Added `stage_sdk_bundle(...)` in `adm-packaging`.
- [x] SDK bundle staging requires `sdk/index.adm`.
- [x] SDK bundle staging optionally includes `package/build_targets.adm` and `package/engine_build_history.adm` when present.
- [x] Added `sdk-bundle-manifest.adm` rendering with staged file hashes and aggregate bundle hash.
- [x] Added packaging tests for successful SDK bundle staging and missing SDK index rejection.
- [x] Added CLI command `stage-sdk-bundle <archive_id> <target_dir> [data_root]`.
- [x] Added Slint `sdk-bundle-target-dir`, `sdk-bundle-text`, and `stage-sdk-bundle` callback.
- [x] Added desktop SDK Bundle input row and status panel.
- [x] Added desktop helper `stage_sdk_bundle_for_ui(...)`.
- [x] Desktop smoke now stages an SDK bundle and verifies manifest, `sdk/index.adm`, and `package/build_targets.adm`.
- [x] CLI smoke stages an SDK bundle from a fresh temporary archive.
- [x] Rebuilt and re-staged the isolated Rust desktop release exe.

## Verification
- [x] `cargo test -p adm-packaging`: passed, 16 tests.
- [x] `cargo check -p adm-cli`: passed.
- [x] `cargo check -p adm-desktop`: passed.
- [x] `cargo run -p adm-desktop -- --smoke`: passed.
- [x] CLI `stage-sdk-bundle` smoke: passed.
- [x] `cargo fmt --check`: passed.
- [x] `cargo check --workspace`: passed.
- [x] `cargo test --workspace`: passed, 95 Rust unit tests.
- [x] `cargo build -p adm-desktop --release`: passed.
- [x] `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`: passed.
- [x] `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`: passed.
- [x] Staged release hash: `fnv64:56ab4faf05a89f31`.

## Follow-up
- [ ] Add richer Slint grouping for release, SDK bundle, game build bundle, and engine history.
- [ ] Add final delivery checklist/doctor command for Rust release readiness.
- [ ] Continue workbench UI refinement.
