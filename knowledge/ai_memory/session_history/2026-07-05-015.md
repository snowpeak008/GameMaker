# 会话记录：2026-07-05-015

## 摘要

继续 goal 模式推进 Rust 全项目重建，完成失败/中断 pipeline 的恢复入口：应用层可从正式存档读取 `pipeline/run_state.adm` 并按已完成 stage 前缀恢复，桌面 GUI 增加 `Resume` 操作，smoke 覆盖恢复路径。

## 完成内容

- `adm-application`
  - `CorePipelineServices` 新增 `build_with_state(...)`。
  - 恢复时先校验 run state 与 pipeline graph，再按 graph 顺序预构建已完成 stage 的依赖产物。
  - runner 继续使用 `run_serial_with_state(...)` 跳过已完成 stage，只执行剩余 stage。
  - completed `design` stage 预构建时不重复调用 AI；应用层会保留已有 `ai/journal.adm`。
  - `AdmApplication` 新增 `resume_core_pipeline(...)`：
    - 打开正式存档工作区并持有锁。
    - 读取并解析 `pipeline/run_state.adm`。
    - 读取已有 AI journal，避免恢复时丢失历史 AI 记录。
    - 恢复执行后重新写入设计/开发/资源/SDK/打包/验证/pipeline/AI 文件并提交。
  - 提取 `commit_core_pipeline_outputs(...)` 复用运行和恢复的写入提交逻辑。
- `adm-desktop`
  - Slint UI 增加 `resume-project` callback 和 `Resume` 按钮。
  - 桌面层新增 `resume_project(...)`，对选中 archive 调用应用层恢复 API。
  - smoke 现在创建并运行项目后，再走一次 resume，再导出 `.admproj`。
- 测试
  - 新增 `core_pipeline_resumes_after_completed_stage_prefix`。
  - 新增 `application_resumes_failed_core_pipeline_from_saved_state`。

## 验证

- `cargo fmt --check`：通过。
- `cargo check --workspace`：通过。
- `cargo test -p adm-application`：通过，7 个应用测试。
- `cargo test --workspace`：通过，当前 53 个 Rust 单元测试通过。
- `cargo run -p adm-cli -- ai-doctor`：通过。
- `cargo run -p adm-desktop -- --smoke`：通过，输出 `Resumed ...` 并完成 `.admproj` 导出。
- `git diff --check -- RUST knowledge\ai_memory`：通过；仅 Git 行尾提示。

## 后续事项

- 给 GUI 增加 stage progress 可点击视图和失败原因展示。
- 将恢复入口扩展为可选择从失败 stage 或指定 stage 重新运行。
- 继续完善真实 AI provider 接入前的 provider task persistence、配置 UI 和错误展示。
- 仍需做可见窗口人工验证和完整 release package 端到端验收。
