# 会话记录：2026-07-05-013

## 摘要

继续 goal 模式推进 Rust 全项目重建，完成 `.admproj` 导出包从 `ADM_PACKAGE_V2` 到 `ADM_PACKAGE_V3` 的包级完整性升级，并保留旧 V2 包导入兼容。

## 完成内容

- `adm-archive`
  - `export_archive_package(...)` 现在导出 `ADM_PACKAGE_V3`。
  - 新增包头字段：`format_version=3`、`payload_hash=fnv64:*`、`file_count=*`。
  - `payload_hash` 绑定 manifest、文件列表、逐文件 hash 和 hex bytes 载荷。
  - 导入 V3 时先校验整体 payload hash，再解析 manifest/files。
  - 导入 V3 时校验 `file_count` 与实际解析文件数量一致。
  - 保留 `ADM_PACKAGE_V2` 解析路径，旧包仍可导入。
  - `collect_files(...)` 增加排序，导出 payload 更稳定。
- 测试
  - 更新导出测试断言 V3 包头、payload hash 和 file count。
  - 新增 V3 payload 篡改拒绝测试。
  - 新增 legacy V2 包导入兼容测试。

## 验证

- `cargo fmt --check`：通过。
- `cargo check --workspace`：通过。
- `cargo test -p adm-archive`：通过，10 个 `adm-archive` 测试。
- `cargo test --workspace`：通过，当前 50 个 Rust 单元测试通过。
- `cargo run -p adm-cli -- ai-doctor`：通过。
- `cargo run -p adm-desktop -- --smoke`：通过，GUI smoke 创建项目、运行流水线、导出 `.admproj` 并显示状态。
- `git diff --check -- RUST`：通过。

## 后续事项

- 继续把核心业务拆成真正的 stage executor 输出，而不仅是 registry 驱动的执行报告。
- 给 GUI 增加失败后 resume 操作和 stage progress 可点击视图。
- 继续完善真实 AI provider 接入前的 provider task persistence、配置 UI 和错误展示。
- 仍需做可见窗口人工验证，以及更完整的 release package 端到端验收。
