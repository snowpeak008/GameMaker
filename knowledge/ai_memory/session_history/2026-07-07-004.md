# 2026-07-07-004

## 摘要

继续执行 Rust UI/data rebuild：完成设计工作台导出/模板/正式存档，接入 AI 访谈 provider 写回，补充开发分析，并将开发流水线 UI 升级为 Step00-14 信息架构与当前 Rust 核心阶段映射。

## 完成内容

- 设计工作台导出支持 `markdown/json/text/prompt`，由真实 `WorkbenchState` 渲染并写入文件。
- 项目模板支持查看、载入内置模板、另存自定义模板。
- AI 访谈支持生成问题、记录用户回答、调用 `AiProvider`，验证输出后写回当前节点设计说明，并保留 replay/provenance 记录。
- 正式存档支持保存/载入 `design/workbench_state.json` 与 `design/project.adm`。
- 开发流水线 UI 阶段模型从 5 个核心阶段展示升级为 Step00-14，并明确映射到当前 Rust 核心阶段。
- Step 重跑路径支持 StepXX 到核心阶段的映射。
- 补充开发新增 Rust 原生需求分析服务，生成结构化补充任务并写入 `patch/supplement_requests.adm`。
- 重新 release build 并 stage 到 `RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`。

## 验证

- `cargo fmt`：通过。
- `cargo test -p adm-application`：通过，36 个测试。
- `cargo test -p adm-desktop`：通过，0 个测试。
- `cargo check -p adm-desktop`：通过。
- `cargo run -p adm-desktop -- --smoke`：通过。
- `cargo build -p adm-desktop --release`：通过。
- `cargo run -q -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`：通过。
- `RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe --smoke`：通过。
- 新 staged exe：`fnv64:f73114d5ddd642f1`，大小 `23059456` bytes，时间 `2026/7/7 1:09:46`。

## 当前状态

- 双击入口已更新：`RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`。
- 真实 Unity PlayMode 验收仍按用户要求不执行。
- 未修改外部 Unity 项目文件。

## 仍未完成

- Step00-14 还只是 UI/状态映射到 5 个 Rust 核心阶段，尚未实现每个 Step 独立 Rust 执行器 parity。
- 运行日志页面仍需 level 过滤、清空、导出 JSONL 等严格日志操作。
- SDK 知识库仍需新增/批准/拒绝审核队列持久化。
- 视觉 polish 和截图验收仍需继续。
