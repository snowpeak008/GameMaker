# 2026-07-16-002 — NEWrust v2 second-development checklist pass

## Summary

Completed the second-development pass from `plan/newdesignplan/remaining_work_checklist.md` at source level. The product executor now routes `game_spec_v2` projects into v2 Step07-14 paths, core P1/P2 bugs are fixed, and the remaining checklist distinguishes source-complete work from real external CLI/Unity/EXE/manual-signature release gates.

## Completed

- Routed product Step07-14 through v2 implementations when `game_spec_v2` is enabled, while preserving the legacy path when disabled.
- Added product-level tests for v2 Step07, Step08-10, and Step11 workspace execution.
- Fixed long project-name `SpecId` panic, Windows case-fold path bypass, RequiresAny cycle detection, Step07 transparent-margin clamping, A09 fail-open checks, A09 no-AI/bounded-repeat hardcoding, A10 sidecar half-publish behavior, and R2 readiness double-write.
- Added persistent VLM review cache support and regression tests.
- Implemented Step12 `sample(n)` behavior and made Step13 manual/performance gating explicit field-driven through `manualReviewRequired` and `performanceBudgetRefs`.
- Removed synthetic A09 `user_playtest_signed=true`; A09 now records `manualSignatureRequired` while A10 still requires external release signing.
- Updated `plan/newdesignplan/remaining_work_checklist.md` with completed/partial status and verification evidence.

## Verification

- `cargo fmt --all -- --check`: passed.
- `cargo check --workspace --locked`: passed.
- `cargo test -p adm-new-pipeline --locked`: passed.
- `cargo test --workspace --locked`: passed.

## Remaining Boundaries

- Real Codex/Claude CLI bridge for Step11 remains an application-layer integration item to avoid a pipeline-to-application crate cycle.
- Real VLM invocation, Unity/headless asset loading, true scenario execution, `tools/verify-standalone.ps1`, EXE smoke, and user playtest signing remain release/acceptance gates.
- No commit, tag, push, or repository synchronization was performed.
