# 会话记录：2026-07-05-042

## 摘要

继续 goal 模式推进 Rust 全项目重建，针对 Slint desktop 纵向内容过长的问题做低风险布局收敛。当前没有大规模重排已有控件，而是将工作台内容放入 `ScrollView`，把窗口高度从 1140px 降到 900px，同时保留全部现有 UI 属性、回调和 smoke 覆盖。

## 完成内容

- `adm-desktop`
  - Slint 引入 `ScrollView`。
  - `MainWindow.height` 从 `1140px` 降到 `900px`。
  - 将原先整页 `VerticalBox` 包入 `ScrollView`。
  - 设置 `viewport-width` 和 `viewport-height`，让长工作台内容可滚动。
  - 保留现有创建/运行、存档、AI、发布、Core Artifacts、SDK、Package、Validation、Projects 等控件和绑定。
- 发布产物
  - 重新构建 release。
  - 重新 staging 到 `RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`。
  - 新 staged hash：`fnv64:8d4e1685bfb02ce7`。

## 验证

- `cargo check -p adm-desktop`：通过。
- `cargo fmt --check`：通过。
- `cargo run -p adm-desktop -- --smoke`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 75 个 Rust 单元测试通过。
- `cargo build -p adm-desktop --release`：通过。
- `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`：通过。
- `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`：通过。

## 后续事项

- 后续仍需做真正的工作台分区/页签，不应长期依赖单页滚动。
- 增加可视窗口手工验收：两个 Rust 窗口选择同一 archive 时，第二个窗口必须显示锁冲突和 owner 信息。
- 继续补齐真实业务深度：完整设计链、开发链、资产链、SDK 验证和游戏打包。
