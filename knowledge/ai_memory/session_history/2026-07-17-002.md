# Session 2026-07-17-002

## Summary

Completed W1.4 at source level for GameSpec v2 Step11: product runs now require a real `WorkspaceTaskAgent`, the real Codex/Claude bridge carries compile and trusted smoke/test command contracts into Unity batchmode verification, and the local filesystem fallback no longer pretends compile-smoke passed.

## Completed

- [x] Added product-mode enforcement so UI/product v2 Step11 fails closed when no configured `WorkspaceTaskAgent` is available; the local filesystem fallback is disabled for product runs.
- [x] Kept the filesystem fallback only for tests/offline contract materialization and changed its `compile_smoke.json` evidence to `status: not_executed` with `verificationRequired: true`.
- [x] Extended `UnityBatchmodeProgramVerifier` to read v2 `WorkspaceChangeSet` command permissions from the work-unit payload and run compile plus trusted test/smoke validation plans.
- [x] Preserved legacy development work-unit behavior as compile-only when no v2 workspace contract is present.
- [x] Added regression coverage for v2 compile+trusted smoke/test plan extraction, legacy compile-only compatibility, product-mode fallback rejection, and explicit fallback non-execution evidence.
- [x] Updated `plan/newdesignplan/remaining_work_checklist.md` so W1.4 is closed only for Step11 post-merge compile and trusted smoke/test, while W4.7/W5 full EXE playable smoke and manual playtest signing remain open.

## Verification

- [x] `cargo fmt --all`
- [x] `cargo test -p adm-new-application workspace_task_bridge_declares_compile_and_trusted_smoke_unity_plans --locked`
- [x] `cargo test -p adm-new-application legacy_development_work_units_keep_implicit_compile_only_unity_plan --locked`
- [x] `cargo test -p adm-new-pipeline --test product_executor game_spec_v2_product_step11_requires_real_agent_when_product_mode_demands_it --locked`
- [x] `cargo test -p adm-new-pipeline filesystem_workspace_agent_renames_from_target_snapshot --locked`
- [x] `cargo test -p adm-new-application --locked`
- [x] `cargo test -p adm-new-pipeline --test product_executor --locked`
- [x] `cargo check -p desktop-tauri --locked`
- [x] `cargo check --workspace --locked`
- [x] `cargo test --workspace --locked`

## Remaining

- [ ] W1.5 remains open: legacy callers still need an explicit migration or retention decision after the v2 Step11 execution bridge.
- [ ] W4 release-gate work remains: true VLM judging, real headless/engine asset loading, real scene execution, standalone verification, EXE smoke, and manual playtest signing.
- [ ] No commit, tag, push, or repository synchronization was performed.
