# 2026-06-23-012 — bug收集文档6.md 第六轮修复

## 摘要

修复 `bug收集文档6.md` 中发现的 BUG-023：第五轮 BUG-018 修复后，无效 `pipeline_adapter` 名称会让 Step02 L5 实体补全配置错误向上传播，导致 Step02 整体崩溃。

## 完成内容

- BUG-023：`EntitySupplementAdapter._call_ai()` 仍保留 `ValueError/ImportError` 配置错误直接抛出的行为，避免底层调用静默吞错。
- `EntitySupplementAdapter.supplement()` 在业务入口捕获 adapter 配置错误，记录错误文本，并降级到本地 genre fallback 实体，不再中断 Step02。
- `SupplementResult` 增加 `error` 字段，`EntityValidator` 将错误透传到 `entity_coverage_report.json` 的 `ai_supplement.error`，便于用户看到 `unknown adapter` 等配置问题。
- 回归测试调整为分层验证：底层 `_call_ai()` 继续抛配置错误，`supplement()` 与 `_stage2_outputs()` 遇到无效 adapter 名称时正常 fallback 输出。

## 验证

- `python -m pytest core\tests\unit\test_l5_supplement.py -q`：29 passed（仅 `.pytest_cache` 写入权限 warning）。
- `python -m pytest core\tests -q`：76 passed（仅 `.pytest_cache` 写入权限 warning）。
- `python -m compileall core pipeline tools\validators\pipeline_quality.py`：通过。
- `python -m black --check pipeline\step_02_design_review_freeze\supplement.py pipeline\step_02_design_review_freeze\supplement_contracts.py pipeline\step_02_design_review_freeze\helpers.py core\tests\unit\test_l5_supplement.py`：通过。
- `python -m flake8 --max-line-length=100 --extend-ignore=E203 pipeline\step_02_design_review_freeze\supplement.py pipeline\step_02_design_review_freeze\supplement_contracts.py pipeline\step_02_design_review_freeze\helpers.py core\tests\unit\test_l5_supplement.py`：通过。
- `python -m mypy --ignore-missing-imports --follow-imports=skip pipeline\step_02_design_review_freeze\supplement.py pipeline\step_02_design_review_freeze\supplement_contracts.py pipeline\step_02_design_review_freeze\helpers.py`：通过。
- `git diff --check`：通过，仅有 LF/CRLF 提示。

## 后续关注

- 无效 adapter 现在不会击穿 Step02，但会在 `ai_supplement.error` 中暴露；真实运行时仍应在 GUI 项目配置中选择 `none`、`codex`、`claude_code` 或 `openai` 等合法值。
- 当前工作树仍包含上一轮 L5 补全开发未提交内容和用户侧旧 bug 文档删除状态；本轮没有回退这些外部状态。
