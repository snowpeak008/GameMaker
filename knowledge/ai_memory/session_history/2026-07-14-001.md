# 2026-07-14-001

## 摘要

将 NEWrust 源码根启动入口从 CMD 替换为与 Python 项目同职责的原生 `AutoDesignMaker.exe`，并完成独立性、真实 GUI 和默认空白项目验证。

## 完成内容

- [x] 分析 Python 根 `AutoDesignMaker.exe` 与 `gui_app.py`：入口位于项目根并通过根定位启动 GUI，产品数据仍属于本项目。
- [x] 新增 workspace package `apps/root-launcher`，Windows release 使用 GUI 子系统并复用桌面产品图标。
- [x] 根启动器只从自身目录解析 `dist/AutoDesignMaker-NEWrust`，校验关键发布清单和资源，不搜索父目录或读取 Python 项目。
- [x] 根启动器直接启动 Tauri 产品 EXE，固定 Rust 自有 `user_data`，默认 `blank`/`zh-CN`，保留显式 `restore`/`en-US`。
- [x] 新增 WebView2 检测、错误诊断文件、参数转发和 6 个路径/环境/缺失资源测试。
- [x] 新增 `tools/build-root-launcher.ps1`，执行 locked release、x64/static CRT 检查、候选自检和同卷原子安装。
- [x] 删除 `NEWrust/Start-AutoDesignMaker.cmd`；便携包内部 CMD 仅保留为既有发布清单兼容文件，根入口不依赖它。
- [x] 同步 README、总计划和原子计划 `005`、`006`、`008`。

## 验证

- [x] 根 `AutoDesignMaker.exe`：398,336 字节，SHA-256 `79bf065bc863cb7d010f85702582972bec6962f1555d5cc1a9bd3c4f8182955a`。
- [x] `--check-launcher` 退出码 0；真实子进程 PID 21204，路径严格为 `NEWrust/dist/AutoDesignMaker-NEWrust/AutoDesignMaker.exe`，窗口句柄有效。
- [x] 新草稿 103/103 节点为 `not_started`，设计备注、玩法系统、AI 消息均为 0；流水线为 `idle` 且阶段数为 0。
- [x] `cargo fmt --all -- --check`、`cargo check --workspace --locked`、`cargo test --workspace --locked` 通过。
- [x] `cargo clippy -p adm-new-root-launcher --all-targets --locked -- -D warnings` 通过。
- [x] `standalone-boundary-gate`：`status=passed`、218 个扫描文件、`forbidden_hit_count=0`。

## 当前状态

- [x] 根原生 EXE 已安装并可直接双击。
- [x] 验证窗口随后干净退出，运行日志记录空白项目初始化和 `desktop runtime stopped cleanly`；当前无遗留产品进程。
