# 2026-07-13-001

## 摘要

完成 NEWrust 独立性、默认空项目启动和 Python/Rust 设计资源语义同步开发，并将总计划与 7 个原子计划按最终实现重新同步。

## 完成内容

- [x] 默认产品启动改为新的独立未绑定草稿，项目与流水线均为空；只有 `ADM_NEWRUST_STARTUP_PROJECT=restore` 才执行旧恢复逻辑。
- [x] 后端 shell、Tauri 默认配置、Web fallback 和便携启动器统一为不自动恢复当前存档。
- [x] 保留历史正式存档和旧草稿；覆盖默认空白关闭不覆写正式存档、显式加载和显式恢复回归测试。
- [x] 扩展独立性门禁到源码、脚本、前端和关键配置，当前扫描 213 个文件，父级 Python 路径命中为 0。
- [x] 新增 `design-sync-audit`，比较 `knowledge/design_data`、`knowledge/schemas`、`pipeline/artifact_layer`，区分字节、格式和语义差异。
- [x] 复核 144 个原始差异均为 CRLF/LF 或 EOF 换行差异；最终语义差异、Rust 缺失和 Rust-only 文件均为 0。
- [x] debug 源码根覆盖必须通过 Rust v2 完整身份校验；release 不读取覆盖路径，旧归档兼容也只读取 Rust 自有数据根。
- [x] 同步 `plan/soloplan/2026-07-13_newrust_independence_and_design_sync.md` 与 `plan/soloplan/dev/001` 至 `007`。
- [x] 修复根目录启动入口失效：原便携脚本的目录通配探测在 `cmd.exe` 下误报 WebView2 未安装，现改为 `for /d` 枚举版本目录；根入口增加 EXE 校验、参数转发和退出码传递。

## 验证

- [x] `cargo fmt --all -- --check`、`cargo check --workspace --locked`、`cargo test --workspace --locked` 通过。
- [x] `npm test`、`npm run build` 通过。
- [x] `adm-new-cli standalone-boundary-gate`：`status=passed`、`forbidden_hit_count=0`。
- [x] `adm-new-cli design-sync-audit`：`status=passed`、`difference_count=0`、`format_difference_count=144`。
- [x] 便携开发快照构建、stage smoke 和事务 finalizer 通过；382 个本地用户数据文件保留。
- [x] `Start-AutoDesignMaker.cmd --check-webview2` 返回 0；根入口真实启动得到有效 GUI 窗口，最终运行草稿为 103 个节点全部 `not_started` 的空项目。

## 后续关注

- [ ] 全 workspace Clippy 的 `-D warnings` 基线仍受既有 `trim_split_whitespace`、`derivable_impls`、`needless_borrow` 告警阻断，本轮未扩大为无关重构。
- [ ] 若未来允许在 UI 中恢复旧草稿，应提供明确的草稿选择器；产品默认启动仍保持空白。
