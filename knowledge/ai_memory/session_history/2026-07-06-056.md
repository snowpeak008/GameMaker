# 2026-07-06-056 — Rust Slint 工作台交互复刻第一轮

## Summary

记录并实施 Rust 桌面 UI 方向修正：继续使用 Slint，但不复刻旧 Python UI 的可拖拽分区边界；将当前不可接受的单页长表单替换为六大任务区工作台壳，并接入 Rust 原生设计知识库读取。

## Completed

- [x] 重新读取 `AI_README.md`、`knowledge/ai_memory/INDEX.md` 和当前上下文。
- [x] 更新 `CONTEXT.md`：明确工作台交互契约、结构化工作台状态、Slint 继续使用、旧版 split-pane 拖拽边界不复刻。
- [x] 新增 `knowledge/decisions/ADR-2026-07-06-rust-workbench-interaction-parity.md`：Rust 必须原生复刻旧工作台六大任务区和 Step00-14 用户可见模型，Python 只作为行为参考。
- [x] 重写 `RUST/apps/adm-desktop/ui/main.slint`：顶部六入口、设计工作台三栏、开发流水线 Step00-14 左侧导航、补充开发、打包阶段、运行日志、SDK 知识库。
- [x] 保留现有 Slint property/callback 名称，避免破坏已有 Rust 桥接与 smoke 路径。
- [x] 新增 `RUST/crates/adm-design/src/workbench_reference.rs`，用 Rust 原生读取 `knowledge/design_data/domain_order.json`、`domains/*.json` 和 `gameplay_system_options.json`。
- [x] 桌面端启动时自动定位并加载 `knowledge/design_data`，填充领域总览、节点摘要、L4 选项组摘要、L5 候选、项目画像和右侧摘要/缺失项/风险/校验页签文本。
- [x] 清理本轮 Cargo 构建/测试产物：`cargo clean` 后 `RUST/target` 不存在。

## Verification

- [x] `cargo fmt`：通过。
- [x] `cargo check -p adm-design`：通过。
- [x] `cargo check -p adm-desktop`：通过。
- [x] `cargo test -p adm-desktop`：通过，该 crate 当前 0 个单元测试。
- [x] `cargo run -p adm-desktop -- --smoke`：通过，现有 AI/打包/导出/阶段模型 smoke 输出保持可用。

## Current Limitations

- [ ] UI 已从单页表单改为六任务区壳，但尚未完成完整像素级细节复刻。
- [ ] 设计工作台当前读取旧知识库结构并展示摘要；真实用户勾选、主选项、L5 JSON 编辑、模板存取、AI 访谈轮次仍需接入结构化 `WorkbenchState`。
- [ ] 开发流水线 UI 已固定显示 Step00-14；底层 Rust runner 仍是简化核心阶段模型，后续需要补齐真实 Step00-14 执行模型。
- [ ] 本轮没有运行真实 Unity，也没有修改任何外部 Unity 项目。

