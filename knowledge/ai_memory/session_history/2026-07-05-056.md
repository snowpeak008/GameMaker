# Session 2026-07-05-056

## Summary
Added a guarded real local Unity build CLI entry with explicit confirmation token policy.

## Completed
- [x] Added `LOCAL_ENGINE_BUILD_CONFIRMATION_TOKEN`.
- [x] Added `validate_local_engine_build_confirmation(...)`.
- [x] Added tests for confirmation token acceptance and rejection.
- [x] Added CLI command `run-unity-build <archive_id> <target_id> <unity_exe> <unity_project_dir> ADM_CONFIRM_LOCAL_ENGINE_BUILD [data_root]`.
- [x] `run-unity-build` validates the confirmation token before loading archives or launching any local process.
- [x] `run-unity-build` uses `LocalProcessEngineBuildRunner` and renders an `EngineBuildExecutionReport`.
- [x] Verified the missing-confirmation path fails with the expected token message and does not launch Unity.
- [x] Rebuilt and re-staged the isolated Rust desktop release exe.

## Verification
- [x] `cargo test -p adm-packaging`: passed, 14 tests.
- [x] `cargo check -p adm-cli`: passed.
- [x] CLI `run-unity-build ...` without confirmation token: failed as expected before launch.
- [x] `cargo fmt --check`: passed.
- [x] `cargo check --workspace`: passed.
- [x] `cargo test --workspace`: passed, 92 Rust unit tests.
- [x] `cargo build -p adm-desktop --release`: passed.
- [x] `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`: passed.
- [x] `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`: passed.
- [x] Staged release hash: `fnv64:6899808e4048c4f5`.

## Follow-up
- [ ] Add visible-window validation for two Rust windows selecting the same archive with owner details visible.
- [ ] Add structured engine execution history persistence after real/dry-run builds.
- [ ] Continue replacing POC text-heavy controls with richer workbench sections.
