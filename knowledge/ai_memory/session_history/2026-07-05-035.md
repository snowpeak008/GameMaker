# 会话记录：2026-07-05-035

## 摘要

继续 goal 模式推进 Rust 全项目重建，补强 Slint desktop 的项目状态一致性：运行、恢复、重跑、导入后会自动同步当前选中存档；刷新项目时会处理原选中存档不存在的情况，避免 UI 保留过期 archive id 或旧详情。

## 完成内容

- `adm-desktop`
  - `DesktopRunSummary` 新增 `archive_id`。
  - `apply_run_summary(...)` 会同步 `selected-archive-id`。
  - `Create + Run` 成功后先选中新建 archive，再刷新项目列表并渲染运行摘要。
  - `Resume`、`Resume Failed`、`Rerun` 成功后保持选中当前 archive。
  - 新增 `ImportProjectResult`，导入动作返回 message 和 archive id，不再让 UI 从提示文本推断。
  - `Import` 成功后自动选中导入 archive、刷新项目列表并保留导入状态提示。
  - `refresh_projects(...)` 会检查当前选中 archive 是否仍存在：
    - 存在则刷新对应详情。
    - 不存在则回退到第一个项目。
    - 没有项目则清空详情、阶段、包和校验模型。
  - desktop smoke 新增“导出后导入到新 data root，并自动选中导入详情”的覆盖。
- 发布产物
  - 重新 `cargo build -p adm-desktop --release`。
  - 重新 staging 到 `RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`。

## 验证

- `cargo run -p adm-desktop -- --smoke`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 75 个 Rust 单元测试通过。
- `cargo build -p adm-desktop --release`：通过。
- `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`：通过。
- `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`：通过。
- `cargo fmt --check`：通过。

## 后续事项

- 继续补齐桌面 GUI 的真实运行、校验、打包闭环。
- 增加发布目录的人工可视化启动验收，确认窗口布局、按钮行为和存档锁交互。
- 继续把用户可能误操作的空路径、旧 archive id、锁冲突路径做成明确 UI 状态。
