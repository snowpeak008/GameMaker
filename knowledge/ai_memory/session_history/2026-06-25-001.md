# 2026-06-25-001 — 集中缓存目录与 Step05 绑定质量优化

## 摘要

执行 `plan/cache_centralization` 与 `plan/step05_optimization`：统一 Python / pytest / mypy 缓存到项目 `.cache/`，补齐 Step00 重跑时历史 sibling draft 输出清理；同时增强 Step02 冻结契约、Step03 程序需求绑定和 Step02 L5 补全触发诊断，降低 Step05 因 traceability 失败被阻断的概率。临时开发计划目录仍只作为本地输入，不提交到 git。

## 完成内容

- `.gitignore` 增加 `.cache/`、`.mypy_cache/` 迁移忽略规则，pytest cache 改为 `.cache/pytest`，mypy cache 改为 `.cache/mypy`。
- 新增 `sitecustomize.py`，并在 `core/__init__.py`、根 `gui_app.py`、`core/ui/gui_app.py`、`conftest.py` 中设置 `PYTHONPYCACHEPREFIX` / `sys.pycache_prefix` 到 `.cache/pycache`。
- `core/save/manager.py` 新增 `prune_sibling_draft_outputs()`，在 Step00 全量重跑时清理同一 active save 关联的 sibling draft `outputs/artifacts`，保留 draft 源包与元数据。
- `core/main.py` 和 `core/ui/workbench.py` 接入 sibling draft 输出清理，避免旧 draft artifacts 干扰新一轮流水线验收。
- `core/engines/generation.py` 的 Stage02 `design_freeze_contract.json` 增加 `frozen_at`、`entities`、`systems`、`entity_stats`。
- 新增 `pipeline/step_03_program_requirements/binding.py`，Stage03 自动将未绑定需求按依赖、实体、来源、语义和默认系统顺序绑定，并输出 `binding_stats`。
- `pipeline/step_02_design_review_freeze/helpers.py` 增加补全触发原因、未映射节点优先级和 `expected_kind`，`supplement.py` 暴露 `EntitySupplementAdapter.should_supplement()`。
- 新增/扩展测试覆盖缓存配置、pycache 前缀、sibling draft 清理、freeze contract systems/entities、Stage03 自动绑定和 Step02 supplement 触发诊断。

## 验证

- `python -m pytest core\tests\unit\test_step05_optimization.py core\tests\unit\test_pipeline_optimization_helpers.py core\tests\unit\test_l5_supplement.py -q`：64 passed。
- `python -m pytest -q`：105 passed。
- `python -m py_compile core\engines\generation.py pipeline\step_02_design_review_freeze\helpers.py pipeline\step_02_design_review_freeze\supplement.py pipeline\step_03_program_requirements\binding.py core\tests\unit\test_step05_optimization.py`：通过。
- `python -m flake8 --max-line-length=100 --extend-ignore=E203,E501 ...`：本轮触碰文件通过。
- `python -m black --check ...`：本轮触碰文件通过。
- `python -m compileall core pipeline tools\validators\pipeline_quality.py tools\asset_production`：通过。
- `git diff --check`：通过，仅 CRLF 工作区提示。

## 后续关注

- 直接对 `core/engines/generation.py` 运行全量 mypy 仍会暴露既有历史弱类型问题；本轮新增 `binding.py` 与测试已通过目标 mypy。
- 后续运行 Python 工具时优先让 `PYTHONPYCACHEPREFIX` 指向 `.cache/pycache`，避免重新产生分散 `__pycache__`。
- 提交前必须继续确认 `plan/` 临时开发目录、bug 文档、`settings/api_config.toml` 不进入暂存区。
- CC-Panes 共享记忆池未写入：`CC_PANES_PROJECT_PATH` / `CC_PANES_WORKSPACE_NAME` / `CC_PANES_CLI_TOOL` 均为空，按规则跳过以避免污染共享池。
