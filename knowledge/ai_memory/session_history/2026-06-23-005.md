# 会话记录：2026-06-23-005

## 摘要

读取 `bug优化文档.md` 第二轮问题，判断 3 项均适合直接修复，并完成代码修复、回归测试和自检。

## 修复内容

- BUG-007：`pipeline/step_02_design_review_freeze/helpers.py::_expected_node_count`
  - 最终 fallback 不再返回已覆盖节点数。
  - 没有可信总节点数且不是 synthetic fallback 场景时，返回 0，让覆盖率报告为未知/0，而不是假 1.0。
- BUG-008：`pipeline/step_01_gameplay_framework/helpers.py`
  - `LoopExtractor.extract()` 和 `SystemDeducer.deduce()` 改为只调用一次 `_load_templates()`。
- BUG-009：`pipeline/step_05_program_review/helpers.py::IntelligentReviewer._report`
  - `requires_action_count` 改为 `blocker_count + critical_count`。

## 测试补充

- 增加无 expected total 的真实 L5 entity 场景，确保不再假报 full coverage。
- 增加 BLOCKER 被计入 `requires_action_count` 的测试。
- 保留 D4 缺 `coreLoops`、Step01 上限、Step02 环路、Step03 fuzzy、Step05 severity 的第一轮回归测试。

## 最新验证

- `python -m core.main --stage D4`：success。
- `python -m core.main --from-step 0 --stop-step 6 --auto-approve --skip-preflight`：Step 00-06 全部 success。
- `python -m pytest core\tests -q`：34 passed；仅 `.pytest_cache` 写入权限 warning。
- `python -m compileall core pipeline tools\validators\pipeline_quality.py`：通过。
- `python tools\validators\pipeline_quality.py`：
  - question coverage：0.4
  - core loop output：1.0
  - system definition：1.0
  - entity coverage：0.4563
  - entity count：47
  - requirement binding：0.9745
  - placeholder rate：0.0
  - asset count：50
  - Step05 warning/blocking：4/0
- `git diff --check`：通过，仅 Windows line-ending 提示。
