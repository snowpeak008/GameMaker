# 2026-06-26-001 — 执行 universal_genre_coverage

## 摘要

按 `plan/universal_genre_coverage/PLAN.md` 完成通用品类覆盖开发。计划目录仅作为本地任务输入，不提交到 git。CC-Panes 共享记忆池因环境变量为空未写入，本轮记忆写入项目本地 `knowledge/ai_memory`。

## 阶段记录

### TASK-1：Step00 通用品类推断覆盖

- `pipeline/step_00_idea_intake/helpers.py` 扩展 `_genre_key()`，改为按有序 `GENRE_DETECTION_RULES` 匹配，避免宽泛 `shooter` / `puzzle` / `arena` 抢先命中更具体品类。
- `GENRE_DEFAULT_EVIDENCE` 覆盖计划列出的 17 个市场品类：`farming_sim`、`card_game`、`bullet_heaven`、`hypercasual`、`idle`、`match3`、`souls_like`、`action_adventure`、`survival_horror`、`looter_shooter`、`battle_royale`、`hero_shooter`、`mmorpg`、`factory_sim`、`exploration`、`metroidvania`、`brawler`。
- 新增回归测试确认每个新品类都能为 CQ-011 提供对应 `genre_template:*` 证据，稀疏输入 coverage 保持在目标线以上。

**验证**：
- `python -B -m pytest core\tests\unit\test_pipeline_optimization_helpers.py::test_step00_uses_genre_inference_for_broad_market_genres -q`：通过。

### TASK-2：Step02 governance / liveops 节点过滤

- `pipeline/step_02_design_review_freeze/helpers.py` 将固定治理前缀过滤改为项目元数据感知过滤。
- `documentation_*`、`help_support_*` 始终排除；`liveops_*`、`data_*`、`retention_*`、`launch_*`、`release_*`、`compliance_*` 仅在非长线运营项目中排除。
- `_project_classification_text()` 只读取明确的 profile / project metadata / 商业模式 / 运营模式 selections，避免 raw text 中的节点 ID 反向污染项目分类。
- `_is_liveops_project()` 默认未知项目保留 liveops-only 分母，避免误删服务型项目质量要求；明确 `buyout` / `offline_single_release` / `离线` / `买断` 时才过滤 liveops-only 节点。
- 新增回归测试覆盖买断离线项目过滤 liveops-only 节点、服务型项目保留 liveops-only 节点、文档节点始终排除。

**验证**：
- `python -B -m pytest core\tests\unit\test_pipeline_optimization_helpers.py::test_step02_entity_coverage_excludes_governance_nodes_from_denominator core\tests\unit\test_pipeline_optimization_helpers.py::test_step02_liveops_projects_keep_liveops_only_nodes_in_denominator -q`：通过。

### 同步提交上一轮未提交修复

- `core/engines/generation.py` 的 documentation 需求过滤同时检查 `requirement` 与 `title`，并按标准化文本识别 `documentation_`。
- `core/ui/save_manager_dialog.py` 保留上一轮存档管理文案和 import 清理，并经 black 格式化。
- `core/tests/unit/test_hades_quality_optimization.py`、`core/tests/unit/test_pipeline_optimization_helpers.py` 保留对应回归覆盖。

## 真实流水线验收

- Hades 当前存档重跑 `python -B -m core.main --from-step 0 --stop-step 8 --skip-preflight --auto-approve`：Step00-08 全部 success。
- Hades 质量报告 `drafts\20260626_080541_34956\outputs\artifacts`：question coverage 1.0，Step02 entity coverage 1.0，entity_count 65，asset_count 85，Stage05 warning 1，blocking 0。
- Stardew Valley 临时验证存档重跑 Step00-08：全部 success。
- Stardew 质量报告 `drafts\20260626_080645_29304\outputs\artifacts`：question coverage 1.0，Step02 entity coverage 1.0，entity_count 65，asset_count 85，Stage05 warning 1，blocking 0。
- Stardew Step00 覆盖报告确认 `genre_template:farming_sim` 证据生效，Step02 覆盖报告确认 60/60 concrete nodes 覆盖。
- 验证后 active save 已恢复为 `save_20260625_230810_e1f5e5`。

## 最终自检

- `python -B -m pytest -q`：114 passed。
- `python -B -m py_compile pipeline\step_00_idea_intake\helpers.py pipeline\step_02_design_review_freeze\helpers.py core\engines\generation.py core\ui\save_manager_dialog.py core\tests\unit\test_pipeline_optimization_helpers.py core\tests\unit\test_hades_quality_optimization.py`：通过。
- `python -m black --check ...`：本轮触碰文件通过。
- `python -m flake8 --max-line-length=100 --extend-ignore=E203,E501 ...`：本轮触碰文件通过。
- `git diff --check`：通过，仅 CRLF 工作区提示。

## 后续关注

- Stage05 warning_count 当前为 1，属于非阻断 warning，仍低于质量门槛；如需归零可单独治理 L4-derived requirement 启发式。
- 提交时继续排除 `plan/universal_genre_coverage/`、其他 `plan/` 临时目录、bug 文档和本地运行配置。
