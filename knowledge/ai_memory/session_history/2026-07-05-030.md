# 会话记录：2026-07-05-030

## 摘要

继续 goal 模式推进 Rust 全项目重建，将 Slint 桌面的 stage detail 从纯文本面板升级为结构化字段和 artifact 列表模型。旧的 `stage-detail-text` 仍保留用于 smoke、日志和兼容输出，但 UI 现在同步持有 label/id/status/message 以及 `stage-artifact-items`。

## 完成内容

- `adm-desktop` Slint UI
  - 新增 `StageArtifactRow` struct。
  - 新增 `stage-detail-label`。
  - 新增 `stage-detail-id`。
  - 新增 `stage-detail-status`。
  - 新增 `stage-detail-message`。
  - 新增 `stage-artifact-items: [StageArtifactRow]`。
  - Stage detail 面板改为显示结构化字段，并重复渲染 artifact rows。
- `adm-desktop` Rust
  - 新增 `StageDetailView`。
  - `DesktopRunSummary` 和 `ProjectInspection` 改为携带 `StageDetailView`。
  - 新增 `stage_detail_from_parts(...)`。
  - `inspect_stage_detail(...)` 改为返回结构化 detail。
  - 新增 `apply_stage_detail(...)`。
  - 新增 `apply_stage_artifacts(...)`。
  - 保留 `StageDetailView::render()` 给旧文本输出和 smoke 使用。
- smoke 覆盖
  - 继续验证 packaging stage detail 文本包含 `package/manifest.adm`。
  - 新增断言 `stage_artifact_items` 行数为 1。
  - 新增断言结构化 `stage_detail_id == packaging`。

## 验证

- `cargo check -p adm-desktop`：通过。
- `cargo run -p adm-desktop -- --smoke`：通过。
- `cargo fmt --check`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 73 个 Rust 单元测试通过。

## 后续事项

- 升级项目列表、AI diagnostics、package、validation 为结构化列表或表格。
- 增加真实 AI 调用的显式手动入口，默认仍保持 dry-run / 不联网。
- 增强失败场景 fixture，让桌面 smoke 也覆盖 `Resume Failed` 的可视入口。
- 继续补齐可交付桌面 GUI 的配置、运行、校验和打包闭环。
