# 2026-07-17-011 — W4.7 Step14 Real Release Evidence Wiring

## Summary

Completed W4.7 at source level. GameSpec v2 Step14 now derives `R1GateEvidence` from real release-tool evidence, explicit pipeline evidence refs, and a separate manual playtest signature file instead of product callers supplying nine unchecked booleans.

## Completed

- Added `R1PipelineGateEvidence` for content completion, AI usage audit, AI-off flow, and anti-overfit evidence refs.
- Added `R1UserPlaytestSignature` for manual R1 playable-build acceptance; it must be `manual_user_playtest`, signed, bound to the standalone evidence id, and carry signer/time/acknowledgement.
- Added `derive_r1_gate_evidence_from_sources(...)`, which reads `gates/standalone-release-evidence.json` from `tools/verify-standalone.ps1/v2` and validates schema, producer, project id, status, source tree cleanliness, freshness, all 21 required release checks, and portable receipt metadata.
- Product Step14 now writes derived `r1_gate_evidence.json` and `r1_gate_evidence_source_report.json` at runtime, then feeds the derived evidence into `run_step14_r1_packaging_gate`.
- Added Step14 regression tests for successful derivation, missing manual playtest signature, and failed `portable_smoke` mapping to an EXE smoke blocker.
- Added a product executor regression proving GameSpec v2 Step14 derives evidence from release source files and no longer requires a prewritten bool-only gate file.
- Updated `plan/newdesignplan/remaining_work_checklist.md` to mark W4.7 source-level wiring complete.

## Verification

- [x] `cargo fmt --all`
- [x] `cargo test -p adm-new-pipeline --test step14_v2 --locked`
- [x] `cargo test -p adm-new-pipeline game_spec_v2_product_step14_derives_gate_evidence_from_release_sources --locked`
- [x] `cargo test -p adm-new-pipeline --test product_executor --locked`
- [x] `cargo test -p adm-new-pipeline --test a09_cross_genre_evaluation --locked`
- [x] `cargo check -p adm-new-pipeline --locked`
- [x] `cargo test -p adm-new-pipeline --locked`
- [x] `cargo check --workspace --locked`
- [x] `cargo test --workspace --locked`
- [x] `cargo run --locked -p adm-new-cli -- --project-root . standalone-boundary-gate`

## Release Hold

- [x] No commit, tag, push, or repository synchronization was performed.
- [x] This does not claim `tools/verify-standalone.ps1` was run for the current dirty tree.
- [x] Actual formal release work remains W5: clean-tree verification, rebuilt package, real EXE smoke, and user playtest signature.

## Next Tasks

- Continue W4.8: expose A05 bounded-completion states in UI/product views.
- Keep W5 release execution separate from source-level evidence wiring.
- Keep repository synchronization blocked until explicit user acceptance.
