# 会话记录：2026-06-23-004

## 摘要

根据根目录 `bug收集文档.md` 修复 pipeline optimization 开发引入的 6 个 bug，并补充回归测试覆盖。

## 修复内容

- BUG-001：`core/design/export_adapter.py::_framework_markdown_from_project_state`
  - 修复 `gameplay.get("coreLoops", )` 默认值缺失导致 `None.get()` 崩溃的问题。
- BUG-002：`pipeline/step_02_design_review_freeze/helpers.py::EntityValidator.validate`
  - 覆盖率改用真实分母：优先 `design_summary.node_count`，其次显式 expected 字段，最后才退回当前可推导节点数。
  - `missing_entities` 现在按缺口填充，PLAN-002 不再无条件返回 1.0。
- BUG-003：`pipeline/step_01_gameplay_framework/helpers.py::SystemDeducer.deduce`
  - 修复 `systems[:max(5, len(systems))]` 无效截断，明确上限为 8。
- BUG-004：`pipeline/step_03_program_requirements/helpers.py::SystemBinder._best_binding`
  - 模糊匹配阈值从 0.18 提升到 0.4。
  - 无依据匹配不再生成 `phase:*` 伪绑定，返回 `unmatched`。
- BUG-005：`pipeline/step_02_design_review_freeze/helpers.py::GraphGenerator._cycles`
  - 修复环路表示尾节点重复，例如 `A -> B -> A` 不再输出 `A, B, A, A`。
- BUG-006：`pipeline/step_05_program_review/helpers.py::IntelligentReviewer._report`
  - 分离 `blocker_count`、`critical_count`、`requires_action_count`。
  - `blocking_issue_count` 只统计 BLOCKER。
  - `core/engines/generation.py` 的 Step05/06 消费侧只把 BLOCKER 作为 blockers，CRITICAL 进入非阻断 warning/action 列表。

## 文档与指标同步

- `tools/validators/pipeline_quality.py` 将 entity coverage 目标改为 `>= 0.38 for PLAN-002 regression`。
- `plan/pipeline_optimization/README.md` 和 `plan/status_snapshot_2026-06-23.md` 更新为真实覆盖率指标。

## 最新验证

- `python -m core.main --stage D4`：success。
- `python -m core.main --from-step 0 --stop-step 6 --auto-approve --skip-preflight`：Step 00-06 全部 success。
- `python tools\validators\pipeline_quality.py`：
  - question coverage：0.4
  - core loop output：1.0
  - system definition：1.0
  - entity coverage：0.4563（47 / 103）
  - requirement binding：0.9745
  - placeholder rate：0.0
  - asset count：50
  - Step05 warning/blocking：4/0
- `python -m pytest core\tests -q`：32 passed；仅 `.pytest_cache` 写入权限 warning。
- `python -m compileall core pipeline tools\validators\pipeline_quality.py`：通过。
- `git diff --check`：通过，仅 Windows line-ending 提示。
