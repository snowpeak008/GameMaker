# Session 2026-07-05-053

## Summary

Added Unity CLI engine build command planning.

## Completed

- Added `EngineBuildCommandPlan` to `adm-packaging`.
- Added `plan_unity_cli_build(...)` for Unity Windows desktop build command generation.
- Added command rendering and command-line rendering without requiring Unity to be installed.
- Added tests for Unity command generation and non-Unity target rejection.
- Added CLI command `plan-unity-build <archive_id> <target_id> <unity_exe> <unity_project_dir> [data_root]`.
- Verified `plan-unity-build` against a real temporary archive.
- Rebuilt and re-staged the isolated Rust desktop release exe.

## Verification

- `cargo test -p adm-packaging`: passed, 10 tests.
- `cargo check -p adm-cli`: passed.
- `cargo run -p adm-cli -- plan-unity-build archive_... windows_desktop_playable ...`: passed.
- `cargo fmt --check`: passed.
- `cargo check --workspace`: passed.
- `cargo test --workspace`: passed, 88 Rust unit tests.
- `cargo build -p adm-desktop --release`: passed.
- `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`: passed.
- `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`: passed.
- Staged release hash: `fnv64:e67156fad22f302f`.

## Follow-up

- Surface Unity build command planning in the desktop workbench.
- Add visible-window validation for two Rust windows selecting the same archive with owner details visible.
- Add real engine build adapters after command planning.
