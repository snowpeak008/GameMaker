# 2026-06-26-005：执行 template_2d_redesign 修复补丁

## 摘要

按 `plan/template_2d_redesign/PLAN_FIXES.md` 修正上一轮模板重建的 4 个歧义：替换模板改为新文件名、删除 Battlerite Royale 服务型槽位、3A 第一个重复 Hollow Knight 改为 Axiom Verge，并用 Hades L5 Complete 的实体节点作为 concrete 参考集。

## 完成内容

- ✅ 18 个替换模板从旧游戏文件名迁移为新游戏文件名，公开模板 `fileName` 与 `template.id` 保持一致
- ✅ 删除 `builtin_large_service_splatoon_3.json`，`large_service` 公开模板收敛为 5 个
- ✅ `builtin_3a_sekiro_shadows_die_twice.json` 迁移为 `builtin_3a_axiom_verge.json`，并重写 Axiom Verge 的模板元数据、设计说明和实体 id
- ✅ `template_index.json` 同步新文件名、Axiom Verge 元数据和服务型模板顺序
- ✅ `core/tests/unit/test_template_l5_expansion.py` 改为从 `builtin_indie_hades_l5_complete.json` 读取 concrete 节点参考集
- ✅ 测试新增旧文件不存在、公开文件名匹配 id、公开分类数量为 `iaa=9 / indie=10 / midcore=3 / 3a=9 / large_service=5` 的约束

## 验证

- ✅ 模板 JSON 全量解析通过
- ✅ 静态自检：公开模板 36 个，旧替换文件均不存在，公开文件名与 id 全部一致
- ✅ `python -B -m pytest core\tests\unit\test_template_l5_expansion.py -q`：3 passed
- ✅ 使用新 `builtin_3a_axiom_verge.json` 导出 D4 源包后运行 Step00-08：全部 success
- ✅ pipeline quality：实体 104、需求绑定率 1.0、占位符率 0、Step05 blocking 0
- ✅ `python -B -m pytest -q`：122 passed
- ✅ `PYTHONPYCACHEPREFIX=.cache\pycache python -B -m compileall ...`：通过
- ✅ `git diff --check`：通过（仅 CRLF 工作区提示）

## 后续关注

- [ ] 本轮只按用户要求同步记忆，代码改动尚未提交；如需入库需单独 git 提交
- [ ] 验证 draft `drafts\codex_template_2d_redesign_fixes_validation` 属于运行时输出，不提交
- [ ] `plan/` 目录仍为本地执行材料，不提交
