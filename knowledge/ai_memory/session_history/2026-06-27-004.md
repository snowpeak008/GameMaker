# Session 2026-06-27-004

## Summary

Completed the follow-up fixes from `plan/ai_config_ui_redesign/BUGS.md`: fixed AI config dialog initialization data loss, preserved migrated/custom labels, removed shared `ImageConfig` aliases in compatibility profiles, and removed `save_ai_config()` input mutation.

## Completed

- Fixed `AIConfigUnifiedDialog._switch_tab()` so the first render no longer writes empty UI variables back into the first dev entry.
- Changed UI type assignment so default labels still follow type changes, while migrated/custom labels are preserved.
- Removed unused UI imports left from the old dialog implementation.
- Changed `compat_profiles_from_entries()` so every compatibility `AIProfile` gets an independent `ImageConfig` copy.
- Removed the implicit `active_profile_id` mutation from `save_ai_config()`.
- Added regression tests for independent image configs, save non-mutation, v2 auto-writeback, and label preservation.

## Verification

- `python -B -m pytest core\tests\unit\test_ai_config.py core\tests\unit\test_config_loader.py core\tests\unit\test_config_validator.py core\tests\integration\test_adapter_configuration.py -q`: 32 passed.
- `python -B -m pytest -q`: 147 passed.
- `PYTHONPYCACHEPREFIX=.cache\pycache python -B -m compileall core pipeline tools\validators\pipeline_quality.py tools\asset_production tools\config`: passed.
- `git diff --check`: passed with only CRLF working-copy warnings.

## Follow-up

- Manually click-test the redesigned AI config dialog in GUI: first-open migration preservation, tab switching, active highlight, Codex file fields, custom JSON validation, save/reopen.
- Continue excluding `plan/`, bug documents, runtime drafts, and ignored local AI config files from commits.
- CC-Panes shared memory was skipped because required environment variables were absent.
