# Session 2026-06-28-020

## Summary

Implemented `newplan/execution_object_save_id_ownership_fix.md`: restored strict execution-object `save_id` validation and added explicit create/save-as ownership transfer with audit records.

## Completed

- Replaced the broad `migrate_save_id()` helper with `transfer_ownership_to_save(new_save_id, source_save_id, reason)`.
- Kept `ExecutionObjectStore.save()` strict: ordinary mismatch between stored `save_id` and expected `save_id` raises `ExecutionObjectError`.
- Added top-level `ownership_migrations` audit records to execution-object stores and updated the schema.
- Added save-manager ownership transfer for active draft execution-object stores before the first sync of a newly created save.
- Wrote active `timeline.jsonl` events for execution-object ownership transfers.
- Added regression coverage for mismatch errors, successful transfer, wrong source rejection, create/save-as active/archive migration, and load-save non-repair behavior.

## Verification

- `python -B -m compileall core\save\manager.py core\engines\execution_objects\workflow.py core\tests\unit\test_draft_archive_paths.py core\tests\unit\test_unattended_recovery.py`: passed.
- `python -B -m pytest core\tests\unit\test_unattended_recovery.py core\tests\unit\test_draft_archive_paths.py -q`: 31 passed.
- `python -B -m pytest -q`: 203 passed.
- `python -B -m compileall -q core pipeline tools\validators\pipeline_quality.py tools\asset_production tools\config tools\design`: passed.
- `git diff --check`: passed with only CRLF working-copy warnings.

## Follow-up

- `newplan/` remains local planning material and should not be committed unless explicitly requested.
