# 2026-07-12-001

## 摘要

抽取当前项目的 AI 记忆机制，生成可复制到其他项目的通用模板包，位置为 `plan/memries/`。

## 完成内容

- [x] 在 `plan/memries/README.md` 写入复制方式和启用流程。
- [x] 在 `plan/memries/COPY_ME/` 生成可直接复制到目标项目根目录的文件树。
- [x] 模板包含 `AGENTS.md`、`AI_README.md`、`knowledge/ai_memory/` 和 `tools/memory/`。
- [x] 将 freshness 维护脚本改成通用版，通过 `memory_config.json` 配置关键文件，不再硬编码 AutoDesignMaker 文件清单。
- [x] 添加 `new_session_note.py`，便于目标项目快速创建下一条会话记录。
- [x] 避免把本项目历史会话、密钥、本地配置和运行产物带入模板。

## 验证

- [x] `python -m compileall tools\memory`：在 `plan/memries/COPY_ME` 内通过。
- [x] `python tools\memory\update_freshness.py`：在 `plan/memries/COPY_ME` 内通过，生成 2 个入口文件哈希。
- [x] `python tools\memory\check_staleness.py`：在 `plan/memries/COPY_ME` 内通过，`stale=[]`、`missing=[]`。
- [x] 清理了 compileall 生成的 `__pycache__`，模板包不包含 Python 缓存。

## 后续关注

- [ ] 复制到已有项目时，若目标项目已有 `AGENTS.md` 或 `AI_README.md`，应合并记忆入口段落，不要直接覆盖。
- [ ] 目标项目第一次接入后，需要编辑 `knowledge/ai_memory/project_understanding/memory_config.json`，加入真实关键文件。

