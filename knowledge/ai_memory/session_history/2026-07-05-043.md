# 会话记录：2026-07-05-043

## 摘要

继续 goal 模式推进 Rust 全项目重建，增强核心流水线的业务 artifact 深度。设计文档现在包含设计支柱、玩法机制和验收风险；开发计划包含 milestone、实现说明和验证步骤；资产计划从 2 个粗任务扩展为视觉、交互反馈、UI、音频四类任务，并记录依赖和验证步骤。

## 完成内容

- `adm-design`
  - `DesignProject` 新增 `design_pillars`、`gameplay_mechanics`、`acceptance_risks`。
  - 新增 `DesignPillar`、`GameplayMechanic`、`DesignRisk`。
  - `GameDesignBrief::to_project()` 自动派生设计支柱、机制和风险。
  - `render_design_document(...)` 新增：
    - `## Design Pillars`
    - `## Gameplay Mechanics`
    - `## Acceptance Risks`
  - 新增测试覆盖 production-ready sections。
- `adm-development`
  - `DevelopmentTask` 新增 `milestone`、`implementation_notes`、`validation_steps`。
  - `DevelopmentPlan::render()` 输出 milestone、notes、validation。
  - 新增测试覆盖实现说明和验证步骤。
- `adm-assets`
  - `AssetTask` 新增 `pipeline_stage`、`dependencies`、`validation_steps`。
  - `AssetPlan::for_genre(...)` 从 2 个任务扩展为 4 个任务：
    - `visual_style`
    - `interaction_feedback`
    - `workbench_ui`
    - `audio_cues`
  - 新增测试覆盖依赖和验证步骤。
- `adm-application`
  - pipeline commit 测试新增断言，确认增强后的 design/development/assets artifact 内容落盘。
- `adm-desktop`
  - desktop smoke 预期从 `asset_tasks=2` 更新为 `asset_tasks=4`。
- 发布产物
  - 重新构建 release。
  - 重新 staging 到 `RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`。
  - 新 staged hash：`fnv64:0d1ad7b57eed8ca7`。

## 验证

- `cargo test -p adm-design -p adm-development -p adm-assets -p adm-application`：通过。
- `cargo fmt --check`：通过。
- `cargo run -p adm-desktop -- --smoke`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 78 个 Rust 单元测试通过。
- `cargo build -p adm-desktop --release`：通过。
- `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`：通过。
- `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`：通过。

## 后续事项

- 继续补齐核心业务深度：将设计支柱/机制/风险推进到开发与资产依赖图，而不是只写在文档中。
- 增加真实游戏打包目标与 SDK 验证。
- 增加可视窗口手工验收：两个 Rust 窗口选择同一 archive 时，第二个窗口必须显示锁冲突和 owner 信息。
