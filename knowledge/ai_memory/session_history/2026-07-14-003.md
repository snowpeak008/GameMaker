# 2026-07-14-003

## 摘要

确认 NEWrust 当前便携版本可迁移到其他 Windows 电脑运行，不会索引或依赖父级 Python 项目；同时记录迁移目录约束、运行环境要求和本机 Unity 绝对路径例外。

## 独立性结论

- [x] 便携目录中 `.py`、`.pyc`、`.pyd` 和 `python*.dll` 数量为 0。
- [x] 当前便携目录及 `user_data` 未发现 `E:\workwork\CrewAi\AutoDesignMaker` 原项目绝对路径。
- [x] 当前 `user_data` 未发现 Python 文本引用；独立性门禁扫描 221 个源码文件，父级 Python 禁止命中为 0。
- [x] 根启动器只按自身目录解析 `dist/AutoDesignMaker-NEWrust`，数据根固定为该目录下的 `user_data`，默认启动空白项目。
- [x] 重定位契约已通过，运行已编译版本不需要 Python、Node.js、Rust 或 Visual Studio。

## 迁移约束

- [x] 使用根入口时必须保持 `.project_root`、根 `AutoDesignMaker.exe` 与完整 `dist/AutoDesignMaker-NEWrust/` 的相对结构，不能只复制根 EXE。
- [x] 目标环境要求 Windows 10/11 x64、Microsoft Edge WebView2 Runtime，以及对便携目录的读写权限。
- [x] 当前便携开发快照 SHA-256 为 `4c07249dcf8362524d68312bd069755fdba9280a72ede0d1a4f4741360b7e895`。
- [ ] 当前保留的 `user_data/settings/project_bindings.json` 含本机 Unity 编辑器和工程绝对路径；迁移后需重新选择目标电脑上的 Unity 编辑器与工程目录，但这不会影响程序启动，也不是 Python 依赖。
- [ ] 正式对外迁移应在提交累计改动后运行 `NEWrust/tools/verify-standalone.ps1`，生成空 `user_data`、clean-clone 认证的正式发布包。
