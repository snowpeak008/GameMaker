# Session 2026-07-05-060

## Summary
Made desktop Unity dry-run persist engine execution history while respecting selected archive locks.

## Completed
- [x] Updated desktop `dry-run-unity-build` callback to detect whether the current window owns the selected archive lock.
- [x] The callback releases the owned selected archive lock before writing engine history.
- [x] The callback reacquires the selected archive lock after the dry-run attempt.
- [x] Successful desktop dry-runs refresh project inspection so engine history and optional package files are visible immediately.
- [x] `dry_run_unity_build_for_ui(...)` now persists `EngineBuildExecutionReport` through `AdmApplication::commit_engine_build_execution(...)`.
- [x] Dry-run UI output now includes `history_file`, `history_records`, and `history_commit_files`.
- [x] Removed the separate engine history smoke fixture because dry-run itself now persists history.
- [x] Desktop smoke still verifies engine history rows and optional package file rows.
- [x] Rebuilt and re-staged the isolated Rust desktop release exe.

## Verification
- [x] `cargo check -p adm-desktop`: passed.
- [x] `cargo run -p adm-desktop -- --smoke`: passed.
- [x] `cargo fmt --check`: passed.
- [x] `cargo check --workspace`: passed.
- [x] `cargo test --workspace`: passed, 93 Rust unit tests.
- [x] `cargo build -p adm-desktop --release`: passed.
- [x] `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`: passed.
- [x] `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`: passed.
- [x] Staged release hash: `fnv64:a9beea8c8c640e55`.

## Follow-up
- [ ] Add richer package/build/engine history grouping in the Slint workbench.
- [ ] Continue SDK/packaging delivery surface work.
- [ ] Continue replacing POC text-heavy controls with richer controls and layout.
