# 会话 2026-06-21-002

> 时间：2026-06-21（下午）  
> 类型：流水线端到端联通修复

---

## 做了什么

### 1. 分层导出架构（彻底重构）

原有 `export_concept_package()` 输出硬编码 AutoDesignMaker 工具描述。现已重构为三包分层导出：

| 包 | 内容 | 步骤 |
|---|---|---|
| `devflow_Concept_v2/` | 项目定位 + 核心循环（10-15条） | Step 00 |
| `devflow_GameplayFramework_v2/` | 玩法系统 + 框架 | Step 01 |
| `devflow_Design_v2/` | 全量域决策 | Step 02+ |

### 2. generation.py 阶段感知加载

新增 `_load_design_for_stage(step_number)` 和 `_load_package_by_source_id(source_id)`：
- step 0 → Concept 包
- step 1 → GameplayFramework 包（降级到 Concept）
- step 2+ → Design 包（降级到 Concept）

### 3. 流水线其他修复

- `artifact_layer/dependency_graph.json` 写入路径修正 → `pipeline/artifact_layer/`
- 所有步骤 `required=True` → `required=False`（全新项目模型）
- `_validate_graphs` 资源流检查降为 warning（不阻断流水线）
- 步骤0导出包命名：`s00_cpt_v2` → `devflow_Concept_v2`
- `_latest_concept_package()` 搜索路径改为 `SOURCE_ARTIFACTS_DIR`
- export_adapter 路径改为只写 `sandbox/source_artifacts/`（删除根目录写入）

### 4. 发现的架构问题（待解决）

**存档系统不包含流水线产出**：
- `sandbox/outputs/artifacts/stage_00~15/` 不在 save_manager 的 ACTIVE_DIRS 内
- 存档只保存设计工作台数据（execution_objects）和项目配置
- 用户即将重新设计存档系统以解决此问题

---

## 下次会话应继续

- [ ] 重新设计存档系统，使其包含流水线产出
- [ ] 验证步骤 02 冻结后步骤 03-08 能否正常运行
