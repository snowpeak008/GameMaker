# 2026-06-27-001：执行 ai_config_optimization_v2

## 摘要

按 `plan/ai_config_optimization_v2` 完成 AI 配置系统 v2：统一到 `settings/ai_config.json`，Profile 绑定 Adapter、LLM 和图片生成配置，新增迁移工具、验证器、统一 GUI 配置入口、状态指示、测试和文档。

## 完成内容

- ✅ 新增 `core/config/ai_config.py`：v2 数据类、默认 4 个 Profile、加载/保存/激活、旧 active key 兼容
- ✅ 新增 `core/config/validator.py`：Profile 验证、CLI 可用性检测、图片配置一致性检查
- ✅ `core/config/loader.py` 新增 `get_active_ai_profile()` / `get_pipeline_adapter()`；`get_api_config()` 改为废弃兼容层并优先读 v2 Profile
- ✅ 新增 `tools/config/migrate_ai_config.py`，支持从 `ai_profiles.json`、`api_config.toml`、旧 `app.toml [model]` 和 `project_settings.pipeline_adapter` 迁移
- ✅ `core/config/integrity.py` 启动时执行迁移检查并确保 `settings/ai_config.json`
- ✅ 适配器基类和 OpenAI/Codex/Claude/Local 适配器支持 `configure()` Profile 注入
- ✅ Step02 补全和 Stage12 实际开发执行优先使用 active AI Profile，缺少 v2 配置时回退旧 `pipeline_adapter`
- ✅ 图片生成唯一开关改为 active Profile 的 `image.enabled`
- ✅ 新增 `core/ui/ai_config_unified_dialog.py`，替代旧 AI 配置对话框；主窗口顶栏新增 AI 状态指示
- ✅ 项目配置对话框移除旧 AI 适配器下拉，避免与 Profile 冲突
- ✅ 新增 `settings/ai_config.example.json`、`docs/AI_CONFIG_GUIDE.md`，更新 `README.md` 和 `AI_README.md`
- ✅ 新增/更新测试覆盖配置模型、验证器、迁移、适配器注入、loader 兼容、图片配置和旧调用废弃警告
- ✅ 新增 `knowledge/ai_memory/project_understanding/ai_config_v2_architecture.md`

## 自查修复

- ✅ 保留 `_codex_command()` / `_claude_command()` 默认无参调用兼容，避免旧测试和旧 monkeypatch 失效
- ✅ `get_api_config()` 的 `DeprecationWarning` 改为测试显式断言，保证全量测试无 warning
- ✅ `settings/ai_config.json` 本轮未生成；现有 `settings/ai_profiles.json` / `settings/api_config.toml` 仍为 ignored 本地旧配置

## 验证

- ✅ 目标测试：27 passed
- ✅ 适配器/Step02/图片/人工门禁关联测试：42 passed
- ✅ `python -B -m pytest -q`：142 passed
- ✅ `PYTHONPYCACHEPREFIX=.cache\pycache python -B -m compileall core pipeline tools\validators\pipeline_quality.py tools\asset_production tools\config`：通过
- ✅ `git diff --check`：通过（仅 CRLF 工作区提示）

## 后续关注

- [ ] 建议人工打开 GUI，检查顶栏 AI 状态与统一 AI 配置对话框的保存/激活流程
- [ ] 若要真实 CLI 检测，需在安装并登录 Codex/Claude CLI 的环境中点击“测试连接”
- [ ] 提交前继续排除 `plan/`、bug 文档、runtime drafts、`settings/ai_config.json`、旧 `settings/api_config.toml`
- [ ] 本轮只按用户要求同步记忆，代码改动尚未提交
- [ ] CC-Panes 共享记忆池本次因环境变量缺失未写入
