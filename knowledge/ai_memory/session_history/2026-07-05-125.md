# Session 2026-07-05-125

## Summary

Fixed the explicit Unity executable path propagation gap so `unity_acceptance_gate.ps1`, standalone external acceptance, and final strict release gate all use the same `Unity.exe` path.

## Completed

- Added `--unity-exe <path>` and `--unity-exe=<path>` support to `adm-cli external-acceptance`.
- Added parser tests for the explicit Unity executable flag.
- Updated `scripts/release_gate.ps1` with `-UnityExe <path>` and pass-through to `adm-cli external-acceptance --unity-exe`.
- Updated `scripts/external_acceptance_doctor.ps1` with `-UnityExe <path>` and pass-through to `adm-cli external-acceptance --unity-exe`.
- Updated `scripts/unity_acceptance_gate.ps1` so its resolved `-UnityExe` path is reused by the final `external-acceptance` step.
- Updated generated handoff instructions:
  - `strict_gate_command` now includes `-UnityExe <path-to-Unity.exe>`.
  - `rerun-external-acceptance` now includes `--unity-exe <path-to-Unity.exe>`.
  - `run-strict-release-gate` now includes `-UnityExe <path-to-Unity.exe>`.
- Updated `RUST/README.md` with explicit Unity executable examples and guidance.
- Regenerated source bundle, handoff bundle, evidence, and final handoff package.

## Verification

- `cargo fmt`: passed through the approved Windows Rust/MSVC environment after direct sandboxed formatting hit Windows access denial.
- `cargo test -p adm-cli`: passed, 12 tests.
- `powershell -ExecutionPolicy Bypass -File .\scripts\release_gate.ps1 -DryRun -RequireExternalAcceptance -UnityExe C:\Unity\Editor\Unity.exe`: printed `adm-cli -- external-acceptance --unity-exe C:\Unity\Editor\Unity.exe --require-ready`.
- `powershell -ExecutionPolicy Bypass -File .\scripts\external_acceptance_doctor.ps1 -DryRun -RequireReady -UnityExe C:\Unity\Editor\Unity.exe`: printed `adm-cli -- external-acceptance --unity-exe C:\Unity\Editor\Unity.exe --require-ready ...`.
- `powershell -ExecutionPolicy Bypass -File .\scripts\unity_acceptance_gate.ps1 -DryRun -ArchiveId archive_1783253820821_24440_1 -UnityExe C:\Unity\Editor\Unity.exe -RequireExternalAcceptance`: printed final `external-acceptance --unity-exe C:\Unity\Editor\Unity.exe --require-ready`.
- `cargo run -q -p adm-cli -- external-acceptance --unity-exe target\adm-test-unity\Editor\Unity.exe dist\AutoDesignMaker-rust target\adm-explicit-unity-external.adm`: passed and reported `unity_ready=true`, `unity_selected=target\adm-test-unity\Editor\Unity.exe`.
- `powershell -ExecutionPolicy Bypass -File .\scripts\release_gate.ps1 -SkipTests -SkipBuild`: passed and regenerated current handoff reports.
- Cross-layer check completed for CLI flag parsing, scripts, README, generated handoff instructions, source bundle, and evidence bundle.

## Current Handoff State

- Local release is ready: `release_accepted=true`, `release_delivery_ready=true`, `release_smoke_ready=true`, `release_hash=fnv64:97814f3263c20abf`.
- Source handoff evidence is ready: `source_ready=true`, `source_handoff_mode=bundled`, `source_file_count=55`, `source_bundle_hash=fnv64:b36ed98bf835572e`.
- Unified handoff bundle is ready: `handoff_bundle_ready=true`, `handoff_bundle_file_count=107`, `handoff_bundle_hash=fnv64:bbd20c7176e4ae2a`.
- Final evidence sync is ready: `handoff-evidence-manifest.adm ready=true`, `file_count=8`, `evidence_hash=fnv64:97009fbf5c859d70`.
- Final handoff package artifact is ready: `final-handoff-manifest.adm ready=true`, `file_count=117`, `package_hash=fnv64:1a7a640c2f5556f6`.
- External acceptance is still not ready in the default local environment: `external_acceptance_ready=false`, `unity_ready=false`, `real_ai_provider_ready=false`.
- Current imported runtime evidence is still CLI smoke evidence: `unity_runtime_runner=cli_smoke_runner`.
- Explicit AI provider acceptance is not ready: `ai_acceptance_ready=false`, `ai_configured_ready=false`.

## Remaining Work And Rough Time

- Configure and run real non-mock AI provider acceptance: about 0.25-1 hour if endpoint, model, and secret are ready.
- Run `scripts/unity_acceptance_gate.ps1 -UnityExe <path-to-Unity.exe>` on a machine with Unity installed: about 1-3 hours if Unity is compatible; about 0.5-1 day if Unity install/import fixes are needed.
- Run final strict gate `scripts/release_gate.ps1 -RequireExternalAcceptance -UnityExe <path-to-Unity.exe>`: about 0.5-1 hour after Unity and AI readiness both pass.
- Estimated remaining engineering workload is roughly 1-2%, but it remains the final delivery blocker because strict completion requires external Unity and real AI evidence.

## Next Tasks

- Run `scripts/ai_acceptance_gate.ps1` with real provider credentials and `-RequireReady`.
- Run `scripts/unity_acceptance_gate.ps1 -UnityExe <path-to-Unity.exe>` on a real Unity machine so the archive and restaged Unity project contain `runner=unity_playmode`.
- Rerun `scripts/release_gate.ps1 -RequireExternalAcceptance -UnityExe <path-to-Unity.exe>` once both external requirements are ready.

