# Session 2026-07-05-124

## Summary

Fixed the Unity runtime acceptance runner mismatch so real Unity runtime validation can produce the `runner=unity_playmode` evidence required by strict external acceptance.

## Completed

- Audited the Unity acceptance evidence chain:
  - `scripts/unity_acceptance_gate.ps1`
  - `adm-cli run-unity-runtime-validation`
  - archive runtime validation import
  - `stage-unity-project`
  - `external-acceptance`
- Found that the generated Unity validator wrote `runner=unity_editor_runtime_validation`, while strict external acceptance requires `runner=unity_playmode`.
- Updated `RUST/crates/adm-packaging/src/lib.rs` so generated `Assets/AutoDesignMaker/Editor/AutoDesignMakerRuntimeValidation.cs` writes `runner=unity_playmode`.
- Updated the packaging test expectation for the generated Unity runtime validation script.
- Restaged `dist/unity-project` from latest formal archive `archive_1783253820821_24440_1`.
- Regenerated source bundle, handoff bundle, evidence, and final handoff package.

## Verification

- `cargo fmt`: passed.
- `cargo test -p adm-packaging`: passed, 37 tests.
- `cargo test -p adm-cli`: passed, 10 tests.
- `cargo run -q -p adm-cli -- stage-unity-project archive_1783253820821_24440_1 windows_desktop_playable dist\unity-project`: passed.
- `powershell -ExecutionPolicy Bypass -File .\scripts\release_gate.ps1 -SkipTests -SkipBuild`: passed.
- `powershell -ExecutionPolicy Bypass -File .\scripts\release_gate.ps1 -DryRun -RequireExternalAcceptance`: passed and confirmed strict gate order.
- Cross-layer check completed:
  - source generator writes `runner=unity_playmode`
  - `dist/unity-project` contains `runner=unity_playmode`
  - `dist/handoff-bundle/unity-project` contains `runner=unity_playmode`
  - `dist/handoff-bundle/source-bundle` contains the updated source generator
  - current runtime evidence remains `runner=cli_smoke_runner`, so the final blocker is still correctly reported until real Unity runs.

## Current Handoff State

- Local release is ready: `release_accepted=true`, `release_delivery_ready=true`, `release_smoke_ready=true`, `release_hash=fnv64:97814f3263c20abf`.
- Source handoff evidence is ready: `source_ready=true`, `source_handoff_mode=bundled`, `source_file_count=55`, `source_bundle_hash=fnv64:cfe921b299fb11e4`.
- Unified handoff bundle is ready: `handoff_bundle_ready=true`, `handoff_bundle_file_count=107`, `handoff_bundle_hash=fnv64:d5e7f8f1c7a3b322`.
- Final evidence sync is ready: `handoff-evidence-manifest.adm ready=true`, `file_count=8`, `evidence_hash=fnv64:f5d47a5be5f90f81`.
- Final handoff package artifact is ready: `final-handoff-manifest.adm ready=true`, `file_count=117`, `package_hash=fnv64:acabbc4f84bb3eab`.
- External acceptance is still not ready: `external_acceptance_ready=false`, `unity_ready=false`, `real_ai_provider_ready=false`.
- Current imported runtime evidence is intentionally still not accepted: `unity_runtime_runner=cli_smoke_runner`.
- Explicit AI provider acceptance is not ready: `ai_acceptance_ready=false`, `ai_configured_ready=false`.

## Remaining Work And Rough Time

- Configure and run real non-mock AI provider acceptance: about 0.25-1 hour if endpoint, model, and secret are ready.
- Run `scripts/unity_acceptance_gate.ps1` on a machine with Unity installed: about 1-3 hours if Unity is compatible; about 0.5-1 day if Unity install/import fixes are needed.
- Run final strict gate `scripts/release_gate.ps1 -RequireExternalAcceptance`: about 0.5-1 hour after Unity and AI readiness both pass.
- Estimated remaining engineering workload is roughly 1-3%, but it remains the final acceptance blocker because strict completion requires external Unity and real AI evidence.

## Next Tasks

- Run `scripts/ai_acceptance_gate.ps1` with real provider credentials and `-RequireReady`.
- Run `scripts/unity_acceptance_gate.ps1` on a machine with Unity installed so the archive and restaged Unity project contain `runner=unity_playmode`.
- Rerun `scripts/release_gate.ps1 -RequireExternalAcceptance` once both external requirements are ready.

