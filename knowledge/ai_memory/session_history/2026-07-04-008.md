# Session History: 2026-07-04-008

## Summary

Verified the failing `test_step05_to_step09_structured_contract_chain.py` regression and identified the real Step09 art task enrichment ordering bug.

## Completed

- [x] Ran `python -m pytest core\tests\unit\test_step05_to_step09_structured_contract_chain.py -q` and reproduced the failure at `assert all(task.get("semantic_policy") for task in art_contract["tasks"])`.
- [x] Checked the test path and confirmed it writes `style_confirmation.json`, reruns Step07, and calls `_stage9_outputs()` with an approved style contract.
- [x] Confirmed the failure is not caused by empty tasks or an empty `style_contract`; empty tasks would make `all(...)` pass.
- [x] Inspected `_stage9_outputs()` and found the current order: base tasks -> `enrich_art_tasks()` -> `ensure_art_tasks_cover_asset_strategy()` -> `enrich_art_tasks_with_semantics()`.
- [x] Confirmed `ensure_art_tasks_cover_asset_strategy()` appends `strategy_generated=True` tasks after `enrich_art_tasks()` has already injected `semantic_policy` and `rework_policy`.
- [x] Confirmed `enrich_art_tasks_with_semantics()` only fills semantic refs and missing `generation_prompt`; it does not inject `semantic_policy` or `rework_policy`, and it does not overwrite an existing `generation_prompt`.

## Verification

- [x] Printed generated Step09 tasks and verified base tasks have `semantic_policy/rework_policy`, while strategy-generated tasks do not.
- [x] Identified the durable fix: in `_stage9_outputs()`, complete all task creation and asset-strategy task completion first, then run `enrich_art_tasks()` on the complete task list, and finally run `enrich_art_tasks_with_semantics()`.

## Follow-up

- [ ] Fix `_stage9_outputs()` ordering so every base and strategy-generated art task receives `semantic_policy`, `rework_policy`, prompts, and acceptance fields consistently.
- [ ] Re-run `core\tests\unit\test_step05_to_step09_structured_contract_chain.py` after the ordering fix.
- [ ] If fixing code, preserve the standard design direction: one complete Step09 task list, one art-pipeline enrichment pass, then semantic enrichment.
