# Session 2026-07-05-090

## Summary
Promoted the persisted Rust project brief into the game build and Unity delivery chain.

## Completed
- [x] Made `project/brief.adm` a required artifact for the `windows_desktop_playable` game build target.
- [x] Updated game build bundle staging so `project/brief.adm` is copied to `content/project/brief.adm`.
- [x] Updated game build delivery doctor checks so the staged bundle must include `content/project/brief.adm`.
- [x] Made package support validation require `project/brief.adm`, matching the package manifest generated by the core pipeline.
- [x] Updated Unity scaffold generation to read `project/brief.adm` and emit `Assets/AutoDesignMaker/Generated/project_brief.adm`.
- [x] Updated the Unity generated content index to reference `project_brief.adm`.
- [x] Updated Unity scaffold doctor checks to content-verify `project_brief.adm` and the generated content index references.
- [x] Updated desktop smoke expectations for `staged_files=6`, `generated_files=16`, and 31 delivery doctor rows.
- [x] Updated desktop smoke to verify the game bundle brief and Unity generated brief rows.
- [x] Updated `RUST/README.md` to document brief delivery in game build and Unity scaffold outputs.

## Verification
- [x] `cargo fmt`: passed.
- [x] `cargo test -p adm-packaging`: passed, 31 tests.
- [x] `cargo check -p adm-desktop`: passed.
- [x] `cargo fmt --check`: passed.
- [x] `cargo run -q -p adm-desktop -- --smoke`: passed; smoke covered game bundle brief, Unity project brief, and updated doctor row counts.
- [x] `cargo test -p adm-application`: passed, 19 tests.
- [x] `cargo check --workspace`: passed.
- [x] `cargo test --workspace`: passed, 117 Rust unit tests.

## Paused State
- [ ] Release and default delivery artifacts were not refreshed because the user needed to shut down the machine.
- [ ] Existing `RUST/dist/` outputs are stale relative to the new brief delivery behavior until refreshed.

## Follow-up
- [ ] Rebuild and re-stage the isolated Rust desktop release.
- [ ] Create a fresh delivery demo archive and refresh `RUST/dist/game-build/windows_desktop_playable`, expecting `staged_files=6`.
- [ ] Refresh `RUST/dist/unity-project`, expecting `generated_files=16` and `Assets/AutoDesignMaker/Generated/project_brief.adm`.
- [ ] Re-run Unity dry-run/preflight, restage SDK bundle if engine history changes, then run release doctor and delivery doctor.
- [ ] Add real-provider non-network config validation presets for common OpenAI-compatible endpoints.
- [ ] Run guarded real Unity build validation on a machine with Unity installed.
- [ ] Continue production-depth pipeline expansion beyond demo content.
