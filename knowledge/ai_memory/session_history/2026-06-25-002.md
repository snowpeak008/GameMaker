# 2026-06-25-002 — 执行 hades_l5_step0008_opt v2

## 摘要

按 `plan/hades_l5_step0008_opt/v2_optimization_plan.md` 执行 Step00-08 质量优化。临时 plan 目录仅作为本地任务输入，不提交到 git。CC-Panes 共享记忆池因环境变量为空未写入，本轮阶段记忆写入项目本地 `knowledge/ai_memory`。

## 阶段记录

### PLAN-B：Step04 environment 英文资产识别

- `core/engines/generation.py` 中 `_asset_type()` 增加英文环境关键词：`room`、`level`、`environment`、`chamber`、`dungeon`、`floor`、`biome`、`tileset`、`backdrop`。
- `_asset_items()` 候选过滤增加英文 `room`、`level`、`chamber`、`dungeon`、`ui`、`hud`，让 Hades 英文内容节点进入资产生成。
- 同步增强英文 `config`、`vfx/effect/feedback/reward`、小写 `ui/hud` 的类型识别。
- Step04 进一步改为优先消费 Stage02 `entity_coverage_report.json` 中的冻结/补全实体，让 Step02 fallback room/scene 实体可以级联生成 environment 资产。
- 新增回归测试 `test_stage4_asset_items_detect_english_environment_entities`，确认 `underworld_room` 生成 `environment`，`boon_choice_ui` 生成 `ui`。
- 新增回归测试 `test_stage4_uses_stage2_supplemented_entities_for_assets`，确认 Stage02 补全 room 实体会生成 2 个 environment 资产。

**验证**：
- `python -m pytest core\tests\unit\test_hades_quality_optimization.py::test_stage4_asset_items_detect_english_environment_entities -q`：1 passed。
- `python -m pytest core\tests\unit\test_hades_quality_optimization.py::test_stage4_uses_stage2_supplemented_entities_for_assets -q`：1 passed。

### PLAN-C：Step07 documentation 任务压缩

- `_program_task_category()` 分类前会移除 `schema=...` 元数据，避免 `schema=skill_card_v1` 这类 L5 实体字段误命中 documentation。
- 新增 `_is_documentation_requirement()`，当需求来自 `documentation_*` 系统、阶段、选择、实体、依赖或输出时，Stage07 不再生成程序任务。
- `_stage7_outputs()` 任务编号改为按实际保留任务连续生成，过滤文档治理需求后不会留下 DEV 编号空洞。
- 新增回归测试确认 `documentation_runtime_decision` 需求被跳过，技能需求即便含 `schema=skill_card_v1` 仍归类为 `combat`。

**验证**：
- `python -m pytest core\tests\unit\test_hades_quality_optimization.py::test_stage7_skips_documentation_requirements_and_ignores_schema_metadata -q`：1 passed。

### PLAN-A：Step02 missing_entities 真实节点追踪

- 新增 `_expected_node_ids()`，优先读取 `parsed` / `design_summary` 中的节点列表，随后从 `Layer 4 设计决策` selections、`L5节点` selections 和 raw text 中恢复真实 node_id。
- 有真实 expected node list 时，`covered_concrete_nodes` 改为 expected node 与实体 node_id 的交集数量，不再把游离实体节点计入真实覆盖。
- `missing_entities` 优先输出真实缺失 node_id；只有 expected count 大于可恢复列表时才继续使用 `UNMAPPED-NODE-xxx` 兜底。
- `EntityValidator` 在调用 supplement 前把 `expected_node_ids` / `missing_node_ids` 注入补全上下文，避免适配器只看到泛化缺口。
- `SupplementRequest` 增加 `missing_node_ids`，prompt 要求优先使用真实缺失节点；fallback 会为最多 48 个真实缺失节点生成可追踪实体。
- 新增回归测试确认 Step02 对 `combat_system_decision` 已覆盖、`ability_choice_decision` / `level_space_decision` 缺失时输出真实节点 ID。
- 新增回归测试确认 AI 返回空结果时，fallback 会使用真实 missing node ids 补齐覆盖。

**验证**：
- `python -m pytest core\tests\unit\test_pipeline_optimization_helpers.py::test_step02_missing_entities_use_real_expected_node_ids core\tests\unit\test_pipeline_optimization_helpers.py::test_step02_entity_coverage_uses_expected_design_node_count core\tests\unit\test_step05_optimization.py::test_supplement_trigger_and_unmapped_priority -q`：3 passed。
- `python -m pytest core\tests\unit\test_l5_supplement.py::test_entity_validator_passes_real_missing_nodes_to_fallback -q`：1 passed。

### PLAN-D：Step00 CQ-011 genre evidence

- `GENRE_DEFAULT_EVIDENCE["roguelike_action"]` 补充 `CQ-011` 运行时流程证据：房间加载、战斗清场、奖励结算、下一房间，由 `room_state_machine` 驱动。
- 新增回归测试确认 Hades/roguelike 稀疏输入能通过 genre inference 回答 CQ-011。

**验证**：
- `python -m pytest core\tests\unit\test_pipeline_optimization_helpers.py::test_step00_roguelike_action_supplies_runtime_flow_evidence -q`：1 passed。

## 最终自检

- `python -m pytest core\tests\unit\test_hades_quality_optimization.py core\tests\unit\test_l5_supplement.py core\tests\unit\test_step05_optimization.py core\tests\unit\test_pipeline_optimization_helpers.py -q`：75 passed。
- `python -m pytest -q`：111 passed。
- `python -m py_compile core\engines\generation.py pipeline\step_00_idea_intake\helpers.py pipeline\step_02_design_review_freeze\helpers.py core\tests\unit\test_hades_quality_optimization.py core\tests\unit\test_pipeline_optimization_helpers.py`：通过。
- `python -m flake8 --max-line-length=100 --extend-ignore=E203,E501 ...`：本轮触碰文件通过。
- `python -m black --check ...`：本轮触碰文件通过。
- `python -m compileall core pipeline tools\validators\pipeline_quality.py tools\asset_production`：通过。
- `git diff --check`：通过，仅 CRLF 工作区提示。
- 真实配置重跑 `python -m core.main --from-step 0 --stop-step 8 --skip-preflight --auto-approve`：Step00-08 全部 success。
- `python tools\validators\pipeline_quality.py --artifacts-dir drafts\20260625_122737_33376\outputs\artifacts`：question coverage 1.0，Step02 entity coverage 0.8447，entity_count 103，asset_count 132，Stage05 warning 1，blocking 0。
- 最新 draft 关键分布：environment 4，Stage06 PASS，Step07 documentation 5。

## 后续关注

- Step02 仍剩 16 个未覆盖节点；当前 0.8447 已超过计划目标，若要接近 1.0 需要继续扩展 fallback 上限或补齐源模板 L5 实体。
- 提交时继续排除 `plan/hades_l5_step0008_opt/`、其他 `plan/` 临时目录和本地配置。
