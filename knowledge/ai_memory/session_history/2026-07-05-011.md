# 会话记录：2026-07-05-011

## 摘要

继续 goal 模式推进 Rust 全项目重建，完成桌面 GUI 对 pipeline、AI、package、validation 状态的可见化，并把对应状态提取到 `adm-ui-model`。

## 完成内容

- `adm-ui-model`
  - `ShellState` 新增：
    - `ai`
    - `package`
    - `validation`
  - 新增 `AiStatusView`。
  - 新增 `PackageStatusView`。
  - 新增 `ValidationStatusView`。
  - `PipelineStatusView`、`AiStatusView`、`PackageStatusView`、`ValidationStatusView` 均提供 `render()`。
- `adm-desktop`
  - Slint UI 新增面板属性：
    - `ai-text`
    - `package-text`
    - `validation-text`
  - Slint UI 新增三块状态面板：
    - AI
    - Package
    - Validation
  - 窗口高度调整为 820px，底部项目面板高度调整为 280px，避免新增状态面板挤压布局。
  - 桌面 crate 新增 `adm-pipeline` 依赖，用于解析 `pipeline/run_state.adm`。
  - 创建并运行项目后，GUI 同步显示：
    - pipeline status
    - AI records/accepted/intervention
    - package entries/support files/artifacts
    - validation status/issues
  - 选择项目时会读取正式存档内容：
    - `content/pipeline/run_state.adm`
    - `content/ai/journal.adm`
    - `content/package/manifest.adm`
    - `content/validation/report.adm`
  - `--smoke` 增加对 package 状态面板的断言，并打印四类状态。

## 验证

- `cargo fmt --check`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 45 个 Rust 单元测试通过。
- `cargo run -p adm-desktop -- --smoke`：通过，并打印：
  - Pipeline: Succeeded
  - AI: records=1 accepted=1 intervention=true
  - Package: entries=4 support_files=6
  - Validation: Passed issues=0
- `git diff --check -- RUST`：通过。

## 后续事项

- 继续把 deterministic core executor 拆成真实业务 stage executor。
- 给 AI provider 增加错误分类与 task persistence。
- 后续 GUI 需要真实列表选择、文件选择器、可点击 stage progress 和失败后 resume 操作。
- 后续仍需可见窗口人工验证和发行包验证。
