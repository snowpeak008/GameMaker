# Session 2026-06-29-005

## Summary

Implemented `newplan/step11_codex_cwd_bug_plan.md`: Step11 Codex tasks now execute from the configured Unity project path instead of the AutoDesignMaker repository root.

## Completed

- Added `ModelTask.cwd` with an empty-string default so existing adapter callers keep their previous behavior.
- Updated `CodexAdapter.generate()` to pass `task.cwd` to `run_codex_exec()` when set, falling back to the adapter root when empty.
- Set `model_task.cwd` and `repair_task.cwd` to Step11 `project_path` for both primary Codex execution and auto-repair retries.
- Added regression coverage for the `ModelTask.cwd` default, Codex cwd override, and Codex root fallback.
- Isolated the legacy Step11 resume-dir test from the current formal save workspace so real save state cannot override the fixture record.

## Verification

- `python -B -m py_compile core\adapters\base.py core\adapters\codex_adapter.py core\engines\generation.py`: passed.
- `python -B -m pytest core\tests\unit\test_model_adapters.py -q`: 8 passed.
- `python -B -m pytest core\tests\unit\test_unattended_recovery.py -q`: 21 passed.
- `python -B -m pytest core\tests\integration\test_plugins.py -q`: 4 passed.
- `python -B -m pytest -q`: 221 passed.
- `git diff --check`: passed with only CRLF working-copy warnings.

## Follow-up

- To recover the failed Factorio draft, stop any active Step11 run, clear `drafts/20260629_231411_38176/outputs/artifacts/stage_11/`, then rerun Step11.
- Keep Step11 Codex execution and repair tasks rooted at the configured Unity `development_path`; do not let them fall back to the AutoDesignMaker repository root.
- `newplan/` remains local planning material and should not be committed unless explicitly requested.
