# 2026-06-24-004 — 执行 hades_quality_optimization：Hades 质量优化与标准化沉淀

## 摘要

执行 `plan/hades_quality_optimization` 开发计划，完成 Hades L5 覆盖扩展、Step07/08 任务可读性与分类增强、Codex CLI Windows shim 修复，并建立质量标准体系。计划目录仍作为本地执行材料，不提交到 git。

## 完成内容

- Hades partial 模板从 39/103 节点扩展到 103/103 节点，超过计划的 80+ 节点目标。
- `core/engines/generation.py` 新增任务标题清理，移除重复范本说明，并为 Stage07 程序任务输出 `category` 和 `priority`。
- Stage08 美术任务现在透传/生成 `asset_type`、`category`、`priority`、`complexity`，并写出 `TEMPLATE_NOTE.md`。
- 修复 Codex CLI Windows 执行入口：`_codex_command()` 优先使用 `codex.cmd` / `codex.exe`，避免 PowerShell 执行策略阻断 `codex.ps1`。
- 新增 `knowledge/governance/quality_standards/`，包含 17 个标准、模板、手册和指标文档。
- 新增 `test_hades_quality_optimization.py`，覆盖 Hades 80+ 目标、标题清理、Stage07/08 任务字段。

## 验证

- `codex.cmd --version`：`codex-cli 0.141.0` 可执行；裸 `codex` 在 PowerShell 会命中被执行策略拦截的 `.ps1`。
- `python -m pytest core\tests -q`：87 passed，仅 `.pytest_cache` 写入权限 warning。
- `python -m compileall core pipeline tools\validators\pipeline_quality.py`：通过。
- Hades partial 模板覆盖节点数：103。
- 内置模板 JSON 解析通过。
- `git diff --check`：通过，仅 CRLF 工作区提示。
- `python tools\validators\pipeline_quality.py`：仍读取旧 draft 历史产物，显示旧 52.43% 覆盖；需 GUI 重新导出 Hades 范本后新 draft 才会反映本轮模板提升。

## 后续关注

- 重新在 GUI 中载入/导出 Hades partial 或目标 Hades 范本，运行 Step02-09 验证新 draft 的质量指标。
- 提交前继续确认 `plan/hades_quality_optimization/` 和根目录临时评分报告不进入暂存区。
