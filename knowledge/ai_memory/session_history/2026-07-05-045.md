# 会话记录：2026-07-05-045

## 摘要

继续 goal 模式推进 Rust 全项目重建，新增 SDK 目标验证模型，并将 SDK 验证接入核心流水线的统一 validation 路径。SDK 知识库现在可以针对目标引擎、目标平台、构建脚本覆盖要求生成 `ValidationReport`，默认 Unity/windows-desktop SDK 配置会通过校验。

## 完成内容

- `adm-sdk`
  - 新增对 `adm-validation` 的依赖。
  - 新增 `SdkTargetProfile`。
  - 新增 `SdkKnowledgeBase::validate_for_target(...)`。
  - SDK 验证覆盖：
    - target engine 不能为空。
    - target platform 不能为空。
    - SDK resources 不能为空。
    - 至少一个 resource 能支持 AI explanation。
    - 需要构建脚本时必须有构建相关覆盖。
    - resource name/purpose/validation checklist 基础完整性。
  - 新增 2 个单元测试：默认 Unity/windows-desktop 通过、缺失 target/checklist 失败。
- `adm-application`
  - 核心 pipeline 的 `validate_pipeline_inputs(...)` 现在合并 SDK target validation。
  - pipeline commit 测试确认默认 validation report 不包含 `code=sdk.` 问题。
- 发布产物
  - 重新构建 release。
  - 重新 staging 到 `RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`。
  - 新 staged hash：`fnv64:88330105f15d5fac`。

## 验证

- `cargo test -p adm-sdk -p adm-application`：通过。
- `cargo fmt --check`：通过。
- `cargo run -p adm-desktop -- --smoke`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 80 个 Rust 单元测试通过。
- `cargo build -p adm-desktop --release`：通过。
- `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`：通过。
- `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`：通过。

## 后续事项

- 继续补齐真实游戏打包目标。
- 将 AI 自动介入与 SDK validation warning/failed 状态绑定。
- 增加可视窗口手工验收：两个 Rust 窗口选择同一 archive 时，第二个窗口必须显示锁冲突和 owner 信息。
