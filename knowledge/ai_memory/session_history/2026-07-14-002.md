# 2026-07-14-002

## 摘要

完成 NEWrust 全局质量加固：修复前端加载失败被误判为空数据、工具面板全量竞态刷新、UI 门禁弱化路径、Shell 轮询重入，并加入默认关闭的安全空白草稿保留策略；同步完成独立性、根 EXE、便携产品和双语 UI 验证。

## 完成内容

- [x] 设计工作台、AI 访谈、补丁、打包、日志、SDK 和存档面板保留真实加载失败，设计页提供双语重试入口。
- [x] 新增工具面板持久控制器，按目标刷新并通过独立请求代次拒绝过期响应。
- [x] UI 门禁改为 Playwright fail-closed，移除系统 Chrome fallback，增加超时与统一资源回收。
- [x] 新增 `draft_retention` 模块；默认不清理，只在显式配置后删除可证明为空白、未绑定、非活动且已解锁的超额旧桌面草稿。
- [x] Shell 改为非重入、可见性感知的递归 `setTimeout` 调度。
- [x] 补齐次级按钮、焦点环和窄屏任务导航样式，提取共享值/DOM 工具。
- [x] 同步 `plan/soloplan/2026-07-14_newrust_quality_hardening.md` 与原子计划 009-015。

## 验证

- [x] Rust workspace format/check/test 通过；desktop-tauri 60 个测试通过，普通 workspace Clippy 退出码为 0。
- [x] Web build/unit/e2e、2573 个双语键、language gate、90 张 Playwright 截图和 93 条 UI baseline 通过。
- [x] 独立性边界扫描 221 个文件，父级 Python 禁止命中 0，7 组资源清单与试运行 `user_data` 保护通过。
- [x] 根 `AutoDesignMaker.exe --check-launcher`、根入口 smoke 和便携产品 smoke 通过；根 EXE SHA-256 为 `79bf065bc863cb7d010f85702582972bec6962f1555d5cc1a9bd3c4f8182955a`。
- [x] 本轮源码已构建并原子安装为便携开发快照：29,105,664 字节，SHA-256 `4c07249dcf8362524d68312bd069755fdba9280a72ede0d1a4f4741360b7e895`；`user_data` 402 个文件、19,210,560 字节按 `preserved_local` 保留。

## 当前状态

- [x] 开发态修复与验证完成，根原生 EXE 保持默认空白项目和 Rust 自有数据根。
- [ ] 正式 release gate 仍需在累计改动提交后，从干净工作树重新运行 `NEWrust/tools/verify-standalone.ps1` 生成 clean-clone 证据。
- [ ] 严格 `-D warnings` 仍会暴露本轮前已有的 workspace 风格警告；应另立低风险清理任务，不能混入功能修复。
- [ ] Unity 实机执行、真实 AI 提供方质量和 Step00-14 产物质量仍属于目标环境用户验收。
