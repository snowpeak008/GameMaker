# 2026-06-24-001 — bug收集文档7.md 第七轮修复

## 摘要

修复 `bug收集文档7.md` 中发现的 AI 补全链路端到端连通性问题：Codex 补全任务使用写权限 sandbox 可能导致 JSON 不从 stdout 返回，Claude Code model adapter 忽略调用方超时。

## 完成内容

- BUG-024：`ModelTask` 增加 `sandbox` 字段，默认仍为 `workspace-write`，保持既有文件生成任务行为不变。
- BUG-024：`run_codex_exec()` 改为使用 `task.sandbox`，不再硬编码 `workspace-write`。
- BUG-024：Step02 L5 实体补全创建 `ModelTask` 时显式设置 `sandbox="none"`，让 Codex 补全作为纯 stdout JSON 任务运行。
- BUG-025：`ClaudeCodeModelAdapter.generate()` 改用 `task.timeout_seconds`，不再硬编码 600 秒。
- 新增 `core/tests/unit/test_model_adapters.py`，覆盖 Codex 默认 sandbox、任务级 sandbox、Step02 supplement sandbox=none、Claude 任务超时透传。

## 验证

- `python -m pytest core\tests\unit\test_model_adapters.py -q`：4 passed。
- `python -m pytest core\tests\unit\test_l5_supplement.py -q`：29 passed。
- `python -m pytest core\tests\unit\test_model_adapters.py core\tests\unit\test_l5_supplement.py -q`：33 passed。
- `python -m pytest core\tests -q`：80 passed（仅 `.pytest_cache` 写入权限 warning）。
- `python -m black --check core\adapters\base.py core\adapters\codex\executor.py core\adapters\claude_code_model_adapter.py pipeline\step_02_design_review_freeze\supplement.py core\tests\unit\test_model_adapters.py core\tests\unit\test_l5_supplement.py`：通过。
- `python -m flake8 --max-line-length=100 --extend-ignore=E203 core\adapters\base.py core\adapters\codex\executor.py core\adapters\claude_code_model_adapter.py pipeline\step_02_design_review_freeze\supplement.py core\tests\unit\test_model_adapters.py core\tests\unit\test_l5_supplement.py`：通过。
- `python -m mypy --ignore-missing-imports --follow-imports=skip core\adapters\base.py core\adapters\codex\executor.py core\adapters\claude_code_model_adapter.py pipeline\step_02_design_review_freeze\supplement.py`：通过。
- `python -m compileall core pipeline tools\validators\pipeline_quality.py`：通过。
- `git diff --check`：通过，仅 LF/CRLF 提示。

## 后续关注

- 真实 Codex CLI 仍需在安装 Codex 且 `pipeline_adapter=codex` 的环境中跑一次 Step02，确认 stdout JSON 能被 `_parse_response()` 接收。
- 当前工作树仍包含前几轮 L5 补全开发未提交内容和用户侧旧 bug 文档删除状态；本轮没有回退这些外部状态。
