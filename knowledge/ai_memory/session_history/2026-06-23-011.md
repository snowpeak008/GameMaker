# 2026-06-23-011 — bug收集文档5.md 第五轮修复

## 摘要

修复 `bug收集文档5.md` 中针对 L5 实体 AI 补全新功能发现的 5 个问题：adapter 配置错误被吞、私有跨模块依赖、重试重复创建 adapter、Step02 默认拉起 AI、旧缓存缺少 `supplement_basis` 后失效。

## 完成内容

- BUG-018：`EntitySupplementAdapter._call_ai()` 现在会让 `ValueError/ImportError` 配置错误直接抛出，不再静默降级。
- BUG-019：Step01 新增公开 `pick_genre_template_key()`，Step02 supplement 改为依赖公开 API，不再导入 `_pick_template_key`。
- BUG-020：AI adapter 实例化移到重试循环外，单次补全最多复用同一个 adapter 实例重试 2 次。
- BUG-021：缺失 `pipeline_adapter` 时默认 `none`，Step02 L5 AI 补全默认关闭；GUI 下拉仍可显式选择 Codex/Claude/OpenAI。
- BUG-022：`validate_entity()` 不再要求 `supplement_basis` 必填，旧版 `ai_supplement_cache.json` 中缺 basis 的实体仍可命中缓存。
- 补充回归测试覆盖公开 genre API、旧缓存兼容、adapter 实例复用、无效 adapter 抛错、默认 none 和非法 adapter 保留。

## 验证

- `python -m pytest core\tests\unit\test_l5_supplement.py -q`：27 passed。
- `python -m pytest core\tests\unit\test_l5_supplement.py core\tests\unit\test_pipeline_optimization_helpers.py -q`：58 passed。
- `python -m pytest core\tests -q`：74 passed（仅 `.pytest_cache` 写入权限 warning）。
- `python -m compileall core pipeline tools\validators\pipeline_quality.py`：通过。
- 新增补全模块 black/flake8/mypy：通过。
- `git diff --check`：通过。

## 后续关注

- 本轮为了修复默认行为，`pipeline_adapter` 缺省值从 `codex` 改为 `none`；已有 `settings/project_settings.json` 中显式为 `codex` 的项目不受影响。
- 当前工作树还包含上一轮 L5 补全开发未提交内容，以及用户侧删除旧 bug 文档的状态；本轮没有回退这些外部状态。
