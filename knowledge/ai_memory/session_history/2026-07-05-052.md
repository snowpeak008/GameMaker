# Session 2026-07-05-052

## Summary

Surfaced game build bundle staging in the Slint desktop workbench.

## Completed

- Added `game-build-target-id`, `game-build-target-dir`, and `game-build-text` Slint properties.
- Added a Build Bundle input row with Stage Bundle button.
- Added `stage-game-build-bundle` Slint callback.
- Added `stage_game_build_bundle_for_ui(...)`.
- Desktop staging now loads the selected formal archive, creates the default Windows desktop build plan, and stages required artifacts into the selected bundle directory.
- Desktop smoke now verifies game bundle staging, manifest creation, and copied design artifact.
- Rebuilt and re-staged the isolated Rust desktop release exe.

## Verification

- `cargo check -p adm-desktop`: passed.
- `cargo run -p adm-desktop -- --smoke`: passed.
- `cargo fmt --check`: passed.
- `cargo check --workspace`: passed.
- `cargo test --workspace`: passed, 86 Rust unit tests.
- `cargo build -p adm-desktop --release`: passed.
- `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`: passed.
- `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`: passed.
- Staged release hash: `fnv64:ed8d684b00e38770`.

## Follow-up

- Add visible-window validation for two Rust windows selecting the same archive with owner details visible.
- Continue replacing POC text-heavy controls with richer workbench sections.
- Add real engine build adapters after bundle staging.
