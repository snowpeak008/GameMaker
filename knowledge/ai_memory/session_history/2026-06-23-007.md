# 2026-06-23-007 — bug收集文档3.md 第三轮修复

## 摘要

修复 `bug收集文档3.md` 中列出的 5 个 v5 后续回归问题，补充对应单元回归测试。

## 完成内容

- BUG-010：Step02 阶段分类移除宽泛 `"build"`，改为 `release_build` / `build_pipeline`，避免 `build_system_decision` 被误归为 `launch_ops`。
- BUG-011：Step01 模板缓存改为按文件签名刷新，并在 pytest autouse fixture 中清理缓存，避免测试间污染和文件创建后缓存不刷新。
- BUG-012：Step04 `MarketResearchSkill` 对 roguelike/fps/puzzle 都先尝试读取 `knowledge/market_data/{genre}.json`，缺失时再走本地 fallback。
- BUG-013：Step04 `asset.required_for_phase` 分类补齐 `progression/social/launch_ops`，与 Step02 六阶段逻辑对齐。
- BUG-014：Step05 `_verdict` 在 `warning_count > 0` 时返回 `WARN`，不再把 1-15 个 warning 标成 `PASS`。

## 验证

- `python -m pytest core\tests -q`：44 passed（仅 `.pytest_cache` 写入权限 warning）。
- 本次改动范围 `black --check` / `flake8` / `mypy --explicit-package-bases`：通过。
- `python -m core.main --from-step 0 --stop-step 6 --auto-approve --skip-preflight`：Step 00-06 全部 success。
- `python tools\validators\pipeline_quality.py --check plan-002`：通过，entity coverage 0.4563 >= 0.38。
- 最新指标：question coverage 0.8667、entity coverage 0.4563、requirement binding 0.9753、placeholder 0.0、asset count 56、Step05 warning/blocking 1/0。

## 后续关注

- 在 DesignEngine 录入真实 L5 实体后继续验证 v5 的高覆盖目标。
- 全仓 `pre-commit run --all-files` 仍受既有 flake8/mypy 问题阻塞，本轮只保证改动范围门禁通过。
