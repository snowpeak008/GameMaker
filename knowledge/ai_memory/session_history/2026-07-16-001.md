# 2026-07-16-001

## 摘要

完成 NEWrust v2 通用 GameSpec/流水线开发计划的主要实现闭环：在保持验收前不提交、不推送的约束下，落地 A02-A10、R0 harness、A08a-A08f R1 垂直切片、A09 跨品类评估与 A10 迁移/发布就绪模型。

## 用户确认

- [x] 用户确认 R1-C0 推荐组合为 `A + M + P1 + V2`。
- [x] 该组合作为 R1 通道防守冻结 fixture 的当前验收输入继续使用。
- [x] 验收前仍禁止仓库同步：不执行 commit、tag、push 或 GitHub Release。

## 完成内容

- [x] A02-A05：建立 GameSpec v2 验证、规范化哈希、ProductEnvelope/ExecutionBudget、能力决策图、ChangeKernel/SpecStore、有界 AI 与审计基础。
- [x] A06-A07：接入 D1-D4 双轨投影与 Step00-06 冻结规格编译，并产出 R1-C0 冻结规格 fixture。
- [x] A08a-A08f：拆分实现 Step07 美术规范/锚点、Step08-10 架构与任务合同、Step11 WorkspaceChangeSet 执行器、Step12 资产生产绑定、Step13 验收场景、Step14 打包与 R1 stop gate。
- [x] A09：实现八类规格级编译、三类全生产验证、网络协作架构级验证、反过拟合三层检查与 R1 数据校准报告。
- [x] A10：实现 sidecar-only v2 迁移预览/应用/回滚，以及 R2 发布就绪聚合门禁；正式发布签署仍要求 clean committed tree。
- [x] 修复 A09 暴露的过拟合问题：移除 Step07 对 `ecodome` 名称的调色板分支，并把 Step08-10 的样例资产 ID 改为通用命名。

## 关键边界

- [x] Python 历史记忆继续作为只读开发背景，不进入产品运行时，不可成为运行依赖。
- [x] A10 迁移采用 sidecar-only 策略，不覆盖原 ProjectState。
- [x] 正式 `tools/verify-standalone.ps1` 需要干净且已提交的工作树；当前验收前本地脏工作区不冒充正式发布证据。
- [x] CC-Panes 共享记忆环境变量缺失，本轮仅更新 NEWrust 本地静态记忆。

## 验证

- [x] `cargo fmt --all -- --check`：通过。
- [x] `cargo check --workspace --locked`：通过。
- [x] `cargo test -p adm-new-game-spec`：通过。
- [x] `cargo test -p adm-new-pipeline`：通过。
- [x] `cargo test --workspace --locked --quiet`：通过。

## 后续

- [ ] 用户验收当前本地实现。
- [ ] 验收通过后再进行提交前审计、正式 standalone 验证、提交、标签和远端同步。
- [ ] 若需要真实产品交付包，需要从 clean committed tree 运行 `NEWrust/tools/verify-standalone.ps1` 生成正式发布证据。
