# 会话记录：2026-07-05-029

## 摘要

继续 goal 模式推进 Rust 全项目重建，完成核心流水线的恢复增强：支持从指定 stage 重跑，并支持根据已保存的 `pipeline/run_report.adm` 找出最后失败或需 AI 介入的 stage 后重跑。该能力已接入 `adm-application`、CLI 和 Slint 桌面 POC，保持原有普通 `resume` 行为不变。

## 完成内容

- `adm-pipeline`
  - 新增 `PipelineGraph::downstream_stage_ids(...)`。
  - 新增 `PipelineRunState::rewind_to_stage(...)`。
  - 新增 `PipelineRunReport::from_report_text(...)`。
  - 新增 `PipelineRunReport::last_unsuccessful_stage_id(...)`。
  - 下游计算按依赖图处理，不按固定列表截断：
    - 重跑 `development` 会清除 `development/sdk/packaging`。
    - 保留独立已完成的 `assets`。
- `adm-application`
  - 新增 `CorePipelineServices::rewind_state_to_stage(...)`。
  - 新增 `AdmApplication::rerun_core_pipeline_stage(...)`。
  - 新增 `AdmApplication::resume_failed_core_pipeline(...)`。
  - 抽出 `preserved_ai_journal(...)`，只在 `design` 仍为 completed 时保留旧 AI journal。
  - 普通 `resume_core_pipeline(...)` 行为不变。
- `adm-cli`
  - 新增 `rerun-stage <archive_id> <stage_id> [data_root]`。
  - 新增 `resume-failed <archive_id> [data_root]`。
- `adm-desktop`
  - Slint 新增 `rerun-stage(string)` callback。
  - Slint 新增 `resume-failed-project()` callback。
  - Stage 列表每行新增 `Rerun` 按钮。
  - Archive 操作区新增 `Resume Failed` 按钮。
  - Desktop smoke 增加 `development` 指定阶段重跑验证。

## 验证

- `cargo test -p adm-pipeline`：通过，10 个测试。
- `cargo test -p adm-application`：通过，13 个测试。
- `cargo check -p adm-cli -p adm-desktop`：通过。
- `cargo run -p adm-desktop -- --smoke`：通过。
- CLI 手动验证：
  - `cargo run -p adm-cli -- demo-core CliRerunSmoke ...`：通过。
  - `cargo run -p adm-cli -- rerun-stage archive_1783214982260_34156_1 development ...`：通过，`rerun_results=3`。
- `cargo fmt --check`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 73 个 Rust 单元测试通过。

## 后续事项

- 将 stage detail 从纯文本继续拆成结构化字段和 artifact 列表。
- 升级项目列表、AI diagnostics、package、validation 为结构化列表或表格。
- 增加真实 AI 调用的显式手动入口，默认仍保持 dry-run / 不联网。
- 增强失败场景 fixture，让桌面 smoke 也覆盖 `Resume Failed` 的可视入口。
- 继续补齐可交付桌面 GUI 的配置、运行、校验和打包闭环。
