# 会话 2026-06-21-001

> 时间：2026-06-21  
> 类型：存档系统完善 + AI 适配器切换 + UI 优化 + Skill 库集成

---

## 做了什么

### 1. 存档管理完善

- `SaveManagerDialog.on_new_save` — 建槽后立即保存当前设计，新建后列表立即显示
- `ask_save_name(default=None)` — 支持默认名称参数，打开时全选文本
- `on_new_save` — 以当前项目名称为默认存档名
- 新增 `on_rename_selected` — 重命名选中存档（修改 manifest + 更新 index）
- 按钮行扩展为8列，新增「重命名」按钮
- 顶部栏：「保存」→「存档管理」，删除「打开」按钮（功能已并入存档管理）
- 清理了2个调试遗留的空存档（save_20260621_001829 / 001826）

### 2. 流水线 AI 适配器可选

- 新增 `core/adapters/claude_code_model_adapter.py`：实现 `ModelAdapter`，调用 `claude --print -p`
- `core/adapters/registry.py`：注册 `"claude"` + `SUPPORTED_ADAPTERS` 常量
- `core/runtime/preflight.py`：`load_project_settings()` 加 `pipeline_adapter` 字段
- `core/engines/generation.py`：第2938行改为通过 `get_adapter()` 调用，读取配置
- `core/ui/unity_config_dialog.py`：项目配置界面重设计（卡片布局）+ AI 适配器下拉
- `core/ui/pipeline_panel.py`：步骤详情新增「AI 适配器」显示行

### 3. Skill 库集成

- 新建 `knowledge/skills/art/`，从官方库拉取：
  - `frontend-design/SKILL.md` ← anthropics/skills
  - `imagegen/SKILL.md` ← openai/codex
- 新建 `core/skill_loader.py`：读取 SKILL.md 的工具函数
- `core/engines/generation.py`：步骤 04/08 注入 frontend-design，步骤 09/11 注入 imagegen

---

## 修改的文件

- `core/adapters/claude_code_model_adapter.py`（新增）
- `core/adapters/registry.py`
- `core/engines/generation.py`
- `core/runtime/preflight.py`
- `core/skill_loader.py`（新增）
- `core/ui/app_window.py`
- `core/ui/pipeline_panel.py`
- `core/ui/save_manager_dialog.py`
- `core/ui/unity_config_dialog.py`
- `knowledge/skills/art/frontend-design/SKILL.md`（新增）
- `knowledge/skills/art/imagegen/SKILL.md`（新增）

---

## 下次会话应继续

- [ ] 验证 AI 适配器切换是否生效（codex vs claude）
- [ ] 验证 skill_guidance.md 是否正确写入步骤输出目录
