# 2026-06-26-006：执行 ai_config_manager 多 AI 配置管理

## 摘要

按 `plan/ai_config_manager/PLAN.md` 完成 GUI 侧 AI 配置管理：新增可命名配置档、LLM/图片模型分离配置、active profile 覆盖旧 `api_config.toml` 且保留回退路径，并将真实图片生成开关从环境变量迁移到当前 AI 配置档。

## 完成内容

- ✅ 新增 `core/config/ai_profiles.py`，支持 `settings/ai_profiles.json` 的创建、读取、保存、激活配置档、LLM/image 配置解析和密钥遮罩
- ✅ `core/config/loader.py` 支持 active AI profile 优先覆盖 `llm` / `image` / `image2`，配置不可用时自动回退旧 `api_config.toml`
- ✅ `core/config/integrity.py` 在完整性校验时确保本地 AI profile 文件可初始化
- ✅ 新增 `core/ui/ai_config_dialog.py`，提供配置档列表、新增/删除/激活、LLM 与图片模型表单、API Key 密码输入和图片启用开关
- ✅ `core/ui/pipeline_panel.py` 在项目配置旁新增 `AI 配置` 按钮
- ✅ `core/engines/generation.py` 的图片生成启用逻辑优先读取 active profile，缺少 profile 时兼容旧环境变量
- ✅ `tools/asset_production/image_tool.py` 使用配置档里的图片模型，不再硬编码 `gpt-image-2`
- ✅ `.gitignore` 忽略 `settings/ai_profiles.json`，避免提交本地 API Key
- ✅ 扩展 `core/tests/unit/test_config_loader.py`，覆盖 profile 缺失回退、active profile 覆盖、image2 映射、图片开关和默认文件创建

## 自查修复

- ✅ 发现旧 `image2` 回退测试未隔离本地 `settings/ai_profiles.json`，后续用户填入真实 AI 配置后可能污染测试结果；已改为临时 missing profile 路径

## 验证

- ✅ `python -B -m pytest core\tests\unit\test_config_loader.py core\tests\unit\test_hades_quality_optimization.py::test_image_generation_manifest_is_skipped_by_default -q`：15 passed
- ✅ `python -B -m py_compile core\config\ai_profiles.py core\ui\ai_config_dialog.py core\config\loader.py core\config\integrity.py core\ui\pipeline_panel.py core\engines\generation.py tools\asset_production\image_tool.py`：通过
- ✅ `PYTHONPYCACHEPREFIX=.cache\pycache python -B -m compileall core pipeline tools\validators\pipeline_quality.py tools\asset_production`：通过
- ✅ `python -B -m pytest -q`：129 passed
- ✅ `git diff --check`：通过（仅 CRLF 工作区提示）

## 后续关注

- [ ] 建议后续人工打开 GUI，点击 `AI 配置` 做一次视觉和保存/激活流程验收
- [ ] `settings/ai_profiles.json` 是本地敏感配置文件，必须继续保持忽略，不提交
- [ ] 本轮只按用户要求同步记忆，代码改动尚未提交；提交前需排除 `plan/`、bug 文档和运行时 draft
- [ ] CC-Panes 共享记忆池本次因环境变量缺失未写入
