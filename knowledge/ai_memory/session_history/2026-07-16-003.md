# Session 2026-07-16-003

## Summary

Completed the post-review NEWrust v2 second-development repair pass. The review's two high-risk regressions were fixed, the v2 product path now has a real UI/project-config switch, Step11 stop/checkpoint behavior is covered, and W1.4 was corrected back to unfinished instead of being over-marked.

## Completed

- [x] Changed v2 Step12 product execution to wait for explicit asset confirmation by default; `auto_accept` now requires an explicit approval file with `explicitAutoAccept=true`.
- [x] Migrated the R1-C0 frozen fixture so Step13 manual review and performance budget gates are present in the fixture itself, not only injected by tests.
- [x] Added `game_spec_v2_enabled` to runtime project settings, Tauri config commands, and the project-config UI, with compatibility for legacy `game_spec_v2.enabled` shapes.
- [x] Bridged the product work-unit stop token into v2 Step11 and fixed v2 product stage status normalization so `Stopped` is not reported as a generic failure.
- [x] Made v2 Step11/12 checkpoint reconciliation fail closed instead of reading legacy work-unit journals.
- [x] Rejected unknown v2 Step07 style-confirmation ids; only the complete `v2_anchor_set` can be confirmed.
- [x] Made Step13 unknown `performanceBudgetRefs` fail instead of being silently dropped.
- [x] Made the Step07 VLM cache write through a temporary file and rename to avoid truncated JSON after crashes.
- [x] Added A09 mutation-rejection negative coverage and restored W1.4 in `plan/newdesignplan/remaining_work_checklist.md` to incomplete.

## Verification

- [x] `cargo fmt --all`
- [x] `npm.cmd test -- settings-style`
- [x] `cargo test -p adm-new-application project_settings_persist_game_spec_v2_switch_without_machine_paths --locked`
- [x] `cargo test -p adm-new-tauri-commands project_config_commands_save_and_run_preflight --locked`
- [x] `cargo test -p adm-new-pipeline --test product_executor --locked`
- [x] `cargo test -p adm-new-pipeline --test step13_v2 --locked`
- [x] `cargo test -p adm-new-pipeline --test a09_cross_genre_evaluation --locked`
- [x] `cargo check --workspace --locked`
- [x] `npm.cmd test`
- [x] `npm.cmd run i18n-test`
- [x] `npm.cmd run build`
- [x] `cargo test --workspace --locked`

## Remaining

- [ ] W1.4 is still open: `write_compile_smoke_evidence` remains evidence bookkeeping only and does not spawn a real build or smoke process.
- [ ] W1.3 still has the previously identified deeper execution-model limitation around isolated workspace copying/atomic rollback.
- [ ] Real Codex/Claude CLI bridge, true VLM judging, Unity/headless loading, EXE smoke, and manual playtest signing remain release/acceptance gates.
- [ ] No commit, tag, push, or repository synchronization was performed.
