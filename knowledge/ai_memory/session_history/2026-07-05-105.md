# Session 2026-07-05-105

## Summary

Added guarded desktop local Unity process launch for Unity build and runtime validation in the Rust rebuild.

## Completed

- [x] Added Slint desktop callbacks and buttons `run-unity-build` / `Run Unity` and `run-unity-runtime-validation` / `Run Runtime`.
- [x] Added desktop helpers that run `LocalProcessEngineBuildRunner` for guarded local Unity build execution and commit build history into `package/engine_build_history.adm`.
- [x] Added desktop guarded runtime validation execution that launches Unity, reads Unity's generated runtime result file, and commits it into `validation/runtime_execution_results.adm`.
- [x] Reused selected-archive lock release/relock behavior while local Unity processes run.
- [x] Added fake Unity child-process support for desktop smoke by copying the current executable to a `fake-unity/Editor/Unity.exe` path and detecting that invocation shape.
- [x] Extended desktop smoke to cover guarded local process build execution, guarded runtime validation execution, engine history awareness, production readiness, and package table awareness without requiring a real Unity install.
- [x] Updated `RUST/README.md` to document desktop `Run Unity` and `Run Runtime` behavior.
- [x] Rebuilt and re-staged the default Rust desktop release.

## Verification

- [x] `cargo fmt --check`: passed.
- [x] `cargo check --workspace`: passed.
- [x] `cargo test --workspace`: passed, 131 Rust unit tests.
- [x] `cargo run -q -p adm-desktop -- --smoke`: passed, including fake Unity local process build/runtime execution.
- [x] Cross-layer check completed for Slint callbacks -> desktop helpers -> `LocalProcessEngineBuildRunner` -> archive commits -> package inspection -> README.
- [x] `cargo build -p adm-desktop --release`: passed.
- [x] `stage-desktop-release`: passed; release hash `fnv64:01e099ca8ea41b42`, bytes `22491648`.
- [x] `release-doctor`: `ready=true`, release hash `fnv64:01e099ca8ea41b42`.
- [x] `delivery-doctor`: `ready=true`, with runtime execution evidence still verified in game, SDK, and Unity outputs.
- [x] `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`: passed.

## Follow-up

- [ ] Run desktop `Run Unity` and `Run Runtime` against a real Unity installation and confirm actual editor output ingestion.
- [ ] Continue real Unity Editor build validation on a machine with Unity installed.
- [ ] Continue real-provider acceptance after safe endpoint/model/secret setup.
- [ ] Decide how to track or package the still-untracked `RUST/` tree in the parent repository.
- [ ] Keep the full project completion goal active until the project is genuinely complete.
