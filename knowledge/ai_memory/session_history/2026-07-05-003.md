# 会话记录：2026-07-05-003

## 摘要

继续 Rust 全项目重建，从基础骨架推进到可运行的核心业务闭环：新增应用服务层、可运行 CLI、正式存档读取/列表/导出、设计到打包的确定性流水线、AI 输出验证门、pipeline gate 和 UI 状态模型。

## 完成内容

- 新增 `adm-application` crate，作为 GUI/CLI 调用的应用服务层。
- `adm-application` 已能创建项目、列出项目、按 ID 加载项目、导出项目包、运行核心流水线。
- `adm-cli` 新增命令：
  - `demo-core [title] [data_root]`
  - `list [data_root]`
  - `export <archive_id> <target_file> [data_root]`
- `adm-archive` 补充：
  - manifest 文本解析
  - 按 `ArchiveId` 加载正式存档
  - 列出正式存档
  - 工作区文本读取
  - 提交时同步删除已从工作区删除的文件
  - `.admproj` 单文件导出包雏形
- `adm-design` 补充游戏设计 brief、设计项目、质量评分、AI 介入判断和设计文档渲染。
- `adm-development` 补充从核心循环生成开发计划的模型。
- `adm-assets` 补充资产计划模型。
- `adm-sdk` 补充默认 SDK 知识库、查询和渲染。
- `adm-packaging` 补充打包 manifest 渲染。
- `adm-validation` 补充 validation report。
- `adm-pipeline` 补充 pipeline run report、gate decision、stage gate、gate evaluator 和 gate 阻断测试。
- `adm-ai` 补充 AI output validator，强制 AI 输出必须 validated 后才能 accepted。
- `adm-ui-model` 补充项目列表项、pipeline 状态视图和 blank shell state。

## 验证

- `cargo fmt --check`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 27 个 Rust 单元测试通过。
- CLI 创建/列表/导出真实运行通过：
  - `cargo run -p adm-cli -- demo-core "Export Game" <temp-data-root>`
  - `cargo run -p adm-cli -- list <temp-data-root>`
  - `cargo run -p adm-cli -- export <archive_id> <target-file> <temp-data-root>`
- `demo-core` 生成的正式存档内容包括：
  - `ai/intervention.adm`
  - `assets/plan.adm`
  - `design/project.adm`
  - `development/plan.adm`
  - `package/manifest.adm`
  - `pipeline/run_report.adm`
  - `project/status.adm`
  - `sdk/index.adm`
  - `validation/report.adm`

## 后续事项

- 下一步应继续把 demo-core 拆成真实业务阶段服务，而不是长期停留在单函数示范流水线。
- `adm-archive` 的 `.admproj` 当前是文本包雏形，后续需要二进制安全编码、导入流程和校验 hash。
- `adm-ai` 后续需要真实 Provider 接入前的 task persistence、retry、budget、model capability routing。
- `adm-pipeline` 后续需要 artifact registry、stage resume、run state persistence 和失败恢复。
- `adm-desktop` 仍是占位；应在 application service 稳定后接 Slint POC。
