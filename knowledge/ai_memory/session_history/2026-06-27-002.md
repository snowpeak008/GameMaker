# Session 2026-06-27-002

## Summary

Executed `plan/ui_improvement_v1`: improved AI configuration visibility in the main UI, enhanced the unified AI config dialog, audited deprecated UI candidates, updated docs, and isolated tests from local ignored AI config.

## Completed

- Added a bottom main-window status bar showing active AI Profile/adapter, DevFlow progress, and system state.
- Wired AI status click to the unified AI config dialog and progress click to the pipeline panel, selecting the first incomplete step when available.
- Enhanced `AIConfigUnifiedDialog` with active Profile highlighting, visible validation status, asynchronous CLI availability checks, an `应用` save-without-close button, save toast, and safer close cleanup for pending validation callbacks.
- Audited UI components: `bottom_panel.py` and `embedded_interview.py` remain runtime-referenced; `workbench.py` has no main-window entry reference but was retained for a separate deletion audit.
- Updated `README.md`, `docs/AI_CONFIG_GUIDE.md`, and `AI_README.md` for the new status-bar entry and UI component map.
- Added test isolation in `core/tests/conftest.py` so ignored local `settings/ai_config.json` cannot affect unit test defaults.

## Verification

- `python -B -m py_compile core\ui\main_window.py core\ui\ai_config_unified_dialog.py core\tests\conftest.py`: passed.
- Targeted previously failing local-config-polluted tests: 3 passed.
- `python -B -m pytest -q`: 142 passed.
- `python -B -c "from core.ui.main_window import MainWindow; from core.ui.ai_config_unified_dialog import AIConfigUnifiedDialog; print('ui imports ok')"`: passed.
- `git diff --check`: passed with only existing CRLF working-copy warnings.

## Follow-up

- Manually click-test the bottom status bar, AI config save/apply flow, and progress jump in the GUI.
- Continue excluding `plan/`, bug documents, runtime drafts, and ignored local AI config files from commits.
- CC-Panes shared memory was skipped because required environment variables were absent.
