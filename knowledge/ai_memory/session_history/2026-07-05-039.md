# 会话记录：2026-07-05-039

## 摘要

继续 goal 模式推进 Rust 全项目重建，将桌面程序发布打包能力从 CLI 接入到 Slint desktop。桌面工作台现在可以填写发布源 exe 和目标目录，点击 `Stage Release` 后生成隔离的 `AutoDesignMaker-rust.exe` 发布目录，并明确保持根目录旧 `AutoDesignMaker.exe` 不被修改。

## 完成内容

- `adm-desktop`
  - 新增 `adm-packaging` 依赖，桌面端复用 `stage_desktop_release(...)`。
  - Slint UI 新增：
    - `release-source-exe`
    - `release-target-dir`
    - `release-text`
    - `stage-desktop-release()` callback
    - `Release Exe` 输入行和 `Stage Release` 按钮。
  - `run_window()` 初始化默认发布源为当前运行 exe，默认目标为 `dist/AutoDesignMaker-rust`。
  - 新增 `stage_desktop_release_for_ui(...)`，输出发布目录、exe、manifest、bytes/hash 和 `legacy_root_exe=not_modified`。
  - 新增 `default_desktop_release_source()` 和 `default_desktop_release_dir()`。
  - desktop smoke 使用临时 fake exe 验证发布目录、`AutoDesignMaker-rust.exe`、`release-manifest.adm` 和 legacy 根 exe 不修改标记。
- 发布产物
  - 重新构建 release。
  - 重新 staging 到 `RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`。
  - 新 staged hash：`fnv64:8dfe7a5357c8e1e3`。

## 验证

- `cargo check -p adm-desktop`：通过。
- `cargo run -p adm-desktop -- --smoke`：通过。
- `cargo fmt --check`：通过。
- `cargo test --workspace`：通过，当前 75 个 Rust 单元测试通过。
- `cargo check --workspace`：通过。
- `cargo build -p adm-desktop --release`：通过。
- `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`：通过。
- `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`：通过。

## 后续事项

- 继续把桌面 GUI 从 POC 推进到可用工作台：减少手填路径，增加更明确的操作分区和校验结果。
- 增加可视窗口手工验收：两个 Rust 窗口选择同一 archive 时，第二个窗口必须显示锁冲突和 owner 信息。
- 后续可以把发布打包结果扩展为结构化文件列表，而不是只显示文本摘要。
