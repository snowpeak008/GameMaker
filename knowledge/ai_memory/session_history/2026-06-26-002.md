# 2026-06-26-002 — 执行 manual_style_confirmation

## 摘要

按 `plan/manual_style_confirmation/PLAN.md` 完成手动美术风格确认开发。计划目录仅作为本地任务输入，不提交到 git。CC-Panes 共享记忆池因环境变量为空未写入，本轮记忆写入项目本地 `knowledge/ai_memory`。

## 阶段记录

### TASK-1：流水线扩展到 Step00-17

- 新增 `pipeline/step_07_art_style_generation/`，生成 `style_options.json`、`generation_log.json`、`generated_images_manifest.json`、`style_options.md` 和本地确定性 PNG 预览。
- 新增 `pipeline/step_08_art_style_confirmation/`，在人工门禁启用且未确认时返回 `waiting_confirmation`，在门禁跳过或已有确认时写入/复用 `style_confirmation.json`。
- 原 Step07-15 整体后移为 Step09-17，并同步 `core/registry.py`、`pipeline/_registry.json`、`pipeline/artifact_layer/registry.json`、`dependency_graph.json` 和步骤 README。
- `core/main.py` 的默认停止步改为 `max_step_number()`，新增 `--skip-all-gates` / `--skip-gate-08`，并在 `waiting_confirmation` 时停止后续 review/validator，等待 GUI 或人工确认。

### TASK-2：人工确认配置与 GUI

- `settings/app.toml` 和默认配置新增 `[manual_gates]`、`[art_style_generation]`。
- 新增 `core/ui/style_confirmation_dialog.py`，支持预览风格候选、选择风格、填写批注、确认或重新生成。
- `core/ui/pipeline_panel.py` 增加“跳过人工确认”选项，能在 Step08 `waiting_confirmation` 后打开确认对话框；确认后重跑 Step08，重新生成会回退重跑 Step07-08。
- 修复 Step08 插件重跑时 `run_import_step()` 重置阶段目录导致手动 `style_confirmation.json` 被删除的问题：插件在导入前保存已批准确认，并在导入后恢复。

### TASK-3：后移阶段兼容

- `core/engines/generation.py` 新增风格生成/确认业务逻辑，并将旧程序计划、美术计划、资产对齐、执行、验证、打包、补丁输出映射到 Step09-16。
- `core/source/importer.py` 改为从 `pipeline/artifact_layer/registry.json` 读取制品注册表，保证新 Step07/08 能正确引用上游制品。
- `core/save/manager.py`、`core/artifact/reviewer.py`、`core/artifact/validator.py`、`core/source/importer.py` 改用动态 `max_step_number()` 处理最终审计阶段。
- `core/engines/execution_objects/integration.py` 的 rollback plan 证据迁移到 Stage16。

## 文档与记忆同步

- 更新 `AI_README.md`、`pipeline/README.md`、`core/README.md`、`pipeline/artifact_layer/README.md`，明确当前为 Step00-17。
- 更新 `knowledge/ai_memory/project_understanding/key_files.md` 与 `architecture.md`，修正旧的 16 阶段、旧 Step07-15 映射和旧 `artifact_layer/registry.json` 路径。

## 验证

- `python -B -m pytest core\tests\unit\test_manual_style_confirmation.py core\tests\integration\test_plugins.py -q`：9 passed。
- `python -B -m pytest core\tests\unit\test_manual_style_confirmation.py -q`：6 passed。
- `python -B -m pytest -q`：120 passed。
- `PYTHONPYCACHEPREFIX=.cache\pycache python -B -m compileall core pipeline`：通过，源树无 `__pycache__` 残留。
- `python -B -m flake8 ... --select=F`：通过。
- JSON 注册表校验：`pipeline/_registry.json`、`pipeline/artifact_layer/registry.json`、`pipeline/artifact_layer/dependency_graph.json` 均通过。
- 依赖图检查：`max_step 17`，`errors []`。
- 真实流水线验证：`python -B -m core.main --from-step 0 --stop-step 8 --skip-preflight --auto-approve --skip-all-gates`，Step00-08 全部 success。
- `git diff --check`：通过，仅 CRLF 工作区提示。

## 自查发现与修复

- 初次只跑 Step07-08 时预检阻止，因为当前 draft 缺少 Step06 artifact validation；改用 Step00-08 真实段验证。
- 发现 Step08 手动确认文件会被导入重置清掉，已加插件保留逻辑和回归测试。
- `black` 在当前环境连 `--version` 都超时，未作为阻断项；本轮用 compileall、flake8 F 类、pytest、真实流水线段验证兜底。
- `compileall` 初次在源树生成 `__pycache__`，已路径校验后清理 41 个目录，并改用 `PYTHONPYCACHEPREFIX=.cache\pycache` 复跑。

## 后续关注

- GUI 需要人工点选 Step08 对话框做一次视觉/交互验收。
- 后续提交前继续确认 `plan/manual_style_confirmation/`、其他 `plan/` 临时目录、bug 文档和 `settings/api_config.toml` 不进入暂存区。
- CC-Panes 共享记忆池本次因环境变量缺失未写入。
