# 会话记录：2026-07-05-004

## 摘要

继续推进 Rust 全项目重建，把核心闭环从 demo 函数推进到更完整的产品服务框架：拆出核心阶段服务，补 `.admproj` V2 可校验导入导出，补 pipeline artifact registry/run state，补 AI task journal/budget/router，并扩展 CLI import。

## 完成内容

- `adm-application`
  - 新增 `CorePipelineServices`，将核心流水线输出构建从 `run_core_pipeline()` 中拆出。
  - 核心流水线现在写入：
    - `pipeline/run_state.adm`
    - `pipeline/artifact_registry.adm`
    - `ai/journal.adm`
  - `ProjectPipelineReport` 增加 artifact registry、run state、AI journal。
  - 新增 `import_project()` 应用服务。
- `adm-archive`
  - `.admproj` 导出升级为 `ADM_PACKAGE_V2`。
  - 导出包按文件记录 `path`、`hash`、hex 编码 `bytes`，支持二进制内容。
  - 新增 `import_archive_package()`，导入时校验 hash。
  - 新增 `write_workspace_bytes()`。
- `adm-pipeline`
  - 新增 `ArtifactRecord`、`ArtifactRegistry`。
  - 新增 `PipelineRunLifecycleStatus`、`PipelineRunState`。
  - `PipelineRunState` 支持 render/from_state_text，作为后续 resume 的基础。
- `adm-ai`
  - 新增 `AiTaskStatus`、`AiTaskRecord`、`AiTaskJournal`。
  - 新增 `AiBudget`。
  - 新增 `AiProviderRouter`，按 capability 选择 Provider 并记录预算失败/Provider 失败。
- `adm-cli`
  - 新增 `import <package_file> [data_root]`。
  - 完成 create/list/export/import CLI 项目迁移闭环。

## 验证

- `cargo fmt --check`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 34 个 Rust 单元测试通过。
- 真实 CLI 链路通过：
  - `demo-core "Portable Game" <source_root>`
  - `export <archive_id> <package> <source_root>`
  - `import <package> <target_root>`
  - `list <target_root>`
- CLI 运行结果确认目标数据根能列出导入后的正式存档。

## 后续事项

- 下一步可进入 Slint POC，让桌面壳接入 `adm-application` 的项目列表、创建、导入、导出和运行核心流水线。
- 真实 AI Provider 接入前，应继续扩展 AI task persistence、retry、provider config 与 secret ref 解析。
- pipeline resume 目前已有 `PipelineRunState` 文本基础，但还没有按 run_state 恢复执行的 runner。
- `.admproj` V2 已二进制安全，但还需要包级 manifest/hash、版本迁移策略和损坏包诊断。
