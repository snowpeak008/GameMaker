# 会话记录：2026-07-05-031

## 摘要

继续 goal 模式推进 Rust 全项目重建，将 Slint 桌面的项目列表、AI provider diagnostics、package 文件清单和 validation issue 显示升级为结构化 row model。旧文本属性继续保留用于 smoke、日志和兼容输出。

## 完成内容

- `adm-desktop` Slint UI
  - 新增 `ProjectRow`。
  - 新增 `AiProviderRow`。
  - 新增 `PackageFileRow`。
  - 新增 `ValidationIssueRow`。
  - 新增 `project-items`。
  - 新增 `ai-provider-items`。
  - 新增 `package-file-items`。
  - 新增 `validation-issue-items`。
  - Project 行内新增 `Select` 按钮，直接设置 `selected-archive-id` 并触发 `select-project()`。
  - AI config 面板改为 summary + provider rows。
  - Package/Validation 面板改为 summary + rows。
- `adm-desktop` Rust
  - 新增 `PackageFileItem`。
  - 新增 `ValidationIssueItem`。
  - `DesktopRunSummary` / `ProjectInspection` 携带 package files 和 validation issues。
  - 新增 `apply_project_items(...)`。
  - 新增 `apply_ai_diagnostics(...)` / `apply_ai_provider_items(...)`。
  - 新增 `apply_package_files(...)`。
  - 新增 `apply_validation_issues(...)`。
  - 新增 manifest / validation report 文本解析 helper。
  - `adm-desktop` 新增直接依赖 `adm-validation`，用于结构化映射 validation report。
- smoke 覆盖
  - 验证 AI provider row model。
  - 验证 project row model。
  - 验证 package file row model：10 行。
  - 验证 validation issue row model：0 行。

## 验证

- `cargo check -p adm-desktop`：通过。
- `cargo run -p adm-desktop -- --smoke`：通过。
- `cargo fmt --check`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 73 个 Rust 单元测试通过。

## 后续事项

- 增强失败场景 fixture，让桌面 smoke 覆盖 `Resume Failed` 的可视入口。
- 增加真实 AI 调用的显式手动入口，默认仍保持 dry-run / 不联网。
- 继续补齐可交付桌面 GUI 的配置、运行、校验和打包闭环。
