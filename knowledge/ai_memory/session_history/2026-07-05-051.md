# Session 2026-07-05-051

## Summary

Added game build bundle staging beyond package planning.

## Completed

- Added `GameBuildBundle` to `adm-packaging`.
- Added `stage_game_build_bundle(...)` with path safety via `ensure_within_root`.
- The staging API copies required artifacts from a formal archive content root into `bundle/content/...`.
- The staging API writes `game-build-manifest.adm` with target metadata, file hashes, byte counts, and bundle hash.
- Added tests for successful bundle staging and missing required artifact rejection.
- Added CLI command `stage-game-build-bundle <archive_id> <target_id> <target_dir> [data_root]`.
- Verified the CLI command against a real temporary archive created by `demo-core`.
- Rebuilt and re-staged the isolated Rust desktop release exe.

## Verification

- `cargo test -p adm-packaging`: passed, 8 tests.
- `cargo check -p adm-cli`: passed.
- `cargo run -p adm-cli -- demo-core BundleDemo ...`: passed.
- `cargo run -p adm-cli -- stage-game-build-bundle archive_... windows_desktop_playable ...`: passed, staged 4 files.
- `cargo fmt --check`: passed.
- `cargo check --workspace`: passed.
- `cargo test --workspace`: passed, 86 Rust unit tests.
- `cargo build -p adm-desktop --release`: passed.
- `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`: passed.
- `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`: passed.
- Staged release hash: `fnv64:a32343a5d9791442`.

## Follow-up

- Surface game build bundle staging in the desktop workbench.
- Add visible-window validation for two Rust windows selecting the same archive with owner details visible.
- Continue replacing POC text-heavy controls with richer workbench sections.
