# 会话记录：2026-07-05-034

## 摘要

继续 goal 模式推进 Rust 全项目重建，新增 Rust 桌面程序的隔离 release staging 出口。该出口会把已构建的 `adm-desktop.exe` 复制为 `RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`，并生成 manifest/readme；它明确不修改根目录旧 `AutoDesignMaker.exe`。

## 完成内容

- `adm-packaging`
  - 新增 `DesktopReleaseSpec`。
  - 新增 `DesktopReleaseBundle`。
  - 新增 `stage_desktop_release(...)`。
  - release staging 会复制源 exe、计算 hash、写入 `release-manifest.adm` 和 `README.txt`。
  - manifest 写入 `legacy_root_exe=not_modified`，明确隔离旧入口。
  - 新增单元测试覆盖成功 staging 和缺失源 exe 拒绝。
- `adm-cli`
  - 增加 `adm-packaging` 依赖。
  - 新增 `stage-desktop-release <desktop_exe> [target_dir]` 命令。
  - 默认输出目录为 `RUST/dist/AutoDesignMaker-rust/`。
  - 命令输出 release dir、exe、manifest、readme、bytes、hash。
- `README`
  - 新增隔离 Rust 桌面发布流程。
  - 明确 staged exe 名称为 `AutoDesignMaker-rust.exe`。
  - 明确命令不会修改旧根目录 `AutoDesignMaker.exe`。
- 生成验证产物
  - `RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`
  - `RUST/dist/AutoDesignMaker-rust/release-manifest.adm`
  - `RUST/dist/AutoDesignMaker-rust/README.txt`

## 验证

- `cargo test -p adm-packaging`：通过，4 个测试。
- `cargo check -p adm-cli`：通过。
- `cargo build -p adm-desktop --release`：通过。
- `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`：通过。
- `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`：通过。
- `cargo fmt --check`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 75 个 Rust 单元测试通过。

## 后续事项

- 继续补齐桌面 GUI 的真实运行、校验、打包闭环。
- 增加发布目录的人工可视化启动验收，确认窗口行为和存档锁交互。
- 后续若要替换根目录正式入口，应单独做显式迁移步骤，不能隐式覆盖旧 `AutoDesignMaker.exe`。
