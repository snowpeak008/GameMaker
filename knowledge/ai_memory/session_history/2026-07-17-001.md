# Session 2026-07-17-001

## Summary

Closed the B6 and W2.1 follow-up issues, then continued the v2 product-path work by completing W1.3 filesystem workspace semantics and wiring W1.2 real Codex/Claude development CLI execution into GameSpec v2 Step11 through an application-layer `WorkspaceTaskAgent`.

## Completed

- [x] Added frozen Step08-10 source-hash validation before Step11/12 reuse existing `stage_10` artifacts; Step11/12 now fail closed if the frozen GameSpec changed after Step10, and the old `stage_10` files are not overwritten.
- [x] Added a product regression test for Step10 hash drift and preserved `trusted_task_graph.json` content on rejection.
- [x] Added Chinese multibyte overlong project-name migration coverage; the test validates no panic, bounded `SpecId`, and successful GameSpec canonicalization.
- [x] Completed W1.3 product-path workspace semantics: target is copied into an isolated work tree, operations apply there first, target replacement uses a backup rollback path, and conflict rejection leaves target unchanged.
- [x] Added `WorkspaceTaskAgent` injection to `ProductPipelineExecutor` and made v2 Step11 prefer an injected agent before the local filesystem fallback.
- [x] Implemented `WorkspaceTaskAgent` for `AiDevelopmentWorkUnitExecutor`; real Codex/Claude CLI execution now maps to `WorkspaceTransactionResult` with scope evidence, committed/rejected outcome, trusted-test hashes, and side-effect state.
- [x] Added `workspace_task_agent_from_config(...)` and connected it in the Tauri pipeline command so UI runs can inject the real v2 Step11 agent when the development CLI configuration is available.
- [x] Updated `plan/newdesignplan/remaining_work_checklist.md`: W1.2 and W1.3 are now source-level complete; W1.4 remains open because the fallback still writes bookkeeping evidence only and complete playable smoke is not implemented.

## Verification

- [x] `cargo fmt --all`
- [x] `cargo test -p adm-new-pipeline filesystem_workspace_agent --locked`
- [x] `cargo test -p adm-new-pipeline --test product_executor game_spec_v2_product_step11_rejects_stage10_source_hash_drift_without_overwrite --locked`
- [x] `cargo test -p adm-new-pipeline --test a10_migration_release a10_migration_handles_overlong --locked`
- [x] `cargo test -p adm-new-pipeline --test product_executor --locked`
- [x] `cargo test -p adm-new-pipeline --locked`
- [x] `cargo check -p adm-new-application --locked`
- [x] `cargo check -p desktop-tauri --locked`
- [x] `cargo test -p adm-new-application development_executor_maps_workspace_task_preflight_failure_to_v2_transaction --locked`
- [x] `cargo check --workspace --locked`
- [x] `cargo test --workspace --locked`

## Remaining

- [ ] W1.4 remains open: the injected real agent path has Unity batchmode compile verification, but the default filesystem fallback still writes `compile_smoke.json` bookkeeping and full playable smoke is not connected.
- [ ] W1.5 remains open: legacy callers still need an explicit migration/retention decision after W1.2-W1.3.
- [ ] W4 release-gate work remains: true VLM judging, real headless/engine asset loading, real scene execution, standalone verification, EXE smoke, and manual playtest signing.
- [ ] No commit, tag, push, or repository synchronization was performed.
