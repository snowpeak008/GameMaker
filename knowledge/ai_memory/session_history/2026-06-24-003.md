# 2026-06-24-003 — 执行 template_l5_expansion：内置模板 L5 实体覆盖扩展

## 摘要

执行 `plan/template_l5_expansion` 的全部阶段开发计划，为内置项目模板补齐 L5 `designEntities`，提升 Step 02-06 的实体驱动输出质量。`plan/template_l5_expansion/` 仅作为本地开发计划输入，不提交到 git。

## 完成内容

- Phase 1：将 `dead_cells`、`hades_l5_partial`、`sekiro`、`crossy_road`、`apex_legends`、`clash_royale` 等 partial 模板补到 39 节点 complete 标准。
- Phase 1 质量对齐：修正 `elden_ring` 既有 complete 实体中 3 点曲线、循环步骤不足和缺失 `supplement_basis` 的历史结构问题。
- Phase 2：为 8 个 Indie 模板补齐 P0 16 核心节点实体。
- Phase 3：为 7 个 3A 模板补齐 P0 16 核心节点实体。
- Phase 4：为服务型、Midcore 和 8 个 IAA 超休闲模板补齐 P0 16 核心节点实体。
- 新增 `core/tests/unit/test_template_l5_expansion.py`，覆盖 complete/P0 覆盖率、schema-kind 匹配、实体 ID、曲线/循环/遭遇/系统卡结构。

## 验证

- `python -m pytest core\tests\unit\test_template_l5_expansion.py -q`：2 passed。
- `python -m pytest core\tests -q`：82 passed，仅 `.pytest_cache` 写入权限 warning。
- `python -m black --check core\tests\unit\test_template_l5_expansion.py`：通过。
- `python -m flake8 --max-line-length=100 --extend-ignore=E203 core\tests\unit\test_template_l5_expansion.py`：通过。
- `python -m mypy --ignore-missing-imports --follow-imports=skip core\tests\unit\test_template_l5_expansion.py`：通过。
- `python -m compileall core pipeline tools\validators\pipeline_quality.py`：通过。
- `git diff --check`：通过。
- 内置模板 JSON 解析通过；排除基础参考 `builtin_indie_hades.json` 后，8 个模板达到 complete>=39，30 个模板达到 P0>=16，缺失列表为空。

## 后续关注

- 提交前继续确认 `plan/template_l5_expansion/` 不进入暂存区。
- 真实 GUI 载入模板后，可抽样检查 Step02 实体覆盖报告是否显示预期的 complete/P0 覆盖等级。
