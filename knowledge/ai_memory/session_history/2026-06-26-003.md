# 2026-06-26-003 — 执行 directory_cleanup_analysis 清理计划

## 摘要

按 `plan/directory_cleanup_analysis.md` 完成目录清理与相关修复。计划文件仍作为本地任务材料保留，不提交到 git。CC-Panes 共享记忆池因环境变量为空未写入，本轮记忆写入项目本地 `knowledge/ai_memory`。

## 完成内容

- 删除临时验证存档 `save_20260626_080638_065042`，并同步清理其关联 draft：`20260626_080638_6740`、`20260626_080645_29304`。
- 更新剩余正式存档 `save_index.json` 和 manifest 进度显示为 `9/18`。
- 删除旧 plan 目录：`pipeline_optimization`、`v5_engineering_plan`、`l5_entity_ai_supplement`、`pytest_basetemp_fix`、`hades_quality_optimization`、`template_l5_expansion`、`cache_centralization`、`step05_optimization`。
- 保留最近 3 个 plan 目录：`hades_l5_step0008_opt`、`manual_style_confirmation`、`universal_genre_coverage`，并保留本轮分析文件 `plan/directory_cleanup_analysis.md`。
- 删除根目录旧 `.pytest_cache/` 和 legacy `sandbox/outputs/`。
- `.gitignore` 改为统一忽略 `plan/`，避免新计划材料继续进入暂存区。
- 修复 `core/save/manager.py` 新建/同步存档进度总数硬编码 16 的问题，改为 `max_step_number()+1`。
- 修复 `core/ui/save_manager_dialog.py` 的默认进度文案，改为动态总步数。
- 修复 `knowledge/ucos/scripts/ucos_init.py`、`knowledge/ucos/scripts/ucos_migrate.py` 中旧 `pipeline_progress.total=16`，改为动态总步数，迁移解析支持任意 `x/y`。

## 自查修复

- 计划文档声称 `.gitignore` 已有 `plan/`，实际只有部分 plan 子目录规则；已改为统一 `plan/`。
- 搜索发现 ucos 脚本仍会写入旧 16 步进度，已同步修复。
- 自检生成的 `core/__pycache__` 已路径校验后删除。
- pytest 临时目录仍保留 63 个：当前策略只自动清理 7 天前目录，本轮没有强删未过期/可能锁定目录。
- drafts 从 44 个降到 42 个；当前 drafts 都在 7 天内，未做进一步批量清理。

## 验证

- `python -B -m pytest core\tests\unit\test_draft_archive_paths.py -q`：9 passed。
- `python -B -m pytest -q`：121 passed。
- `PYTHONPYCACHEPREFIX=.cache\pycache python -B -m compileall ...`：通过。
- `python -B -m flake8 ... --select=F`：通过。
- 旧 16 步进度字面量搜索：无命中。
- `.pytest_cache`：不存在；`sandbox/outputs`：不存在；源树 `__pycache__`：不存在。

## 后续关注

- pytest `sandbox/pytest_*` 目录有 63 个，但都未超过 7 天；继续依赖现有 7 天自动清理策略，若 Windows 锁释放后可单独手动清理。
- drafts 仍有 42 个，当前都在 7 天内；后续可单独做保留最近 N 个 + 所有关联存档 draft 的自动清理策略。
- 提交前继续确认 `plan/directory_cleanup_analysis.md` 和剩余 plan 目录不进入暂存区。
