# 2026-06-24-002 — 提交范围纠正：本地 bug 文档和临时计划不入库

## 摘要

根据用户纠正，修正 git 提交范围：`bug收集文档*.md`、`bug优化文档*.md` 和本轮临时开发计划 `plan/l5_entity_ai_supplement/` 属于本地审查输入和执行过程材料，不应进入 git 库。

## 完成内容

- 从上一笔提交中移除 bug 收集文档和 `plan/l5_entity_ai_supplement/` 临时开发计划。
- `.gitignore` 增加小范围忽略规则，防止同类本地文档再次被误加。
- `knowledge/ai_memory/code_conventions/anti_patterns.md` 的提交禁令补充本地 bug 文档和临时开发计划。

## 后续规则

- bug 收集/优化文档只作为本地输入读取和处理，不提交到 git。
- 临时开发执行计划只作为本地任务材料使用，不提交到 git；正式架构文档、治理文档、测试和实现代码可按项目规则提交。
- 提交前需要用 `git status --short` 和 `git diff --cached --name-only` 检查暂存范围。
