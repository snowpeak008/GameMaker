# 会话记录: 2026-07-05-097

## Summary

Added stale workspace doctor and cleanup commands for the Rust formal archive lifecycle.

## Context

- Continued the active long-running full-project completion goal.
- Latest memory `2026-07-05-096` was read before coding.
- `data_root/workspaces` accumulated formal archive workspace session directories after create/run/resume flows.
- The parent repository still sees `RUST/` as untracked.

## Completed

- Added `WorkspaceInspection`, `ArchiveWorkspaceDoctorReport`, and `ArchiveWorkspaceCleanupReport` in `adm-archive`.
- Added `ArchiveRepository::inspect_workspaces()` to report workspace count, active count, stale count, total bytes, and per-workspace file/byte details.
- Added `ArchiveRepository::cleanup_stale_workspaces()` to delete only workspaces not referenced by an active archive lock.
- Active workspace preservation is based on `session_id=` values found in `archives/*/.archive_lock`.
- Added application wrappers `AdmApplication::inspect_workspaces()` and `AdmApplication::cleanup_stale_workspaces()`.
- Added CLI commands:
  - `workspace-doctor [data_root]`
  - `workspace-cleanup [data_root]`
- Updated `RUST/README.md` with the new workspace lifecycle commands.
- Rebuilt and re-staged the isolated Rust desktop release after the archive/application changes.

## Verification

- `cargo fmt`: passed.
- `cargo test -p adm-archive`: passed, 13 tests.
- `cargo test -p adm-application`: passed, 22 tests.
- `cargo check -p adm-cli`: passed.
- CLI smoke:
  - `cargo run -q -p adm-cli -- run-core "Workspace Cleanup Smoke" ... .\target\workspace-cleanup-smoke`: passed.
  - `workspace-doctor` reported `workspace_count=2`, `active_count=0`, `stale_count=2`.
  - `workspace-cleanup` reported `removed_count=2`, `skipped_active_count=0`.
  - A second `workspace-doctor` reported `workspace_count=0`.
- `cargo fmt --check`: passed.
- `cargo check --workspace`: passed.
- `cargo test --workspace`: passed, 127 Rust unit tests.
- `cargo run -q -p adm-desktop -- --smoke`: passed.
- `cargo build -p adm-desktop --release`: passed.
- `cargo run -q -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`: passed; release hash `fnv64:282bb54c6c935e88`, bytes `22083072`.
- `release-doctor`: passed with `ready=true`, release hash `fnv64:282bb54c6c935e88`.
- `delivery-doctor`: passed with `ready=true`.
- `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`: passed.
- Cross-layer check completed for archive repo -> application wrapper -> CLI command -> README workflow; no missing layer found.

## Changed Files

- `RUST/README.md`
- `RUST/apps/adm-cli/src/main.rs`
- `RUST/crates/adm-application/src/lib.rs`
- `RUST/crates/adm-archive/src/lib.rs`
- `RUST/dist/AutoDesignMaker-rust/`
- `knowledge/ai_memory/INDEX.md`
- `knowledge/ai_memory/project_understanding/freshness.json`
- `knowledge/ai_memory/session_history/2026-07-05-097.md`
- `knowledge/ai_memory/session_history/index.json`

## Artifacts

- `RUST/target/workspace-cleanup-smoke/`
- `RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`
- `RUST/dist/AutoDesignMaker-rust/release-manifest.adm`

## Follow-up

- Add desktop-facing workspace doctor/cleanup controls if project operators need it in the GUI.
- Continue lock recovery UX: distinguish current-window lock release from external stale lock clearing.
- Continue production-depth pipeline expansion beyond demo-derived content.
- Run guarded real Unity build validation on a machine with Unity installed.
- Keep the full project completion goal active until the project is genuinely complete.
