# 2026-06-24-005 — 修复 pytest 临时目录与 draft 生命周期管理

## 摘要

处理第九至第十一轮审查问题：稳定 Windows pytest 临时目录/缓存路径，修复 Hades 质量测试冗余断言与 magic number，补齐 draft 生命周期保留、关联清理和 step0 重跑清理策略。bug 文档和临时 plan 仍只作为本地输入，不提交到 git。

## 完成内容

- 新增 `pytest.ini` 与根目录 `conftest.py`：默认测试范围限定为 `core/tests`，pytest basetemp 动态写入 `sandbox/pytest_<timestamp>`，缓存写入 `sandbox/pytest_cache`，保留显式 `--basetemp` 覆盖能力。
- `.gitignore` 补充 pytest 遗留缓存目录与 `plan/pytest_basetemp_fix/`，继续禁止本地 bug 文档和临时计划入库。
- `core/engines/generation.py` 提取 `MAX_TASK_TITLE_LENGTH`，`test_hades_quality_optimization.py` 提取 `TOTAL_HADES_TEMPLATE_NODES` 并删除冗余断言。
- `core/save/manager.py` 新增 `prune_old_drafts()`、`reset_current_draft_outputs()`，删除存档时清理关联 draft，并在 `draft_meta.json` 写入 `linked_save_id`，兼容旧 `linked_archive_path`。
- `core/main.py` 与 `core/ui/gui_app.py` 启动时执行 draft 保留策略；CLI/GUI 从 step0 运行前清理当前 draft 旧 artifacts。
- `test_draft_archive_paths.py` 增加路径隔离、draft prune、删除存档清关联 draft、step0 artifacts reset、`linked_save_id` 写入的回归覆盖。
- 自检时修复 `conftest.py` docstring 的 Windows 路径非法转义警告，并清理 `core/main.py`、`core/ui/workbench.py` 的未使用 import/变量。

## 验证

- `python -m pytest -q`：90 passed。
- `python -m pytest core\tests\unit\test_draft_archive_paths.py core\tests\unit\test_core_paths.py -q`：11 passed。
- `python -m pytest core\tests\unit\test_core_paths.py -q --basetemp=sandbox\pytest_tmp_explicit`：4 passed。
- `python -m flake8 --max-line-length=100 --extend-ignore=E203 ...`：通过。
- `python -W error::SyntaxWarning -m py_compile ...`：通过。
- `python -m mypy --ignore-missing-imports --follow-imports=skip ...`：通过。
- `python -m compileall core pipeline tools\validators\pipeline_quality.py`：通过。
- `git diff --check`：通过，仅 CRLF 工作区提示。

## 后续关注

- 历史 `drafts/` 一次性删除脚本未执行；该操作会不可逆删除用户运行数据，必须由用户明确确认后再做。
- `.pytest_tmp/` / `.pytest_cache_tmp/` 旧目录受 Windows ACL 限制无法删除，当前已忽略且新 pytest 配置不再使用。
- CC-Panes 共享记忆池未写入：`CC_PANES_PROJECT_PATH` / `CC_PANES_WORKSPACE_NAME` / `CC_PANES_CLI_TOOL` 均为空，按规则跳过以避免污染共享池。
