# 2026-06-23-006 — v5_engineering_plan 全量执行

## 摘要

检查并执行 `plan/v5_engineering_plan` 中 Phase 0-4 的可代码化开发项：补齐 Step00-05 helper 能力、工程基础设施、质量验证命令、多品类模板验证和文档。

## 完成内容

- Step00：补 `key_systems`、核心问题词表、品类推导 evidence、`needs_ai_supplement` 标志，Hades 稀疏 Concept 覆盖率提升到 0.8667。
- Step01：`genre_templates.json` 增加 strategy/rpg/moba，模板加载加进程缓存，显式 loop 分隔符更健壮，system_layer 前缀清洗。
- Step02：真实 L5 实体编号改为按 L5 连续编号，kind 推断补 weapon/enemy/resource/room/audio/config，`launch_ops` 阶段关键词补充；保留“无可信分母覆盖率为 0”的 BUG-007 修复。
- Step03：扩展 `SCHEMA_ROUTES`，weapon/ability/character/room 支持多需求生成，绑定增加中英文 token/领域语义评分，质量报告增加需求密度统计。
- Step04：核心 kind 支持多资产生成、P0 resolution、roguelike 本地市场参考库优先读取。
- Step05：`intelligent_review_report` 增加 verdict，placeholder >50% BLOCKER，内容深度聚合告警，资产类型覆盖/P0 检查，L1 项目配置系统绑定豁免。
- Phase0 基础设施：新增 `.pre-commit-config.yaml`、`core/tests/conftest.py`、`knowledge/governance/L5_ENTITY_GUIDE.md`、`tools/dev/scaffold_step.py`。
- 质量工具：`tools/validators/pipeline_quality.py` 增加 `--check plan-002`，并更新 v5 target 描述。

## 验证

- `python -m pytest core\tests -q`：39 passed（仅 `.pytest_cache` 写入权限 warning）。
- `python -m black ... --check`（本次改动范围）：通过。
- `python -m flake8 ...`（本次改动范围）：通过。
- `python -m mypy ... --ignore-missing-imports --explicit-package-bases`（本次改动范围）：通过。
- `python -m compileall ...`（本次改动范围）：通过。
- `python -m core.main --from-step 0 --stop-step 6 --auto-approve --skip-preflight`：Step 00-06 全部 success。
- `python tools\validators\pipeline_quality.py --check plan-002`：通过，entity coverage 0.4563 >= 0.38。
- 最新质量指标：question coverage 0.8667、core_loop 1.0、system definition 1.0、entity coverage 0.4563、entity count 47、requirement binding 0.9753、placeholder 0.0、asset count 56、Step05 warning/blocking 1/0。

## 注意事项

- v5 计划中 `entity_coverage >= 0.75/0.80`、真实 L5 实体数 >=40、真实实体下资产数 >=80 明确依赖用户在 DesignEngine 录入 L5 实体；当前 Hades 源仍是 0 个真实 L5，因此代码侧保持合成实体质量和透明告警。
- `pre-commit run --all-files` 已尝试执行，但全仓失败在大量既有 flake8/mypy 问题和 `tools/dev/error_logger_generator.py` black 解析错误上；为避免无关格式化污染，已回滚 pre-commit 对 tracked 文件的批量 black 改写。本次改动范围的 black/flake8/mypy 均通过。

## 后续关注

- 在 DesignEngine 为 Hades 录入 >=40 个真实 L5 实体后，重跑 Step00-06 验证 entity coverage >=0.75、资产数 >=80。
- GUI 中手动验收 D4 handoff 和 Step 00-06 展示路径。
- 验证 Codex/Claude adapter 切换、保存/加载端到端和旧流水线存档过滤。
