# 会话记录：2026-07-05-040

## 摘要

继续 goal 模式推进 Rust 全项目重建，将 Slint desktop 的 SDK 信息从隐含在 stage/package 文本里，提升为结构化 SDK 面板。桌面工作台现在会从正式存档的 `content/sdk/index.adm` 读取 SDK 资源、用途、风险和验证清单，并在新建、恢复、导入和刷新项目时保持一致。

## 完成内容

- `adm-desktop`
  - Slint 新增 `SdkResourceRow`。
  - Slint 新增 `sdk-text` 和 `sdk-resource-items`。
  - 主状态区从 AI/Package/Validation 三栏扩展为 AI/SDK/Package/Validation 四栏。
  - `DesktopRunSummary` 和 `ProjectInspection` 新增 SDK 文本与 SDK 资源行。
  - 新增 `inspect_sdk(...)`，统计 SDK resource/risk/validation 数量。
  - 新增 `inspect_sdk_resources(...)`，从正式存档 `sdk/index.adm` 读取结构化资源。
  - 新增 `sdk_resource_items_from_text(...)`、`append_csv_value(...)`、`count_csv_items(...)`。
  - `create/resume/rerun/resume_failed` 的运行摘要现在从 archive content 读取 SDK artifact。
  - desktop smoke 覆盖：
    - SDK 面板显示 `resources=1`。
    - SDK 验证项数量为 3。
    - SDK row model 数量为 1。
    - 导入项目后 SDK row model 仍然存在。
- 发布产物
  - 重新构建 release。
  - 重新 staging 到 `RUST/dist/AutoDesignMaker-rust/AutoDesignMaker-rust.exe`。
  - 新 staged hash：`fnv64:f80f2814ed3d3b42`。

## 验证

- `cargo check -p adm-desktop`：通过。
- `cargo fmt --check`：通过。
- `cargo run -p adm-desktop -- --smoke`：通过。
- `cargo check --workspace`：通过。
- `cargo test --workspace`：通过，当前 75 个 Rust 单元测试通过。
- `cargo build -p adm-desktop --release`：通过。
- `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`：通过。
- `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`：通过。

## 后续事项

- 继续把桌面 GUI 从 POC 推进到可用工作台：把 design/development/assets 也拆成结构化可读面板。
- 增加可视窗口手工验收：两个 Rust 窗口选择同一 archive 时，第二个窗口必须显示锁冲突和 owner 信息。
- 后续可把 SDK 面板改成可检索/可过滤资源列表。
