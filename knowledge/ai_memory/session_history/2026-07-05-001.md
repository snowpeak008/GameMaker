# 会话记录：2026-07-05-001

## 摘要

使用 `grill-with-docs` 方式梳理 AutoDesignMaker 从 Python/Tkinter 项目彻底重建为 Rust 桌面工作台的开发方向，并在 `plan/rustplan/` 写入完整开发计划、迁移矩阵、注意事项和开发保证书。

## 完成内容

- 确认 Rust 版是完整重建，不保留 Python 运行时代码；现有项目只作为领域、流程和历史问题参考。
- 确认新 Rust 代码统一放在 `RUST/`，与现有项目共用同一个 Git 仓库。
- 确认先搭底层框架，再实现可见功能和完整业务链条。
- 确认产品核心仍是游戏设计、游戏开发流水线、游戏打包、游戏 SDK 知识库与接入辅助。
- 确认双击启动仍保留；多个进程可分别打开不同正式存档，但同一个正式存档必须互斥，不能被两个会话同时编辑。
- 确认内部正式存档采用目录式权威格式，单文件包只作为导出、备份或分享格式。
- 确认同项目第一版流水线串行执行；多项目并行通过不同进程和不同工作区完成。
- 确认 AI 是必需核心能力，但按需介入：内容不足、评分不足、生成、审查、SDK 解释等场景触发，输出必须验证后才能接受。
- 确认 AI Provider 按 capability 可插拔，业务层不能绑定具体供应商。
- 更新 `CONTEXT.md`，补充 Rust 重建相关术语。
- 新增 Rust 重建 ADR，记录完整重建、桌面壳、数据模型、产品范围、框架优先、Cargo workspace、草稿/正式存档、存储格式、流水线模型、AI 介入和 Provider 插拔等决策。
- 在 `plan/rustplan/` 写入 5 份开发规划文档。

## 生成的计划文档

- `plan/rustplan/00_Rust重建总纲.md`
- `plan/rustplan/01_迁移与重写矩阵.md`
- `plan/rustplan/02_框架优先开发计划.md`
- `plan/rustplan/03_开发注意事项.md`
- `plan/rustplan/04_开发保证书.md`

## 验证

- 确认 `plan/rustplan/` 下 5 份计划文档均已写入。
- 使用 `rg` 检查计划文档标题结构和关键术语覆盖，包括 Rust、Cargo workspace、Slint、正式存档、草稿、互斥、AI、Pipeline、SDK、打包。
- 确认 `plan/` 目录被 `.gitignore` 忽略，本次按用户要求保留在本地目标路径，不修改忽略规则。

## 后续事项

- 下一步应从 `RUST/Cargo.toml` 和 workspace crate 骨架开始，而不是先做可见 UI 或业务链条。
- 第一批 crate 建议从 `adm-foundation`、`adm-config`、`adm-archive`、`adm-runtime`、`adm-ai`、`adm-pipeline` 开始。
- Slint 需要早期 POC 验证复杂界面能力；若失败，需新 ADR 再切换 GUI 技术。
- 真实 AI Provider 接入应晚于 mock provider、AI task 记录、输出验证和错误诊断框架。
- 旧 Python 项目的 `saves/`、`drafts/`、旧 JSON schema 和旧测试只能作为迁移参考，不能作为 Rust 版兼容承诺。
