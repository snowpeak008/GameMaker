# Session 2026-07-05-107

## Summary

Added a one-command Windows release gate script for the Rust rebuild.

## Completed

- [x] Added `RUST/scripts/release_gate.ps1`.
- [x] The script imports the Visual Studio build environment through `vcvars64.bat`.
- [x] The script runs `cargo fmt --check`, `cargo check --workspace`, `cargo test --workspace`, `cargo build -p adm-desktop --release`, `stage-desktop-release`, `release-doctor`, `delivery-doctor`, and `release-acceptance`.
- [x] The script supports `-DryRun` for command preview and `-SkipTests` / `-SkipBuild` for controlled local iteration.
- [x] The script verifies the final `dist/AutoDesignMaker-rust/release-acceptance.adm` contains `accepted=true` and `smoke_ready=true`.
- [x] Updated `RUST/README.md` to document the scripted local release gate.

## Verification

- [x] `powershell -ExecutionPolicy Bypass -File .\scripts\release_gate.ps1 -DryRun`: passed and printed the expected command sequence.
- [x] `powershell -ExecutionPolicy Bypass -File .\scripts\release_gate.ps1`: passed.
- [x] The scripted gate ran `cargo fmt --check`: passed.
- [x] The scripted gate ran `cargo check --workspace`: passed.
- [x] The scripted gate ran `cargo test --workspace`: passed, 133 Rust unit tests.
- [x] The scripted gate ran `cargo build -p adm-desktop --release`: passed.
- [x] The scripted gate ran `stage-desktop-release`: release hash `fnv64:97814f3263c20abf`, bytes `22492160`.
- [x] The scripted gate ran `release-doctor`: `ready=true`.
- [x] The scripted gate ran `delivery-doctor`: `ready=true`.
- [x] The scripted gate ran `release-acceptance`: `accepted=true`, `delivery_ready=true`, `smoke_ready=true`.
- [x] Cross-layer check completed for README workflow -> PowerShell script -> CLI commands -> `release-acceptance.adm`.

## Follow-up

- [ ] Run desktop `Run Unity` and `Run Runtime` against a real Unity installation and confirm actual editor output ingestion.
- [ ] Continue real-provider acceptance after safe endpoint/model/secret setup.
- [ ] Decide how to track or package the still-untracked `RUST/` tree in the parent repository.
- [ ] Decide whether to wire `scripts/release_gate.ps1` into CI once repository tracking is settled.
- [ ] Keep the full project completion goal active until the project is genuinely complete.
