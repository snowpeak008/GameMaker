# 会话 2026-06-20-002

> 时间：2026-06-20  
> 类型：项目结构整理 + ucos 迁移 + 记忆系统联通

---

## 做了什么

### 1. ucos 目录迁移

- `ucos/` → `knowledge/ucos/`（符合知识层语义）
- `core/paths.py`：`KNOWLEDGE_DIR` 加入 `sys.path`，所有 `from ucos.xxx` 导入无需改动
- `UCOS_DIR` 路径常量更新为 `KNOWLEDGE_DIR / "ucos"`
- 两个脚本 `parents[2]` → `parents[3]`（深度加一）
- `AI_README.md` 路径引用更新

### 2. ucos ↔ AI 访谈联通

新建 `core/design/ai_ucos_bridge.py`，每次成功的 AI 访谈响应后写入四类数据：
- `ucos/knowledge/episodic/turns/` — 原始对话记录
- `ucos/knowledge/short_term/` — AI 路由决策（带衰减）
- `ucos/knowledge/semantic/staging/` — 高置信设计决策（≥ 0.75）
- `ucos/knowledge/episodic/episodes/` — 完整设计生成事件

`core/ui/ai_interview_window.py`：成功响应后调用 `record_interview_turn()`

### 3. artifact_layer 迁移

- `artifact_layer/` → `pipeline/artifact_layer/`
- `core/artifact/registry_loader.py` 路径更新

### 4. 项目清理

- 删除 `_archive/`（历史文档）
- 删除根目录 `ai_runtime/`（旧版遗留，现已写入 `sandbox/workspace/ai_runtime/`）
- 删除 `.claude/` 项目内残留（错误创建的记忆文件）
- 删除 `plan/`（两份计划均已实现）

---

## 关键发现

1. **ucos 之前完全孤立** — 无任何入口调用，数据沉默；联通后每次 AI 访谈均写入
2. **ai_runtime/ 根目录是遗留** — 现在代码实际写入 `sandbox/workspace/ai_runtime/`
3. **artifact_layer 归属** — 属于 pipeline 定义层，迁移后结构更清晰

---

## 修改的文件

- `core/design/ai_ucos_bridge.py`（新增）
- `core/ui/ai_interview_window.py`（添加 ucos 钩子）
- `core/paths.py`（sys.path + UCOS_DIR）
- `core/artifact/registry_loader.py`（REGISTRY_PATH）
- `knowledge/ucos/scripts/ucos_sync.py`（parents[3]）
- `knowledge/ucos/scripts/ucos_query.py`（parents[3]）
- `AI_README.md`（路径引用）
- `knowledge/ai_memory/INDEX.md`（本次更新）

**Git commit**：本次提交

---

## 下次会话应继续

- [ ] 验证 AI 访谈 → ucos 写入是否正常
- [ ] 考虑是否需要将 ucos context_builder 的读取结果注入 AI 访谈 prompt（闭环）
