# Session 2026-07-05-055

## Summary
Added engine build execution adapters with dry-run reporting for Unity build commands.

## Completed
- [x] Added `EngineBuildExecutionMode`, `EngineBuildExecutionStatus`, and `EngineBuildExecutionReport`.
- [x] Added `EngineBuildRunner`.
- [x] Added `DryRunEngineBuildRunner`.
- [x] Added `LocalProcessEngineBuildRunner` for future real process execution.
- [x] Added report rendering for engine build executions.
- [x] Added tests for dry-run reporting and missing local executable rejection.
- [x] Added CLI command `dry-run-unity-build <archive_id> <target_id> <unity_exe> <unity_project_dir> [data_root]`.
- [x] Added Slint `dry-run-unity-build` callback and Dry Run button in the Unity Build row.
- [x] Added desktop helper `dry_run_unity_build_for_ui(...)`.
- [x] Desktop smoke now verifies dry-run Unity build output contains `mode=dry_run`, `launched=false`, and the Unity execute method.
- [x] Rebuilt and re-staged the isolated Rust desktop release exe.

## Verification
- [x] `cargo test -p adm-packaging`: passed, 12 tests.
- [x] `cargo check -p adm-cli`: passed.
- [x] `cargo check -p adm-desktop`: passed.
- [x] `cargo run -p adm-desktop -- --smoke`: passed.
- [x] CLI `dry-run-unity-build` against a fresh temporary archive: passed.
- [x] `cargo fmt --check`: passed.
- [x] `cargo check --workspace`: passed.
- [x] `cargo test --workspace`: passed, 90 Rust unit tests.
- [x] `cargo build -p adm-desktop --release`: passed.
- [x] `cargo run -p adm-cli -- stage-desktop-release .\target\release\adm-desktop.exe`: passed.
- [x] `.\dist\AutoDesignMaker-rust\AutoDesignMaker-rust.exe --smoke`: passed.
- [x] Staged release hash: `fnv64:db980a5d1bcfc665`.

## Follow-up
- [ ] Add an explicit guarded real-run CLI command for local engine builds once UI confirmation/state policy is ready.
- [ ] Add visible-window validation for two Rust windows selecting the same archive with owner details visible.
- [ ] Continue replacing POC text-heavy controls with richer workbench sections.
