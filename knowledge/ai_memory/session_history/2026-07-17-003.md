# Session 2026-07-17-003

## Summary

Closed W1.5 by freezing the legacy execution retention boundary instead of deleting the old execution surfaces: GameSpec v2 product Step11 remains authoritative through `WorkspaceTaskAgent + WorkspaceChangeSet`, while `WorkUnitExecutor`, `AiDevelopmentWorkUnitExecutor`, and `CodexPatchRunner` now have code-level retained/prohibited caller contracts and regression tests.

## Completed

- [x] Added `plan/newdesignplan/contracts/W1_5_legacy_execution_boundary.md` with the explicit retention decision, allowed callers, prohibited callers, v2 replacement, and impact surface.
- [x] Updated A08c and `remaining_work_checklist.md` so W1.5 is closed by a documented and tested retention strategy, not by unsafe deletion.
- [x] Added `WORK_UNIT_EXECUTOR_*` boundary constants in `adm-new-pipeline` and exported them for auditability.
- [x] Added `AI_DEVELOPMENT_EXECUTOR_*` boundary constants in `adm-new-application`; the executor is allowed as the v2 bridge but prohibited as a direct v2 work-unit commit path.
- [x] Added `CODEX_PATCH_RUNNER_*` boundary constants in `adm-new-patch`; the runner remains for CLI patch/R0 and is prohibited from GameSpec v2 product stages.
- [x] Added tests that assert the v2 product Step11 prohibition for all three retained surfaces.

## Verification

- [x] `cargo fmt --all`
- [x] `cargo test -p adm-new-pipeline work_unit_executor_retention_boundary_excludes_v2_product_step11 --locked`
- [x] `cargo test -p adm-new-application development_executor_boundary_allows_v2_bridge_but_not_direct_v2_commit --locked`
- [x] `cargo test -p adm-new-patch codex_patch_runner_retention_boundary_excludes_gamespec_v2_products --locked`
- [x] `cargo test -p adm-new-pipeline --locked`
- [x] `cargo test -p adm-new-application --locked`
- [x] `cargo test -p adm-new-patch --locked`
- [x] `cargo check --workspace --locked`
- [x] `cargo test --workspace --locked`

## Remaining

- [ ] W4 release-gate work remains: true VLM judging, real headless/engine asset loading, real scene execution, standalone verification, EXE smoke, and manual playtest signing.
- [ ] W5 release flow remains: commit only after explicit user acceptance, then clean-tree standalone verification, rebuilt package, R1 stop-gate acceptance, and optional push/tag.
- [ ] No commit, tag, push, or repository synchronization was performed.
