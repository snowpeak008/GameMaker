# 2026-06-24-006 — 修复 Codex sandbox、图片配置与 pytest basetemp 清理

## 摘要

处理 `bug收集文档12_codex_sandbox_version.md`：修复 Codex CLI 0.141.0 不支持 `sandbox=none` 的 Step02 阻断，补齐图片 API 配置继承与旧图片工具导入问题，增加 pytest 历史 basetemp 自动清理，并让 Stage09/Stage11 输出图片生成清单。bug 文档和临时 plan 仍只作为本地输入，不提交到 git。

## 完成内容

- `pipeline/step_02_design_review_freeze/supplement.py` 将 Step02 L5 实体补全 Codex sandbox 从非法 `none` 改为 `read-only`。
- `core/config/loader.py` 增加统一 API 配置文档读取、`openai_endpoint()`，并让 `image2` 可继承 `[image]` 与 `[llm]` 的本地配置。
- `tools/asset_production/image_api_config.py` 支持 `[image2]`、`[image]`、`[llm]` 兼容回退；只有 `[llm]` 时图片模型默认仍为 `gpt-image-2`，避免把文本模型误当图片模型。
- `tools/asset_production/image_tool.py` 修复旧导入路径，改用 `core.config.loader`。
- `core/engines/generation.py` 在 Stage09/Stage11 写入 `generated_images_manifest.json`；真实图片生成默认跳过，只有设置 `AUTODESIGNMAKER_ENABLE_IMAGE_GENERATION=1` 才调用外部图片 API。
- `conftest.py` 增加旧 pytest basetemp 清理，只删除严格匹配 `pytest_YYYYMMDD_HHMMSS_xxxxxx` 且超过 7 天的目录。
- `.gitignore` 补充本地评分、检查、优化、标准化和完成报告类文档忽略规则。
- 新增/更新单元测试覆盖 Codex sandbox、API 配置继承、旧图片工具导入、图片生成 manifest 默认跳过、pytest 旧目录清理。

## 验证

- `python -m pytest -q`：97 passed。
- `python -m black --check ...`：通过。
- `python -m mypy --ignore-missing-imports --follow-imports=skip ...`：通过。
- `python -m flake8 --max-line-length=100 ...`：通过。
- `python -m py_compile ...`：通过。
- `python -m compileall core pipeline tools\validators\pipeline_quality.py tools\asset_production`：通过。
- `git diff --check`：通过，仅 CRLF 工作区提示。

## 后续关注

- Stage09/Stage11 的真实图片生成是显式开关能力，默认不产生网络调用和 API 费用；需要实图验收时设置 `AUTODESIGNMAKER_ENABLE_IMAGE_GENERATION=1`。
- 提交前继续确认 bug 文档、plan 临时执行目录和 `settings/api_config.toml` 不进入暂存区。
- CC-Panes 共享记忆池未写入：`CC_PANES_PROJECT_PATH` / `CC_PANES_WORKSPACE_NAME` / `CC_PANES_CLI_TOOL` 均为空，按规则跳过以避免污染共享池。
