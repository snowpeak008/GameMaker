# Session 2026-06-28-009

## Summary

Implemented `plan/perf_fix/FINAL_PLAN.md`: improved GUI startup responsiveness, reduced design-workbench render work on high-frequency interactions, and optimized save/load bookkeeping for unchanged workspaces.

## Completed

- Deferred startup integrity checks and draft pruning until after `MainWindow` is created, with visible/logged startup-check warnings.
- Added lazy `CommercialDesignApp` creation with an active-view guard so delayed construction cannot steal focus from the pipeline panel.
- Added centralized result-panel cache invalidation and routed frequent checklist/risk/not-applicable interactions through `render_nodes()` instead of full `render()`.
- Added autosave scheduling for option-group and text-field state changes that previously only refreshed result text.
- Added `current_save_id_readonly()` for close-time checks without save-system write side effects.
- Made workspace migration helpers return whether they actually changed files, including JSON normalization and source package manifest writes.
- Added `mtime_ns` file-map caching that reuses only cached SHA256 values while recomputing cheap metadata.
- Limited current draft snapshots to the latest 5 after sync.
- Added a `load_save` fast path that skips copy-back and full snapshot creation when migration did not change active workspace content.
- Added focused save-manager regression tests for manifest idempotence, SHA256 cache reuse, snapshot trimming, and load fast path.

## Verification

- `python -B -m compileall core\ui\gui_app.py core\ui\main_window.py core\ui\app_window.py core\save\manager.py`: passed.
- `python -B -m pytest core\tests\unit\test_draft_archive_paths.py -q`: 16 passed.
- `python -B -m pytest -q`: 174 passed.
- `python -B -m compileall core pipeline tools\validators\pipeline_quality.py tools\asset_production tools\config`: passed.

## Follow-up

- Manual GUI timing/click-through remains useful for the lazy design panel, checklist/risk/not-applicable interactions, and load-save fast path on a large PNG-heavy archive.
- `plan/perf_fix/` remains local planning material and should not be committed unless explicitly requested.
