# 2026-07-14-005

## 摘要

完成开发流水线与 AI 配置界面精细化修复，定位并修复 Step07 在 Windows 上误用 POSIX `codex` shim、导致真实图片调用全部降级的问题，并基于当前“植物大战僵尸”产物独立评估 Step00-06。

## 完成内容

- [x] 将流水线左侧步骤区宽度从 220px 增至 330px，步骤卡最小高度从 62px 增至 124px；中等宽度下保持横向溢出，不压缩卡片。
- [x] Step07 图片只在当前选中 Step07 时显示，切换到其他步骤立即隐藏，仅展示所选步骤内容。
- [x] AI 配置列表只显示用户配置的名称，移除内部 ID/配置类型副文本，并将“设为当前配置项/当前配置项”操作缩小到 30px 高。
- [x] Windows CLI 定位仅接受 `.com/.exe/.cmd/.bat`，跳过 npm 的无扩展名 POSIX shim，优先使用可由 Windows 直接启动的 `codex.cmd`。
- [x] Step07 提供方请求提升到 1536x1024，扩充项目角色、敌人、环境和玩法约束，代表性资产优先使用资产名称，并强化排除无关通用素材的负向提示。

## 根因与迁移说明

- 当前 Step07 清单记录 `provider_generated_count=0`、`fallback_count=5`、`failed_count=5`；用户看到的是 640x384 的确定性降级图，不是模型生成结果。
- 同机直接以 `codex.cmd` 执行图像调用成功，证明账号、参数和图像能力可用；失败点位于可执行文件定位。
- 旧的降级图片不会被代码修复自动替换，需要使用新版本重新运行 Step07。

## 产物评分

- Step00：76/100
- Step01：72/100
- Step02：70/100
- Step03：62/100
- Step04：45/100
- Step05：47/100
- Step06：38/100

## 验证与交付

- [x] Rust `cargo check --workspace` 与 `cargo test --workspace` 通过。
- [x] Web 单元、E2E、构建、双语、language gate、90 张 UI 截图和 93 项 UI baseline 全部通过。
- [x] 独立性边界扫描 224 个源文件，父 Python 项目运行依赖命中 0。
- [x] 开发快照桌面 EXE SHA-256 为 `42e2f76a3de47a962cadd8e0ebe1d69c8b447eddce6032297051abd10ce21318`；根启动器 SHA-256 为 `4c4052b90c1aca3b0008b740ec78d147c4e3535d9ea8d1512facf5cfb5a736bb`。
- [x] 便携构建前后 `user_data` 均为 1201 个文件、44,983,881 字节，启动器自检和便携烟雾测试通过。
- [x] 受保护清理删除 94 项生成物，其中 Cargo 缓存约 13.3 GB；产品目录与 `user_data` 未删除。
- [ ] 当前工作树仍有累计未提交修改，因此本次是开发快照；正式发布证据需在干净 Git 树上重跑。
