# 2026-07-15-002

## 摘要

将独立 Rust 项目发布到 `https://github.com/snowpeak008/GameMaker`，冻结首个可复现基线 `v0.1.0`；随后完成通用游戏规格编译器总体计划、10 个有序原子计划及二次复核，并完成第一项 A01 GameSpec 基础开发。

## Git 与独立发布

- [x] NEWrust 保持独立 Git 根，远端 `origin` 指向 GameMaker 仓库。
- [x] 首个基线提交为 `1a74408680cc73f07440228068139218546fc458`，标签为 `v0.1.0`。
- [x] 公开上传前检查忽略规则、超大文件和凭证模式；构建产物、EXE、本机 `user_data` 和环境配置未进入版本库。
- [x] 修复安全白名单在干净克隆中的行尾哈希不稳定，并为 `.rc` 固定 LF 策略。
- [x] 完整独立验证 21 项全部通过，包括干净搬迁克隆、反伪造扫描、Web/UI、Rust、便携构建、EXE smoke、资源完整性和 x64 静态 CRT。

## 计划决策

- [x] 总体计划位于 `plan/2026-07-15_universal_game_spec_compiler.md`。
- [x] A01–A10 原子计划位于 `plan/dev/`，顺序和总体阶段无循环依赖。
- [x] 保留 D1–D4 与 00–14 外部入口，但内部改为 GameSpec 的确定性编译和验证。
- [x] 使用项目级 `game_spec_v2` 开关、影子生成和语义差异报告，禁止大爆炸替换。
- [x] AI 只能提交有界候选补丁；单写者、schema、引用、不变量和用户确认决定是否提交。
- [x] 核心字段晋升需要至少三个结构不同的样例共同证明，类型专属内容进入版本化扩展。

## A01 实现

- [x] 新增独立领域 crate `adm-new-game-spec`，仅依赖 serde/serde_json，不依赖应用层 crate。
- [x] 定义项目意图、正交能力、实体/组件/关系、动作、条件/效果、状态机、资源、空间、时间、交互、内容、表现、技术约束、验收场景、追踪和扩展。
- [x] 增加受验证 `SpecId`、带 `SpecKind` 的跨对象引用、严格未知字段拒绝和确定性 `BTreeMap`/`BTreeSet` 集合。
- [x] 新增通道防守、三消网格、分支叙事和回合战术四个样例；它们至少覆盖三种空间、时间和控制结构。
- [x] 新增 6 项测试，验证 ID、依赖隔离、核心词汇反过拟合、类型标签拒绝、跨结构差异和语义往返。

## 验证

- [x] `cargo fmt --all -- --check`
- [x] `cargo check --workspace --locked`
- [x] `cargo test --workspace --locked`
- [x] `tools/verify-standalone.ps1 -SelfTest`，安全 findings 保持 415，未新增白名单。
- [x] A01 未接入现有 app、保存或流水线路径，因此当前产品行为保持不变。

## 后续

- [ ] 执行 A02：引用完整性、状态闭合、资源约束、规范化 JSON 和稳定内容哈希。
- [ ] A02 未通过前，不开始动态能力图和旧状态接入。
- [ ] 当前 CLI 缺少 CC-Panes 项目环境变量，本轮只更新仓库内记忆，未写共享池。
