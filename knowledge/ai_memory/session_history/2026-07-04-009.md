# 会话记录：2026-07-04-009

## 摘要

排查 GUI 多开/存档加载报错，确认当前失败主要来自正式存档 archive lock 与后台存活进程冲突，并补充最小防护。

## 完成内容

- 确认 `saves/save_20260704_220446_2f433d/.archive_lock` 由仍存活的 `pythonw.exe` PID `24096` 持有。
- 确认该进程关联 draft `drafts/20260704_230912_24096`，最后运行状态为 Step08-10 成功，存档进度更新到 `11/15`。
- 确认第二个窗口自动加载同一个 `current_save_id` 时会因为 archive lock 被拒绝，这是当前多开加载报错的直接原因。
- 在主窗口关闭逻辑中增加流水线运行中拦截：先请求停止并保持窗口打开，避免关闭 UI 时后台任务/锁状态混乱。
- 在 GUI 入口增加正常退出时的 archive lock 兜底释放。
- 在存档管理“保存到选中存档”前增加 archive lock 检查，防止第二个窗口写入另一个窗口正在打开的存档。
- 补充存档/GUI 源码级回归测试。

## 验证

- `python -m pytest core\tests\unit\test_draft_archive_paths.py -q`：35 passed。
- `python -m compileall core\ui\gui_app.py core\ui\main_window.py core\ui\save_manager_dialog.py core\tests\unit\test_draft_archive_paths.py`：通过。

## 后续事项

- 如果需要立即恢复当前存档加载，先确认或关闭仍存活的 PID `24096`，不要直接手删 `.archive_lock`。
- 多窗口并行编辑同一个正式存档仍不支持；后续如要支持，应设计只读打开/复制打开模式，而不是放宽写锁。
- CC-Panes plan 召回未执行，因为 `CC_PANES_PROJECT_PATH` / `CC_PANES_WORKSPACE_NAME` 环境变量为空；本次只使用仓库内 `knowledge/ai_memory`。
